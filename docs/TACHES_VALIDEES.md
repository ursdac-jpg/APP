# Tâches validées — fichier central, toutes les tâches du projet, commun à toute IA travaillant sur ce dépôt

Ce fichier est **commun à tous les comptes/sessions Claude** (ou toute autre IA) qui travaillent sur ce projet — contrairement à la mémoire personnelle d'un assistant, qui n'est jamais visible d'un compte à l'autre. C'est ici, et seulement ici, que doit vivre le suivi partagé des tâches de code validées avec Denis.

**Centralisé le 2026-08-27, à la demande explicite de Denis** : avant cette date, les tâches étaient éparpillées entre plusieurs documents (`docs/ETAT_DES_CHANTIERS_2026-08-24.md`, la mémoire personnelle de l'assistant...). Ce fichier-ci devient désormais LE point d'entrée unique. Pour les chantiers déjà très détaillés ailleurs (`docs/ETAT_DES_CHANTIERS_2026-08-24.md` notamment, riche et à jour au 24/08), ce fichier garde un résumé court avec un renvoi, plutôt que de tout dupliquer - mais toute tâche qui n'avait aucune autre maison écrite (mémoire personnelle uniquement) est désormais recopiée ici en entier.

## Règle d'usage, à respecter par toute IA qui ouvre ce fichier

- **Avant toute tâche, lire aussi `docs/TRAVAILLER_AVEC_DENIS.md`** : longueur des réponses (code `[détaillé]` / `[relais IA]`, court par défaut) et gestion des jours de fatigue de Denis.

- Une tâche qui commence par **`[CLOS]`** est terminée, codée, ET vérifiée par un vrai test (jamais juste "ça devrait marcher") : **ne jamais la relire en détail, ne jamais la re-vérifier, ne jamais la rouvrir** — passer directement à la suivante. Exception unique : si Denis demande explicitement un historique/récapitulatif de ce qui a été fait, s'appuyer sur ce fichier pour répondre.
- Une tâche marquée **`[À FAIRE]`** reste à traiter : la lire entièrement avant de coder quoi que ce soit.
- Dès qu'une tâche est codée ET vérifiée par un vrai test de bout en bout (pas une fonction isolée, voir `docs/LECONS_A_NE_PAS_REPRODUIRE.md`, section 10, Règle 8), la marquer `[CLOS]` avec la date, en une seule ligne courte — jamais un paragraphe entier une fois close.
- Ne jamais supprimer une ligne `[CLOS]` : la garder comme trace, courte, pour l'historique.

---

## Grands chantiers stratégiques (ordre validé le 2026-08-23/24)

Détail complet, coût/risque/valeur : `docs/ETAT_DES_CHANTIERS_2026-08-24.md` (parties 1 et 2) et `docs/AUDIT_STRATEGIQUE_ERIP_2026-08.md`. **Revérifier contre `git log` avant d'agir sur un point ancien** - discipline déjà en place dans ce document source. Séquencement décidé le 2026-08-24 : finir tous les modules restants, PUIS accessibilité, PUIS découpage de `js/app.js` en dernier. **Annulé le 2026-09-13 (décision Denis) : il n'y aura pas de découpage de `js/app.js`** - risque déjà évalué, ne vaut pas le coût ; le projet continue de grossir (mises à jour continues, pas de pause pour un tel chantier). Toute dette jusqu'ici "rattachée au découpage" (ex. B.7 de `BRIQUES_COMMUNES.md`, l'ancien panneau Word « Projet XXL ») doit être traitée directement le jour où on retouche cet écran, jamais reportée sur ce chantier.

> **DÉCISION DENIS 2026-09-10 - ordre de fin de projet.** Les gros chantiers restants - **2. Accessibilité** (+ mode accompagné/autonome, option non-francophone, version allégée des fenêtres, socle typographique, langage visuel commun), **3. Découpage de `js/app.js`** (+ dette B.7) - sont repoussés **tout à la fin**, une fois **toutes les autres tâches `[À FAIRE]` terminées** (petites modifs, cohérence, contenu). La robustesse / fiabilité / audit vient juste avant eux. **[MAJ 2026-09-11]** Le 3e chantier initialement listé ici (6. Refonte Découverte + Renommage APP) est clos sans suite - la refonte avait déjà eu lieu en juillet sans être tracée comme telle, et le renommage n'est pas une tâche de code (action GitHub de Denis). Audit robustesse (1er tour + rattrapage + 2e tour) terminé. **[MAJ 2026-09-11, decision finale de Denis] Le projet passe en mode entretien, plus en croissance.** Denis n'a plus vocation à faire grandir APP - la seule évolution prévue est du **contenu** (Lexique, Comprendre les chiffres, Comprendre le cadre, éventuellement l'élargissement de `data/metiers.js` au-delà des 125 métiers actuels), jamais du code structurel. Dans ces conditions, les 2 chantiers restants (Accessibilité, Découpage de `js/app.js`) perdent leur justification - voir leurs entrées ci-dessous, closes en pause sans suite. **Toutes les tâches de code sont closes.** Reste ouvert et normal : l'ajout de contenu (fiches Lexique, digests trimestriels, métiers), qui ne nécessite aucun chantier.

## [CLOS - 2026-09-18] 2e passage IA obligatoire pour « Découvrir mes compétences » (rédaction du CV)

Plan complet : `docs/CHANTIER_DECOUVERTE_2E_PASSAGE_REDACTION.md` (8 sous-étapes, un commit chacune, `npm test` + navigateur à chaque fois). Le module « Découvrir mes compétences » n'écrivait jusqu'ici jamais dans `dossier.ia.cv` (accroche/missions/points forts vides, CV « brut » sans rédaction) ; il reçoit désormais un vrai 2e passage assistant, dédié à la rédaction, distinct du 1er passage (extraction de compétences, `decouverte-competences.md`, inchangé).

- **[FAIT]** `prompts/decouverte-redaction.md` - repris de l'essentiel de `prompts/cv.md` (même schéma JSON de sortie à l'octet près), avec deux ajouts : correction silencieuse des fautes/incohérences, et le récit + les réponses aux questions ciblées traités comme matière factuelle **principale** (pas un simple complément, contrairement à un profil déjà structuré). Enregistré dans `FICHIERS_PROMPTS_EXTERNES`/`promptsExternesCharges` (`js/app.js`) sous la clé `decouverteRedaction`.
- **[FAIT]** `finaliserEtNaviguerVersResultats()` (`decouverteParcours.js`) scindée en `finaliserMappingDossier()` (fragments → dossier, garde-fou `decouverteDejaAppliquee`) et `terminerParcoursDecouverte()` (masquage + navigation vers `resultats`) - nécessaire pour insérer les nouvelles étapes entre les deux.
- **[FAIT]** Nouvelle étape technique 9 « Vos expériences » - dernière occasion d'ajouter/corriger une expérience avant la rédaction, réutilise l'éditeur d'expériences partagé (jamais `ouvrirFenetreExperiences()` elle-même, dont le rerender par défaut aurait affiché la mauvaise page).
- **[FAIT]** Nouvelles étapes 10 (« Choisissez votre assistant »), 11 (« Collez la réponse »), 12 (« Choisir ce qui ira sur le CV ») - réutilisent **tel quel** le pipeline déjà écrit pour « Créer un nouveau CV » (`contenuRectangleChoixIA`/`wireRectangleChoixIA`/`contenuAccordeonImportIA`/`wireImportIA`/`ouvrirRelectureIACV`/`analyserReponseIACV`/`creerBrouillonChoixIACV`/`appliquerBrouillonChoixIACV`, `js/app.js`), avec le pontage `etatAccordeon`/`etatAccordeonValide` = `accordeonPourType('cv')` documenté par le plan. `wireChoixAssistantIA`/`wireRectangleChoixIA` gagnent un `clePromptOverride` facultatif (comportement inchangé partout ailleurs) pour utiliser `decouverteRedaction` au lieu de `cv`.
- **[FAIT]** Barre de navigation du module : ancien repère « Mon CV » (aboutissement) renommé **« Rédiger mon CV »** (couvre désormais les étapes 9 à 12) ; nouveau repère final **« Mon CV »** ajouté (mise en page/export, déjà générique - `pageResultats()` elle-même, aucun nouvel écran).
- **Bug trouvé et corrigé pendant les tests navigateur** : `wireRectangleRelectureIA()` rappelle toujours son `rerender` après la validation - avec une navigation vers une autre étape dans ce `rerender`, la page « Mon CV » tout juste affichée par la finalisation du parcours était aussitôt écrasée par un nouveau rendu de l'écran de relecture. `rerender` devient un no-op volontaire pour ce consommateur (aucune autre situation ne l'appelle).
- Vérifié par plusieurs parcours Découverte complets au navigateur (récit → import → compétences → compléter → expériences → choix assistant, clé de prompt vérifiée par interception de `promptCache` → collage d'une réponse CV simulée → relecture → « Mon CV »), clair et sombre, aucune erreur console. Non-régression vérifiée sur « CV prêt » (clé `cv` par défaut toujours utilisée) et sur le raccourci `entretien-accueil` (pret/maj). `npm test` 931 verts tout du long. `docs/BRIQUES_COMMUNES.md` mis à jour (nouvelle brique « Pipeline rédaction IA du CV », Découverte ajoutée comme consommateur de l'éditeur d'expériences).
- **Hors code, non bloquant** : relecture du prompt sur de vrais essais (comme tout prompt de ce projet, jamais figé au premier jet) ; la page d'introduction du module dit encore « le seul échange avec un assistant en ligne se fait par copier-coller » - à corriger au prochain passage sur ces textes (devient deux échanges).

## [OUVERT - 2026-09-11] Audit robustesse, module par module

Ouvert une fois les `[À FAIRE]` de petite taille traités (ménage, dérives des pages d'intro). Méthode : relecture « œil neuf » en 3 angles par module (Règle 10, `docs/LECONS_A_NE_PAS_REPRODUIRE.md` section 10) - hypothèses devenues obsolètes, ruptures de parcours (retour en arrière, session interrompue, rechargement), incohérences entre les différents chemins d'un même module. Ordre choisi : modules les plus récents d'abord (jamais eu de vrai passage terrain), avant les plus anciens déjà rodés par plusieurs vagues de correction réelle.

- **[CLOS - 2026-09-11] ATS (« Les mots de votre CV »)** - `modules/ats/`. Prompt (`prompts/ats.md`) et parseur (`resultatParser.js`) revérifiés champ par champ : aucune dérive, les 9 placeholders et les clés JSON correspondent exactement. `detectionTexteCache.js` non modifié (heuristique déjà prudente, testée). 3 défauts trouvés et corrigés, les 3 angles couverts :
  - **[FAIT - `f33f6ba`]** Écran « Choisir l'assistant » : bouton fantôme `btnAtsVersChez` (« Continuer → ») resté d'une maquette, visible en production sous une légende « ce bouton est provisoire », redondant avec les pastilles d'assistant et forçant ChatGPT sans tenir compte du clic réel. Retiré (bouton + légende + câblage mort).
  - **[FAIT - `3fb7fe9`]** Écran « Chez l'assistant » : un retour en arrière depuis « Coller la réponse » (bouton Retour de la barre du bas) ramenait sur un écran **sans aucun bouton** - `_etatTransitionIA.phase` restait à `'revenu'` (posée au 1er clic sur « Je suis de retour »), phase que `htmlBanniereTransitionIA()` ne sait pas afficher. `_atsBrancherChezAssistant()` ramène maintenant cette phase à `'ouvert'` (état cohérent : l'assistant est déjà ouvert dans un autre onglet), au lieu de ne rien afficher.
  - **[FAIT - `662ad25`]** Incohérence entre modes : `sansOffre` (réservé au mode métier par `prompts/ats.md`) n'était pas revérifié côté code - si l'assistant le remplissait quand même en mode offre, l'écran Résultat affichait « Sans offre précise » alors que la personne avait bien fourni une offre. `parserResultatAts()` reçoit désormais le mode réel et force `sansOffre` à `null` s'il est fourni en mode offre. Opt-in, aucun appel de test existant modifié.
  - `npm test` 781 verts (+1 test dédié).
- **[CLOS - 2026-09-11] Regard recruteur** - `modules/regard-recruteur/`. Les 3 angles passés en revue, **aucun défaut trouvé** : le module a déjà les garde-fous qui manquaient à ATS avant correction.
  - `promptBuilder.js` / `rapportResponseParser.js` : les 8 placeholders et le schéma JSON correspondent exactement à `prompts/regard-recruteur.md`. `modeAnalyse` est **repris du contexte connu par l'application** (jamais de la réponse de l'assistant) - protège déjà contre l'incohérence de mode qu'ATS ne couvrait pas.
  - Retour en arrière « Chez l'assistant » : `RR_VUE_RETOUR['coller']` renvoie directement à `'choix-assistant'` (jamais à `'chez'`), donc aucun retour ne peut retomber sur une phase `'revenu'` périmée comme dans le bug ATS. En plus, `vueChezHTML()` affiche **son propre bouton** « Je suis de retour » indépendamment de `htmlBanniereTransitionIA()` - l'écran ne peut jamais se retrouver sans aucune action possible, y compris après une restauration disquette mi-parcours.
  - Aucun bouton ni texte de maquette resté en production (grep `provisoire`/`TODO`/`FIXME` : rien).
  - Prochain module : Comprendre le cadre.
- **[CLOS - 2026-09-11] Comprendre le cadre** - `modules/comprendre-le-cadre/`. Les 3 angles passés en revue, **aucun défaut trouvé**.
  - Chaîne de retour en arrière (`_comprendreLeCadreAllerVersPrecedent`, un cran fixe par écran) revérifiée point par point contre les transitions AVANT de chaque écran (`orienteur → recherche-prompt1 → recherche-sujets → recherche-questions → recherche-prompt2 → recherche-resultat`) : parfaitement symétrique, aucun écran orphelin.
  - Réponse d'assistant illisible (`_comprendreLeCadreParserReponse1` renvoie `null`) : dégradation propre, message honnête + réponse brute affichée, **pas de bouton « continuer » rendu** (pas de cul-de-sac, le seul chemin est « Revenir » pour réessayer) - même qualité que le correctif fait sur ATS, déjà en place ici.
  - **Point vérifié, PAS un défaut** : ce module est absent de `collecterEtatsModulesPourSauvegarde()` (js/app.js) - la recherche libre n'est jamais restaurée après un rechargement de session. Confirmé **décision volontaire et documentée** : `docs/CHANTIER_COMPRENDRE_LE_CADRE_RECHERCHE_LIBRE.md` ligne 83, « Option A : rien n'est gardé » (Denis - une réponse juridique/réglementaire ancienne ressortie telle quelle serait un risque réel). Message clair déjà affiché à la personne sur l'écran résultat.
  - Prochain module : Comparer mes pistes.
- **[CLOS - 2026-09-11] Comparer mes pistes** - `modules/comparer-pistes/` (2920 lignes, le plus gros des 4 modules récents). Angles navigation + parseurs passés en revue, **aucun défaut trouvé**.
  - Assistant de navigation `ecranSuivantLogique()` / `ecranPrecedentLogique()` (9 écrans, routage dynamique selon la forme de comparaison choisie et `anglesVus`) : vérifiés symétriques pour le cas `mixte` (le plus complexe, visite 2 angles). Le raccourci « écran 7 → écran 4 en un clic Retour » (saute les écrans 5/6 déjà vus) est cohérent avec le repère affiché (`_comparerNavIndex` regroupe les écrans 3 à 6 sous un seul repère « Comparer ») - pas une violation de la règle « un cran à la fois ».
  - 4 parseurs de réponse (`comparerParserDetection` / `Collecte` / `AllerPlusLoin` / `Frise`) : tous retournent `{ erreur }` sur une entrée vide, illisible ou incomplète, jamais un `throw`.
  - Pont disquette déjà branché (`comparerPistesExporterEtatPourSauvegarde` / `RestaurerEtatDepuisSauvegarde`), déjà dans `collecterEtatsModulesPourSauvegarde()`.
  - **Profondeur de revue plus limitée que les 3 modules précédents** (module 2x plus gros que le suivant) : les mécanismes à plus haut risque (navigation, parseurs, sauvegarde) ont été vérifiés, pas une lecture ligne à ligne intégrale des 2920 lignes.

**Bilan du 1er tour (les 4 modules jamais passés au terrain)** : 3 défauts trouvés et corrigés, tous sur ATS. `npm test` 781 verts.

**[CLOS - 2026-09-11] 2e tour, modules plus anciens.** Denis a confirmé l'enchaînement. 8 périmètres revus en parallèle ("parcours CV" scindé en 3 vu sa taille). `npm test` 781 verts après chaque correctif, vérifié navigateur pour les points touchant l'UI.

- **Bilan** - aucun défaut réel trouvé. Confirmé de première main (lecture directe du code) que le patron de bug ATS (écran vide après retour) ne s'y reproduit pas. Revue ciblée sur les mécanismes à risque (code dispersé dans `js/app.js`, ~75 fonctions, trop volumineux pour une lecture exhaustive) - écrans Candidature/Clôture/rapport et mode sombre pas couverts si un passage plus approfondi est souhaité un jour.
- **Carnet** - aucun défaut réel trouvé.
- **Mes Repères** (hors Regard extérieur, déjà audité) - aucun défaut réel trouvé. Un résidu CSS mort signalé (`.reperes-picker-annuler`, jamais utilisé), laissé tel quel (impact nul).
- **Cohérence transversale** - **[FAIT - `0cc867f`]** Plusieurs couleurs codées en dur au lieu des variables CSS déjà theme-aware : le badge "À enrichir" et le bandeau "Incohérences mineures" étaient illisibles en mode sombre (fond quasi blanc sur texte sombre) pour un cas très courant (au moins un constat de ce type dans le rapport). Corrigé, ainsi que 4 autres points du même ordre (badge "Facultative", bouton "Changer ce document", bordures de cartes). Les panneaux d'aperçu document (toujours blancs, papier imprimable) ne sont pas concernés, déjà une convention actée.
- **Lexique** - **[FAIT - `c8feee0`]** Une collection/parcours ouverte directement depuis une carte thème de l'accueil renvoyait, via son bouton Retour, vers l'écran "Collections et parcours" - jamais visité dans ce chemin. Même mécanisme de retour personnalisé que la Bibliothèque (`_lexiqueBibliothequeRetour`), répliqué pour les collections. + un tiret cadratin corrigé. Signalé mais non corrigé (piège latent, jamais déclenché aujourd'hui, tous les appelants gardent l'id avant d'appeler ces fonctions) : `_lexiqueRenduFiche`/`_lexiqueRenduCollection` retournent un `<p>` sans navigation si jamais un id invalide leur parvenait.
- **Composeur CV** - **[FAIT - `56a61d0`]** Collision de numéro de règle : `composeurRegles.js` et `composeurStrategies.js` déclaraient chacun une règle "R005" différente (échelle de police vs sélection de stratégie) - aucun effet utilisateur aujourd'hui, mais aurait rendu trompeur un futur système d'explication des choix (contrat §7.3). Renuméroté en R007. + le bandeau de disponibilité (Projet XXL) lisait les langues non plafonnées au lieu de la liste déjà plafonnée par le thème - une personne avec beaucoup de langues voyait ce bandeau déborder.
- **Éditeur/export Word CV** (cv-editor hors bibliothèques tierces, cv-core, cv-mise-en-page) - **aucun correctif : pipeline confirmé mort.** L'app route désormais tout export Word CV via le Composeur (`modelesDisponibles.js` ne liste plus que 'composeur'). Les 16 anciens modèles classiques + Chic + Nouveaux modèles + la coloration + `rendreTemplate.js` (déjà soupçonné) sont du code **structurellement inatteignable**, confirmé par traçage précis des appels. Des défauts réels y ont été trouvés (perte silencieuse de la rubrique "savoirs" sur 3 modèles, régression du bug `libelleFormation` sur un générateur) mais **non actionnés** : corriger du code mort n'a aucun effet, à garder en mémoire seulement si ce pipeline est un jour réactivé.
- **Export HTML/PDF CV** (cv-pdf-html) - **[FAIT - `075ddf6`]** Le bouton "Réinitialiser" du panneau de réglages effaçait tout le travail de mise en forme manuelle sans confirmation ni retour arrière, contrairement au bouton "Style aléatoire" voisin qui propose déjà "Annuler" - risque réel de perte de travail perçue comme définitive pour ce public. Même filet appliqué (mécanisme réutilisé, pas dupliqué). + le Mini CV A5 ne reprenait jamais l'expérience personnelle (Projet XXL) contrairement à l'A4 (déjà corrigé plus tôt) - disparaissait silencieusement sur ce seul format. Fusionnée dans la rubrique "engagements", le renderer A5 sait déjà lire cette forme.

**Dette confirmée/étendue à cette occasion** (hors périmètre d'un correctif ciblé, à traiter dans un passage dédié) : la dette déjà signalée sur `rendreTemplate()` (rattrapage du 1er tour) est confirmée pour cv-editor - tout le pipeline des anciens modèles Word CV est mort, pas seulement `rendreTemplate()` isolément. Nettoyage complet (suppression des fichiers + mise à jour de `BRIQUES_COMMUNES.md`) à faire ensemble, pas au fil de l'eau.

**[CLOS - 2026-09-11] Rattrapage du 1er tour : 5 modules oubliés.** En vérifiant la liste réelle de `modules/` contre les 2 tours documentés (à la demande de Denis, avant d'enchaîner sur le 2e tour), 5 modules actifs et routés ne figuraient dans aucune des deux listes. Décision de Denis : les passer d'abord par la méthode du 1er tour (3 angles + doublons inter-modules). Revue faite sur les 5 en parallèle, `npm test` 781 verts après chaque correctif, vérifié navigateur pour les points touchant l'UI.

- **Préparer un entretien** (`modules/entretien-editor/`) - **[FAIT - `4ec615b`]** Bug réel : `questionsAnticipees` est un objet `{question, pistes, amorce}` depuis un chantier antérieur, mais l'export "Texte à copier" affichait `[object Object]` pour chaque question - le correctif existait déjà, mais dans un fichier jumeau **jamais chargé** (`modules/entretien-editor/exportBlocsTexteLettreEntretien.js` au lieu de `modules/cv-editor/exportBlocsTexteLettreEntretien.js`, seul chargé par `index.html`). Corrigé au bon endroit, copie orpheline supprimée.
- **Co-construire ma lettre** (`modules/lettre-editor/` + `modules/lettre-core/`) - **[FAIT - `f385c39`]** Même classe de bug : `normaliserDonneesLettre()` avait 2 copies, seule celle de `lettre-core` (chargée) manquait le champ `texteCourt` - la version courte rédigée par l'assistant n'était donc jamais utilisée à l'export Word "A4 Essentiel"/"A5" (toujours une troncature brute à la place), alors que `docs/CHANTIER_L1_CO_LETTRE_MOTIVATION.md` affirme cette fonction livrée. Champ ajouté au fichier réel, 2 copies mortes supprimées (dont un doublon strict sans lien avec ce bug, `cv-editor/modelesDisponiblesLettre.js`).
- **Regard extérieur** (`modules/regard-exterieur/`) - **[FAIT - `4542a47`]** 2 défauts mineurs : le « Qui voir » personnalisé d'un rapport « Deuxième Regard » disparaissait selon l'écran de consultation (paramètre manquant sur un appel) ; le bouton d'aide de l'écran de questions pointait un sélecteur CSS qui n'existe sur aucun élément réel (clic sans effet, sans erreur). Les deux corrigés.
- **Comprendre les chiffres** - **[FAIT - `4542a47`]** Texte obsolète : l'aide guidée de *Comprendre le cadre* présentait encore « Comprendre les chiffres » comme un de ses outils internes, alors qu'il est un module séparé depuis le 2026-09-06. Corrigé. Reste (non bloquant, signalé par l'audit) : une contradiction de documentation interne entre `ARCHITECTURE_TECHNIQUE.md` et son propre statut CLOS, et une fonction `_comprendreLesChiffresRenduAVenir()` jamais appelée (code mort sans impact) - à nettoyer au prochain passage, pas urgent.
- **Découverte de mes compétences** - **[FAIT - `399d4e4`]** Incohérence trouvée entre les 2 paliers voisins du même onglet (l'année, rendue obligatoire pour les formations plus tôt dans la session, ne l'était pas pour les certifications) + un tiret cadratin en production. Les deux corrigés. Le reste du module confirmé sain : le patron de bug ATS (écran vide après reprise) ne s'y reproduit pas (déjà couvert par le patron "rond bleu"), aucun doublon inter-modules.
  - **Signalé, pas corrigé (mineur, décision consciente à l'origine)** : `infos.formation.actif` est délibérément préservé après transmission (correctif d'une régression réelle passée, qui masquait les paliers Certifications/Formations), mais `infos.engagement.actif`/`infos.savoirFairePerso.actif` sont remis à `null` - un retour sur l'étape 8 via "Modifier" réaffiche Oui/Non comme jamais répondu pour ces 2 rubriques (l'élément déjà ajouté reste visible et correct, aucune perte de donnée). Cosmétique, laissé tel quel pour ne pas retoucher une zone déjà corrigée après une régression réelle sans demande explicite de Denis.

**Dette plus large signalée, hors périmètre de ce rattrapage (à examiner séparément)** : `rendreTemplate()` (`modules/cv-editor/rendreTemplate.js`) et les fichiers `template.html`/`style.css` qu'il consommait (cv-editor, lettre-editor, entretien-editor) semblent être du code mort dans son ensemble - la génération de document passe désormais entièrement par le DOCX natif (`apercuDocxIntegre.js`). À confirmer et nettoyer dans un passage dédié, pas décidé à la légère pendant un audit de correctifs ciblés.

**[FAIT - 2026-09-11] Balayage ciblé** (moins cher qu'un 2e tour complet) : les 2 défauts trouvés sur ATS sont des *patrons* de bug, pas des cas isolés - vérifié si les autres modules qui partagent le même mécanisme (`_etatTransitionIA` / `htmlBanniereTransitionIA()`) y sont exposés.
- **Bouton de maquette resté en production** : grep sur tout `js/app.js` / `data/metiers.js` / `modules/` (« ce bouton est provisoire », « bouton provisoire », « test du parcours en maquette »...) - **rien trouvé ailleurs**. ATS était un cas isolé.
- **Écran vide sur un retour arrière (phase `'revenu'` non gérée)** : tous les autres consommateurs de `_etatTransitionIA` (page Action, Bilan, les 4 parcours CV de `data/metiers.js`, Regard extérieur, Découverte, Cohérence transversale, Regard recruteur) utilisent soit le patron « rond bleu » (bouton de collage toujours présent sur le même écran, activé/désactivé selon la phase, `phase === 'revenu'` explicitement géré), soit un écran « coller la réponse » où le bouton Importer est **toujours rendu**, indépendamment du bandeau de transition. **Aucun ne reproduit le défaut d'ATS** - ATS avait introduit son propre écran « chez l'assistant » séparé, reposant uniquement sur le bandeau, un patron différent (et plus fragile) que celui déjà en place partout ailleurs.

Conclusion : la classe de bug trouvée sur ATS ne se reproduit nulle part ailleurs dans l'app. Le 2e tour complet (relecture module par module) reste en réserve mais n'est plus urgent au vu de ce balayage.

- [CLOS] (vérifié le 2026-08-27) 1. Cohérence transversale CV / lettre / entretien (nom UI : "Cohérence de mon dossier"). Cette ligne était une copie périmée de `docs/ETAT_DES_CHANTIERS_2026-08-24.md`, écrite la veille de la création du module. Le module `modules/coherence-transversale/` a été construit et vérifié en direct dans le navigateur le 2026-08-25 (591/591 tests). Chaîne complète confirmée dans le vrai code : `hostDataAdapter.js` lit la lettre (`lireLettreTexte()` -> `dossier.ia.lettre.lettre.texte`) ET l'entretien (`lireEntretienTexte()`, texte recomposé depuis `dossier.ia.entretien`) ; `dossierTransversalCollector.js` les fusionne ; `verificationsDeterministes.js` compare CV/lettre mot pour mot ; `promptBuilder.js` injecte `{LETTRE}` + `{PREPARATION_ENTRETIEN_OU_NON_FOURNIE}` ; `prompts/coherence-transversale.md` analyse explicitement les paires CV x lettre x entretien ; `ui.js` affiche une carte par document et applique les corrections. Rien à coder. (Détail dans la mémoire assistant `chantier-coherence-transversale`.)
- [CLOS - 2026-09-11, en pause sans suite] 2. Accessibilité (+ socle typographique commun) - navigation clavier, lecteurs d'écran, contrastes. Vérifié à zéro le 2026-08-24 (0 `aria-live`, 0 skip-link, 0 `role="navigation"`). **Décision de Denis (2026-09-11)** : le projet n'a plus vocation à grandir (voir ci-dessous, point 3) - cette idée était un "petit plus" que Denis voulait apporter, **jamais une demande réelle remontée du terrain**. Sans besoin concret signalé, pas de raison de lancer un chantier de conception aussi lourd (plusieurs sous-parties non cadrées : mode accompagné/autonome, option non-francophone, version allégée des fenêtres, socle typographique, langage visuel commun). **À rouvrir seulement si un vrai besoin d'accessibilité remonte un jour du terrain** - dans ce cas, probablement à traiter à une échelle plus ciblée que les 6 sous-parties listées ici, pas tout d'un coup.
- [CLOS - 2026-09-11, en pause sans suite] 3. Découpage de `js/app.js` - 28941 lignes au 2026-08-24 (grossit d'environ 4200 lignes/mois), aucun bundler. **Décision de Denis (2026-09-11)** : le projet est désormais en mode entretien, pas en croissance - la seule évolution prévue est du contenu (Lexique, Comprendre les chiffres, Comprendre le cadre, éventuellement `data/metiers.js`), qui ne touche jamais `js/app.js`. Le bénéfice du découpage (rendre les futurs chantiers de fonctionnalité plus sûrs/rapides) ne se matérialise que s'il y a de futurs chantiers de fonctionnalité - ce n'est plus le cas. Le risque (casser une app déjà utilisée en vrai chez le partenaire Inkéo/PCGI87, pour un fichier qui n'a de toute façon plus vocation à recevoir de nouvelles fonctions) ne se justifie plus. **Nettoyage du code mort envisagé en alternative, tenté puis abandonné le même jour** : l'ancien pipeline Word CV (16 modèles classiques + Chic + Nouveaux modèles + coloration, confirmé inatteignable lors de l'audit robustesse) s'est révélé, à la vérification symbole par symbole avant suppression, **entremêlé avec du code encore réellement utilisé** dans les mêmes fichiers - `chargerLibrairieDocxNatif()` (réutilisée par le Composeur, le Mini CV A5, ET l'export Word de la lettre/l'entretien), les palettes de couleurs (`PALETTES_COULEURS_CV_BASE`/`obtenirPalettesCouleursCV()`, réutilisées par le sélecteur de couleurs du Composeur), et `MODELES_AVEC_DOCX_NATIF_CV` (le Composeur s'y ajoute lui-même plutôt que d'avoir sa propre liste). Une suppression brute aurait cassé le sélecteur de couleurs et potentiellement tout l'export Word natif. Un nettoyage sûr demanderait d'abord d'extraire ces éléments encore vivants dans un fichier à part, puis de supprimer le reste - un vrai refactor, pas un simple nettoyage, écarté pour la même raison que le découpage. **À rouvrir seulement si un besoin concret l'exige.**
  - **Rattaché ici (Denis 2026-09-04) : résorber la dette B.7 de `BRIQUES_COMMUNES.md`** (chantier « La mise en page » du CV). En clair : (1) extraire en fonctions autonomes les 5-6 macros Word encloses dans la fermeture de `construirePaletteCouleurs()` (`js/app.js` ~29995→32351) — Sobre (`btnSobreXXL`), Créatif (`btnCreatifXXL`), dé (`btnProposerModeleXXL` / `tirerModeleAleatoireXXL`), Optimisé (`btnCvOptimiseXXL`), Pipette (`btnPipetteLibreXXL`), Mise en page (`btnMiseEnFormeUltimeXXL`) ; (2) rebrancher `_mepClicMoteur()` (`js/app.js` ~14321) sur ces fonctions au lieu de `.click()` sur les boutons cachés ; (3) supprimer `construirePaletteCouleurs` (~2 345 l.) + la branche `colonneGauche` du CV (rendue masquée `.mep-ancien-gauche`, `js/app.js` ~14071) + les résidus `personnalisationOuverte` / `personnalisationEnGrandApercu` / `construireBoutonsFormatPage` si orphelins. **Zéro régression fonctionnelle Word obligatoire** : Allure / dé / Optimisé / Pipette / Mise en page pilotent le rendu Word via ces macros. Vérif navigateur Word ET PDF sur `nouveau` / `maj` / `pret`. Détail du verrou : dette B.7 + `docs/CADRAGE_MISE_EN_PAGE_2026-09-03.md` (ligne « Reste (hors chantier) »).
- [CLOS - 2026-09-08] 4. ATS, renommé **« Les mots de votre CV (ATS) »** (jamais un score : comparaison de vocabulaire CV / offre ou fiche métier ; « (ATS) » visible à côté du nom sur la présentation seulement, message partout « pas un logiciel de tri, mais la même vérification »). Conception : `docs/CHANTIER_ATS.md`, maquette `docs/MAQUETTE_ATS_PARCOURS_2026-09-03.html`, plan `docs/PLAN_ATS_2026-09-03.md`. Construit en sous-étapes committées (1610c27..b811782) : prompt dédié `prompts/ats.md` (mis au point sur 6 tests réels, passe ChatGPT + Gemini ; relu par Denis le 2026-09-08, 3 retouches appliquées) + fixtures `docs/tests-manuels-ats/` ; `modules/ats/` (index.js page routée famille 2 + detectionTexteCache.js + resultatParser.js, ~30 tests) ; fiche Lexique `ats` trouvable ; route `ats` + intro activée + tuile d'accueil « Outils d'analyse ». `npm test` 780 verts, checkLexique propre. Vérifié navigateur bout en bout (offre + métier, reprise/gel, bouton Retour sans boucle présentation / module, disquette, clair + sombre, mobile). `BRIQUES_COMMUNES.md` MAJ (consommateur, Historique 2026-09-08). **Reste hors code** (non bloquant) : `AIDE_PAGES.ats` (aide contextuelle par écran, que ses modules frères n'ont pas non plus, optionnel) + vidéo de démonstration + test terrain.
- [CLOS - 2026-09-08] 5. **Regard recruteur** — **CHANTIER TERMINÉ.** (fusion Simulation recruteur + Préparation aux inquiétudes + ce qui est récupérable de Recruteur virtuel). **Uniquement du conseil : ne modifie jamais le CV** (décision Denis, clôture option A). Conception : `docs/CHANTIER_REGARD_RECRUTEUR.md`, schéma de sortie figé `docs/CHANTIER_REGARD_RECRUTEUR_SCHEMA_SORTIE_2026-09-03.md`, maquette (cible visuelle) `docs/MAQUETTE_REGARD_RECRUTEUR_PARCOURS_2026-09-03.html`, protocole de test du prompt `docs/PROTOCOLE_TEST_PROMPT_REGARD_RECRUTEUR_2026-09-03.md`. Construit et vérifié en sous-étapes committées (`13c2de1..a7107c8`) : `modules/regard-recruteur/` (`rapportResponseParser.js` autonome 18 tests + `promptBuilder.js` autonome 11 tests + `index.js` page routée famille 2, 8 écrans, 9 tests helpers + `regard-recruteur.css` + `ARCHITECTURE_TECHNIQUE.md`), prompt `prompts/regard-recruteur.md` (relu le 2026-09-08, 5 retouches croisées parser/schéma). Route `regard-recruteur` (garde `typeof`) + `pageIntroRegardRecruteur` activée + détour/reprise famille 2 + trouvable dans la recherche d'accueil. `npm test` 780 verts. Vérifié navigateur bout en bout, clair + sombre : 8 écrans, mode texte (4 axes au lieu de 6), **masquage image = outil de rectangles dans le navigateur** (`htmlVerificationDocument({mode:'image'})`, décision 4 tranchée par Denis 2026-09-08 : canvas, dessiner/enlever, une image à la fois, « Enregistrer » télécharge la copie masquée), réponse hors format → écran « réponse illisible », détour « Revoir la présentation », encart « Continuer / Recommencer » + gel + pulse à la reprise, aller-retour disquette, bouton Retour un cran à la fois sans boucle présentation ⟷ module. 4 corrections post-livraison sur retours Denis (ciblage persistant, scroll en haut, boucle du Retour, outil de rectangles). `BRIQUES_COMMUNES.md` MAJ (consommateur, Historique 2026-09-08). **Hors code, comme tout module** (non bloquant) : relecture finale du prompt par Denis + protocole de test tour 1 sur de vrais CV ; parcours cliqué par Denis ; vidéo de démonstration ; test terrain ; `AIDE_PAGES` par écran (optionnel, comme les modules frères). **Évolutions possibles** (`ARCHITECTURE_TECHNIQUE.md`, pas des manques) : vignette des images ; fusion des deux zones « Garder comme Repère » ; export Word natif ; renvois vers d'autres modules en clôture (chantier « Cohérence entre les modules »). **Chantier séparé, plus tard** : extraction du socle commun détachable (« Niveau 2 »), quand `js/app.js` est libre.
- [CLOS - 2026-09-11] 6. Refonte du panneau Découverte "Informations complémentaires" / Renommage de l'application en "APP" - **tâche périmée dans son ensemble, close sans suite.**
  - **Volet "refonte" : périmé, déjà fait sans que ce soit tracé comme tel.** La note d'origine (25 juillet 2026) disait le panneau "pas clair visuellement". Vérifié au `git log` le 2026-09-11 : le commit `97570be` du **27 juillet 2026** (2 jours plus tard) s'intitule littéralement *"Refond de l'étape Informations complémentaires du parcours Découverte"*, suivi de 48 commits de polissage continu jusqu'à aujourd'hui (barre d'étapes visuelle, aide contextuelle, système de paliers Continuer/Modifier cohérent...). Le seul point technique concret resté ouvert par la note d'origine (duplication du mécanisme Formation) est fermé ci-dessous.
  - **Volet "renommage APP" : pas une tâche de code.** Précision de Denis le 2026-09-11 : renommer le dépôt/projet sur GitHub (ancien nom -> nouveau) est une action que Denis fait lui-même, hors de tout code. Rien à coder ici. Si "APP" doit un jour apparaître dans l'interface elle-même (titre, logo), ce sera une demande explicite et distincte, pas la continuation de cette ligne.
  - **[Investigué en profondeur le 2026-09-11, à la demande de Denis, avant tout code]** Le volet « Formation » du panneau n'est plus aussi périmé que la note d'origine (juillet) le laissait penser : le mécanisme « Niveau d'études » a **déjà** été aligné sur celui de « Mon projet » (mêmes briques `TYPES_FORMATION_MON_PROJET` / `NIVEAUX_RNCP_TITRE_CQP` / missions). **Seul le palier « Formations » (recherche dans `CATALOGUE_FORMATIONS_COURANTES`) reste un mécanisme séparé**, qui écrit directement dans `dossier.formations` avec un schéma plus pauvre (`niveau: ''`, `missions: ''` toujours vides). Ce catalogue a une vraie valeur d'accessibilité (recherche par secteur, noms reconnaissables) pour un public qui ne sait pas toujours situer son diplôme sur une échelle RNCP - **à ne pas supprimer**, contrairement à l'idée de fusion brutale envisagée en juillet.
  - **[FAIT - 2026-09-11, commit `d786521`, étape 1/2] Défaut réel trouvé et corrigé** : une formation ajoutée via le catalogue (niveau vide) partait avec un tiret parasite en tête (« - CAP Cuisine ») - pas seulement à l'écran, mais **dans le CV Word téléchargé et dans tous les prompts envoyés à l'assistant** (13 endroits touchés, pas 2-3 comme estimé au départ). Fonction partagée `libelleFormation(f)` créée (`js/app.js`), les 13 appelants corrigés dans le même commit. Risque nul (changement d'affichage seul, aucun mécanisme de saisie touché). `npm test` 781 verts, vérifié navigateur.
  - **[FAIT - 2026-09-11, commit `1f842fa`]** Année rendue obligatoire pour ajouter une formation via le catalogue (jusque-là, seul l'intitulé était vérifié - incohérent avec le palier « Niveau d'études » qui exige niveau + intitulé + année). Message d'erreur dédié si l'année manque. Vérifié navigateur : sans année, rien n'est ajouté ; avec année, ajout correct.
  - **[FAIT - 2026-09-11, commit `b9bb260`] Demande de Denis : « pourquoi formation apparaît une fenêtre à part ? »** Le bouton « Parcourir la liste » ouvrait `ouvrirFenetreCatalogue()` dans une fenêtre séparée - Denis voulait la liste directement dans la page, sur le modèle de « Secteur le plus proche » (déjà en ligne, sans fenêtre). Remplacé par un bloc toujours présent dans le DOM (`catalogueFormationsInline`, masqué par défaut), ouvert/fermé par un bouton bascule ; un clic sur un nom remplit le champ et referme la liste. Un **deuxième** point d'ouverture de l'ancienne fenêtre a été trouvé et corrigé au passage (la réponse « Oui » à « Avez-vous suivi une formation à mentionner ? » ouvrait aussi la fenêtre - elle affiche maintenant le bloc en ligne). `ouvrirFenetreCatalogue()` non touché : toujours utilisé par Certifications, Loisirs, Engagements, Expériences perso. `npm test` 781 verts, vérifié navigateur bout en bout, 0 erreur console.
  - **[FAIT - 2026-09-11, commit `d732290`] Retour de Denis sur le bloc précédent : « pas des pastilles qui remplissent la page ».** Le bloc en ligne du point précédent (28 exemples sur 7 secteurs) restait bien trop lourd visuellement une fois déplié. Remplacé par un menu déroulant natif (`<select>` avec un `<optgroup>` par secteur), une seule ligne au repos. Le bouton bascule « Voir/Masquer les exemples » disparaît (plus rien à ouvrir/fermer, le menu est toujours là). Choisir un exemple remplit le champ et le menu revient à son état par défaut. `npm test` 781 verts, vérifié navigateur bout en bout, 0 erreur console.
  - **[FAIT - 2026-09-11, commit `ca059c9`] Retour de Denis : deux boutons "Continuer" actifs en même temps sur le dernier palier (Formations)**, une fois rempli - celui du palier (repliait juste la carte) et celui de l'onglet (faisait vraiment avancer vers Engagement). Redondant uniquement sur ce dernier palier : Niveau d'études et Certifications gardent leur bouton local, qui reste nécessaire (il déverrouille le palier suivant). `enveloppePalier()` accepte un flag `sansBoutonContinuer` (utilisé seulement pour "catalogue") ; le Continuer global replie désormais lui-même la carte Formations. Vérifié navigateur : un seul bouton actif à la fois, "Modifier" affiche la carte repliée normalement ensuite.
  - **[DÉCISION DENIS - 2026-09-11] Étape 2 (la vraie fusion) : abandonnée, pas un [À TRANCHER] en réserve.** L'idée était de faire du catalogue un outil de recherche pour préremplir le même formulaire structuré (type/niveau/missions) au lieu d'un mécanisme séparé qui écrit directement. Décision de Denis : **on ne fait pas cette fusion** - touche trop de fonctions pour un bénéfice incertain, à revoir seulement si un vrai besoin remonte du terrain. **Ce sous-point du chantier 6 est clos** (le volet Formation du panneau Découverte reste tel quel, avec ses 3 correctifs déjà faits ci-dessus : tiret parasite, année obligatoire, catalogue en ligne, menu déroulant, un seul Continuer).
  - **Précision de Denis le 2026-08-28 sur le nom** : l'application ne s'appelle plus "ERIP". "ERIP" est le nom d'un **dispositif** ; c'est pendant un stage dans ce dispositif que Denis a eu l'idée de l'application, d'abord construite pour ce stage, puis élargie et renommée. Le nom retenu est **APP**, sigle à double lecture assumée : **Accompagnement de Parcours Professionnel** (sens principal) et **Analyse de Pratiques Professionnelles** (clin d'œil, sujet d'une présentation d'une heure que Denis a faite en formation). Ne rien renommer dans le code / les textes visibles / les fichiers `docs/*_ERIP_*` sans que Denis ait tranché OÙ et COMMENT "APP" apparaît (titre, logo, interface). Les documents dont le nom contient "ERIP" (Constitution, Contrat d'ancrage, Audit stratégique, Feuille de route...) restent valables tels quels, à repasser plus tard dans ce chantier.

## [CONSTRUIT - 2026-09-07, test terrain en attente] Module « Reformuler et présenter mon CV » (4e tuile de « Mes documents »)

**Construit et vérifié au navigateur, étape par étape (commits `8692dff` → `a3b1b7c`). Reste : test terrain (vrais CV, vrais assistants).** Cadrage et journal complet : `docs/CHANTIER_REFORMULER_ET_PRESENTER_CV.md`. Prompt : `prompts/reformuler-cv.md`.

- **Besoin** : un CV complet et à jour mais mal présenté. On met à jour le **langage** et le **vocabulaire attendu** (poste, entreprise, secteur), **sans rien ajouter ni corriger sur le fond** (chiffres, dates, contenu : d'autres modules font ça). **Aucune question, aucune précision à saisir** (décision Denis 2026-09-07, revient sur l'idée des marqueurs des premières heures).
- **4e tuile** (« Reformuler et présenter mon CV », `bi-brush`, 2e position dans `MES_DOCUMENTS_PARCOURS`), réutilise le tuyau de « Mettre à jour mon CV » via `_prepLEMode = 'reformuler'`. Aiguillage des 4 parcours dans la bulle « i » de la carte.
- **Parcours, ZÉRO fenêtre modale** (exigence Denis, répétée) : tuile → intro → page dépliante « Préparer » (dépôt CV + relecture + **poste/domaine visé rendu nécessaire**) → **une seule page « Échange »** (choix de l'assistant + collage de la réponse, deux blocs dépliants, pas même la fenêtre « Avant de continuer vers X ») → **un seul aller-retour**, l'assistant renvoie **deux versions dans la même réponse** (« la plus proche de votre CV » / « notre version recommandée » + `[PISTE]`) → écran « Vérifier » (5a : choisir ; 5b : résumé « ce qui a changé », `[ORIGINE]` visibles, « Modifier le texte » = édition libre) → **deux sorties** : « Récupérer le texte » (à copier pour Canva/ailleurs) et « Remplir un de nos modèles » (**structuration déterministe** du format contrôlé du prompt, sans assistant ni fenêtre, puis `naviguerVers('objectif')` = pipeline « Mettre à jour »).
- **Garde-fous du prompt** : rien d'ajouté, rien de supprimé, sens et niveau de langue conservés. Compétences rapprochées du vocabulaire de l'offre si fournie ; `[ORIGINE : mots du CV]` après chaque intitulé reformulé ; jamais de montée en niveau. `[PAS_UN_CV : ...]` si le texte reçu n'est pas un CV. Le socle B.8 (recherche web) ne s'applique pas.
- **Recherche d'accueil** branchée (`MODULES_RECHERCHABLES`). Lexique : rien à ajouter. **`BRIQUES_COMMUNES.md`** : dette **B.10** (page unique « choix assistant + collage », à extraire si un 2ᵉ module la réutilise).
- **Limite assumée** : structuration « modèle » best effort ; si l'assistant s'écarte du format, la personne corrige à l'écran suivant. L'identité (jamais transmise à l'assistant) se saisit dans « Vos informations ».

## [CLOS - 2026-08-30] Module « Analyser ma candidature » : chantier consolidation

**TOUT est fait.** Le module « Analyser ma candidature » est terminé, plus rien en attente (même pas d'optionnel). Historique complet : `docs/CONSOLIDATION_BILAN_PLAN_2026-08-29.md` + `docs/CONSOLIDATION_BILAN_RETOURS_2026-08-30.md`.

- Vagues 1-2, 3.6, 3.2, 3.5, **3.3 « Préparer »** (P1-P4), **modernisation « Choisir l'assistant » M1/M2 + « Coller la réponse » M3/M3+** + bloc 1.5 + bouton de collage rectangulaire (M4).
- **Lot 3** refonte visuelle du rapport (3a-3h), **Lot 4** « On organise votre CV », **Lot 5** restyle léger `blocEditionCV`.
- **R1-R17** (R1 et R9 fermés en fin d'audit), **D11**, point D (fusion Prompt 1 → fermé définitivement), point F.
- **D5a** (`fb9346f`) : question « Continuer / Recommencer » à chaque entrée quand un travail est en cours.
- **D5d** (`0d05ce3`) : « Retour » d'un écran de travail → page de présentation → accueil (drapeau `_etatBilan.voirIntro`).
- **D5b** (`d38070b`) : plus de boîte de dialogue ; on atterrit sur la page de présentation (mode détour) avec bandeau « Vous avez une analyse en cours » + boutons « Continuer mon analyse » / « Recommencer à zéro » (confirmation courte + `bilanRecommencerAnalyse()`).
- **D5c** (`e9bdf12`) : bouton permanent « ⓘ Revoir la présentation » en haut à droite de chaque écran de travail (`bilanBoutonRevoirPresentation()`), non destructif.

**Audit 2026-08-30 : `3.1` et `3.4` étaient DÉJÀ FAITS** dans une session antérieure — le plan doc ne l'avait jamais acté, ce qui a fait croire pendant des semaines qu'un gros chantier d'architecture restait. `_etatAgirBilan` / `_etatCorrectionBilan` / `_etatAssistanceBilan` = 0 occurrence ; `htmlBilanAgirChoix` retiré, cartes dans `htmlBilanRapport`. `2.3` = tranché non (D7). **Rien de tout ça n'était à faire.**

**Point E** (case « important pour plus tard » inter-module) : reporté par Denis jusqu'à la refonte de la page d'accueil (voir [[project_sequencement_modules_accessibilite_decoupage]]).

## [CLOS - 2026-09-06] Module « Comprendre le cadre » (carte « Se tenir informé ») : vérification, migration, recherche d'accueil, recherche libre

Module construit sur un autre compte (voir la mémoire `project_se_tenir_informe_comprendre_le_cadre` et `modules/comprendre-le-cadre/ARCHITECTURE_TECHNIQUE.md`). Travail de cette série de sessions (2026-09-05 / 2026-09-06), sous délégation complète de Denis (« tout est en mode automatique », « tu peux y aller ») :

- [CLOS - 2026-09-06] **Les 14 rayons (~86 fiches) vérifiés sur les sources officielles** (WebFetch / WebSearch, contournement du cutoff de connaissances), corrigés, puis **migrés** de `docs/CONTENU_COMPRENDRE_LE_CADRE/contenu/<rayon>/` vers `modules/comprendre-le-cadre/contenu/<rayon>/`, un rayon = un commit, test navigateur à chaque fois. `docs/CONTENU_COMPRENDRE_LE_CADRE/` supprimé. Ordre : justice, juridique, jeunes, handicap, budget, accompagnement, emploi, former, logement, garde, mobilité, francais, etranger, creer.
  - 2 sources fausses corrigées dans `liens-verifies.txt` : `aide-alimentaire` pointait vers une page chômage (repointé `solidarites.gouv.fr`), `115-siao` vers la page APL (repointé DIHAL). Voir [[LECONS 9.32]].
  - Incident : `git mv` + éditions non indexées = 5 fichiers committés avec l'ancien contenu, rattrapage `641e5ef`. Voir [[LECONS 9.31]].
- [CLOS - 2026-09-06 (cc387eb)] **8 fiches recommandées écrites** par le circuit veille : `budget-saisie-sur-salaire`, `emploi-licenciement`, `emploi-demission`, `former-financer-sa-formation-demandeur-emploi`, `jeunes-contrat-apprentissage`, `logement-expulsion`, `logement-demande-de-logement-social`, `handicap-aah-et-travail`.
- [CLOS - 2026-09-06 (c5a8794)] Paragraphe « garde périscolaire / mercredis » ajouté au rayon Garde ; fiche « régularisation par le travail » ajoutée au rayon Venir de l'étranger (option 2 choisie par Denis : fiche dédiée, garde-fou renforcé — interroge le besoin, jamais la situation).
- [CLOS - 2026-09-06 (68aabfc)] **Paquet A des adresses** : toutes les sources accessibles ouvertes et datées `2026-09-06` dans `liens-verifies.txt` (donc liens cliquables, LECONS 9.16). Restent pour Denis au navigateur (paquet B) : ~20 adresses sur des sites qui bloquent les robots (Banque de France, CAF, travail-emploi.gouv.fr, francetravail.fr, immigration.interieur, education.gouv.fr, Action Logement, monenfant.fr).
- [CLOS - 2026-09-06 (49e4e55)] **Paquet B ouvrable des adresses** : les 27 sources documentaires qui s'ouvrent normalement (pas celles sur des sites qui bloquent les robots) ouvertes, vérifiées (WebFetch + navigateur intégré + WebSearch de recoupement, [[LECONS 9.32]]) et datées `2026-09-06` dans `liens-verifies.txt`. 4 URL corrigées au passage : `service-public` (`.fr` → `.gouv.fr`, 301), `france-vae` (`france-vae.fr` ne résout plus → `vae.gouv.fr`), `mesdroitssociaux-simulateur-cmg` (301), `enic-naric` (`www.enic-naric.fr` ne résout plus → `france-education-international.fr/expertises/enic-naric`, confirmé par Denis au navigateur). `npm test` 713 verts ; navigateur : sources datées cliquables dans « Le tableau des sources officielles » + pied de fiche, `enic-naric` d'abord laissée « pas encore vérifiée » puis datée après confirmation de Denis. **Reste pour Denis** : ~67 sources sur des sites qui bloquent les robots + paquet C (chantier territoire). Note : `enic-naric` et `france-education-international-enic-naric` pointent désormais sur la même URL (doublon à arbitrer, la 2e reste non datée).
- [CLOS - 2026-09-06] **Paquet B : 89 sur 93 datées, terminé.** Denis a ouvert au navigateur toutes les sources sur des sites qui bloquent les robots, en deux passes (53 puis 9), datées `2026-09-06` dans `liens-verifies.txt` via un script jetable. 4 URL corrigées à la 2e passe : `bo-travail` (`/publications-et-ressources/bulletins-officiels-et-documents-opposables`), `interieur-etrangers` (`immigration.interieur.gouv.fr/Immigration`), `economie-clauses-sociales` (`/daj/guide-sur-les-aspects-sociaux-de-la-commande-publique`), `travail-emploi-ecoles-de-production` (`info.gouv.fr/actualite/les-ecoles-de-production...`, article 2021 à surveiller). `npm test` 713 verts. **Restent 4 adresses mortes (404 réel)** : `francetravail-accompagnement-global` / `-intensif` (page `france-travail-vous-accompagne` supprimée, y compris `.html`) et `interieur-cir-formation-linguistique` / `interieur-parcours-linguistique-au-dela-cir` (section CIR réorganisée). Non datées ; leur remplacement touche les fiches qui les citent = décision de contenu (voir `docs/PAQUET_B_BLOQUE_A_DATER.md`). Le ground est déjà couvert par d'autres sources datées.
- [CLOS - 2026-09-06 (004cc12)] **Recherche d'accueil branchée** : les 14 rayons remontent depuis la barre de recherche de l'accueil (`data/baseConnaissancesERIP.js` : entrée `MODULES_RECHERCHABLES` réécrite + bloc `RAYON_LABEL` dans `rechercherBaseConnaissances()`). Aucune modification de `js/app.js` (type `module` + cible `route:`).
- [CLOS - 2026-09-06 (27bc4f1)] `outils/veille.html`, section « Comment ça marche » : étape ajoutée sur la manipulation récurrente de datation des adresses dans `liens-verifies.txt`.
- [CLOS - 2026-09-06 (634656b, 3a0f321, c21f9fb)] **« Affiner ma recherche » : la recherche libre en deux temps.** L'orienteur « Par où commencer ? » renommé « Affiner ma recherche » ; les 14 cases (voie rapide) conservées + un champ libre avec garde-fou confidentialité. Parcours : champ libre → 1er texte de recherche (l'assistant structure en sous-chapitres, peut poser des questions) → sélection des sous-chapitres → écran « Quelques précisions » (questions de l'application : 3 universelles + 2-3 par rayon coché, 14 jeux distincts, garde-fou renforcé handicap/étranger/justice ; + questions de l'assistant récupérées ; + champ libre) → 2e texte de recherche → résultat remis en forme dans un cadre d'avertissement.
  - **Persistance : rien n'est gardé** (option A, décision Denis 2026-09-06 : on ignore le temps écoulé et les changements législatifs ; message clair à la personne « copiez ce que vous voulez garder »). `_comprendreLeCadreRecherche` vit en mémoire de page, remis à zéro à chaque nouvelle recherche et sortie du module.
  - **Textes de recherche non inventés** : socle + ton recopiés de `outils/veille.html` (`socleRecherche()` / `TON`), 2 ajouts (« réponds toujours en français » + bloc `[REFORMULATION]`). Dette de duplication à inscrire dans `BRIQUES_COMMUNES.md`.
  - Déviation assumée par rapport à la maquette validée (Denis au courant) : sélection des sujets et réponses aux questions **séparées en deux écrans** au lieu d'un — un pas = une tâche.
  - Construit en 3 blocs, `npm test` 713 verts, parcours complet vérifié au navigateur clair + sombre à chaque bloc.
  - **Contexte à retenir** : cette voie « recherche libre » figurait dans la maquette validée dès l'origine mais n'avait jamais été codée (découpée hors périmètre sans décision de Denis, module marqué `[CLOS]` quand même). Denis l'a redécouvert lui-même. Voir [[LECONS 9.30]].

**Ajouts du 2026-09-06 (session « paquet B + rayon retraite »)** :
- [CLOS - 2026-09-06 (`6400050`, `2c0cb6c` + commit veille)] **Nouveau rayon « Travailler après 50 ans et retraite » (`seniors`)**, 15e rayon du module. Décidé par Denis : le sujet retraite n'était traité nulle part (voir `docs/IDEES_A_RECLASSER.md`). Périmètre V1 volontairement restreint à la fin de carrière et la retraite (pas l'index seniors, le CVE, l'entretien de mi-carrière).
  - Câblage `index.js` : `RAYON_LABEL` + `COMPRENDRE_LE_CADRE_FAMILLES` (famille « Situations et publics », après « Moins de 26 ans ») + `ORIENTEUR_CASES` + `COMPRENDRE_LE_CADRE_QUESTIONS_RAYON.seniors` (3 questions orientées besoin). Recherche d'accueil et « Affiner ma recherche » se branchent seules (lisent `RAYON_LABEL`).
  - 5 fiches (`contenu/seniors/`) : retraite progressive, cumul emploi-retraite, départ volontaire, mise à la retraite, où s'informer. 8 sources dans `liens-verifies.txt` (section dédiée) datées `2026-09-06` : 6 pages `travail-emploi.gouv.fr` validées par Denis + `info-retraite.fr` + `lassuranceretraite.fr` (WebSearch). 3 structures « à qui s'adresser » sans portée/date (chantier territoire).
  - Bug corrigé au passage : `verifie_le` manquant/mal placé sur 3 fiches (pied affichait « date inconnue ») ; règle : `verifie_le` avant `lexique:` dans le front-matter, comme le reste du module.
  - Veille (bloc 3) : entrée `travail-emploi-retraite` dans `SITES_BLOQUES` de `outils/veille.html` (`module:'cadre'`, `veille:true`) ; ligne `seniors` dans le tableau des sujets de `VEILLE_PROMPTS.md` § 4 ; cadence trimestrielle § 5 ; domaine « Retraite et fin de carrière » ajouté à la liste fixe du prompt `[BALAYAGE]` § 5bis.
  - `npm test` 713 verts, `checkLexique` 0 erreur, navigateur clair vérifié à chaque bloc.
  - Les 5 fiches sont **relues et validées par Denis le 2026-09-06** (README du rayon mis à jour). La mesure « retraites femmes / parentalité 1er septembre 2026 » ira dans le **prochain** digest Balayage (juillet-sept 2026), pas dans une fiche.

- [CLOS - 2026-09-06] **Chantier territoire (national / régional / départemental)** (`docs/CHANTIER_TERRITOIRE_REGION_DEPARTEMENT.md`). En démarrant le bloc 1, constat : les 4 morceaux de code (parseur du 4e champ `portee` ; rattachement à chaque structure ; `_comprendreLeCadreRenduStructures` = 3 sections « Partout en France » / « En Nouvelle-Aquitaine » / « En [département] » + repli honnête + section « À vérifier selon votre territoire » + cas « autre département » ; lien annuaire câblé ; prompt de collecte qui demande la portée) **étaient déjà implémentés** (le module a été construit avec ce chantier comme cible). Le tagging (morceau 4) est fait aussi : **les 117 structures du paquet C ont toutes une portée**, taguées pendant la vérification des adresses. Vérifié le 2026-09-06 (données + logique de bucketing pour un utilisateur 24 ; le rendu DOM avait été testé au build du module). **Reste hors chantier** : dater les ~110 structures du paquet C (3e colonne vide = lien pas cliquable, LECONS 9.16), même mécanique que le paquet B, non bloquant.

- [CLOS - 2026-09-06] **Circuits de veille consolidés.** (1) *veille -> fiche* : registre `docs/NOUVEAUTES_A_STATUER.md` + prompts `[TRIAGE]` (§ 5bis.2) et `[CONTROLE-SOURCE]` (§ 5septies) - doc + boutons `veille.html` (`4117425`, `b69abb2`). (2) *veille -> mots* (Lexique) : `[LEXIQUE]` retargeté vers le format objet `data/lexique.js` (avant : visait `CORPUS_LEXIQUE.md`, 42 fiches, périmé), `[TRIAGE-MOTS]` (§ 5quater.1), champ « termes existants » + boutons dans `veille.html`, section « Mots du Lexique » du registre, `CORPUS_LEXIQUE.md` marqué périmé, « Comment ça marche » complété (`aa3c4d5`, `2933f98`, `ce2af11`). Test navigateur : sortie = objet `data/lexique.js` valide (entrée CVE). Modèle de travail : Claude opère, Denis décide ([[feedback_claude_operateur_denis_decideur]]).

**Reste sur ce module (PAS pour cette série de sessions)** :
- **Datation du paquet C** (~103 structures « à qui s'adresser », 3e colonne de `liens-verifies.txt` vide ; checklist `docs/PAQUET_C_A_DATER.md`, Groupe A mécanique / Groupe B URL générique à corriger). Non bloquant : les structures s'affichent, juste pas en lien cliquable. La portée est déjà renseignée.
- **Lexique (`data/lexique.js`) : chantier de finalisation** encore ouvert (voir `docs/CORPUS_LEXIQUE.md` en-tête, `docs/CHANTIER_LEXIQUE.md`) - séparé du circuit veille -> mots, qui est prêt à alimenter le corpus quand Denis le voudra.
- **Digest Balayage « Ce qui a changé récemment »** : [PARTIEL - 2026-09-14] premier jet publié le 2026-09-06 (`modules/comprendre-le-cadre/contenu/balayage/2026-06.md`, avril-juin 2026), puis **complément manuel fait le 2026-09-14** (`a45a26e`) : les 7 domaines encore marqués « à compléter » vérifiés un par un (sources officielles - Légifrance, solidarites.gouv.fr, service-public.fr, préfecture, Cap Métiers Nouvelle-Aquitaine...), complétés quand un changement daté du trimestre a été trouvé (SMIC, FIE, revalorisation RSA/AAH/prime d'activité, tarification des titres de séjour, fin de l'aide au permis France Travail, aide régionale emploi-compétences) ou confirmés « rien de nouveau retenu » (Handicap, Jeunes). Domaine **« Retraite et fin de carrière »** ajouté (rien retenu ce trimestre, un décret à venir versé dans « Annoncé, pas encore en vigueur »). Une fausse information (un décret attribué au contrat d'engagement jeune) trouvée dans un résumé de recherche a été écartée après vérification directe sur Légifrance - à garder en tête : toujours vérifier un fait de balayage sur la source elle-même, pas sur le résumé d'un moteur de recherche. **Reste** : relecture de Denis avant de retirer la mention « premier jet » ; territoire infra-régional (Dordogne, Haute-Vienne, DREETS) toujours sans changement trouvé propre au trimestre ; le digest juillet-septembre 2026 n'est pas encore dû (trimestre pas terminé).
- **Socle méthode + tableau des sources** : [CLOS - 2026-09-06] câblé (décision Denis, option A). `comment-lire-un-taux-de-chomage` déplacée dans `modules/comprendre-les-chiffres/contenu/methode/` (avec 2 fiches sœurs neuves : « taux INSEE ou personnes inscrites ? », « comment lire une évolution »), affichée dans le dépliant des pièges du module. Les 2 autres fiches (`comment-savoir-quelle-regle-s-applique`, `reconnaitre-une-source-officielle`) + `sources.txt` restent dans « Comprendre le cadre », affichées dans deux dépliants en bas de l'accueil (« Comment lire ces fiches », « Le tableau des sources officielles »). Les sources ouvrables du tableau sont devenues cliquables (datation du paquet B ouvrable, 2026-09-06, `49e4e55`) ; celles sur des sites qui bloquent les robots restent en texte jusqu'à datation par Denis au navigateur.
- Les 8 fiches recommandées supplémentaires éventuelles.
- [À FAIRE] **Piste `data.inclusion` pour « Près de chez moi »** (ajoutée le 2026-09-08, demande de Denis) : utiliser la base open data / l'API `data.inclusion` (80-100 k structures d'insertion, 19 thématiques, licence ouverte, MAJ hebdo) comme source de structures par territoire et par rayon, au lieu de la collecte manuelle. Piste non cadrée : questions ouvertes (API dans une app statique, confiance dans la donnée agrégée vs LECONS 9.16, cohabitation avec le paquet C, rayon pilote). Détail : `docs/CHANTIER_TERRITOIRE_REGION_DEPARTEMENT.md` § 10. À instruire en mode A avant tout code, après la datation du paquet C. **[Vérifié 2026-09-11 contre le fichier réel]** La condition de départ est remplie : `modules/comprendre-le-cadre/liens-verifies.txt` compte 368 lignes de structure, **les 368 ont une date ET une portée renseignées** (0 ligne vide sur les deux colonnes) - le motif d'urgence donné dans `CHANTIER_TERRITOIRE_REGION_DEPARTEMENT.md` § 10 (« ~110 structures restent non datées, ça coûte cher ») **n'est plus d'actualité**, la veille manuelle a comblé le retard depuis le 2026-09-08. La piste reste valable pour d'autres raisons (entretien continu, couverture plus large) mais n'est plus urgente au sens où elle a été posée. Cadrage Mode A toujours nécessaire avant tout code, mais sans pression de calendrier.

## [CLOS - 2026-09-06] « Se tenir informé » : finalisation (session du 2026-09-06)

Série de passes de contenu et de consolidation sur les deux modules de « Se tenir informé »
et l'outil de veille, en large autonomie (Denis valide les décisions, Claude opère).

- [CLOS] **Circuit veille -> mots (Lexique)** : `[LEXIQUE]` retargeté de `docs/CORPUS_LEXIQUE.md` (périmé) vers `data/lexique.js` ; `[TRIAGE-MOTS]` ajouté ; bloc « Nourrir le Lexique » de `veille.html` recâblé, avec sélecteur qui charge `data/lexique.js` pour lister les termes existants (parité avec « Ma base »), champ `gabarit` (notions), note `collection`. `docs/VEILLE_PROMPTS.md` § 5quater aligné. `checkLexique` 0/0, 154 fiches.
- [CLOS] **Rayon « Travailler après 50 ans et retraite »** (`seniors`, 15e rayon de « Comprendre le cadre ») : 5 fiches (retraite progressive, cumul emploi-retraite, départ volontaire, mise à la retraite, où s'informer), câblage complet (ORIENTEUR_CASES, RAYON_LABEL, familles, questions), sources + structures dans `liens-verifies.txt`, intégré aux prompts de veille.
- [CLOS] **Fiche `budget-surendettement` scindée en deux** (`f1d66d6`) : `budget-surendettement` (dépôt, commission, recevabilité) + `budget-surendettement-dossier-recevable` (effets de la recevabilité, 3 issues, FICP, dettes non effaçables). README rayon budget à 9 fiches. Relecture des nombres recoupée avec la page Banque de France.
- [CLOS] **Comprendre les chiffres : marqueurs `[à vérifier]` comblés** — `ue-2026.md` : 0 restant (11 comparaisons France/UE-27 tirées de l'API de diffusion Eurostat + bloc « Freins comparés » via lfsa_igar, ilc_caindformal, isoc_ci_in_h). `portrait-2026.md` : 13 remplis via le comparateur INSEE + publications nationales (âge, diplôme, temps partiel, secteurs, créations, illectronisme, écart salarial, salaires par secteur, tourisme), 17 restants (freins régionaux, BMO, fonction publique... derrière PDF / Data Browser). `2026-06.md` : 4 restants (note DREETS + dashboard Data Emploi). En-têtes passés en « relu et validé par Denis ». Bug de parsing `verifie_le:` vide corrigé.
- [CLOS] **`outils/veille.html` — prompts de « Comprendre les chiffres » alignés sur les fichiers réels** : `[CHIFFRES-UE]` (11 indicateurs + codes de table + API de diffusion + format deux lignes), `[CHIFFRES]` (part de longue durée régionale ajoutée), `[PORTRAIT]` (niveau de formation des demandeurs, freins détaillés). `docs/VEILLE_PROMPTS.md` § 5ter.3 aligné.
- [CLOS] **Paquet C des adresses : terminé.** Groupe A + B2 vérifiés au navigateur par Denis ; Groupe B1 (~60 clés) vérifié par contrôle automatique (chaque URL ouverte, organisme confirmé) et daté. 1 correction B1 : `cap-emploi-dordogne` -> `capemploi-24.com` (ancien domaine parké). Plusieurs corrections B2 (prud'hommes -> lannuaire, france-vae -> vae.gouv.fr, msa -> dlg.msa.fr/lfp, presanse -> spst19-24, france-microcredit -> service-public F21375, psad -> ac-bordeaux/ia24). `INAÉ Nouvelle-Aquitaine` ajouté en structure régionale (rayon Accompagnement). **Plus aucune ligne de structure sans date.**
- [CLOS] **Paquet B des adresses : clos.** Les 4 adresses mortes orphelines (`francetravail-accompagnement-global`/`-intensif`, `interieur-cir-formation-linguistique`/`-parcours-linguistique-au-dela-cir`) supprimées de `liens-verifies.txt` et des blocs `sources:` des collectes.

`npm test` 713 verts tout du long, test navigateur à chaque bloc, commits directs sur `master`.

## [CLOS - 2026-09-06] Module « Comprendre les chiffres » (2e sous-carte de « Se tenir informé »)

Extraction du digest « Chiffres » en module autonome. **Mode A** (conception, maquette validée
`docs/MAQUETTE_COMPRENDRE_LES_CHIFFRES_2026-09-06.html`, cahier `docs/CHANTIER_COMPRENDRE_LES_CHIFFRES.md`
D1-D24) puis **autonomie complète** donnée par Denis pour coder les 10 blocs, consigne n°1 :
implémentation identique à la maquette. Feu vert donné en cours de route pour aller chercher les
vraies données (vérification par Denis après publication, corrections via l'outil de veille).

- **Bloc 1 - coquille** (`436fff1`) : `modules/comprendre-les-chiffres/` (index.js + css + ARCHITECTURE),
  hub « Se tenir informé » à 2 sous-cartes, page de présentation + rectangle de complémentarité
  réciproque, route `js/app.js`, `index.html`, stub `tests/_domStub.js`, retrait du digest Chiffres
  de `COMPRENDRE_LE_CADRE_OUTILS`.
- **Bloc 2 - prompts de veille + contenu** (`a8e025e`) : `docs/VEILLE_PROMPTS.md` § 5ter réécrit en
  3 sous-prompts (`[CHIFFRES]` trimestriel + `[LECTURE]`, `[PORTRAIT]` annuel, `[CHIFFRES-UE]`
  annuel). `outils/veille.html` : rectangle renommé, 2 dépliants annuels, `SITES_BLOQUES` avec un
  champ `module` ('cadre'/'chiffres') + 6 sources statistiques (séries INSEE, STMT, PDF DREETS,
  Eurostat, BMO, **SI-Terr Cap Métiers**), guide « Comment ça marche » complété. 3 fichiers
  `contenu/` créés.
- **Visibilité de la sous-carte sur l'accueil** (retours Denis) : puce sur la carte « Se tenir
  informé » + description élargie, infobulle « i », aide guidée « Explication rapide » réécrite
  au style maison (nom en gras + parenthèse, jamais « nom : clause »).
- **Remplissage des fichiers de contenu** (`82209e7`) : chiffres officiels via WebSearch/WebFetch
  (taux de chômage localisé T1 2026 des 4 territoires, demandeurs 24/87, emploi salarié
  France/Nouvelle-Aquitaine, offres 24/87, population/densité/pauvreté INSEE, profil des
  demandeurs, chiffres UE Eurostat). Le reste marqué `[à vérifier]` avec sa source. Chaque valeur
  porte sa source cliquable en fin de ligne.
- **Bloc 3 - couche 1** (`...`) : chargement asynchrone du digest, parseur tolérant, 5
  cartes-indicateurs fidèles à la maquette (question, valeur + infobulle de source, tendance,
  phrase, comparaisons, graphe SVG en barres), archive, dépliant des 6 pièges, lecture du
  territoire. 4 niveaux de lecture (département / région / France / Union européenne).
- **Bloc 4 - couches 2 et 3 + vue UE** : 3 dépliants méthode + 8 dépliants portrait (freins en
  premier, replié et neutre) + 2 cartes éditoriales, carte enquête BMO, tableau de synthèse
  France / moyenne UE-27 + dépliant des freins comparés.
- **Bloc 5 - croiser plusieurs chiffres** : cases par unité (% / nombres), garde-fou
  anti-mélange, graphe combiné multi-séries (légende, infobulle par trimestre).
- **Bloc 6 - vue d'ensemble** : garde-fou CIP, lecture du territoire en clair, tableau de
  synthèse, dépliants ouverts, renvoi « à lire avec », bouton imprimer + `@media print`.
- **Blocs 7-8 - partie B** : porte A (question chiffrée précise, un temps, résultat parsé du
  bloc `[CHIFFRES]`) ; porte B (comprendre le territoire, deux temps, `[SUJETS]` -> cases ->
  second texte). Même socle de prompt que « Affiner ma recherche ». Rien enregistré.
- **Bloc 9 - import manuel + docs** : volet « Chiffres » de l'import manuel de `veille.html`
  calé sur le format réel (`docs/CHANTIER_IMPORT_MANUEL_VEILLE.md` § 2 clos).

Chaque bloc : `npm test` 713 verts, test navigateur clair + sombre, un commit. Bouton Retour
construit d'emblée sur la logique de « Comprendre le cadre » (consigne Denis, aucune reprise
nécessaire). Nouvelles leçons : [[LECONS 9.34]], [[LECONS 9.35]], [[LECONS 9.36]].

**Reste (hors code)** : contenu **relu et validé par Denis le 2026-09-06** (l'en-tête des `.md`
et de `CLAUDE.md` a été mis à jour, le blocage « pas de prod avant relecture » est levé). Les
lignes `[à vérifier]` qui subsistent sont des **données pas encore récupérées** (bloquées
derrière un PDF ou une base) : à compléter au fil des passages de veille (`[CHIFFRES]`,
`[PORTRAIT]`, `[CHIFFRES-UE]`, import manuel), pas un blocage. Vidéo de présentation et test
terrain restent à faire.

## Tâches ajoutées le 2026-08-27 (interface d'accueil et généralisation du patron "page d'introduction")

- [CLOS - 2026-08-31] Retirer la barre de progression (7 étapes) de la page d'accueil. Fait avec le Bloc 1 du chantier accueil : `htmlActionsSession()` extrait (Restaurer / Réinitialiser gardés), l'accueil ne rend plus la liste des 7 étapes. Puis (`a220f48`) tout le bandeau `.progression` retiré de l'accueil, gardé uniquement dans les 3 parcours CV de « Mes documents ».
- [CLOS - 2026-08-31, vérifié navigateur 2026-09-01] Barre de recherche remontée en haut de la page (Bloc 3 + Bloc 4 du chantier accueil). `.resultats-erip-dropdown` s'ouvre vers le BAS (`top:100%`), encart flottant blanc opaque juste sous la barre, aligné à sa largeur, jamais de navigation auto, même encart pour « aucun résultat ». Loupe `bi-search` en accent. Placeholder qui tourne (5 exemples). Couverture élargie : nom de module (groupe « Dans l'application », `MODULES_RECHERCHABLES` / `ouvrirCibleRechercheApp`) + définition Lexique affichée inline + bouton « En savoir plus » (jamais de redirection auto). Moteur (`rendreResultatsRechercheERIP` / `rechercherBaseConnaissances` / `rechercherMetiersDepuisTexte`) **jamais modifié**. La « position calculée en JS » du recueil n'a pas été nécessaire : le `position:absolute` dans un parent `position:relative` place l'encart correctement et de façon robuste (vérifié : aligné, opaque, vers le bas, sous la barre, redimensionnement OK).
  - **Contenu de la recherche élargi (décidé le 2026-08-27, matin)** : au-delà de métier/domaine/compétence (déjà existant), la recherche doit aussi couvrir : le nom d'un outil/module (ex. "Lexique", "Mon Carnet"), un mot du Lexique (ex. "CDI"), et un frein avec ce qu'on propose pour lui (une fois le futur module construit - nom encore hésitant entre "Ressources" et "Solutions", pas tranché).
  - **Vision élargie donnée par Denis le 2026-08-27 (matin), après avoir montré 4 captures d'écran du vrai moteur de recherche actuel** : "enrichir" la barre de recherche veut dire beaucoup plus que lui donner une place en haut de page - trois choses distinctes : (1) qu'elle devienne le lien vers TOUS les modules de l'app, pas seulement métiers/compétences ; (2) reproduire fidèlement le mécanisme riche déjà existant (voir description technique juste en dessous) plutôt qu'une version appauvrie ; (3) lui ajouter un nouvel "index de sites de référence" (mots-clés -> sites externes nommés et vérifiés, cliquables) ET un index nourri par le futur travail de veille de Denis (analyses ponctuelles qu'il juge pertinentes, enregistrées, qui nourrissent cet index) - ces deux nouveaux index ne sont PAS des modules séparés, ils vivent comme un complément de données de la barre de recherche elle-même. **Portée pas encore tranchée ("on va en discuter")** - Denis lui-même signale que ça mérite une vraie discussion avant de coder.
  - **Description technique du vrai moteur actuel, vérifiée dans le code le 2026-08-27 (matin), à ne perdre sous aucun prétexte en le reconstruisant** : `rendreResultatsRechercheERIP()` + `rechercherBaseConnaissances()` (`js/app.js` ligne ~779) cherchent dans métiers, compétences, savoir-être, savoirs, environnements de travail ET domaines d'activité en une seule recherche. Un "moteur de pertinence" séparé (`rechercherMetiersDepuisTexte()`) ajoute des métiers suggérés par le SENS de la recherche, pas seulement par correspondance exacte de texte (groupe "Métiers qui pourraient vous correspondre"), sans dupliquer les métiers déjà trouvés par correspondance exacte. Résultats groupés par catégorie avec icône. Cliquables : un métier (ouvre le choix de parcours), une compétence/savoir-être/savoir (ouvre les métiers associés), un environnement de travail (idem), un domaine d'activité (idem) - les certifications restent de simples pastilles non cliquables. Chaque métier a un lien "Fiche ROME" externe (jamais intercepté par le clic qui ouvrirait le choix de parcours). Un clic EN DEHORS des résultats (sur l'overlay) vide le champ et referme la liste. Suivi d'usage Umami débounce (600ms après la dernière frappe, jamais à chaque caractère), avec un événement séparé pour les recherches sans résultat. **Point encore à clarifier avec Denis** : il décrit aussi un comportement de "clic sur la barre elle-même après un 'aucun résultat' qui vide le texte" - pas retrouvé littéralement dans le code lu jusqu'ici (seul le clic EXTÉRIEUR vide le champ), à confirmer si c'est le même comportement décrit autrement ou un 2e mécanisme distinct.
  - **Comportement du résultat de recherche précisé par Denis (2026-08-27, matin), testé dans la maquette** : jamais de navigation automatique dès la validation de la recherche - un résultat cliquable apparaît (nom trouvé en gras + une phrase de contexte "un outil de notre plateforme, dans [tel groupe]"), positionné fixe en bas de l'écran, par-dessus tout le reste de la page (au-dessus des cartes et de toute fenêtre ouverte). La personne doit cliquer dessus pour continuer. Exemple testé : taper "ATS" fait apparaître le résultat "ATS - un outil de notre plateforme, dans Outils d'analyse" ; taper "créer un nouveau cv" fait apparaître un résultat cliquable qui ouvre la fenêtre "Mes documents" une fois cliqué. Un mot sans correspondance affiche un message honnête sous la barre plutôt qu'un résultat inventé.
  **2 corrections trouvées en testant "ATS" pour de vrai (2026-08-27, matin)** : (1) la maquette exigeait de valider (Entrée) avant de chercher, alors que le vrai moteur cherche déjà au fil de la frappe (`input`, pas seulement `submit`) - corrigé pour matcher le comportement réel. (2) Pas de tolérance casse/accents - corrigé en reprenant EXACTEMENT `normaliserTexte()` (`data/metiers.js:631` : minuscules + suppression des accents en Unicode NFD + tiret/underscore remplacés par espace), jamais une nouvelle logique inventée. Testé et vérifié : "ATS", "REPÈRES", "Mobilité", "cArNeT" trouvent tous leur résultat malgré la casse et les accents.
  **Position du résultat corrigée (2026-08-27, matin), signalée par capture d'écran** : positionné en bas d'écran fixe (demande initiale de Denis), le résultat atterrissait n'importe où sur la page selon la position de la barre - déconnecté visuellement d'elle, superposé à d'autres cartes sans lien apparent. Corrigé : le résultat sort maintenant juste SOUS la barre de recherche (position calculée par JS à partir de la position réelle de la barre, recalculée au redimensionnement), jamais ancré au bas de l'écran. **Fond blanc opaque du résultat confirmé par Denis** (inspiré de Google, qui n'est pas non plus transparent) - aucun changement à faire là-dessus.
  **"Aucun résultat" unifié avec le résultat trouvé (2026-08-27, matin), signalé par 3 captures d'écran** : le message "aucun résultat" vivait dans un simple texte de page normale, qui poussait donc les cartes vers le bas à chaque frappe - jamais ce que fait le vrai moteur (capture d'écran à l'appui : "aucun résultat" utilise exactement le même encart flottant blanc que les vrais résultats). Corrigé : un seul et même encart flottant sert aux deux cas (résultat trouvé = cliquable ; aucun résultat = message centré, non cliquable) - jamais deux fonds différents pour ces deux états. Vérifié : les cartes ne bougent plus du tout pendant la frappe, quel que soit le texte tapé.
  **Reproduction fidèle demandée par Denis (2026-08-27, matin)** : après 3 captures d'écran du vrai moteur, Denis a demandé d'arrêter la version simplifiée et de reproduire exactement le vrai gabarit. Fait : `carteMetierHTML()` recopiée fidèlement (mêmes champs, même structure - secteur/savoir-faire/savoir-être/savoirs/lien Fiche ROME), avec les vraies classes CSS adaptées aux tokens de la maquette (`.groupe-resultats-erip`, `.grille-resultats-erip`, `.carte-resultat-erip`, `.carte-resultat-texte`, `.carte-resultat-cliquable` - valeurs copiées de `css/style.css`). **Décision de fond retenue (avis de Claude donné puis appliqué)** : ne pas remplacer les cartes métier riches par une version simplifiée (perte réelle d'info immédiate) - réutiliser à la place le 2e gabarit déjà existant dans la vraie app, le style "pastille" léger déjà utilisé pour compétences/savoir-être, pour les nouveaux types de résultats (outils/modules) qui n'ont pas la même forme de données qu'un métier. Les deux gabarits cohabitent dans le même encart flottant, jamais un remplacement de l'un par l'autre. Testé : "boula" affiche la carte Boulanger identique à la capture (secteur, savoir-faire/être/savoirs, Fiche ROME D1102) ; "ats" affiche la pastille "ATS" cliquable ; un mot inconnu affiche le message centré.
  **3 précisions de comportement données par Denis (2026-08-27, matin), à respecter impérativement au moment de coder** :
  1. **Ne jamais perdre le clic sur un métier ou une compétence** : aujourd'hui, cliquer sur un métier trouvé ouvre le choix de parcours (candidater directement, CV, lettre...) ; cliquer sur une compétence ouvre les métiers de la base qui la partagent, avec la même possibilité de candidater. Comportement déjà vérifié dans le vrai code (`ouvrirPanneauChoixParcours()`) - à préserver tel quel, jamais écrasé par la réorganisation de la barre de recherche. Reproduit dans la maquette par un message qui confirme explicitement ce qui se passerait (pas juste fermer silencieusement).
  2. **Un mot déjà dans le Lexique ne redirige JAMAIS automatiquement vers le module Lexique** : sa définition doit apparaître directement dans les résultats de recherche, avec un bouton optionnel "En savoir plus dans le Lexique" pour qui veut les liens entre mots et les suggestions. Testé dans la maquette avec la vraie définition de "CDI" (recopiée depuis `data/lexique.js`, jamais inventée).
  3. **Idée de suivi Umami des mots tapés, déjà satisfaite** : Denis a proposé de suivre ce que les gens tapent dans la barre pour enrichir sa couverture au fil du temps - **déjà fait** dans le vrai code (`recherche_utilisee`/`recherche_sans_resultat`, débounce 600ms, déjà documenté plus haut dans ce fichier) - rien à construire de plus, juste continuer à s'appuyer dessus une fois la nouvelle barre en place.
  - **Effet dynamique à reproduire (décidé le 2026-08-27, matin)** : Denis a aimé l'exemple de texte qui change toutes les 5-6 secondes dans le champ vide de la maquette "Boussole" (`docs/MAQUETTE_ACCUEIL_BOUSSOLE_2026-08-27.html`) - à reprendre pour la vraie barre de recherche, pour montrer sans texte fixe que plusieurs types de recherche sont possibles.
  - **Texte de "Me préparer à candidater" corrigé deux fois (2026-08-27, matin)** : (1) "au clavier ou à la voix" retiré - jugé par Denis comme une fausse promesse (dépend du micro de la personne ET de l'assistant choisi, deux conditions jamais garanties par l'app). (2) "avec l'aide d'un assistant IA" retiré aussi, à la demande explicite de Denis - décision prise pour CETTE carte précise, ne pas en déduire que le mot "IA" est banni ailleurs (le chantier "vocabulaire IA neutre sur toute l'app" reste un chantier à part, portée pas encore tranchée). Texte final : "Rédiger une lettre de motivation et vous entraîner à un entretien d'embauche." **À vérifier plus tard, pas fait aujourd'hui** : le vrai texte d'aide de l'app (`AIDE_PAGES.cv`, entrée `#btnPreparationAccueil`, `js/app.js`) contient la même formulation "au clavier ou à la voix" pour Co-construire ma lettre ET Préparer un entretien - possible qu'il faille corriger ce texte aussi, à confirmer avec Denis avant d'y toucher.
  - **Textes des 3 sous-cartes de "Mes documents" affinés (2026-08-27, matin)**, titres inchangés, seulement les phrases courtes : "Créer un nouveau CV" → "Créez votre CV : il vous permettra ensuite de produire vos autres documents de candidature." (généraliste, évite de re-lister lettre+entretien une 3e fois puisque "Mes documents" le fait déjà juste au-dessus) ; "J'ai déjà un CV" → "Déposez votre CV complet : il vous permettra ensuite de créer votre lettre de motivation." (Denis a volontairement laissé tomber la mention de l'entretien d'embauche ici, jugée redondante à force d'apparaître partout). **"Mettre à jour mon CV" corrigée aussi (2026-08-27, matin)** : "Vous le déposez, puis vous le complétez" jugé trompeur par Denis - laisse croire qu'on modifie le document déposé lui-même, alors qu'en réalité seul le CONTENU est repris/mis à jour (l'app ne garantit jamais de modifier la mise en forme du fichier déposé). Premier essai "sans repartir de zéro" retiré ensuite par Denis - nier une peur la fait exister dans la tête de la personne, jamais la mentionner même pour la nier. Texte final, aligné sur le même principe que les 2 autres sous-cartes (mentionner l'accès aux autres documents) : "Déposez votre CV : mettez à jour son contenu, puis préparez vos autres documents de candidature."
  - **Texte de "Mes documents" élargi (2026-08-27, matin)** : "Créer, déposer ou mettre à jour votre CV" jugé trop étroit par Denis - les 3 parcours derrière cette carte ne s'arrêtent pas au CV, ils mènent aussi à la lettre de motivation et à la préparation d'entretien d'embauche une fois le CV prêt (déjà vrai dans le vrai parcours de l'app, voir `AIDE_PAGES.cv` : "Une fois ce CV prêt, il sert aussi de base pour rédiger une lettre de motivation et préparer un entretien d'embauche"). Nouveau texte : "Le point de départ pour votre CV, votre lettre de motivation et votre préparation d'entretien d'embauche."
  - **Cohérence des icônes (2026-08-27, matin)** : les 6 icônes des cartes d'accueil doivent toutes utiliser les vrais tracés Bootstrap Icons, en version CONTOUR (pas "-fill") pour rester cohérentes entre elles - Mes documents = `bi-folder2`, Me préparer à candidater = `bi-person` (2 essais ratés avant : `bi-person-check-fill` détonnait des autres icônes en contour, puis `bi-person-check` en contour avait un rendu confus - torse sans tête, jugé "horrible" par Denis - `bi-person` simple, avec tête, a réglé le problème), Boîte à outils = `bi-briefcase` (même erreur initiale corrigée : la version `-fill` avait été utilisée par erreur, alors que l'app réelle utilise déjà la version contour), Outils d'analyse = `bi-graph-up`, Se tenir informé = `bi-newspaper` (un premier essai avec `bi-broadcast`, signal radio, jugé pas assez clair par Denis - remplacé), Vous hésitez encore = `bi-signpost-2` (déjà utilisée aujourd'hui, tracé recopié depuis la source officielle plutôt que redessiné à l'œil - 2 essais dessinés à la main ratés avant ça). Texte de "Vous hésitez encore ?" : garder tel quel le texte déjà existant dans l'app ("Explorez d'autres pistes : ateliers, formations, immersions, autres métiers...") - Denis a refusé une reformulation de Claude ("...pour y voir plus clair") jugée comme une fausse promesse de clarté, même logique que la décision "Ressources" vs "Solutions" plus haut : ne jamais promettre un résultat qu'on ne maîtrise pas.

- **[CLOS - 2026-09-01] Réorganisation de la page d'accueil.** Doc de travail : `docs/CHANTIER_ACCUEIL_DECISIONS_2026-08-31.md`. Livré : 6 cartes d'accueil (`htmlCarteAccueil` + `ouvrirCarteAccueil`), 12 tuiles redistribuées, panneau unique par carte + bouton « ← Retour », infobulle « i » par carte (texte validé `INFOS_CARTE_ACCUEIL`, survol + clic, `position:fixed` sur `document.body`), barre de recherche remontée + élargie (modules, Lexique) moteur inchangé, bandeau `.progression` retiré de l'accueil, « bientôt disponible » retiré des sous-cartes, disquette « Sauvegarder » (mot sur l'accueil / icône seule en parcours), 3 couleurs d'accent en plus (violet/turquoise/framboise) + pastille de couleur par bouton, tous les pulses en couleur d'accent, **un seul pulse persistant à la fois** (coordinateur `_pulsePersistantDemander`, priorité disquette > aide > préférences, l'infobulle « i » passe devant — `722b972`), sweep des logos de module (LECONS 9.9), pied de page 3 boutons homogènes, nettoyage (fonction doublon `ouvrirChoixPreparationAccueil`, CSS mort). **Décidé** : pas de sous-groupes avec sous-titres dans « Boîte à outils » (4 sous-cartes, inutile). **Reporté** (quand les modules existeront) : texte des pages de présentation ATS / Regard recruteur / Se tenir informé / Comparer mes pistes.
  - **[CLOS - 2026-09-01, vérifié 2026-09-10 : `docs/CHANTIER_BOUTON_REVOIR_PRESENTATION.md` marqué CLOS, 5 étapes FAIT]** : bouton « Revoir la présentation » harmonisé (`htmlBoutonRevoirModule()` partagé, note 1re fois adaptée) + généralisation du patron. Présent sur Cohérence (référence), Bilan, Découvrir mes compétences, Comparer mes pistes, Carnet, Lexique, Mes Repères, Co-construire ma lettre, Préparer un entretien ; câblé mais invisible (pas d'écran de travail) sur ATS, Regard recruteur, Se tenir informé. Jamais touché : parcours « Mes documents ». **Reste 1 décision UX pour Denis** : aligner le flux de reprise du Bilan (D5a/D5b : on atterrit sur la présentation avec un bandeau) sur celui de Cohérence (dernier écran + encart latéral + gel), ou garder D5a/D5b.
  - **LIEN OBLIGATOIRE avec le recueil de contenu (décision de Denis, 2026-08-28)** : la création de la page d'introduction de chaque module est un **sous-chantier** de celui-ci. Au moment de traiter un module, il faut IMPÉRATIVEMENT ouvrir `docs/PAGES_INTRODUCTION_MODULES_RECUEIL.md` (structure canonique en section 2, texte des 6 cartes en section 3) ET `docs/IDEES_A_RECLASSER.md` (entrées du 2026-08-28 : banque de fonctions par module, idées de modules nouveaux, grille d'évaluation) pour s'inspirer au maximum : lexique, phrasé, blocs. Les idées de **fonctions** qui y figurent peuvent être implémentées à cette occasion si elles sont mûres, sinon rester en réserve. Ce recueil s'enrichit encore (récolte multi-IA en cours) - le relire tel qu'il est le jour où le sous-chantier démarre.
  - **Rappel (Denis, 2026-08-28)** : les 2 pages d'introduction déjà en production (Repères = `_reperesRenduAccueil()` ; Cohérence de mon dossier = `ctHtmlExplication()`) devront probablement être **revues** pour être alignées sur le patron canonique - voir `docs/PAGES_INTRODUCTION_MODULES_RECUEIL.md` section 4 (verdict : complémentaires, ni au-dessus ni en dessous ; 4.4 liste ce qu'il manque à chacune : bloc "Ce que ce n'est pas" titré, phrase de raison d'être, encart "Cet outil travaille avec...", rappel sauvegarde pour Cohérence).
  - **DÉCISION 2026-08-28 - PAS de carte ni de module "Ressources" sur l'accueil.** En comparant avec la plateforme partenaire **PCGI 87** (dans laquelle APP est déjà référencée sous "ERIP - TRE"), on a constaté que l'espace "catalogue de ressources" (annuaire de 200+ structures, aides, urgences, guides) est **déjà entièrement couvert par PCGI 87, et mieux**. Faire un module Ressources dans APP = doublon avec la plateforme qui héberge APP. Décision : **pas de module autonome, pas de carte d'accueil dédiée.** À la place, un simple **bloc de données `data/freins.js`** (les 17 codes de frein qui existent déjà dans `prompts/regard-exterieur.md` + leurs ressources vérifiées + synonymes), consommé par : (1) le rapport Regard extérieur, (2) Mes Repères (un frein devient un Repère qui porte ses pistes), (3) la barre de recherche élargie. Optionnel : une vue-liste légère "Ce qui me freine" à l'intérieur de Mes Repères ou du parcours Regard extérieur, jamais une carte de premier niveau. Détail : `docs/CHANTIER_RESSOURCES.md`. **À expliciter dans les docs de conception** : ce non-choix est délibéré et évite un conflit / doublon avec le site partenaire. Deux pistes discutées séparément :
  - Regrouper les 3 parcours CV derrière UNE carte d'entrée ("Mes documents") qui, une fois cliquée, propose les 3 parcours - **précisé par Denis : pas une fusion des 3 choix en un seul, ils restent distincts, juste regroupés derrière une seule tuile d'accueil** pour libérer de la place sur la page d'accueil (notamment pour mieux organiser la Boîte à outils, point suivant). Avis de Claude : coût réel (un clic de plus sur l'action la plus fréquente de l'app), mais échange concret contre de la place plutôt qu'un gain seulement esthétique - plus favorable avec cette justification, à condition que la tuile "Mes documents" reste sans ambiguïté (voire un aperçu des 3 options au survol plutôt qu'un clic à l'aveugle). Voir l'écran réel avant de trancher, maquette recommandée.
  - Réorganiser la Boîte à outils qui déborde - **avis de Claude : favorable**, en catégorisant les outils (2-3 groupes avec sous-titres) plutôt qu'une longue liste plate.
  - **Point de non-régression à ne pas oublier lors de l'implémentation de la maquette "Porte d'entrée"** : les vraies fenêtres d'action (`ouvrirFenetreERIP()`, ex. "Mes documents") ne doivent JAMAIS se fermer au clic extérieur - uniquement croix + Échap. Le vrai site est déjà conforme, mais la maquette avait ce bug (corrigé dans la maquette). Détail complet et raison : voir l'entrée dédiée dans la section "Autres tâches en attente" plus bas ("Empêcher les vraies fenêtres d'action de se fermer au clic extérieur").
- [CLOS - 2026-08-31, commit `29a2b51`] Infobulle "i" par carte/outil. Trigger `bi-info-circle` accent à côté du titre du panneau, bulle `INFOS_CARTE_ACCUEIL` (`data/metiers.js`) : intro + une ligne par sous-carte, angle complémentaire, jamais mot pour mot. Ouverture au survol ET au clic (épinglée), `position:fixed` déplacée sur `document.body` (l'ancêtre `.fenetre-erip-boite` a un `transform` qui piégeait `position:fixed`), fermeture au clic extérieur / Escape / croix (z-index dédié), repositionnement au resize + scroll du corps, tactile = clic. Historique de la demande ci-dessous. Exemple concret donné par Denis : une personne dont le CV est déjà complet ne comprend pas forcément pourquoi cliquer sur "J'ai déjà un CV" - le texte court peut dire QUOI mais pas toujours POURQUOI/dans quel cas précis. Réutiliser le mécanisme déjà existant et déjà corrigé dans la vraie app (`.bulle-info-hover` / `initBulleInfoHoverFlottante()`, `js/app.js` - contraste clair/sombre déjà réglé, positionnement qui ne déborde jamais l'écran déjà réglé), jamais en reconstruire un nouveau. Contenu exact (quelles cartes en ont besoin, quel texte) pas encore défini - à faire au moment de coder, pas seulement une intuition générale.
  **2 vrais bugs trouvés et corrigés dans la maquette "Mes documents" (2026-08-27, matin)**, à surveiller dans le vrai code plus tard : (1) une fenêtre sans `max-height`/`overflow-y` peut devenir plus haute que l'écran - le contenu du haut (dont la croix de fermeture) se retrouve poussé hors champ par le centrage vertical, rendant la croix quasi impossible à cliquer. Toujours donner une hauteur maximale avec défilement interne à toute fenêtre modale. (2) Un bloc de titre positionné (`position:relative`, placé après la croix dans le HTML) passait AU-DESSUS de la croix de fermeture dans l'ordre d'empilement, même sans se voir visuellement - la croix recevait alors les clics d'un autre élément. Corrigé avec un `z-index` explicite sur le bouton de fermeture. **Leçon générale : tout bouton de fermeture doit avoir un z-index explicite et supérieur au reste du contenu de la fenêtre, jamais compter sur l'ordre du HTML seul.**
  **Portée précisée par Denis (2026-08-27, matin)** : l'infobulle reste volontairement GÉNÉRALE et courte, jamais le détail précis (nombre de gabarits de CV, fonctionnalités exactes) - ce détail-là vivra plus tard sur une page d'introduction dédiée par carte/module, réutilisant le patron déjà validé pour Mes Repères et Cohérence transversale (voir mémoire `project_page_introduction_modules`), avec bouton "Je commence" à la fin. Sur mobile/tactile, l'infobulle s'ouvre au clic plutôt qu'au survol (le mécanisme actuel, `mouseover`/`focusin`, n'a rien de prévu pour le tactile - à corriger). **Emplacement précisé par Denis (2026-08-27, matin), après un premier essai au mauvais endroit** : jamais sur la carte d'accueil elle-même (elle a déjà son texte descriptif, personne n'ira cliquer sur une bulle en plus) - l'infobulle vit à L'INTÉRIEUR de la fenêtre qui s'ouvre (à côté du titre "Mes documents", avec l'icône du classeur devant, cohérence avec la carte d'accueil), et son texte doit être COMPLÉMENTAIRE à ce que les sous-cartes montrent déjà, jamais une redite. **Revu le 2026-08-27 (matin, un peu plus tard)** : Denis a en fait aimé le premier essai qui listait aussi les 3 items avec icône+nom+texte complémentaire, juste mal placé la première fois (sur la carte d'accueil au lieu de dans la fenêtre) - remis en place à l'intérieur de la fenêtre, EN PLUS du paragraphe général, avec un texte différent de celui déjà visible sur chaque sous-carte (vraiment complémentaire, pas une redite) : Créer un nouveau CV -> "Vous avancez par étapes simples, une question à la fois." ; J'ai déjà un CV -> "Vos informations sont reprises telles quelles, rien à ressaisir." ; Mettre à jour mon CV -> "Seul le contenu est repris - vous choisissez ensuite la présentation." **Double déclenchement demandé (2026-08-27, matin)** : clic (utile sur mobile, reste "épinglé" ouvert) ET survol à la souris - **le premier essai en CSS pur (`:hover + .info-bulle`) avait un vrai défaut, trouvé par Denis** : la bulle grandissait à l'intérieur de la fenêtre modale (qui a un `overflow-y:auto`), ce qui ajoutait une fausse barre de défilement et déformait la fenêtre au survol. Corrigé en passant la bulle en `position:fixed` (positionnée par JS via `getBoundingClientRect()`, recalculée au survol/clic et au redimensionnement) au lieu de `position:absolute` à l'intérieur du conteneur qui défile - une fenêtre `fixed` échappe complètement au calcul de hauteur de défilement de ses ancêtres, vérifié par test (`scrollHeight` identique bulle ouverte/fermée). Le survol est maintenant géré en JS (`mouseenter`/`mouseleave`), plus fiable et testable que le CSS `:hover` pur.
  **"i" agrandi et pulse d'attention ajoutés (2026-08-27, matin)** : le rond du "i" est passé de 22px à 28px (Denis le trouvait trop discret). Un pulse bleu doux (`box-shadow` qui s'étend puis disparaît, 3 cycles sur environ 5,4 secondes, jamais infini/frénétique) se déclenche automatiquement à l'ouverture de la fenêtre "Mes documents", pour attirer l'œil vers l'infobulle sans être agressif - respecte `prefers-reduced-motion` (pas d'animation si l'utilisateur a demandé moins de mouvement). Un premier texte ("aucune paperasse à préparer") jugé trop simpliste par Denis, retiré - il veut garder une certaine prestance dans le ton, pas glisser vers un langage infantilisant.
  **Mot "zéro" à bannir des textes visibles pour ce public (2026-08-27, matin)** : "CV construit depuis zéro" retiré - pour un public en fragilité, ce mot rappelle une situation de manque et ajoute une charge cognitive/émotionnelle inutile. Texte final de "Créer un nouveau CV" : "Construisez votre CV : il vous permettra ensuite de produire vos autres documents." Ces pages d'introduction détaillées se brancheront sur le futur chantier Accessibilité : un mode "accompagné" (montre la page d'introduction avant chaque module) et un mode "autonome" (saute directement à l'action, comportement actuel de l'app) - **rien de tout ça n'est à construire aujourd'hui**, seulement les 6 cartes + une infobulle générale courte par carte. Les pages d'introduction et le bascule accompagné/autonome restent derrière Accessibilité (chantier stratégique 2, pas commencé).
  ~~Ancienne décision (abandonnée puis réouverte) : chaque carte porte une courte phrase descriptive, jugé suffisant pour remplacer l'infobulle~~ - la phrase courte reste une bonne base, mais ne suffit pas seule pour tous les cas. Le panneau "💡 Besoin d'aide ?" / "🧭 Visite guidée" reste utile en plus, pas à la place.
- [CLOS - 2026-09-01, décision Denis] Réorganiser le contenu de la Boîte à outils. Résolu par la refonte de l'accueil : les 12 tuiles identiques ont éclaté dans les 6 cartes, chaque carte EST désormais un groupe. Il ne reste que 4 sous-cartes dans « Boîte à outils » (Découvrir mes compétences / Mes Repères / Mon Carnet / Lexique), chacune avec sa phrase descriptive et son logo. **Denis 2026-09-01 : pas de sous-titres / sous-groupes, inutile avec 4 sous-cartes.** Historique de la réflexion ci-dessous. **Avis de Claude : la page d'accueil elle-même est propre (16/20, confirmé 15/20 pour les 3 cartes CV après un second regard), c'est cette fenêtre précise qui pose problème (11/20)** - 8 actions de nature différente avec un poids visuel identique, aucun repère pour s'orienter. Pistes concrètes : regrouper en 2-3 catégories avec sous-titre (ex. "Construire mes documents" / "Réfléchir et me situer" / "Suivre mon parcours", à affiner) ; réutiliser le système de couleurs par famille déjà construit pour le Lexique (`--famille-comprendre`/`--famille-explorer`/`--famille-personnel`/`--famille-accompagnement`, voir `css/style.css`), jamais appliqué ici ; ou transformer les boutons en petites cartes avec une phrase descriptive (comme les 3 cartes CV de l'accueil) plutôt qu'icône + un mot seul.
  - **[CLOS - 2026-08-31, commit `3171865`, 608 tests + vérif navigateur]** Le texte d'aide qui décrit cette fenêtre au survol de la carte "Boîte à outils" (`AIDE_PAGES.cv`, entrée `#btnPreparationAccueil`, `js/app.js`) ne listait que 6 outils. Lexique et Cohérence de mon dossier ajoutés, « Six outils » → « Huit outils » ; « Analyser ma candidature » : « contrairement aux autres outils » → « à la plupart des autres » (Cohérence exige aussi un CV). Reste ouvert : la réorganisation de fond de cette fenêtre (8 boutons identiques → 2-3 catégories).
  - **Tentative de maquette "vision 20/20" du 2026-08-27 (soir), rejetée par Denis, gardée comme point de départ** : une première maquette interactive ("Boussole") proposait une page unique en défilement avec un fil vertical reliant les sections - jugée par Denis trop proche d'un CV/d'une page éditoriale qu'on lit, pas assez d'une application qu'on manipule ("je perds des cartes", tout déployé d'un coup au lieu d'avancer par clic). Direction à reprendre pour une future itération : des zones qu'on ouvre une par une au clic, rien déplié tant qu'on n'a pas cliqué dessus, écran calme et aéré - jamais un long scroll façon document. Aucune maquette validée à ce jour, aucune charte graphique tranchée. **Le fichier complet (HTML autonome, s'ouvre dans n'importe quel navigateur) est conservé dans `docs/MAQUETTE_ACCUEIL_BOUSSOLE_2026-08-27.html`** - toute IA qui reprend ce dépôt peut l'ouvrir et le lire directement, contrairement à un lien d'Artifact Claude qui reste privé au compte qui l'a publié. Ne pas relire ce fichier comme une décision prise : c'est un premier jet rejeté, utile pour ne pas repartir de zéro, pas pour être reproduit tel quel.
  - **Décision de scope du 2026-08-27 (soir)** : la refonte ambitieuse de toute la page d'accueil (nouveau modèle d'interaction, nouvelle charte graphique à inventer) est mise en pause - jugée trop incertaine (aucune maquette validée) pour le quota hebdomadaire restant de Denis (~25%). Chantier repris en priorité, plus modeste et déjà bien cadré : regrouper les 8 boutons en 2-3 catégories en réutilisant le style de carte déjà existant dans l'app (pas de nouvelle charte graphique) - portée contenue à `data/metiers.js` et `js/app.js` (AIDE_PAGES), atteignable sur le quota restant.
  - **Maquette simple "Porte d'entrée" du 2026-08-27, structure validée par Denis** : conservée dans `docs/` (voir plus bas), 6 cartes d'accueil au lieu de 3 CV + 1 Boîte à outils actuellement : **Mes documents** (Créer un nouveau CV / J'ai déjà un CV / Mettre à jour mon CV), **Me préparer à candidater** (Co-construire ma lettre / Préparer un entretien - nom choisi parmi 3 propositions), **Boîte à outils** (Découvrir mes compétences / Mes Repères / Mon Carnet / Lexique - ne garde que ce nom-là désormais, n'est plus l'entrée unique de tout), **Outils d'analyse** (Analyser ma candidature / Cohérence de mon dossier), **Veille informationnelle** (carte réservée, marquée "en développement", pas cliquable puisque le module n'existe pas encore), **Vous hésitez encore ?** (inchangée). **Décisions de style confirmées par Denis le 2026-08-27 (matin)** : une seule couleur partagée pour toutes les cartes (le bleu déjà utilisé aujourd'hui), jamais un système de couleurs par famille façon Lexique - jugé trop "loisirs/aventure", pas assez "accompagnement professionnel" ; chaque sous-carte à l'intérieur d'une fenêtre (Boîte à outils, Mes documents...) doit porter une courte phrase descriptive comme les cartes de l'accueil, pas juste une icône et un mot. **Ordre des 6 cartes CONFIRMÉ par Denis le 2026-08-27 (matin)**, dans cet ordre de lecture (gauche a droite, haut en bas, memes cartes qui s'enchainent naturellement sur plusieurs lignes selon la largeur d'ecran, pas de position fixe en pixels) :
    1. Mes documents
    2. Me préparer à candidater
    3. Boîte à outils
    4. Outils d'analyse
    5. Se tenir informé (nom confirmé le 2026-08-27, anciennement "Veille informationnelle") - **la sous-carte a l'interieur s'appelle "Actualités"** (decide le 2026-08-27, matin : "veille informationnelle" juge trop technique/jargon de metier par Denis - illisible pour un beneficiaire exterieur au monde de l'insertion - "Actualites" reste large, moins intimidant, moins pretentieux). **precision du 2026-08-27 (matin)** : cette carte d'accueil ne doit PAS afficher de badge "en developpement" (ca donnerait une impression que quelque chose est casse). Elle fonctionne comme "Mes documents" : un clic ouvre une fenetre avec des sous-cartes a l'interieur - pour l'instant une seule, "Actualités" (d'autres pourront s'y ajouter). Le message "ce module est en construction, voici ce qui est prevu" n'apparait qu'un niveau plus loin, quand on clique sur la sous-carte elle-meme, pas avant.
    6. Vous hésitez encore ?
    **Précision confirmée par Denis (2026-08-27, matin)** : Regard extérieur n'a pas de carte à part - c'est câblé et fusionné avec Mes Repères, accessible uniquement depuis l'intérieur de ce module, jamais une entrée séparée sur l'accueil ni dans une fenêtre de groupe.
    **Intégration des futurs modules confirmée par Denis (2026-08-27, matin)** :
    - ATS → sous-carte dans **Outils d'analyse**.
    - Regard recruteur → sous-carte dans **Me préparer à candidater**.
    - Répertoire des freins et ressources → renommé **"Ressources"** (nom confirmé définitivement le 2026-08-27 - hésitation avec "Solutions" tranchée : "Solutions" contredirait le principe même du module, pas de promesse de solution miracle, et "ressources" est déjà le mot utilisé pour "Vous hésitez encore ?" ; nom choisi par Denis : le module ne se contente pas d'identifier les freins, il apporte des solutions - "ressources" reflète mieux ça que "répertoire des freins"). Sous-carte dans **Boîte à outils**, description AFFINÉE et confirmée par Denis (2026-08-27, matin) : jamais des solutions miracles - des pistes de réflexion et des sites, un premier geste de secours, toujours à confronter avec le conseiller/accompagnateur socio-professionnel de la personne (ou, si elle n'en a pas, un message invite à se rapprocher d'une structure adaptée à sa situation). Phrase de carte proposée par Claude à partir de ce monologue : "Des pistes et des sites pour un premier pas face à un frein repéré - toujours à voir avec votre conseiller, ou la structure la plus proche si vous n'en avez pas." Comme Veille informationnelle, module pas encore construit : la carte reste normale, le message "en construction" n'apparaît qu'au clic dessus.
    **Maquette "Porte d'entrée" mise à jour (2026-08-27, matin)** : les 3 sous-cartes pas encore construites (ATS, Regard recruteur, Ressources) apparaissent maintenant au même titre que les outils déjà faits, aucune distinction visuelle - demande explicite de Denis. Icône de "Vous hésitez encore ?" corrigée aussi : plus de boussole inventée, un signpost simple qui reprend l'esprit du `bi-signpost-2` déjà utilisé dans la vraie app.

  - **[FAIT - 2026-08-31, 608 tests + vérif navigateur] Intégration provisoire des 4 pages de présentation dans la Boîte à outils** (`ouvrirChoixPreparationAccueil`, `data/metiers.js` : 8 → 12 tuiles). Maquettes **validées par Denis le 2026-08-31** : `MAQUETTE_INTRO_ATS.html`, `MAQUETTE_INTRO_REGARD_RECRUTEUR.html`, `MAQUETTE_INTRO_SE_TENIR_INFORME.html`, `MAQUETTE_INTRO_AIDE_DECISION.html`. Rendues en pages routées (`pageIntroAts` / `pageIntroRegardRecruteur` / `pageIntroSeTenirInforme` / `pageIntroAideDecision`, `data/metiers.js` ; routes `ats-intro` / `regard-recruteur-intro` / `se-tenir-informe-intro` / `aide-decision-intro`, `js/app.js`), même mécanisme que `pageCoLettre` (`htmlPageIntroModuleParcours`), **bouton d'entrée désactivé** + note "en cours de préparation" (`config.ctaDesactive` / `config.ctaNote`, petite extension de `htmlPageIntroModuleParcours`). "Retour" → menu Boîte à outils. Stub des 4 fonctions dans `tests/_domStub.js`. `AIDE_PAGES.cv` `#btnPreparationAccueil` : une ligne pour les 4 outils "en préparation". Aucune distinction visuelle des tuiles (demande Denis). La tuile « Comparer mes pistes » est **provisoire** : cette fonction n'est **pas un module à carte** (décision §2.1 : fonction transversale accessible depuis une fiche métier / « Vous hésitez encore ? » / une recherche).
  - **[CLOS - 2026-08-31] Re-placer ces 4 pages dans la nouvelle structure** : les tuiles provisoires ont disparu avec la suppression de l'ancienne `ouvrirChoixPreparationAccueil` (12 tuiles). ATS → sous-carte de « Outils d'analyse » ; Regard recruteur → sous-carte de « Me préparer à candidater » ; Se tenir informé → carte d'accueil dédiée (sous-carte « Actualités ») ; **« Comparer mes pistes »** → sous-carte de la carte 6 « Vous hésitez encore ? » (**décision Denis 2026-09-01 revue** : c'est devenu une carte d'accueil à part entière, plus « fonction transversale »). Les 4 gardent leur page routée (`ats-intro` / `regard-recruteur-intro` / `se-tenir-informe-intro` / `aide-decision-intro`), CTA désactivé + `_NOTE_MODULE_EN_PREPARATION`. Le jour où un module est construit, adapter le texte de sa page et porter le fichier dans le dossier du module.
    - **Exigences fonctionnelles à reporter le jour où chaque module sera construit** (notées aussi en tête de chaque fichier de maquette ; **synthèses multi-IA 2026-08-31** dans les 3 docs de chantier ci-dessous) :
      - **ATS** - `docs/CHANTIER_ATS.md` : détecter les techniques de triche dans le **texte** du CV (mots-clés en blanc sur fond blanc, texte invisible mais sélectionnable, blocs de mots dans les marges, police quasi nulle). À la détection : ne jamais s'en servir en silence ; signaler à la personne et **expliquer les risques** (candidature souvent écartée d'emblée, doute jeté sur tout le CV, ne remplace pas le travail sur le contenu), puis proposer de reformuler depuis l'expérience réelle. Récolte multi-IA : jamais de score ni de couple « présents / absents » (score implicite) → blocs qualitatifs ; mots-clés toujours sourcés (offre / fiche ROME) ; nom à changer (« Les mots de votre CV »).
      - **Regard recruteur** - `docs/CHANTIER_REGARD_RECRUTEUR.md` : donner "l'avis d'un recruteur" sur la reprise (ou non) des **couleurs / codes visuels de l'entreprise** dans le CV (technique courante, certains recruteurs y sont sensibles) - dire si le CV le fait déjà + comment un recruteur pourrait le percevoir, jamais comme une obligation. Entrées : image du CV + nom de l'entreprise (réutiliser "couleurs de l'entreprise" déjà capturé côté Découverte, commit `f77769e`). Récolte multi-IA : principe directeur « il ne dit jamais ce que pense le recruteur, il dit ce qu'un recruteur pourrait se demander » ; 5-6 axes bornés ; chaque doute reformulé en question ; pas de niveau de certitude ; mode texte en repli.
      - **Se tenir informé** - `docs/CHANTIER_SE_TENIR_INFORME.md` (section 16, synthèse multi-IA 2026-08-31) : le coeur du module = un **fichier texte de base à part** (`sources.txt` + `data/veille/*`), maintenu manuellement par Denis (au moins 1 fois par mois), qui alimente aussi le **Lexique** (nouveaux termes) et le **bloc `data/freins.js`** (acteurs / pistes de réflexion). Récolte multi-IA : réduire fortement les 14 rubriques (découpage par fonction : Comprendre / Trouver une source / Vérifier du récent / Territoire / Définitions) ; objet documentaire unique ; nom « Se tenir informé » remis en cause (« Mieux comprendre », « Comprendre le cadre »…). Points non tranchés : voir §16.12.
      - **Aide à la décision d'orientation** (module futur, pas une des 3 maquettes) - `docs/CHANTIER_AIDE_DECISION_ORIENTATION.md` (synthèse multi-IA 2026-08-31).

- [EN GRANDE PARTIE FAIT - nuit du 30 au 31/08, délégation complète de Denis] Généraliser le patron "page d'introduction de module" à tous les modules de la Boîte à outils.
  - **[CLOS] Mon Carnet** : `_carnetRenduIntro()` (commit `2a01513`), route `carnet`, affichée à l'entrée par la tuile, sautée depuis le panneau compact ("Voir tout"). Bouton permanent "Revoir la présentation du module", "Retour" repasse par l'intro. Vérifié navigateur.
  - **[CLOS] Lexique** : `_lexiqueRenduIntro()` (commit `b5f9faa`), route `lexique`, affichée par la tuile, sautée sur les liens profonds de la barre de recherche de l'accueil. Bouton permanent + "Retour" par l'intro. `checkLexique.js` sans erreur.
  - **[CLOS] Co-construire ma lettre + Préparer un entretien** : ce sont des cascades de fenêtres (pas des pages routées) → présentation rendue dans une fenêtre ERIP défilante via `ouvrirIntroModuleParcours()` + `_introBloc*()` (`data/metiers.js`, commit `6031a33`). Affichée seulement depuis la tuile (`{ avecIntro: true }`), jamais depuis le parcours guidé. CTA → cascade existante ; "Retour" → menu Boîte à outils.
  - **[CLOS] Cohérence + Repères** : déjà équilibrées le 2026-08-30 (fenêtres parallèles).
  - **[CLOS - `3696da1`, 2026-09-09] Page de présentation de « Mes Repères » remise au gabarit commun.** `_reperesRenduAccueil()` réécrit sur `.cv-section` + `<h4>` + emoji comme `_carnetRenduIntro` (fini le petit gris majuscule) ; 4 types en grille de cartes ; Journal + Regard Extérieur + Aller plus loin regroupés en un bloc accent ; ~90 lignes de CSS bespoke `.reperes-accueil-*` retirées ; contenu inchangé, rien de fonctionnel touché. Maquette `docs/MAQUETTE_INTRO_REPERES_2026-09-09.html`, cahier `docs/CHANTIER_INTRO_REPERES_2026-09-09.md`. Vérifié navigateur (1re visite / détour, clair + sombre + orange), `npm test` 780.
  - **[CLOS] Découvrir mes compétences** : page de présentation routée (`decouverte-intro`, `801ba08`) + parcours consolidé implémenté (fusions 1+2+3 et 6+7, parcours = page routée `decouverte`, barre de navigation du module) + refonte visuelle et accompagnement (`60eb040`) + 6 retours de Denis traités (section "Nuit du 31/08 (5e partie)" plus bas). Maquettes : `docs/MAQUETTE_INTRO_DECOUVERTE.html`, `docs/CONSOLIDATION_DECOUVERTE_PROPOSITION_2026-08-31.md`.
  - **[DÉTACHÉ vers `docs/CHANTIER_BOUTON_REVOIR_PRESENTATION.md` le 2026-09-01]** : position du bouton "Revoir la présentation" (en haut à gauche, libellé + icône-logo unifiés) + généralisation des 3 comportements du patron (points 1/2/3 ci-dessous) à Cohérence + Découverte. Traité comme un chantier à part, plus tard.
  - Historique de la demande initiale ci-dessous.
  - Généraliser le patron "page d'introduction de module" (page évolutive + barre d'icônes + tableau de bord modifiable, voir mémoire `project_page_introduction_modules.md`) à tous les autres parcours/modules de la Boîte à outils et au-delà. Chaque module doit gagner sa propre page d'introduction qui explique la démarche à venir, puis SE TRANSFORME EN PLACE en page de travail une fois commencée - jamais de saut de page en page ni de fenêtre en fenêtre séparée.
  - **3 comportements OBLIGATOIRES du patron, à implémenter dans CHAQUE module (Denis 2026-08-30).** Le module Bilan / « Analyser ma candidature » les a déjà (commits `D5a`..`D5d`, voir lignes 37-40) : **c'est l'implémentation de référence à copier**, jamais réinventer.
    1. **« Retour » ne saute jamais l'intro.** Depuis un écran de travail du module, « Retour » ramène à la **page d'introduction du module** (la dernière page vue), jamais directement à l'accueil. Ce n'est que depuis la page d'introduction elle-même que « Retour » mène à la vraie page précédente (accueil / Boîte à outils). Modèle : `D5d` (`_etatBilan.voirIntro`) ; déjà fait aussi sur Repères (`_reperesForcerAccueil` + `onclickPrecedent`).
    2. **Bouton permanent intro ⇆ travail, en haut à gauche de chaque écran de travail.** Un seul bouton, toujours au même endroit : il affiche « Revoir la présentation » quand on est sur un écran de travail (ouvre l'intro en mode détour, **non destructif**, le travail en cours est gardé tel quel) ; il change de libellé (« Revenir à mon travail » / « Continuer ») quand on est sur l'intro en mode détour, et ramène exactement là où on était. Pour **toutes les cartes, tous les modules**. Modèle : `D5c` (`bilanBoutonRevoirPresentation()`) ; Repères a déjà « ⚓ Revoir l'explication ». (Position : Denis dit « en haut à gauche » - le Bilan l'a en haut à droite, à harmoniser au moment du chantier.)
    3. **La question « Continuer / Recommencer » quitte la Boîte à outils et vit sur la page d'accueil du module.** Aujourd'hui, cliquer un module déjà entamé depuis la Boîte à outils ouvre une boîte de dialogue `confirmerAction('Reprendre …')` (ex. `ctDemarrer()`, `demarrerBilanCandidature()`). **À retirer de ce point de clic.** À la place : on atterrit toujours sur la page d'introduction / d'accueil du module, et **un bandeau** (rectangle en tête, jamais une modale) y apparaît quand le module contient déjà un travail engagé - document déposé (fichier **ou** texte collé) et/ou aller-retour avec un assistant déjà fait - avec le choix explicite **« Continuer » / « Recommencer »**. Réintroduire un document alors qu'il en existe déjà un déclenche la même question explicite. Modèle : `D5a`/`D5b` (bandeau « Vous avez une analyse en cours », `bilanRecommencerAnalyse()`).
       - **Périmètre du point 3** : tous les modules qui reçoivent un document CV / lettre de motivation / préparation d'entretien d'embauche, sous forme fichier **ou** texte collé. Aujourd'hui : « Analyser ma candidature » (déjà fait), « Cohérence de mon dossier ». À venir / à refondre : « Découvrir mes compétences » (refonte), « Coécrire ma lettre de motivation », « Préparer un entretien d'embauche ». **Et tout futur parcours qui demande ce type d'information** : prévoir ce comportement dès sa conception.
       - Précision à confirmer au moment du chantier : déclencheur exact du bandeau - au chargement de la page d'accueil dès qu'un travail existe (comme `D5b`), et/ou au moment précis où un document est (re)reconnu. Ne pas trancher seul, demander à Denis.

## Petits chantiers indépendants, module Bilan/CV (résumé - détail complet dans `docs/ETAT_DES_CHANTIERS_2026-08-24.md`, partie 3)

- [CLOS - 2026-08-28, branche `logiciels-rubrique-cv`, commit `6ed5bd5`, 596/596 tests + test navigateur réel complet] Logiciels absents du rendu CV final. `dossier.logiciels` (Excel, Canva...) était saisi, transmis à l'assistant lettre/entretien, présent dans `objetCV.logiciels` - mais aucun générateur de CV ne l'affichait. Corrigé : rubrique « **Logiciels et outils** » dédiée (jamais une fusion dans « Compétences »).
  - **Décisions de placement prises avec Denis (2026-08-28)** : colonne **latérale** (thèmes 2 colonnes) / colonne principale (thèmes 1 colonne) ; position **après « Compétences », avant « Langues »** ; titre **« Logiciels et outils »** ; sur **tous les formats** (PDF A4 détaillé / A4 Intégral / A5, Word Sobre/Institutionnel/Moderne/Projet XXL).
  - **Fichiers touchés (8)** :
    - `modules/cv-composeur/composeurStrategies.js` : `'logiciels'` ajouté aux 3 `ordreRubriques` (chronologique/mixte/parCompetences), juste après le bloc compétences.
    - `modules/cv-composeur/composeurComposants.js` : entrée `logiciels` dans `COMPOSEUR_REGISTRE_COMPOSANTS` (`colonnesCompatibles: ['principale','laterale']`) - sinon `composeurRubriqueCompatibleColonne` bloquait le passage en latérale.
    - `modules/cv-composeur/composeurComposition.js` : `RUBRIQUES_SUPPORTEES` + repli sans stratégie ; `contenuRetenu.logiciels` (passe-plat plafonné, jamais de relecture de `dossier`) ; capacité `logiciels: 8` (A4 détaillé) et `logiciels: 4` (A5) ; `RUBRIQUES_PREFEREES_LATERALE`, `ORDRE_LATERALE_XXL`, les 2 `ORDRE_LATERALE_FINAL_XXL`, l'ordre 1 colonne XXL (`ordre1Colonne`) ; fonction A5 `composeurComposerA5Portrait` (`logicielsA5`, `hauteurRubrique`, `ordrePriorite`, objet retourné).
    - `modules/cv-composeur/composeurRender.js` (Word) : `ICONES_PAR_TITRE['Logiciels et outils'] = '💻 '` ; branche `dessinerRubrique` `rubrique === 'logiciels'` (calquée sur `certifications`, garde-fou liste vide) ; `TITRES_RUBRIQUES_A5.logiciels` + branche A5 `construireRubriqueA5`.
    - `modules/cv-pdf-html/cvPdfTemplateA4.js` : `_PDF_ORDRE_RUBRIQUES_PAR_DEFAUT` (avant `langues`) ; fabricant `logiciels` dans `_pdfFabricantsRubriques` (`_pdfBlocListeSimple('Logiciels et outils', contenu.logiciels, 'competences', ...)` - icône réutilisée, aucune dessinée).
    - `modules/cv-pdf-html/cvPdfTemplateA5.js` : `_PDF_A5_TITRES_RUBRIQUES.logiciels`, `_PDF_A5_ICONES_RUBRIQUES.logiciels = 'competences'`, branche `_pdfA5ConstruireRubrique`, `TOUTES_CLES_A5`.
    - `modules/cv-pdf-html/cvPdfPanneauReglages.js` : `_PDF_NOMS_RUBRIQUES.logiciels` (libellé de la mini-barre flottante).
  - **Écart assumé avec le plan initial** : le plan listait `modules/cv-editor/exportDocxNatifCV.js` + `_NouveauxModeles.js` + `coloriationDocxNatifCV.js` pour le Word. Vérification dans le code : `MODELES_CV_DISPONIBLES` ne contient que `composeur`, dont le Word passe par `genererDocxComposeur` (`composeurMoteur.js`) → `composeurConstruireDocument` (`composeurRender.js`). `exportDocxNatifCV*` ne sert plus que les 16 modèles classiques retirés du sélecteur - **non touché** (aucun bénéfice utilisateur, risque de régression sur du code mort). `coloriationDocxNatifCV.js` idem : le Composeur colore ses titres via `titreSection()`, pas via ce fichier.
  - **Tests navigateur réels** (serveur `serveur_dev.py 8123`, dossier de test avec 4 logiciels, `construireDonneesPdfCV` + `construireHtmlPdfA4/A5` + `genererDocxComposeur` + décompression du `word/document.xml`) :
    - PDF A4 détaillé / A4 Intégral / A5 portrait : rubrique « Logiciels et outils » présente avec les 4 logiciels, après les compétences, aucune autre rubrique déplacée/disparue (comparé au rendu sans logiciels).
    - Word Sobre / Institutionnel / Moderne / Projet XXL, formats A4 détaillé / A4 Intégral / A5 : génération OK, `document.xml` contient la rubrique + les logiciels ; latérale après « Compétences professionnelles » (thème 2 colonnes), juste après le bloc compétences (thème 1 colonne).
    - Les 3 stratégies (chronologique / mixte / parCompetences) : « Logiciels et outils » toujours directement après le bloc compétences (ou en tête de la colonne latérale quand les compétences sont un bandeau en colonne principale).
    - CV **sans** logiciels : aucun titre « Logiciels et outils » nulle part (PDF + Word).
  - **Note layout PDF 2 colonnes** : l'algorithme glouton du PDF (`cvPdfTemplateA4.js`) répartit les blocs entre 2 colonnes de contenu par longueur de HTML - ajouter un bloc réel (comme n'importe quel contenu supplémentaire) peut faire basculer d'autres blocs d'une colonne à l'autre. L'**ordre de lecture** est préservé, aucune rubrique perdue ; le rééquilibrage fin par mesure DOM du panneau interactif prend ensuite le relais. Comportement inhérent, pas une régression introduite ici.

  <details><summary>Plan verrouillé initial (2026-08-28, conservé pour mémoire)</summary>

  **RÉSUMÉ (quelques lignes)** : créer une **rubrique « Logiciels » dédiée** (pas une fusion dans « Compétences » : les 3 regards - employeur / bénéficiaire / CIP - attendent une rubrique identifiable, et fondre la donnée sans étiquette donne une sensation de saisie « perdue » à un public à confiance fragile). Il n'y a plus qu'**un modèle de CV** (le Composeur) et **2 générateurs** (PDF `cvPdfTemplateA4/A5.js`, Word `exportDocxNatifCV.js` + `_NouveauxModeles.js`), tous pilotés par un **moteur de décision partagé** (`composeurComposition.js` + `composeurStrategies.js`) qui fixe l'ordre et la répartition des rubriques en colonnes. Ajouter une rubrique = la déclarer dans ce moteur (tous ses ordres, y compris les variantes XXL) + une fonction de rendu dans 3 endroits (Composeur, PDF, Word), en réutilisant le rendu d'une liste simple type `certifications`/`langues`, jamais un nouveau composant. **Estimé 1 à 2 h dont ~40 min de tests navigateur** (5 rendus × avec/sans logiciels). Réversible par `git revert`, aucune migration de données.

  **DÉCISIONS DE DESIGN À TRANCHER PAR DENIS AVANT DE CODER** :
  - Colonne : latérale (comme compétences/langues) ou principale ? → *reco : latérale, courte liste de mots*.
  - Position : après « Compétences », avant « Langues » ? Groupée avec les langues ?
  - Titre : « Logiciels » ou « Logiciels et outils » ?
  - Sur tous les formats (A4 / A5 / Projet XXL) ? → *reco : oui, cohérence*.
  - (Rubrique absente si `dossier.logiciels` vide = comportement standard, pas une décision.)

  **FICHIERS À TOUCHER (ordre de travail)** :
  1. `modules/cv-composeur/composeurStrategies.js` : ajouter `'logiciels'` dans chaque `ordreRubriques` (~3-5 stratégies), à la position décidée.
  2. `modules/cv-composeur/composeurComposition.js` : `RUBRIQUES_SUPPORTEES` (+`'logiciels'`) ; les listes latérales `RUBRIQUES_PREFEREES_LATERALE` (~l.821), `ORDRE_LATERALE_XXL` (~l.853), les ordres XXL (~l.1100-1101) ; le fallback par défaut (~l.319).
  3. Le moteur de contenu qui construit `composition.contenuRetenu` : exposer `contenuRetenu.logiciels` (liste de chaînes, pas de troncature agressive - c'est court).
  4. `modules/cv-composeur/composeurRender.js` : fonction de rendu de la rubrique (réutiliser le rendu « liste simple » de `langues`/`certifications`).
  5. `modules/cv-pdf-html/cvPdfDonnees.js` : passer `contenu.logiciels` dans l'objet retourné.
  6. `modules/cv-pdf-html/cvPdfTemplateA4.js` + `cvPdfTemplateA5.js` : dans `_pdfFabricantsRubriques`, ajouter `logiciels: function () { return _pdfBlocListeSimple('Logiciels', contenu.logiciels, 'competencesCles', iconesActives); }` ; vérifier que `_pdfEtendreOrdreRubriques` et le filtre par fabricants laissent passer `'logiciels'` ; réutiliser une icône existante (jamais en dessiner une, voir le chantier icônes).
  7. `modules/cv-editor/exportDocxNatifCV.js` + `exportDocxNatifCV_NouveauxModeles.js` : ajouter le bloc « Logiciels » dans l'énumération, sur le modèle exact du bloc `certifications` (juste après `competencesToutes`).
  8. `modules/cv-editor/coloriationDocxNatifCV.js` : si le titre de rubrique a besoin d'une couleur, ajouter l'entrée `logiciels`.

  **PIÈGES / À QUOI FAIRE ATTENTION** :
  - **Le calcul de place** (`composeurComposition.js`) répartit les rubriques entre colonnes selon l'espace disponible. Une rubrique en plus change ce calcul → **tester que l'ordre ET la colonne des AUTRES rubriques n'ont pas bougé, sur A4, A5 ET XXL** (c'est LE risque de régression principal).
  - `objetCV.logiciels` est **déjà** dans `normaliserDonneesCV.js` - ne pas re-normaliser, ne pas relire `dossier` ([[LECONS 9.13]], une seule source).
  - **Rubrique vide** : si `logiciels` est `[]`, le rendu ne doit RIEN afficher (pas de titre seul). Les blocs `certifications`/`langues` gèrent déjà ce garde-fou - le copier tel quel.
  - **Doublon** : vérifier que `competencesCles` (R006, `cvPdfDonnees.js:112`) et la fusion `contenuRetenu.competences` n'incluent pas déjà les logiciels (si la Solution A avait été partiellement tentée). Nettoyer d'abord si besoin.
  - **Word = 2 fichiers**, ne pas oublier `_NouveauxModeles.js`.
  - **Tests navigateur obligatoires** (les templates de rendu ne sont pas couverts par Node) : CV avec 3-4 logiciels → PDF A4 + A5 + XXL + Word → logiciels au bon endroit, aucune autre rubrique déplacée/disparue ; CV sans logiciels → rendu identique à avant.
  - `npm test` doit rester vert : chercher les tests qui portent sur `ordreRubriques` / `RUBRIQUES_SUPPORTEES` et les mettre à jour. *(Vérifié : aucun test Node ne référence ces symboles.)*
  </details>
- [CLOS - 2026-08-28, branche `vendor-bootstrap-local`, testé en navigateur] Auto-héberger Bootstrap CSS + Bootstrap Icons. Fichiers identiques au CDN (`bootstrap@5.3.2`, `bootstrap-icons@1.11.3`) téléchargés dans `vendor/` : `vendor/bootstrap.min.css`, `vendor/bootstrap-icons/bootstrap-icons.min.css` + `vendor/bootstrap-icons/fonts/bootstrap-icons.woff2` et `.woff`. Le `@font-face` du CSS des icônes pointe en relatif vers `fonts/...` - la mise en dossier respecte ce chemin, **aucune retouche du CSS nécessaire**. `index.html:9-10` : les 2 `<link>` pointent maintenant local. `.gitattributes` créé (`vendor/** -text`, `*.woff* binary`) pour garder les copies exactes. **Vérifié en direct** : Bootstrap chargé (styles OK), toutes les icônes `bi-*` s'affichent (test visuel : star/house/pencil/briefcase/chevron + les 6 cartes d'accueil), plus AUCUNE requête vers `jsdelivr`/un CDN (seul `cloud.umami.is` reste, analytics assumé), zéro erreur console, 596/596 tests. Les 8 avertissements « Tracking Prevention » disparaissent. Zéro régression visuelle (fichiers byte-identiques au CDN).
- [CLOS - fusionné dans `master` le 2026-08-28 (`--no-ff`), sur décision explicite de Denis "tu fusionnes, tu appliques tout, tu notes fini ; si je constate des désaccords je corrigerai plus tard"] Carte 3 : écran de préparation avant génération + corrections d'ordre + axe lisibilité. **596/596 tests, testé en navigateur à chaque étape.** Résumé de ce qui a été livré :
  - **Écran "Vos chiffres"** avant la génération (phrases à trous fournies par le 1er prompt via `phraseAChiffrer`, une par expérience couplée par `extraitConcerne` -> zéro carte grise, le fan-out multi-expériences a été retiré).
  - **Problème d'ordre corrigé** : l'écran "À compléter vous-même" (contexte candidature) passe AVANT la génération IA, plus jamais après. Nouvel ordre : [Vos chiffres] -> completude -> génération -> relecture -> clôture.
  - **Écran "poste visé" simple SUPPRIMÉ** : un seul endroit pour renseigner le poste, le panneau "Candidature" riche (ouvert depuis la completude). Panneau nettoyé : un seul bouton "Je valide" (plus d'accordéon), "Je réponds à une offre" ouvre le formulaire directement, champ métier/domaine juste sous le choix, "Je cherche simplement un emploi" valide directement. Tout gaté `depuisBilan` -> "Mon projet" inchangé.
  - **Écran "À compléter vous-même"** : le fond bleu plein suit l'action à faire (poste manquant -> "Aller compléter" plein + pulse ~15 s ; renseigné -> "C'est fait" plein).
  - **Axe `lisibilite`** : renommé "Lisibilité et **structure**" (registre + prompt) ; le prompt interdit tout jugement sur le visuel non visible (mise en page, police, couleurs, pages) ; une reco de structure (CV structuré) = mécanisme `structure`, exclue du lot de génération, affichée en carte de guidage "Organisation de votre CV -> ouvrir l'atelier CV", sans bouton "Générer".
  - **Fenêtre "choix assistant IA" du lot** (Carte 2 + Carte 3) : passée en version complète (4 étapes numérotées `ETAPES_DETAIL_CHOIX_IA_LOT` + rappel confidentialité + vidéo).
  - **LECONS** : règle 2bis ajoutée (tracer tout le parcours réel - écran d'avant, écran d'après, textes, fonctions, boutons - AVANT toute maquette ou proposition).
  - **Suite possible** (notée dans la section "Consolider le Bilan sur UNE page" plus bas) : balayage des docs référentiels pour "Lisibilité et forme" (FAIT le 2026-08-28).
  <details><summary>Historique de conception (conservé)</summary>

  **Maquette de travail** : `docs/MAQUETTE_CARTE3_PREPARATION_2026-08-28.html` (autonome, 100% simulée, 4 scénarios). Denis a validé écrans + graphisme + cohérence.
  - ✅ **Bloc 1** (`cd698d9`) : champ optionnel `phraseAChiffrer` dans le 1er prompt du bilan (`prompts/bilan-v1.md` schéma + précisions) + consigne renforcée sur `extraitConcerne` pour rattacher les recos `impact`/`credibilite` à la bonne expérience ; `modeles/recommandation.js` + `diagnosticResponseParser.js` portent le champ (optionnel) ; test ajouté. Aucune logique de diagnostic touchée.
  - ✅ **Bloc 2** (`3f508cc`) : les 2 écrans dans `js/app.js` (`htmlBilanAssistancePreparationMetier` + `htmlBilanAssistancePreparationChiffres` + branchers + dispatcher + routage dans `bilanPoursuivreAssistanceFinalisation`). Écran métier affiché si `posteCibleActuel()` vide, écrit `dossier.metierCible`. Écran chiffres : une carte par reco famille1 portant `phraseAChiffrer` (dédoublonnée), phrase avec `<input>` à la place des `___`. Saisies dans `_etatAssistanceBilan.metierSaisi` / `.chiffres` (transitoire). **Constaté en test : le couplage du Bloc 1 fait passer famille1 de 9 à 2 items (une reco = une expérience via `extraitConcerne`), le fan-out ne se déclenche plus en pratique.**
  - ✅ **Bloc 3** (`e25070b`) : `bilanAssistanceObjectifsParId()` construit `{ recoId: [objectifs] }` (poste visé sur chaque reco famille1 + phrase reconstruite avec les chiffres). Nouveau 3e paramètre `objectifsParId` sur `bilanOuvrirChoixAssistantLot()`, transmis à `bilanDemanderAmeliorationLot()`. Passe par `{OBJECTIFS_OU_NON_FOURNIS}` (par reco, déjà câblé), PAS par l'en-tête (le contexte candidature mémorisé est figé à la création du diagnostic). **Aucun prompt modifié au Bloc 3.** Vérifié : le bloc "Objectifs exprimés par la personne" du prompt généré contient bien le poste + la phrase chiffrée.
  - ✅ **Bloc 4** (`db3a9b1`) : fan-out multi-expériences retiré. `orchestrationAssistance.js` : `selection-requise` supprimé, le cas "plusieurs expériences, aucun extrait" rejoint `hors-automatisation` ; `bilanRepartirRecommandationsAssistance` a 4 groupes (plus 5). `app.js` : bloc `selectionRequise.forEach` retiré ; `bilanConstruireEtatRelectureAssistance` toujours `remplacement`/`selectionne:true` ; écran relecture toujours "Après application" (badge "Recommandé" + "À ajouter" retirés). 3 tests remplacés par 2. **-46 lignes nettes.** Note : le mode d'écriture `'ajout'` de `ecritureRelecture.js` n'est plus produit par personne mais reste comme primitive générique (tests conservés).
  - ✅ **Texte "Analyser à nouveau"** (`60cb0be`) : message enrichi de `htmlBilanCorrectionCloture` (CV corrigé pas l'original + points qui restent/apparaissent).

  ### PROBLÈME MAJEUR TROUVÉ PAR DENIS EN TESTANT (2026-08-28) - à corriger avant merge
  Denis a testé le parcours Carte 3 complet (via `demo-carte3-console.js` sur le serveur de test) et a trouvé une **incohérence de fond** : le contexte de candidature (métier précis / offre / entreprise) est demandé **APRÈS** la génération IA, à l'écran de clôture "À compléter vous-même" (Famille 2 -> `bilanOuvrirFenetreCandidature`). Or il devrait être demandé **AVANT** le 2e prompt. Denis est très remonté (module Bilan = source de frustration majeure), menace d'effacer le module. **Il faut corriger ça.**

  **PLAN EN 3 POINTS validé par Denis (2026-08-28) - à faire dans cet ordre :**
  1. ✅ **FAIT ET COMMITÉ (`e0629d4`), testé en navigateur (parcours complet famille1+famille2), 596/596 tests.** L'ORDRE : l'écran `completude` (Famille 2, contexte candidature) passe AVANT la génération. `bilanAssistanceEcranApresPreparation()` route vers `'completude'` si `_etatAssistanceBilan.repartition.famille2.length`, sinon `'generation'` ; appelé par `bilanAssistancePremierEcranFamille1` + `bilanAssistanceValiderMetier` + `bilanAssistanceValiderChiffres`. L'écran `preparation-metier` est sauté quand une reco famille2 existe (pas de doublon avec le panneau riche de la completude). Bouton "C'est fait" de la completude : enchaîne sur `'generation'` si famille1 reste, sinon cloture. `brancherEvenementsAssistanceRelecture` : va toujours directement à `bilanTerminerAgirVersCloture()`. Nouvel ordre : préparation -> completude -> génération -> relecture -> cloture.
  2. ✅ **FAIT ET COMMITÉ (`cfad003`), testé en navigateur, 596/596 tests.** L'écran "poste visé" simple (`htmlBilanAssistancePreparationMetier` + branchers + `bilanAssistanceValiderMetier` + `bilanAssistanceMetierVisePresent`) est SUPPRIMÉ. Un seul endroit pour renseigner le poste visé : le panneau "Candidature" (`bilanOuvrirFenetreCandidature`) ouvert depuis l'écran "completude". `bilanAssistanceObjectifsParId()` lit `posteCibleActuel()` (`dossier.metierCible`/`secteurCible`). `CONFIG_BLOC_CANDIDATURE_BILAN` retirée.
  3. ✅ **FAIT ET COMMITÉ (`cfad003`), testé en navigateur, 596/596 tests.**
     - Doublon "Terminer" / "Je valide" : `bilanOuvrirFenetreCandidature` n'utilise plus l'habillage accordéon `blocEditionCV` (qui ajoutait le "Terminer"). Affiche directement `contenuModeRecherche(true)` / `contenuCandidature()`, un seul bouton "Je valide".
     - "Je réponds à une offre d'emploi" (`depuisBilan`) ouvre DIRECTEMENT le formulaire entreprise/offre (`ouvrirFormulaireCoordonneesEntreprise`), plus de bouton "Renseigner..." intermédiaire.
     - "Un métier précis" / "Plusieurs métiers d'un même domaine" : champ texte (métier ou domaine visé) affiché DANS la colonne du choix retenu, juste sous la pastille, en continuité du choix (jamais tout en bas). Écrit `dossier.metierCible` / `dossier.secteurCible`.
     - "Je cherche simplement un emploi" (`depuisBilan`) = validation directe (ferme le panneau).
     - Tout est gaté `depuisBilan` : "Mon projet" est inchangé (vérifié : `contenuModeRecherche(false)` identique).

  **Merge** : branche `carte3-ecran-preparation` (11 commits, points 1+2+3 FAITS) - Denis doit valider le parcours complet avant merge. 596/596 tests Node sur la branche.
  **Serveur de test** : port 8137 (pas 8123 = autre session). Script démo : `scratchpad/demo-carte3-console.js` (faux bilan + bypass IA, à coller dans la console F12 après avoir ouvert http://localhost:8137/index.html).
  **Où ça s'insère** : parcours Carte 3 ("Je préfère être accompagné(e)"), immédiatement APRÈS le clic sur la carte, AVANT l'écran "Générer les N propositions". Aucun écran existant retiré, la barre de navigation n'est pas concernée.
  **2 nouveaux écrans** :
  1. **"Quel poste visez-vous ?"** - sauté si `dossier.metierCible` (ou équivalent) déjà rempli. Champ texte, première lettre auto-majuscule, touche Entrée = Continuer (si texte). Boutons : `Retour` (bouton, à gauche) | `Continuer sans préciser` `Continuer` (à droite). **Non bloquant** : sans poste -> l'IA reste générale ; avec poste -> le prompt de génération adapte le vocabulaire au métier.
  2. **"Vos chiffres"** - UN SEUL écran, toutes les expériences concernées (recommandations `impact`/`credibilite`), chacune dans un rectangle : titre de l'expérience + dimension + recommandation + **une phrase à trous** avec des champs `<input>` en ligne à la place des `___` (ex. "Sur ce poste, j'accueillais en moyenne ___ personnes par jour…"). Boutons : `Retour` (bouton) | `Passer cette étape` `Continuer` (à droite). Trous laissés vides -> l'IA reste générale sur ces points, jamais de chiffre inventé.
  **OPTION A retenue par Denis** : les phrases à trous sont **fournies par le 1er prompt du bilan** (un petit champ optionnel par recommandation `impact`/`credibilite` : une phrase courte avec `___`). Pas de questions génériques. Risque connu : alourdit un peu le Prompt 1 (déjà dense, voir [[LECONS 1bis]]) - garder ce champ minimal.
  **Transport technique** : le tuyau `objectifsParId` -> `{OBJECTIFS_OU_NON_FOURNIS}` du prompt "lot" existe déjà mais est toujours passé `{}` (voir `bilanOuvrirChoixAssistantLot`, `js/app.js:15959`). Il faut l'alimenter avec les phrases complétées + le métier visé. Attention : `objectifsParId` sert AUSSI au ciblage multi-expériences (correctif 2026-08-25) - ne pas casser cet usage.
  **Écran de fin (déjà existant, `htmlBilanCorrectionCloture`)** : les 2 boutons restent (`Finaliser et télécharger mon CV` -> ouvre l'Atelier CV, AUCUNE IA ; `Analyser à nouveau mon CV` -> relance le bilan sur le CV corrigé). Denis veut le texte explicatif enrichi (voir maquette écran de fin) : dire que "Analyser à nouveau" repasse le CV **corrigé** (pas l'original) pour voir l'amélioration ET les points qui restent/apparaissent.
  **DÉCISION TRANCHÉE PAR DENIS (2026-08-28) - couplage recommandation <-> expérience par le 1er prompt** : aujourd'hui l'app ne sait pas quelle recommandation concerne quelle expérience, donc elle teste CHAQUE reco sur CHAQUE expérience (3 recos x 3 expériences = 9 propositions, dont 6 marquées "n'apporte pas d'élément solide" et affichées grisées + décochées sur l'écran de relecture - "3 sélectionnées sur 9"). Denis n'en veut plus. **Nouveau fonctionnement voulu** :
  - Le **1er prompt du bilan** attache chaque phrase à trous (voir Option A) à **une expérience précise** du CV ("reco 1 -> expérience Agent d'accueil", etc.).
  - L'écran "Vos chiffres" montre donc **une phrase par (reco, expérience) identifiée** (dans l'exemple : 3 phrases, pas 9).
  - La génération IA ne tourne QUE pour ces paires.
  - L'écran de relecture affiche **uniquement les cartes utiles** (3), **zéro carte grisée** - les recommandations non couplées à une expérience ne sont plus jamais montrées comme des cartes vides.
  - Conséquence code : le fan-out multi-expériences du correctif du 2026-08-25 (`selectionRequise` -> push par expérience candidate avec `_multiDestination`/`pertinent`, badge "Recommandé", cartes grises `pertinent:false`) est remplacé par le couplage venu du prompt. Vérifier `bilanClasserRecommandationAssistance` (`orchestrationAssistance.js`), `bilanPoursuivreAssistanceFinalisation` et `bilanConstruireEtatRelectureAssistance` (`js/app.js`).
  **Méthode d'implémentation** : blocs testés un par un, test navigateur réel avec bypass du passage IA (injection d'une fausse réponse), zéro régression sur les modes `libre`/`copier`/Assistance existants.
  </details>
- [CLOS - fusionné dans `master` le 2026-08-28 avec le reste de la Carte 3] Axe `lisibilite` du 1er prompt du bilan ("Lisibilité et forme" -> "Lisibilité et structure") - l'IA ne peut pas juger la FORME. **Diagnostic (2026-08-28)** : le 1er prompt reçoit `candidature.cv` = `texteProfilEffectif('cv')` (texte structuré, PAS un blob : les rubriques et l'ordre sont préservés ; l'anonymisation est MANUELLE, la personne relit, aucune anonymisation automatique - voir `relectureConfidentialite.js:59`). L'IA peut donc juger l'ordre/la présence des rubriques, la longueur du contenu, les fautes, les formulations confuses. Elle NE peut PAS voir la mise en page, la police, les couleurs, les colonnes, l'aération, le nombre de pages.
  - ✅ **Point 1 - renommage** (commit à venir) : `axeAnalyseRegistry.js` `nom: 'Lisibilité et structure'`. Le libellé se propage partout (registre = source unique) - vérifié en navigateur (`BILAN_CATALOGUE_AXES`, `bilanNomsAxesDepuisCatalogue()`). L'`id` `lisibilite` inchangé. Aucun test ne codait l'ancien libellé. 596/596.
  - ✅ **Point 2 - resserrage du prompt** (commit à venir) : `prompts/bilan-v1.md` section 2 (axe) + section 3 étape 3 ("Première impression") réécrites : "uniquement ce qui se vérifie dans le texte fourni" + interdiction explicite de tout jugement sur mise en page / police / couleurs / colonnes / marges / aération / nombre de pages ("Tu ne vois pas le document lui-même"). "longueur" -> "longueur du CONTENU (volume de texte, jamais un nombre de pages)".
  - ✅ **Point 3 - FAIT ET COMMITÉ (`8276056`), testé en navigateur, 596/596.** `orchestrationAssistance.js` : nouveau mécanisme `'structure'` (reco `lisibilite` + CV structuré) au lieu de `'hors-modalite'` ; nouveau groupe `repartition.structure` (5 groupes). Le CV importé (`structurationDisponible` faux) garde `'hors-modalite'` (reformulation utile). `js/app.js` : les recos `structure` sortent automatiquement du lot de génération (`bilanAssistanceExcluesEnAttente` = `horsAutomatisation.concat(horsModalite)`, sans `structure`). `htmlBilanAssistanceRecommandationsNonAutomatisables()` affiche une carte de guidage dédiée ("Organisation de votre CV - à revoir vous-même" + bouton "Ouvrir l'atelier CV" -> `bilanOuvrirEditionCV()`), sans bouton "Générer". Texte de l'écran `aucune-automatisation` ajusté. 4 tests orchestration mis à jour. **Vérifié le 2026-08-28 : la cible `bilanOuvrirEditionCV()` ("Modifier mon CV") est la BONNE** - "Organiser mon CV en rubriques" (= `structurerTexteExistant()`) est réservé aux CV NON structurés (relance une structuration), or une reco `structure` n'arrive en Carte 3 que si `structurationDisponible === true`. Rien à changer, point clos. Reste juste, non bloquant : un "j'ai réorganisé" pour la cohérence de progression (`_bilanStatutsCorrection`) - marginal, à voir au chantier consolidation.
  - ✅ [FAIT - 2026-08-28] Balayage doc : "Lisibilité et forme" -> "Lisibilité et structure" dans `docs/REFERENTIEL_ANALYSE_BILAN_CANDIDATURE.md`, `docs/REFERENTIEL_REGLES_EVALUATION.md`, `docs/PHILOSOPHIE_EVALUATION_BILAN_CANDIDATURE.md`, `docs/ARCHITECTURE_PROMPT1_BILAN_CANDIDATURE.md`, `prompts/bilan-v1-extraction-prototype.md`. Renommage seul (le contenu de l'axe dans le prototype de fusion reste l'ancienne version, il sera revu dans le chantier fusion diagnostic/extraction).
  **Option B (plus tard, plus gros)** : remplacer l'axe IA par un contrôle déterministe côté app à partir des données structurées du CV (rubrique manquante, expériences sans dates, CV trop long en nombre d'expériences/lignes...) - "l'IA classe, le code garantit". Note pour plus tard. **Nuance à garder en tête** : pour un CV construit DANS l'app (pas importé), la "structure" du texte envoyé est celle du gabarit de l'app, pas un choix de la personne - l'axe `lisibilite` y sera quasi toujours 🟢, ce qui est acceptable.
- [EN COURS - **session 2026-08-30 (soir) : gros bloc fait.** Passe maquette complete (icones sans visage, encart de triage R13, pastilles de verdict, ecran « Organiser votre CV » Option 1, Accompagne = 4 ecrans) + **refonte VISUELLE de `htmlBilanRapport` en code** (tous les blocs en `.bloc-depli` replies, tons de la maquette, ambre au lieu du rouge) + Lot 4 (ecran « On organise votre CV », cas B sans carte grisee) + Lot 5 (blocEditionCV restyle leger) + R2/R16/R17/D11. Parcours Accompagne rejoue bout-en-bout OK. Etat detaille : `docs/CONSOLIDATION_BILAN_RETOURS_2026-08-30.md` (commits `a565cde` -> `a05ba4f`). **RESTE, en chantiers SEPARES** : fusion des ~5 collecteurs de contexte de candidature en `htmlPanneauCandidature()` unique (dette B.1) ; fusion `bilan-v1.md` + rubriques ; la partie « Preparer » one-page au-dela de l'ecran structuration ; R8 (cible exacte de la carte « reorganiser » de l'Accompagne). Detail « OU EN EST LE CHANTIER » en tete de `docs/CONSOLIDATION_BILAN_PLAN_2026-08-29.md`] **Consolider le Bilan sur UNE page** (retravailler l'architecture du module en même temps que les pages d'introduction). Denis : "mille fenêtres, des petites fenêtres dans des fenêtres, de page en page". Plan complet : `docs/CONSOLIDATION_BILAN_PLAN_2026-08-29.md` (voir son « Journal d'exécution » pour l'avancement au fil de l'eau). **Avancement 2026-08-30**, un commit par bloc, `npm test` + navigateur à chaque fois :
  - **Vague 1 (additif pur)** - 1.1 nom « Analyser ma candidature » (`e87f326`), 1.2 page d'introduction `htmlBilanIntro()` (`64f04b6`), 1.3 Carte 1 enrichie passage/pourquoi/chiffre (`9030e9a`), 1.4 couche 2 des axes « On regarde : … » (`4c71751`), 1.5 reporté (voir 3.3).
  - **Vague 2** - 2.1 réduit (`21d4dcd`) : fin de la fenêtre-dans-fenêtre du panneau Candidature du Bilan ; 2.2 **sauté** (les 2 écrans de choix d'assistant sont déjà complets) ; 2.3 reporté (voir 3.4).
  - **Vague 3 (partielle)** - 3.6 barre d'étapes 5 pastilles (`193a335`), 3.2 `dossier.bilanAvancementCorrection` sérialisé / DEC-2 (`700ea79`), 3.5 **déjà fait** (clôture déjà unique).
  - **3.3 « Préparer » : FAIT le 2026-08-30** (`013a0aa` P1 / `cde5fc0` P2 / `69e11c1` P3 / P4 docs+ménage). `htmlBilanPreparer()` : une page, 4 blocs `.bloc-depli` (Votre CV / Relire et masquer / Votre situation / L'offre visée) qui remplacent la cascade de 2 à 4 modales d'avant-module. `_etatBilan.preparer = { relectureFaite, situationTouchee, cvTexte }` (patron `.correction`/`.assistance` - **fait sans attendre 3.1**, le conteneur suffit). Aiguillage dans `pageBilanCandidature()` **et** `brancherEvenementsBilanCandidature()` au même rang. `demarrerBilanCandidatureAvecDepot()` → `bilanEntrerPreparation()`. Corps du ciblage extrait en brique partagée (`bilanCorpsCiblageOffreHTML`/`bilanCablerCiblageOffre`/`bilanLireCiblageOffre`). `bilanOuvrirChoixSituation()` + `bilanOuvrirCiblageOffreEmploi()` **retirées** (0 appelant). Relecture déplacée dans le bloc 2 → dépôt `cvDejaRelu:true`. Testé navigateur : 3 entrées (intro / Cohérence transversale / Recommencer), flux complet, clair+sombre, mobile 375, CV déjà présent, situation déjà connue. `npm test` 603. **Modernisation « Choisir l'assistant » + « Coller la réponse » faite le 2026-08-30** : M1 `7293b80` (jetons / mode sombre) + M2 `8d9f3e0` (`htmlChoixAssistantBilanCorps` : étiquettes empilées, bloc teinté, sous-étiquettes, 4 temps visibles d'un coup, `role="alert"` ; `lignePastillesAssistantsIA` gagne `sousLabel` ; sert Bilan plein écran + modale de lot, PAS encore Action/Découverte/Cohérence) + M3 `13afc93` (`htmlBilanEtapeImportIA` carte `.bilan-zone-collage` + correctif mode sombre sur `htmlCollageInstantane` et `htmlBanniereTransitionIA`) + M3+ `f12aa48` (phase `decompte` de `htmlBanniereTransitionIA` en « hero » : gros chiffre en pastille, piste qui se vide, repli « Si l'onglet ne s'ouvre pas » ; `#compteurDecompteIA` conservé) + **bloc 1.5** `623b76e` (phase `ouvert` : bouton « Rouvrir le site de [assistant] » + phrase, câblé sur `ouvrirAssistant*EnAttente` dans les 3 parcours — fonction nouvelle) + **M4** `f52d81a` (`htmlCollageInstantane` : bouton de collage **rectangulaire** « Coller la réponse » au lieu du rond 96x96 — décision Denis, cohérence ; états `.rond-collage-desactive` / `.pulse-collage-retour` inchangés ; touche tous les consommateurs). **Bloc 3.3 + sa modernisation : CLOS.** Testé navigateur clair + sombre, `npm test` 603. Maquettes : `ac3bf3c` / `de7197e` / `0375f2c` / `e4ed73e`.
  - **RESTE = un effort dédié** : **3.1** fusion des machines à états (`_etatAgirBilan`/`_etatCorrectionBilan`/`_etatAssistanceBilan` → `_etatBilan`, aucun bénéfice visible en soi, test navigateur exhaustif obligatoire) ; **3.4** rapport + choix de correction sur un écran / DEC-1 (+ bloc 2.3). Plus la **passe future** : fusion des ~5 collecteurs de contexte de candidature en un `htmlPanneauCandidature()` unique (inventaire + maquette avant code). Point de retour arrière après la partie faite = `700ea79`. **Carte trouvée en travaillant la Carte 3 le 2026-08-28 (à jour, vérifiée dans le code)** :
  - **Bonne nouvelle 1 - les "écrans" de travail sont DÉJÀ sur la page** (pas des modales) : `pageBilanCandidature()` (`js/app.js`) est le re-rendu maître ; il rend l'écran courant selon 3 variables d'état séparées :
    - `_etatAgirBilan` (entrée dans la zone "Agir")
    - `_etatCorrectionBilan = { mode: 'libre'|'copier'|'cloture', index, confirmationRecoId }` -> dispatchée par `htmlBilanCorrection()` / `brancherEvenementsCorrection()`
    - `_etatAssistanceBilan = { ecran: 'preparation-chiffres'|'generation'|'relecture'|'completude'|'aucune-automatisation', repartition, etatRelecture, chiffres }` -> dispatchée par `htmlBilanAssistance()` / `brancherEvenementsAssistance()`
    **Levier de consolidation le plus net** : fusionner ces 3 variables en UNE seule machine à états d'écran. Elles rendent déjà toutes dans la même zone de page, le découpage en 3 est purement historique (RC-08 a unifié la clôture mais pas le reste).
  - **Bonne nouvelle 2 - une seule primitive de modale** : `ouvrirFenetreERIP()` (une seule fenêtre ERIP à la fois, jamais d'empilement). Tout ce qui est "fenêtre" y passe. Un seul point de passage à réoutiller.
  - **Les modales qui restent** (candidature à la donnée périphérique, pas au parcours) : `bilanOuvrirFenetreCandidature()` (Candidature), `ouvrirFormulaireCoordonneesEntreprise()` (Coordonnées entreprise - ouverte DEPUIS la modale Candidature, la remplace en place = modale dans modale), `bilanOuvrirFenetreCoordonnees()` (Mes coordonnées / identité), `bilanOuvrirEditionCV(preOuvrir)` (Modifier mon CV, `tresLarge`, accordéon multi-blocs), `bilanOuvrirChoixAssistantLot()` (choix assistant IA).
  - **Duplication choix assistant IA** : `htmlBilanEtapeChoixIA()` (plein écran, 1er diagnostic, `ETAPES_DETAIL_CHOIX_IA`) VS `bilanOuvrirChoixAssistantLot()` (modale, lots Carte 2 + Carte 3). ✅ **2026-08-28 (`220ba77`)** : la modale du lot a reçu le traitement complet (étapes 1-2-3-4 via `ETAPES_DETAIL_CHOIX_IA_LOT`, rappel confidentialité, vidéo `export-ia-texte`). Restent 2 implémentations DISTINCTES (une plein écran, une modale, contenus d'étapes différents car parcours différents) mais plus de version "raccourcie". Une vraie fusion en un composant unique reste possible dans ce chantier.
  - **Sous-système dupliqué avec "Mon projet"** : `blocEditionCV()` / `wireBlocEditionCV()` (+ état `etatEditionCVBlocOuvert`, `_etatEditionCVSousSections`, `_etatEditionCVDerniereValidation`) est un éditeur accordéon parallèle qui refait `blocERIP()` / `wireBlocERIP()` de "Mon projet". Même idée ("éditer les rubriques du CV"), 2 impls. `CONFIG_BLOC_CANDIDATURE_BILAN` (sa variante bilan) a été supprimée le 2026-08-28 (bilanOuvrirFenetreCandidature n'utilise plus l'accordéon).
  - **Contrat de persistance à décider AVANT d'ajouter de l'état** : `_etatAssistanceBilan` / `_etatCorrectionBilan` sont transitoires, PAS sérialisés par la disquette (`sauvegarderSession()`). Seul `dossier.*` (metierCible, experiences, modeRecherche, typeRecherche...) l'est. Une machine à états d'écran unifiée doit trancher : quoi survit à un rechargement, quoi non.
  - **Point d'entrée** : `naviguerVers('bilan')` -> `pageBilanCandidature()`.
- [PISTE - pas un chantier ouvert] Savoir-faire/savoir-être CV : couplage de données à dissocier. `savoirFaireCV()` (`js/app.js:2972`) = `dossier.competencesCV` moins `dossier.savoirEtreCV` ; un savoir-être reste AUSSI dans `competencesCV`. **Re-vérifié dans le code le 2026-08-29 : toujours ainsi.** Rien de cassé, module fonctionnel - accepté tel quel par Denis. À reconsidérer **seulement si nécessaire pendant la refonte de la page d'accueil** (Denis, 2026-08-29). Ne PAS le traiter comme un `[À FAIRE]`.
- [CLOS] (vérifié le 2026-08-27) Surlignage inline téléphone/email dans la zone de saisie. Fait le 2026-08-22 ("point 1, stabilisation Bilan") et GÉNÉRALISÉ : `construireHTMLSurbrillanceDetection()` + `detecterCoordonneesSensibles()` (`data/metiers.js`) posent un calque `<mark>` ambre directement sur le textarea, à l'intérieur du composant partagé `htmlVerificationDocument()` / `cablerVerificationDocument()` qui a lui-même remplacé les 3 écrans de vérification/masquage qui divergeaient (wizard CV/Lettre, Préparer un entretien, Bilan). Cohérence transversale l'utilise aussi (`modules/coherence-transversale/collecte/relectureConfidentialite.js` appelle `htmlVerificationDocument`). Détecte téléphone, e-mail, réseaux (LinkedIn/GitHub), URL, âge/date de naissance étiquetée, et nom (formes étiquetées + recoupement avec l'e-mail). Les modules qui n'envoient pas de texte libre collé à une IA (Repères, Regard extérieur, Carnet, Lexique) ne sont pas concernés par nature.
- [CLOS - reformulé en règle de méthode] (2026-08-27) Audit UX "designer senior" complet du module Bilan. La demande d'origine (2026-08-22) visait un état du module qui n'existe plus (RC-07, RC-08, stabilisation 15 points, Assistance à la finalisation). Denis considère le module bouclé. La partie réutilisable a été convertie en **Règle 10 de la section 10 de `docs/LECONS_A_NE_PAS_REPRODUIRE.md`** : relecture "œil neuf" en 3 angles (hypothèses obsolètes / ruptures de parcours / incohérences entre modes) avant de déclarer n'importe quel module fini. Plus un chantier ouvert.
- [SORTI DES TÂCHES le 2026-08-28, versé dans `docs/IDEES_A_RECLASSER.md`] Pré-remplissage de "Organiser mon CV en rubriques" / un seul import IA qui donne le CV rangé en rubriques ET le diagnostic. La voie imaginée (fusionner les 2 prompts) est morte - testée, déployée, retirée le jour même, devenue la règle [[LECONS §1bis]] "jamais un prompt fourre-tout". Le besoin reste valable, une autre solution est à chercher, probablement pendant le chantier "consolider le Bilan sur une page". Décision de Denis : ce n'était jamais une tâche prête à coder, c'est une idée.
- [CLOS pour le concret, reliquat théorique versé dans le chantier Accessibilité] (vérifié le 2026-08-27) RC-03, bouton "Retour". Le problème réel (Retour ramenait à l'accueil ou à un écran fixe) est réglé sur les vrais carrefours de navigation : `naviguerVers()` capture la page réellement quittée dans `_pageOrigineAvantResultats` (page Action) et `_pageOrigineAvantProjet` (Mon projet), avec repli (`js/app.js:465`, `:3144`, `:6054`, `:12029` - "Vague 2" du commit `4484392`, 2026-08-22) ; le Bilan, Cohérence transversale et Repères passent tous `options.onclickPrecedent` à `barreNavigation()` pour viser le bon écran précédent. Il reste quelques `barreNavigation('cv', ...)` codés en dur sur des sous-écrans où l'accueil est une destination acceptable. La refonte "brancher entièrement `barreNavigation` sur `history.pushState`/`popstate`" n'a jamais été faite et ne cause aucun bug observé : rattachée au chantier stratégique 2 (Accessibilité), qui inclut l'harmonisation de la navigation. Plus un chantier ouvert à part.
- [CLOS] (2026-08-27, branche `nettoyage-code-mort-bilan-correction`) Nettoyage du code mort des modes de correction ET d'assistance du Bilan (ex-"Fusion du choix Guide/Libre"). **Vérifié exhaustivement dans le vrai code** (voir méthode en [[LECONS 9.23]]), supprimé, testé : `npm test` 596/596 verts (seul le fichier de test supprimé en moins) + test navigateur réel du parcours complet Bilan -> rapport -> les 3 cartes (`J'écris` -> `libre`, `Aidez-moi` -> `copier`, `Accompagné` -> Assistance) -> bouton "Corriger" -> sous-état confirmation -> écran de clôture, + les 3 écrans Assistance restants (`generation`/`completude`/`aucune-automatisation`), zéro erreur console, aucune fonction supprimée encore joignable (`typeof` = `undefined` en navigateur), aucun 404 pour le script retiré. **En plus des 2 modes prévus (`guide`/`texte`), 3 écrans Assistance orphelins du même type ont été trouvés et retirés dans le même commit (accord de Denis, option 1)** : `entree`, `classification`, `selection-experience`. Total : -529 lignes nettes dans `js/app.js`.
  Historique de la vérification (conservé) : depuis RC-08 (22/08) puis l'ajustement du 24/08, l'écran "Comment souhaitez-vous travailler sur votre CV ?" ne mène qu'à `libre`, `copier` et `Assistance à la finalisation`. Modes internes morts :
  - **`guide`** : provably dead. `_etatCorrectionBilan.mode` ne reçoit une valeur que via `bilanOuvrirModeCorrection(mode)` (`js/app.js:13376`) ; ses 3 seuls appelants passent `'libre'` (13350), `'copier'` (13355), `'libre'|'texte'` (15893). Zéro `'guide'`. Aucun `onclick`/`data-*` HTML, aucune restauration de session (`sauvegarderSession()` ne sérialise pas `_etatCorrectionBilan`), aucun test qui force la valeur. Commentaire `js/app.js:15064` : "jamais vers 'guide'".
  - **`texte`** : unreachable de l'extérieur depuis le 24/08. Seul appelant `bilanOuvrirModeCorrection('texte')` = ligne 15893, DANS `brancherEvenementsCorrectionTexte` lui-même (boucle fermée). La carte "J'écris avec mes mots" fait `assurerCVStructure()` -> toujours `bilanOuvrirModeCorrection('libre')`. Commentaire `js/app.js:13347` : "le mode 'texte' ne sert plus à ce point d'entrée".
  - **`parcoursCorrection.js`** (`bilanDeterminerParcoursCorrection()`) : zéro appelant runtime. Références : son propre test, `index.html:323` (`<script>`), une ligne de `.claude/settings.local.json`, des commentaires/docs. Son en-tête se déclare déjà orphelin (vérif exhaustive du 22/08).

  **À SUPPRIMER (un seul bloc, un seul commit)** : `modules/bilan-candidature/correction/parcoursCorrection.js` + `tests/bilanParcoursCorrection.test.js` + `<script>` `index.html:323` + `htmlBilanCorrectionGuide()`/`brancherEvenementsCorrectionGuide()` + `htmlBilanCorrectionTexte()`/`brancherEvenementsCorrectionTexte()` + les branches `=== 'guide'`/`=== 'texte'` dans `bilanOuvrirModeCorrection()`, `htmlBilanCorrection()`, `brancherEvenementsCorrection()` + simplifier le repli "Bientôt disponible" de `bilanOuvrirModeCorrection()` (ne restent que `libre`/`copier`, toujours valides) + mettre à jour les commentaires "4 méthodes guide/libre/copier/texte" et les docs (`INVENTAIRE_FONCTIONNEL_BILAN_CANDIDATURE.md` §11/§14, `ETAT_DES_CHANTIERS_2026-08-24.md`, `EVOLUTIONS_DIFFEREES_BILAN_CANDIDATURE.md`).
  **À NE PAS TOUCHER (partagé par `libre`/`copier`/`cloture`)** : `bilanDeclencherActionCorrection()` (dont la logique de bascule de confirmation, aujourd'hui commentée "modes 'guide' ET 'libre'" - retirer juste la mention `guide` du commentaire, jamais la logique), `_bilanStatutsCorrection`, `bilanBoutonQuitterCorrection`/`bilanCablerQuitterCorrection`, `htmlBilanCorrectionLibre`/`htmlBilanCorrectionCopier`, l'écran de clôture (`htmlBilanCorrectionCloture`/`brancherEvenementsCorrectionCloture`). Au moment de coder : grep chaque helper appelé DANS `htmlBilanCorrectionGuide`/`Texte` pour vérifier qu'il est aussi appelé ailleurs avant de le supprimer (ne retirer que ce qui devient réellement sans appelant).
  **Non-régression exigée par Denis (le Bilan a été "une croix" à stabiliser)** : `npm test` tous verts sauf le fichier supprimé ; PUIS test navigateur réel obligatoire (les tests Node ne chargent jamais `app.js`, voir [[LECONS 9.23]] règle 3) du parcours complet Bilan -> rapport -> les 3 cartes (`J'écris` -> libre, `Aidez-moi` -> copier, `Accompagné` -> Assistance) -> écran de clôture, zéro erreur console, coup d'oeil mode sombre.
- [PISTE - selon retour de terrain, PAS un chantier ouvert] Aide à la décision avant réécriture du CV. Décision de Denis le **2026-08-29** : on ne l'implémente pas. Bénéfice jugé trop faible pour le coût - une manipulation copier-coller de plus **avant même de commencer**, et un retour d'assistant probablement très technique que peu de personnes iront lire ; le mode le plus utilisé (Accompagné) demande justement le moins d'effort à la personne, donc la question « est-ce que ça vaut l'effort ? » y a le moins de sens. Reste : le prototype `prompts/aide-decision-avant-reecriture.md` + `docs/CHANTIER_AIDE_DECISION_AVANT_REECRITURE*.md`, gardés en réserve. À ne reconsidérer **que si** des retours terrain montrent une vraie demande (« j'aurais aimé savoir si ça valait le coup avant de m'y mettre »). Ce n'est ni sur la liste des tâches, ni sur la liste des chantiers. Vérifié 2026-08-29 : aucun câblage dans le code (`grep` sur `js/` `modules/` `data/` `index.html` = 0).

## Autres tâches en attente (jusqu'ici en mémoire personnelle uniquement, désormais ici aussi)

- [CLOS - 2026-08-31, commit `4c41685`, 608 tests + rendu vérifié] Raccourci de dictée vocale uniformisé : `Windows + H`, **en gras d'un bloc** (`<strong>`), avec `white-space:nowrap` pour ne jamais couper le raccourci en fin de ligne. 8 emplacements corrigés : Carnet (astuce + encart note), Repères, Regard extérieur, Découverte (encart récit), `data/metiers.js` (intro Découverte), Cohérence (collecte + entretien blanc), aides contextuelles de `js/app.js` (Boîte à outils + récit Découverte). Formes abandonnées : « Win + H », « touches Windows et H », mention non grasse.
  - **RÈGLE À APPLIQUER DÉSORMAIS (Denis 2026-08-31)** : toute nouvelle mention de la dictée vocale (nouveau module, nouvel écran, correction sur un module existant) s'écrit **exactement** `<strong><span style="white-space:nowrap;">Windows + H</span></strong>`. Jamais « Win + H », jamais « touches Windows et H », jamais laissé en graisse normale. À vérifier au même titre que les accents et le tiret court sur tout texte produit ou édité. Voir aussi `docs/LECONS_A_NE_PAS_REPRODUIRE.md` §1. Restent hors périmètre (fichiers de maquette, non corrigés) : `docs/MAQUETTE_INTRO_CARNET.html`, `docs/MAQUETTE_INTRO_DECOUVERTE.html`.
- [FAIT - 2026-08-28] Ton des 2 prompts Regard extérieur (`prompts/regard-exterieur.md`, `regard-exterieur-approfondissement.md`) : ajout d'un paragraphe **« Ton »** (parler à la personne, mots simples, ni langue de bois - « défi »/« challenge » interdits - ni dramatisation ni minimisation, frein dit au présent jamais comme une étiquette, chaleur sans compassion appuyée, sans jamais toucher règles 1 à 8 ni la traçabilité), d'un **exemple avant/après** (froid/jargon vs juste), et d'un **indice de registre** sur les champs texte du schéma JSON. Estimation avant : ton actuel ~14/20 ; après règle + exemple + indices ~18/20. Non vérifiable par test (les prompts ne sont pas exécutés par Node, et le rendu réel dépend de l'IA de la personne) - Denis a validé les formulations dans le chat. Banque de tournures fournie par Denis versée dans `docs/IDEES_A_RECLASSER.md` (3 axes intégrés au prompt, le reste pour le Bilan / une future réponse guidée / le Lexique).
- [CLOS - 2026-08-29] Balayage du ton sur les AUTRES prompts qui dialoguent avec la personne. **Constat après vérification prompt par prompt de `prompts/` (17 fichiers, hors `Old/`) : rien à faire au-delà des 2 Regard extérieur** (traités le 2026-08-28, entrée ci-dessus). Le patron « Ton » vise les prompts qui parlent à la personne de **sa situation** (repèrent un frein de vie, conseillent sur son parcours) - or les seuls de cette nature étaient les 2 Regard extérieur. Classement du reste :
  - **Produit un document destiné à l'employeur** (registre emploi = correct, jamais un défaut de ton) : `cv.md`, `lettre.md`, `lettre-v1.md`, `entretien.md`, `entretien-accueil.md`, `bilan-titre-accroche.md`, `bilan-v2.md`, `bilan-v2-lot.md`.
  - **Extrait / classe / reformule pour un document** (aucune interaction de conseil) : `extraction-cv.md`, `bilan-v1-extraction-prototype.md`, `decouverte-competences.md` (JSON `fragments`/`reciteNettoye`/`questionsCiblees` ; les questions comblent des manques factuels, jamais des freins de vie - Denis le retravaillera en chantier à part).
  - **Diagnostique une candidature, registre Bilan** (exclusion explicite de Denis) : `bilan-v1.md`, `coherence-transversale.md` (constats sur les pièces, déjà « bienveillante, jamais culpabilisante »), `coherence-transversale-entretien.md` (entretien blanc + retour, déjà « avec tact, jamais frontal »), `aide-decision-avant-reecriture.md` (variante hypothétique du diagnostic Bilan, son format exact) - **précision ajoutée le 2026-09-13** : ce dernier appartient au prototype « aide à la décision avant réécriture », abandonné par Denis le jour même de cette entrée (voir `docs/ETAT_DES_CHANTIERS_2026-08-24.md` partie 1) ; le fichier reste en réserve, hors du module actif « Comparer mes pistes », jamais chargé par aucun code.
  - Option marginale non retenue : une micro-touche universelle sur les 2 `coherence-transversale` (« parle à la personne, jamais comme un système qui rend un compte rendu » + interdire « l'analyse révèle que ») - jugée sans valeur suffisante, ils sont déjà tempérés.

- [CLOS - fusionné dans `master` le 2026-08-28 (commits `ce3b08e`, `16b4ae5`, `08d9c41`, `b7aef4a`, `3ae3e34`), 596/596 tests, testé en navigateur (accueil + Bilan : zéro « IA » visible, zéro robot, zéro erreur console)] Vocabulaire « IA » neutre sur toute l'app. **Raison** : Denis (content de l'IA lui-même) fait l'app pour un public dont une partie est réticente à l'IA - le mot ne doit pas se voir. **Plus aucune chaîne « IA » visible à l'écran.** Il subsiste des « IA » dans des commentaires de code et des noms internes (`analyserReponseIACV`, `V2_IA`, événements Umami…) - volontairement hors périmètre.
  **FAIT** : toutes les têtes de robot 🤖 → bulle de dialogue 💬 (`&#128172;`) ; « assistant IA » → « assistant » partout ; « l'IA » / « l’IA » → « l’assistant » (apostrophe courbe, pour ne jamais casser une chaîne JS single-quote) ; « une IA » → « un assistant » ; « réponse IA » → « réponse de l’assistant » ; « analyse IA » → « analyse » ; « passage IA » → « passage par l’assistant ». ~30 fichiers, texte visible + commentaires au passage. Regard extérieur utilisait déjà un télescope (laissé).
  **TAIL FINI le 2026-08-28** (`b...` sur `master`, via un petit script Node - jamais `sed`/`perl` pour ça, voir [[LECONS §9.24]]) : « Importer les informations IA » -> « ... les informations », « Infos IA » -> « Infos », « Contenu ajouté par l'IA » -> « ... par l'assistant », « réponse de l'IA » (4x + 2x Découverte) -> « réponse de l'assistant », « recommandation spécifique de l'IA » -> « ... de l'assistant », « (informations, IA, aperçu) » -> « (informations, envoi, aperçu) », « L'IA propose » -> « L'assistant propose », « même conversation IA » -> « même conversation », « absent du CV envoyé à l'IA » (4x) -> « ... à l'assistant », « Niveau annoncé par l'IA » -> « ... par l'assistant », « transmis à l'assistant IA que vous choisirez » (Bilan + Cohérence transversale) -> « ... à l'assistant que vous choisirez », « Analyse assistée par IA » -> « Analyse assistée ». **Vérifié : plus aucune chaîne « IA » visible sur l'accueil ni le Bilan, zéro erreur console, 596/596 tests.** Reste seulement 1 occurrence dans un COMMENTAIRE de code (`decouverteAnalyse.js:9`, hors périmètre « texte visible »).
  **Périmètre STRICT : uniquement le TEXTE VISIBLE à l'écran.** Pas les prompts (`prompts/*.md`), pas les noms d'événements Umami (`bilan_assistant_choisi`, `*_ia_*`…), pas les noms de fonctions/variables, pas les `data-*`, pas l'id de vidéo `export-ia-texte`, pas les commentaires de code. Uniquement les chaînes rendues à l'utilisateur.
  **Règle de remplacement (décidée par Denis)** :
  - « assistant IA » -> « assistant » (retirer « IA », garder « assistant »).
  - « l'IA », « une IA », « la réponse de l'IA », « passage IA », « transmis à l'IA »… -> reformuler AU CAS PAR CAS avec « l'assistant » / « votre assistant » / « l'analyse » selon ce qui sonne juste dans la phrase - jamais un « assistant » qui reste bancal. Objectif : le mot « IA » n'apparaît nulle part à l'écran.
  - **Icône tête de robot 🤖 (`&#129302;`, ~9 occurrences, toutes sur les écrans « Choisissez votre assistant IA » : page Action, Bilan, Cohérence transversale, Découverte, Regard extérieur + aides contextuelles `js/app.js` ~28181-28406) -> bulle de dialogue** (`&#128172;` ou l'icône Bootstrap `bi-chat-left-text`). Aussi le `🤖` littéral dans `modules/decouverte-competences/decouverteParcours.js:982`.
  **Taille** : ~100 chaînes contenant « IA » dans `js/app.js` + les modules (`modules/*/*.js`), + `index.html`. Balayage réel, pas un simple chercher-remplacer. **Complétion partielle acceptable** (Denis : « s'il reste quelques têtes de robot ou quelques "IA", ce n'est rien de gênant ») - aucun risque fonctionnel, juste une incohérence cosmétique.
  **Méthode** : grep `\bIA\b` dans les chaînes de texte, trier comment (comment/data/événement à ignorer) vs texte visible, corriger par bloc, test navigateur sur les écrans les plus vus (accueil, choix assistant, écran de collage, rapport Bilan). `npm test` doit rester vert (aucun test ne devrait porter sur ces chaînes, à vérifier).
- [À FAIRE] Mode navigation débutant/avancé (rebaptisé "accompagné" / "autonome" le 2026-08-27) - idée pour le futur chantier Accessibilité, pas encore conçue : bascule globale entre un mode "accompagné" (page d'introduction détaillée avant chaque module, vidéos, explications) et un mode "autonome" (comportement actuel de l'app, action directe). **Décision de Denis (2026-08-27, matin) : en attendant que ce chantier existe, l'app reste par défaut en mode "accompagné" pour tout le monde** - raisonnement : pour une personne autonome, un bouton "je continue sans lire" de plus n'est pas contraignant ; pour une personne qui manque d'autonomie, être plongée directement dans l'action sans savoir à quoi s'attendre l'est beaucoup plus. Le patron "page d'introduction" n'est aujourd'hui implémenté que pour 2 modules (Mes Repères, Cohérence transversale) - pas gênant jusqu'ici que les autres en soient dépourvus, pas plus gênant d'attendre d'avoir le temps/les ressources pour les faire correctement plus tard. **[Constat périmé, corrigé le 2026-09-11]** : ce n'est plus vrai - les **16** pages d'introduction du code ont toutes une page dédiée aujourd'hui (voir `docs/AUDIT_PAGES_INTRO_2026-09-10.md`). Le mode « accompagné par défaut » est donc déjà atteint pour tout le monde, sans bascule ; ce qui reste réellement à faire ici, c'est la bascule **« autonome »** elle-même (sauter l'intro pour qui le souhaite) - toujours rattachée au chantier Accessibilité, priorité inchangée.
- [ABANDONNÉ - 2026-09-09, décision Denis] **Module « Mes preuves »** (carte « Me préparer à candidater », frère de « Regard recruteur »). Après un après-midi de cadrage (versions successives : trois portes → deux portes → référentiel de compétences supprimé → champ libre « une compétence + un contexte + un métier visé »), et un **test de prompt sur deux assistants** (ChatGPT + Gemini, cas boulangerie → boucherie) : les deux sorties se contentent de reformuler l'entrée, le « pont » de transférabilité reste générique, aucune valeur ajoutée que « Préparer un entretien » ne produise déjà mieux face à une offre. Le module n'a pas de territoire propre. Les fichiers de conception (`docs/CHANTIER_MES_PREUVES.md` avec la réponse de test collée en fin, `docs/MAQUETTE_MES_PREUVES_PARCOURS_2026-09-08.html`, `prompts/mes-preuves.md`) ont été **archivés dans un commit dédié puis supprimés** le 2026-09-09 : récupérables dans l'historique git si la matière sert un jour à enrichir « Préparer un entretien ». Tous les renvois « Envoyer vers Mes preuves » / « module à venir » retirés du code et des docs dans le même passage.
- [ABANDONNÉ - 2026-09-09, décision Denis] **Lexique : grand sujet « Les compétences » + référentiel de compétences.** N'existait que pour nourrir « Mes preuves » (liste à cocher + définitions courtes). « Mes preuves » abandonné (entrée ci-dessus) → cette tâche aussi. Autre raison indépendante donnée par Denis : 80-120 fiches compétences transformeraient le Lexique en répertoire lourd et peu consulté, ce qui dénature le module. Le brief rédigé pour l'autre compte est marqué caduc : `docs/BRIEF_IA_LEXIQUE_COMPETENCES_2026-09-09_CADUC.md`. Si un jour la page « méthode » d'un autre module a besoin des 4 notions (savoir-faire, savoir-être, transversale, transférable), les définir en une phrase sur place, sans fiche Lexique.
- [CLOS] (vérifié le 2026-08-27) Disquette (export/import de session) : couverture du module Regard extérieur. Le point de blocage de 2026-08-25 (aucune page pour réafficher une analyse restaurée) a été levé entre-temps par le chantier "rapport en accordéons / Mes rapports" : `regardExterieurExporterEtatPourSauvegarde()` / `regardExterieurRestaurerEtatDepuisSauvegarde()` (`modules/regard-exterieur/index.js:2116`) persistent le tableau partagé `_regardExterieurRapports`, et sont câblés dans `collecterEtatsModulesPourSauvegarde()` / `restaurerEtatsModules()` (`js/app.js:27607`), eux-mêmes appelés depuis `sauvegarderSession()`, `restaurerSession()` ET `importerSessionFichierSelectionne()`. La vue "Mes rapports" (module Repères) réaffiche les rapports restaurés. Rien à coder. Reste seulement, si Denis le souhaite un jour, le petit message de rappel "pensez à sauvegarder" au bas d'une page Regard extérieur (Bilan et Cohérence transversale l'ont) - cosmétique, pas un vrai chantier.
- [EN COURS - Phases 1 / 2 / 3a / 3b FAITES] **Refonte de la carte « Mes documents »** (les 3 parcours CV `nouveau` / `pret` / `maj`). Denis 2026-09-01 : parcours le plus utilisé, fonctions de base de l'app.
  - **Phases 1 + 2 + 3a** : cartes « Préparer ma lettre et mon entretien » et « Mettre à jour mon CV » (pages d'intro routées + page « Préparer » dépliante), carte « Créer un nouveau CV » (intro routée + barre du module). Voir `docs/PLAN_REFONTE_MES_DOCUMENTS_2026-09-01.md` §6.
  - **Page « Créer mon CV » (ex-page Action)** (2026-09-02, mode `nouveau`) : faux choix + barre dans barre retirés, section « Et maintenant ? » (lettre + entretien), puis passe accordéon par accordéon (Adaptation replié/facultatif, Informations transmises en récap lecture, Choisissez votre assistant au standard, Importer / Exporter au thème). Aperçu / Composeur : **non refait** (chantier à part). Commits `3cce28c` → `754ff70`. Audit : `docs/AUDIT_PARCOURS_CREER_CV_2026-09-02.md`.
  - **Phase 3b — [CLOS] 2026-09-02** : les 4 écrans Activités / Actions / Environnement / Attentes fusionnés en **une page `pageVotreParcours()`** (route `votre-parcours`, parcours `nouveau` : Objectif → Votre parcours → Mon projet). 4 questions repliables, familles de choix = tiroirs distincts des cartes-pastilles, aperçu gris / choix qui remontent, compteur « X choisis sur [limite] ». Question 2 : 6 catégories → 4 par redistribution (`data/actionsProfessionnelles.js`, jamais un fourre-tout). Les 4 anciennes fonctions + `pageSelectionCatalogue` retirées (~280 lignes ; `trouverItemParId()` re-ajoutée hors zone supprimée). `dossier.activites/.actions/.environnement/.valeurs` inchangés. Plan `docs/PLAN_FUSION_VOTRE_PARCOURS_2026-09-02.md`, maquette v3 `docs/MAQUETTE_VOTRE_PARCOURS_FUSION_2026-09-02.html`. Commits `b868832` → `9a56874`. 646 tests verts, vérifié navigateur bout-en-bout, non-régression pret/maj.
    - **Re-vérification navigateur du 2026-09-02** : les 3 parcours de « Mes documents » (`nouveau` / `maj` / `pret`) repassés à la main. `nouveau` complet jusqu'à « Créer mon CV » ; `maj` et `pret` jusqu'au sélecteur d'assistant ; pages partagées en aval (`projet` / `revelation` / `resultats`) rendues sans fuite du mode guidé. Repli de navigation de « Mon projet » : `|| 'valeurs'` (route supprimée) devenu `|| 'votre-parcours'`. 2 correctifs trouvés au passage : libellé « Rediger » → « Rédiger » (`b931c26`), commentaire `CREER_CV_NAV_ETAPES` (`4c64015`). Détail : `docs/AUDIT_PARCOURS_CREER_CV_2026-09-02.md` §4.
  - **[CLOS 2026-09-03 — volet code neuf] Chantier « La mise en page » du CV** (= le rectangle « La mise en page » de « Vos documents », l'ex-« refonte du panneau Composeur / Aperçu et finalisation »). Approche « B allégé » : modèle canonique `dossier.reglagesMiseEnPageCV` (`modules/cv-mise-en-page/`, 63 champs, testé Node) + 1 traducteur par moteur existant (PDF iframe, Word Composeur), **sans fusionner les moteurs**. UI 3 niveaux (Simple / Je débute / Je veux tout régler = 6 sections) + grand aperçu PDF refait au langage de la maquette v6 (barre claire + encart + 3 groupes qui font apparaître la vraie mini-barre par rubrique) + Annuler / Refaire pas à pas + mémoire du niveau (`localStorage`) + corrections de revue (pastille sans icône, confirmation avant le dé, accent = couleur d'entreprise). **Audit zéro-régression fonction par fonction** sur toute la carte « Créer mon CV » : `docs/AUDIT_ZERO_REGRESSION_CREER_MON_CV.md` — 2 régressions trouvées et corrigées (taille du texte globale ; bloc photo). Sous-étapes 0→10, 1 commit chacune. Cadrage / suivi : `docs/CADRAGE_MISE_EN_PAGE_2026-09-03.md`.
    - **[CLOS 2026-09-04] Lot moteur** (commits `00341e5` `e359407` `737fbbf` `b8cf094` `7ca4cae`) : les réglages « montrés + expliqués » de l'audit 9bis reçoivent un effet réel sur les 2 moteurs via **un circuit partagé** (`reglagesMiseEnPageCV.<clé>` → traducteur → whitelist `composeurResoudreThemeGeneration` → `theme` → `composition` → Word `composeurRender` **et** PDF `cvPdfTemplateA4`). Couverts : Densité · Alignement (gauche/justifié) · Veuves-orphelines · Interligne · Espacement des paragraphes · Marges de page · **liste unique « rubriques à afficher / masquer »** (7 clés → `theme.rubriquesMasquees` vide `contenuRetenu`, chokepoint unique, + `permisMasque` pour l'en-tête). **Ordre des rubriques en Word = NON fait, asymétrie assumée (option A, Denis 2026-09-04)** : ordre Word laissé au Composeur, réordonnancement libre seulement en PDF (glisser-déposer du grand aperçu). `npm test` 703.
    - **Reste (dette B.7 de `BRIQUES_COMMUNES.md`, rattachée au découpage `js/app.js`)** : supprimer l'ancien panneau Word « Projet XXL » (`construirePaletteCouleurs`, ~2 345 l.) + la branche `colonneGauche` du CV — aujourd'hui rendus **masqués** (5 macros Word encloses, déclenchées à distance par `_mepClicMoteur`).
  - **Photo du 2026-09-02, largement périmée depuis - corrigée le 2026-09-04.** « Reste (audit §4, points 3/5/6/7) » a été quasi entièrement absorbé par des chantiers postérieurs, jamais répercuté ici :
    - **Point 3 (densité des cartes de sélection) - FAIT** : « Votre objectif » (5bis, `MAQUETTE_OBJECTIF_RESTRUCTURE`, cartes compactes grille 3 colonnes) et « Votre parcours » (3b, fusion Activités/Actions/Environnement/Attentes).
    - **Point 5 (allègement « Mon projet » + « Faire le point ») - FAIT** : c'est très exactement le chantier « Refonte du parcours guidé » (`ETAT_REFONTE_PARCOURS_GUIDE_2026-09-02_SOIR.md`, se nomme lui-même « sous-phase allègement »), TERMINÉ CÔTÉ CODE - renommage Vos informations/Votre profil, sous-sections à plat, jetons modernisés, catalogues inline.
    - **Point 6 (bloc Lexique « Conditions de travail ») - FAIT le 2026-09-04** (ligne périmée corrigée ici) : voir l'entrée `[CLOS - 2026-09-04]` « Lexique : nouveau grand sujet « Conditions de travail »» plus bas dans ce même fichier - 12 fiches neuves.
    - **Point 7 (module « Défendre mes conditions / négociation ») - PAS FAIT.** Nouveau chantier, jamais cadré.
    - **« Bande comparative des 3 modes (idée +2) »** : pas « reste ouvert » comme disait cette photo - **retirée** par la décision v3 du 2026-09-01 (§8 du plan, point 12), redondante avec le reste.
    - **Refonte du panneau Composeur (Aperçu et finalisation)** : FAITE par le chantier « La mise en page » du CV (2026-09-03/04), sauf la suppression de l'ancien panneau Word (dette **B.7**, rattachée au découpage `js/app.js`, état des lieux fait le 2026-09-04 - voir `BRIQUES_COMMUNES.md`).
- [CLOS 2026-09-04 (résidu 6conv-4 bouclé, `a5a3675`) — corps exécuté le 2026-09-03] **Passe de convergence des autres parcours sur les écrans / fenêtres de « Vos informations » — priorité au module Bilan.** Demandé le 2026-09-02, **remonté en priorité majeure le 2026-09-03** : le chantier « Refonte du parcours guidé » (étapes 5bis + 6) a construit une nouvelle architecture d'écrans que **Bilan, Découverte et « Modifier / Corriger mon CV » ne partagent pas encore** — ils gardent les anciennes fenêtres / pastilles, **sans aucune régression fonctionnelle** (scoping délibéré via `_vosInformationsRenduEnCours`, `depuisBilan`, `pageActuelle === 'objectif'`), mais visuellement et structurellement en retard. Le Bilan est le plus concerné (il réutilise le plus de machinerie). **Décider ensemble comment adapter la nouvelle architecture au Bilan** (et aux 2 autres), en réutilisant les composants, jamais en recopiant.
  - **PROFONDEUR CHOISIE : B** (alignement visuel + mise à plat, sans fusionner les moteurs `blocEditionCV` / `blocERIP` — cette fusion reste dette B.3). Décision Denis 2026-09-03.
  - **SUIVI DES SOUS-LOTS** (un commit + `npm test` + navigateur chacun ; trace tenue à jour ici, demande Denis 2026-09-03) :
    - **[FAIT] 6conv-1 (`931ae1e`)** — drapeau `_vosInformationsRenduEnCours` renommé `_renduInfosUnifie` et posé vrai aussi pendant `bilanOuvrirEditionCV()`. « Modifier mon CV » rend désormais les jetons de la brique commune (civilité / photo / permis / véhicule / catégories / certifs Oui-Non) + les catalogues en menu déroulant inline + « Autre » (plus de `ouvrirFenetreCatalogue`). Câblage inchangé. Aperçu « récupéré depuis votre CV » préservé. Vérifié navigateur.
    - **[FAIT] 6conv-2 (`1a72401`)** — `blocEditionCV` honore `config.sousSectionsPlates` : sous-sections à plat (`.sous-section-plate` / `.ss-titre-plate`), contenu toujours visible, plus de chevron `▸` ni de bouton « Je passe à l'étape suivante » / « Terminer ». Aperçu « récupéré depuis votre CV » + bouton « Revenir aux informations initiales » conservés. `wireBlocEditionCV` : câblage déclenché dès `sousSectionsPlates`. Flag ajouté à `CONFIG_BLOC_TITRE_ACCROCHE` + `CONFIG_BLOC_COMPETENCES_CV`. Vérifié navigateur (Identité/Mobilité/Langues, Loisirs/Engagements, Formations/Certifications à plat ; jetons + catalogues inline ; saisies persistées).
    - **[FAIT] 6conv-3a (`85ec0b0`)** — fenêtre « Candidature » du Bilan : `contenuModeRecherche` gagne `var jetons = !depuisBilan || _renduInfosUnifie` ; la fenêtre pose `_renduInfosUnifie` → mode / type de recherche en jetons radio (comme `pageObjectif`), champ cible métier/domaine + entreprise en ligne conservés (`depuisBilan`). Fenêtre « Coordonnées » : rien à aligner (`construireChampsIdentiteFenetre`, aucun jeton). Vérifié navigateur.
    - **[FAIT] 6conv-3b — OPTION C** (cadré 2026-09-03, décisions Denis : approche affinée + option **2a** aiguillage + les 3 sous-lots) :
      - **6conv-3b-1 (`1fecb9f`)** — `contenuCiviliteRecruteurCandidature` / `contenuCouleurEntrepriseCandidature` + les 2 groupes de `ouvrirFormulaireCoordonneesEntreprise` → jetons de la brique commune (via `_renduInfosUnifie` ; câblage bascule `.actif` **et** `.is-actif`). Aucun changement de flux.
      - **6conv-3b-2 (`dce3024`)** — `verifierAvantPasserAction` : stage/alternance/PMSMP sans structure → `ouvrirDemandeCoordonneesEntreprise` (comme offre). Variante immersion : « Connaissez-vous la structure d'accueil ? », « Continuer sans » ne reclasse rien. `ouvrirFormulaireCoordonneesEntreprise` variante immersion : libellés « structure d'accueil », pas de champ « offre », **option 2a** → structure/lien écrits dans `dossier[stage|alternance|immersion]`, recruteur + couleur dans `rechercheCandidature`, `typeRecherche`/`typeCV` non touchés.
      - **6conv-3b-3 (`bf5840e`)** — `contenuCandidature` (immersion) : recruteur + couleur retirés quand `pageActuelle === 'objectif'`. Conservés dans la fenêtre Candidature du Bilan. Écran Objectif conforme à `MAQUETTE_OBJECTIF_RESTRUCTURE` v5.
      - **Couleur → « La mise en page »** : **[FAIT 2026-09-03, chantier « La mise en page » sous-étape 9]** bouton « Accent = couleur de l'entreprise » (`_htmlBoutonAccentEntreprise()`, `js/app.js`) rendu dans « Je débute » + « Tout régler › Les couleurs » quand `dossier.rechercheCandidature.couleurEntreprise` est un hex valide ; le clic réutilise le circuit du champ « Accent » (annulable). Voir `docs/CADRAGE_MISE_EN_PAGE_2026-09-03.md`.
      - **[FAIT] Passe nettoyage (`836ef48`)** : retrait du code mort repéré — `ouvrirFenetreCandidatureRapide`, `blocObjectifDetails`, `blocTypeContratTemps`, `blocAccepte`, la branche offre/spontanée/reconversion de `contenuCandidature()` **et** de `wireObjectifDetails()`, `GROUPE_CANDIDATURE` (–101 lignes nettes). Vérifié navigateur (immersion + offre + fenêtre Bilan + « Passer à l'action »). ~~Point mineur restant : « Continuer sans » côté immersion ne pose pas de drapeau anti-re-prompt.~~ **RÉGLÉ 2026-09-10 (vérifié contre le code)** : `_immersionStructurePassee` est bien posé à `true` sur « Continuer sans » (`js/app.js` ~7349 / ~7473), lu par la garde `if (!imm.structure && !_immersionStructurePassee)` (~7280), réarmé au changement d'objectif (~32668) et à `reinitialiserSession` (~4590). C'est le polissage `a2c8adc`.
    - **[FAIT] 6conv-4 (`e6d881f`)** — `bilanDeclencherActionCorrection()` (`resolution.type === 'extrait'`) : pour une expérience **PRO**, ouvre l'éditeur d'expériences en ligne du parcours guidé (`blocERIP(CONFIG_BLOC_EXPERIENCES_PRO)` + `wireBlocERIP`), pré-positionné sur l'expérience ciblée. Même forme de donnée. `experiencesPerso` garde `ouvrirFenetreModifierExperienceUnique` (hors périmètre).
    - **[FAIT] 6conv-4 résidu (2026-09-04)** — la carte « Modifier cette expérience » de `pageResultats` (`data-modifier-exp-page`, `js/app.js` ~22572) ouvre elle aussi cet éditeur en ligne, plus l'ancienne modale. `bilanOuvrirFenetreExperienceProInline(index)` généralisée en `ouvrirFenetreExperienceProInline(index, rerender)` (re-rendu paramétrable : `pageBilanCandidature` par défaut pour le diagnostic, `pageResultats` pour cette carte). Contexte `'page'` = toujours `dossier.experiences` (pro). Vérifié navigateur : éditeur pré-rempli sur l'expérience ciblée, « Valider » → `dossier.experiences[i]` mis à jour, « Terminé » → `pageResultats` re-rendue, ancienne modale absente, 0 erreur console. `npm test` 703. Seul `experiencesPerso` (diagnostic Bilan) reste sur l'ancienne fenêtre.
    - **[FAIT partiel] 6conv-5 (`cdf6097`)** — Découverte : `obtenirDefinitionEtape` enveloppe le rendu de toutes les étapes avec `window._renduInfosUnifie = true` (restauré en `finally`) → les composants partagés (`contenuMobilite`, `contenuMissionsFormationBrouillon`) rendent la brique `.jeton`. + jetons propres à l'écran étape 8 : « De quoi disposez-vous ? » (`data-type-formation-infocompl` → `choixRadioVosInfos`) et « Quel est le niveau ? » (`data-niveau-rncp-infocompl`, double attribut conservé → jeton à la main). Câblage inchangé (re-rendu). **Reste (autre passe, non urgent)** : civilité (étape identité) + recruteur / couleur (étape accueil) restent `.pastille` — sur des écrans sans jetons, non gênant. Vérifié navigateur (étape 8 : 5 groupes en jetons, 0 `.pastille`, clics OK).
    - **[FAIT] 6conv-6 (2026-09-03, vérif seule, aucun changement de code)** — « Modifier mon CV » : les 5 blocs (Titre et accroche / Vous / Parcours / Compléments / Compétences) rendus à plat, 0 chevron, 0 bouton « étape suivante », jetons + catalogues inline, aperçu « récupéré depuis votre CV » présent + retrait par ✕ OK. Fenêtre Candidature du Bilan (immersion) : recruteur + couleur en jetons, durée/dates/heures inline. Éditeur d'expérience depuis le diagnostic : formulaire inline pré-rempli, bascule « en poste », « Terminé ». Testé **clair + sombre + mobile 375** : aucun débordement horizontal, jetons qui s'enroulent proprement, thème sombre appliqué (variables). `pageBilanCandidature()` sans erreur. **Seule erreur console : le 404 connu `cloud.umami.is`.** `npm test` 646 verts.
  - **CONVERGENCE BILAN — TERMINÉE** (6conv-1 → 6conv-6). 6conv-5 (Découverte) : fait partiel (écran Mobilité/Formations ; pastilles identité/accueil restent, non urgent).
  - **[FAIT] Dette B.3 (fusion `blocEditionCV` → `blocERIP`) — RÉSORBÉE 2026-09-03** (`3c55190` B.3-1, `4715e0b` B.3-2/3/4). Détail dans `docs/BRIQUES_COMMUNES.md` B.3. `blocEditionCV` / `wireBlocEditionCV` + 3 variables d'état retirés (−231 lignes) ; « Modifier mon CV » tourne sur le moteur unique. Zéro pollution des écrans `blocERIP` (var dédiée `_editionCvBlocsOuverts`). Vérifié navigateur clair + sombre + mobile.
  - **[FAIT] Polissage (`a2c8adc`)** : (a) jetons Découverte étapes identité (civilité) + accueil (recruteur, couleur d'entreprise) via `choixRadioVosInfos` → **Découverte entièrement en jetons** ; (b) drapeau `_immersionStructurePassee` → l'invite « Connaissez-vous la structure d'accueil ? » ne se re-affiche plus après réponse (rearme sur changement d'objectif + `reinitialiserSession`).
  - **[FAIT] Étape 8 — aide contextuelle / l'ampoule (2026-09-03)** — `AIDE_PAGES` / `AIDE_FENETRES` (`js/app.js` ~31685) revues pour les 6 écrans du parcours `nouveau` : `objectif` (`.objectifs` → `.grille-objectif` + `#blocERIP-candidature` + `#blocERIP-projet`), nouvelle entrée `votre-parcours` (`.vp-question`), `projet` (`.grille-mon-projet` + `[data-blocs-tout]` en remplacement de `.resume-bloc-erip .pastille-mini-conteneur` morte + texte `.barre-navigation-fixe`), `revelation` (`.accordeon` → `.bloc-erip`, `#blocERIP-metiers` seul), nouvelle entrée `assistant` (`.assistant-rect` + `.barre-navigation`), `resultats` (`.cv-section` / `.ligne-etapes-action` mortes → `[data-rect-doc]` + `.et-apres`). Entrée commune « Où vous en êtes » (`.barre-etapes-bilan`, le stepper) en tête des 6. Ménage : `_AIDE_ETAPES_QUESTION_MULTI` et `AIDE_FENETRES['candidature-rapide']` supprimés (consommateurs retirés). Vérifié navigateur : 0 entrée écartée par le filtre de la visite guidée sur les 6 écrans. `npm test` 646 verts.
  - **CHANTIER « REFONTE DU PARCOURS GUIDÉ » — TERMINÉ CÔTÉ CODE** (étapes 1→8, convergence Bilan 6conv-1→6, dette B.3, polissage a+b). Reste : vérification manuelle de bout en bout par Denis (`nouveau` / `maj` / `pret` / `domaine` / Découverte).
  - **Ce que le parcours guidé a gagné et que Bilan / Découverte / Modifier n'ont pas encore** :
    - **Brique commune `htmlJetons()` / `.jeton`** (étape 6) : jetons case à cocher / radio modernisés (coche dessinée, halo focus, thème sombre). Bilan/Découverte/Modifier gardent `.pastille` / `.pastille-selection` / `.btn` (voir dette **B.6** de `BRIQUES_COMMUNES.md`).
    - **Sous-sections « à plat »** (`sousSectionsPlates`, étapes 5bis A5 + 6a-bis/quater) : plus d'accordéons internes qui se replient seuls, contenu toujours visible, plus de bouton « Je passe à l'étape suivante ». Bilan (`blocEditionCV`) et les fenêtres modales gardent le pliage.
    - **Restructuration de l'écran Objectif** (6a-5) : durée / dates / heures d'une immersion déplacés dans « Vos disponibilités » ; (à venir) bloc recruteur retiré d'Objectif ; couleur d'entreprise déplacée vers « La mise en page ». Les modales **candidature rapide** et **fenêtre Candidature du Bilan** gardent l'ancienne disposition (`contenuCandidature` non scopé pour elles).
  - **Bloc recruteur + bloc couleur d'entreprise sur l'écran Objectif — à traiter ICI, décision Denis 2026-09-03** (6a-5 du parcours guidé a été clos à 6a-5a, ces 2 points renvoyés à cette passe). État : `contenuCiviliteRecruteurCandidature()` + `contenuCouleurEntrepriseCandidature()` sont rendus à **3 endroits** (Objectif immersion via `contenuCandidature`, modale « candidature rapide », fenêtre Candidature du Bilan) ; sur Objectif ils n'apparaissent que pour les objectifs d'immersion. La maquette v5 ne les veut pas sur Objectif (au plus une ligne d'encart « facultatif : … », saisie reportée). **Cible (option C, validée Denis)** : (1) retirer les 2 blocs de l'écran Objectif ; (2) la question **recruteur** est demandée à « Passer à l'action » pour **tous** les objectifs — aujourd'hui `verifierAvantPasserAction()` n'ouvre le formulaire entreprise que si `typeRecherche === 'offre'`, jamais pour une immersion → ajouter le point de capture immersion (sinon régression : l'immersion n'a aucun autre endroit) ; (3) la **couleur d'entreprise** rejoint « La mise en page » (`rectDoc('mise-en-page')` / Composeur, `construireContenuApercuFinalisation`), avec le chantier `MAQUETTE_MISE_EN_PAGE`. Faire les 3 endroits dans la même passe (même code partagé), jamais un drapeau `pageActuelle` provisoire dans `contenuCandidature`.
  - **Le point de friction déjà identifié pendant l'étape 3 (sous-étape 3.2) : c'est le module Bilan (« Analyser ma candidature »).** Quand on corrige une expérience depuis le diagnostic du Bilan, `bilanDeclencherActionCorrection()` ouvre l'**ancienne fenêtre modale** `ouvrirFenetreModifierExperienceUnique(index, 'experiences')`. Le parcours guidé, lui, a depuis un **formulaire d'expérience en ligne** (`CONFIG_BLOC_EXPERIENCES_PRO` / `contenuExperiencesProInline` / `wireExperiencesProInline`, `js/app.js`). **Aucun problème fonctionnel** : les deux écrivent exactement la même forme `{poste, entreprise, lieu, dateDebut, dateFin, missions}`, `dateFin: ''` = « encore en poste » (vérifié pendant 3.2). C'est un **écart purement visuel / d'expérience** : inline d'un côté, vieille modale de l'autre. Décision Denis 2026-09-02 : on ne touche pas au Bilan pendant le chantier du parcours guidé ; on aligne la fenêtre du Bilan sur le composant en ligne à la fin, « juste pour que ça colle aux autres ». **[FAIT depuis - voir 6conv-4 / 6conv-4 résidu ci-dessus, 2026-09-04] Vérifié à nouveau le 2026-09-11 contre le code : `bilanDeclencherActionCorrection()` appelle bien `ouvrirFenetreExperienceProInline()` pour une expérience pro, plus l'ancienne modale. Ce paragraphe décrivait l'état AVANT le correctif - conservé pour l'historique, plus une tâche ouverte.**
  - **Autres endroits à repasser dans la même logique** : (a) dette **B.1** de `docs/BRIQUES_COMMUNES.md` - le composant partagé d'import CV / contexte de candidature construit pour le Bilan le 2026-08-30, à généraliser carte par carte à tout module qui importe un CV (résorption chantier par chantier, jamais en une fois - décision Denis 2026-08-30) ; (b) le parcours **Découverte**, qui partage `contenuFormations` / `contenuMobilite` (gardés compatibles exprès) mais garde son propre câblage / palier pour Formations, Mobilité, Expériences perso - candidat à l'alignement sur les composants de « Vos informations ».
  - **But** : même information → même écran → mêmes composants → même résultat. Réutiliser « Vos informations » comme source, ne jamais recopier (règle « une seule source de vérité », [[LECONS §10]] Règle 13).
- [CLOS - 2026-09-04, les 5 collecteurs traités] **Dette B.1 (`BRIQUES_COMMUNES.md`) : contexte de candidature, résorbée chantier par chantier.** Décision Denis 2026-09-04 : jamais les 5 en bloc (conforme à la décision du 2026-08-30), un collecteur à la fois, 4 sessions le même jour.
  - **[CORRIGÉ, rien à coder] Collecteur #3 - `bilanOuvrirCiblageOffreEmploi`** : n'existe déjà plus, retiré le 2026-08-30 (le jour même de la création du tableau B.1, jamais répercuté dans la ligne #3). Ses champs vivent depuis dans `bilanCorpsCiblageOffreHTML()`, déjà unique. Ligne corrigée dans `BRIQUES_COMMUNES.md`.
  - **[FAIT] Collecteur #4 - Cohérence transversale (2026-09-04)** : `ctOuvrirCollecteComplement` (`modules/coherence-transversale/ui.js`) migré vers `bilanCorpsCiblageOffreHTML()` / `bilanCablerCiblageOffre()` / `bilanLireCiblageOffre()` (`data/metiers.js`, mêmes fonctions que le bloc 4 du Bilan) au lieu de sa propre copie des champs offre/entreprise/site/type de structure. Gain au passage : détection d'URL dans l'offre (2ᵉ champ pour coller le contenu) + champ libre pour « Autre » type de structure, déjà présents côté Bilan, absents avant côté Cohérence. `questionsPersonne` (propre à Cohérence) inchangé. Vérifié navigateur : écriture des 4 champs, priorité contenu collé > lien, « Autre » → texte libre, clair + sombre, 0 erreur console. `npm test` 713 verts.
  - **[FAIT] Collecteur #5 - Découvrir mes compétences (2026-09-04)** : `blocRechercheCandidature` (`modules/decouverte-competences/decouverteParcours.js`) migré vers la brique canonique `contenuCiviliteRecruteurCandidature()` / `contenuCouleurEntrepriseCandidature()` + `wireCiviliteEtCouleurRecruteurCandidature(rerender)` (`js/app.js`, mêmes fonctions que Mon projet/Bilan) au lieu de sa propre copie. Entreprise/site/lienOffre restent propres à Découverte (hors périmètre de la brique). Ids `decouverteNomRecruteur`/`decouverteCouleurEntreprise` → `candNomRecruteur`/`candCouleurEntreprise`. Vérifié navigateur : écriture des 6 champs (entreprise/civilité/nom/couleur/hex), clair + sombre (mode sombre re-testé après un faux positif de mesure - le vrai rendu est correct), 0 erreur console. `npm test` 713 verts.
  - **[FAIT] Collecteur #2 - Formulaire coordonnées entreprise (2026-09-04)** : `ouvrirFormulaireCoordonneesEntreprise` (`js/app.js`, fenêtre utilisée par « Passer à l'action » ET « Candidater depuis la recherche ») migré vers la même brique canonique, avec re-rendu localisé de la zone civilité/couleur (jamais de réouverture complète du panneau - LECONS 9.5). **Changement de comportement assumé (décision Denis)** : tout le formulaire passe en écriture immédiate (au lieu d'attendre « Valider »), harmonisé avec les 3 autres consommateurs de la brique - jamais de saisie perdue si la personne quitte sans valider. Vérifié navigateur : chemin offre ET immersion, écriture des 6 champs, bouton Retour, clair + sombre, 0 erreur console. `npm test` 713 verts.
  - **[FAIT] Collecteur #1, dernier - Mon projet/Bilan (2026-09-04)** : le cœur de `contenuModeRecherche()`/`wireModeRecherche()` était déjà une source unique (paramétrée par `depuisBilan`) - rien à faire là. Le reliquat interne `contenuCoordonneesEntrepriseBilanInline()` (fenêtre Candidature du Bilan) migré vers `bilanCorpsCiblageOffreHTML()` + `bilanCablerCiblageOffre()` + `bilanLireCiblageOffre()`. **Bug trouvé et corrigé dans la brique canonique** (`bilanCablerCiblageOffre()`, `data/metiers.js`) : le champ libre « Autre » (`#ciblageTypeStructureAutre`) n'était pas dans la liste des champs qui déclenchent `onChange` - invisible pour les 2 autres consommateurs (ils relisent tout au clic final), bloquant pour ce nouveau consommateur en écriture immédiate. Vérifié navigateur : mode métier ET domaine, écriture des 6 champs, « Autre » → texte libre, civilité/couleur, clair + sombre, 0 erreur console. `npm test` 713 verts.
  - **BILAN B.1 : les 5 collecteurs sont résorbés.** Reste, par choix délibéré et déjà tracé ailleurs : dette **B.6** (styles de jetons résiduels hors parcours guidé) et **B.7** (deux moteurs de rendu du CV, rattachée au découpage `js/app.js`) - distinctes de B.1, non traitées aujourd'hui.
- [CLOS - 2026-09-07] **RC-03 : correctif d'une vraie boucle Retour, signalée par Denis en usage réel juste après l'audit complet.** Sur Co-construire ma lettre ET Préparer un entretien, l'écran de dépôt (1er écran de travail) affichait l'intro en mode *detour* (`_coLettreVoirPresentation()`/`_prepaEntretienVoirPresentation()`, prévu pour "Revoir la présentation") au lieu du mode normal - l'intro en detour revient elle-même sur le dépôt via son propre bouton, créant une boucle à 2 écrans au lieu de continuer vers l'accueil de la carte. Nouvelles fonctions `_coLettreRetourIntro()`/`_prepaEntretienRetourIntro()`, même patron que `_prepLERetourIntro()` (déjà correct) et `ctRetourDepuisCollecte()` (Cohérence, déjà correct - non affectée). Vérifié navigateur : les 2 boucles ne se reproduisent plus, "Revoir la présentation" toujours non destructif, Cohérence re-vérifiée, 0 erreur console. `npm test` 713 verts.
- [CLOS - 2026-09-07] **Préparer un entretien : ajout de "Ou coller le texte" pour le CV et la lettre**, demandé par Denis (ce module n'offrait que le dépôt de fichier, contrairement aux autres modules migrés). N'appelle jamais `analyserDocumentDepose()` (réservée aux vrais fichiers) : construit directement un résultat d'analyse équivalent (`texteDisponible: true`) quand le texte est collé. Vérifié navigateur : collage CV + lettre, jusqu'à l'écran de vérification (2 documents en mode texte), clair + sombre, 0 erreur console. `npm test` 713 verts.
- [CLOS - 2026-09-07] **Préparer un entretien : les 3 écrans (dépôt, vérification, entreprise/poste) fusionnés en UNE SEULE page**, demandé par Denis (« je ne veux pas avoir des fenêtres séparées [...] tout ça doit être visible sur la même page »). 3 blocs `<details class="bloc-depli">` successifs (`_prepaEntretienRendrePreparer()`), même patron que Cohérence/« Préparer ma lettre et mon entretien ». Contrainte technique respectée : `htmlVerificationDocument()` utilise des ids DOM fixes (une seule instance à la fois) - seul le document actif (CV avant lettre, `_prepaEntretienDocActif()`) affiche le composant, l'autre affiche un simple message d'attente. **Bug réel corrigé avant mise en ligne** : `dossier.entretienDirect` vaut par défaut `{structure:'', poste:''}` (toujours "vrai") - une vérification naïve aurait affiché le bloc entreprise comme déjà validé dès l'ouverture du module ; nouvelle fonction `_prepaEntretienEntrepriseValidee()` (vérifie un poste réellement renseigné). « Modifier » (entreprise) protégé du saut automatique par un drapeau une-seule-fois. Détail complet : `docs/CHANTIER_ELIMINATION_FENETRES_DEPOT_CV.md`. Vérifié navigateur de bout en bout (dépôt texte collé, vérification, skip lettre, formulaire entreprise, transition réelle vers le choix d'assistant, `dossier.cvTexte`/`entretienDirect` bien renseignés, Retour sans boucle, detour non destructif, "Modifier" fonctionnel), clair + sombre, 0 erreur console. `npm test` 713 verts.
- [CLOS - 2026-09-07] **Préparer un entretien, page consolidée : vérification complémentaire demandée par Denis (dépôt par vrai fichier + formulaire au clavier)** - aucun changement de code, uniquement des tests sur la page déjà committée (`a9acd4e`). CV et lettre déposés par vrai fichier (`.txt` réel et `.png` réel, upload via un vrai `File`/`DataTransfer`) : mode texte et mode image (rotation + masquage par rectangles sur le canvas) vérifiés pour les deux documents, `dossier.cvTexte`/`dossier.lettreMotivation.texte` bien renseignés, « Changer » fonctionnel. Formulaire « Entreprise et poste visé » testé au clavier réel : majuscule automatique, Entrée passe au champ suivant, Entrée sur le dernier champ valide (directement si l'entreprise est remplie, sinon affiche l'incitation), « Compléter maintenant » puis nouvelle saisie + Entrée valide. 0 erreur console. Détail complet : `docs/CHANTIER_ELIMINATION_FENETRES_DEPOT_CV.md`.
- [CLOS - 2026-09-04] **RC-03 : audit complet du bouton Retour sur tous les modules, refait une fois les 3 modules du chantier d'élimination des fenêtres terminés** - demande de Denis (« une fois que tu auras fini ces trois modules, je veux un audit complet des boutons retour pour tous les modules »). Détail : `docs/ETAT_DES_CHANTIERS_2026-08-24.md`, entrée RC-03.
  - Re-grep exhaustif de tous les appels `barreNavigation()` du dépôt, y compris les pages neuves de Co-construire ma lettre/Cohérence/Préparer un entretien : tous passent un `onclickPrecedent` cohérent, **aucune nouvelle incohérence trouvée**.
  - Les 2 patrons à 2 niveaux (Découverte, Comparer mes pistes) re-vérifiés ligne par ligne (pas seulement supposés corrects) : leur bouton de retour écran par écran recule correctement, ne redescend à la présentation qu'au premier écran.
  - **Tentative de reproduction EN DIRECT (navigateur) des 2 cas d'origine signalés par Denis** : Créer mon CV (2ᵉ page) et Analyser ma candidature (aller-retour) - les deux ont été rejoués pas à pas, **aucune anomalie reproduite** avec le code actuel. Hypothèses : déjà corrigés par un des nombreux correctifs faits plus tôt le même jour (B.1-B.6, RC-03 lots précédents), ou décrivent le patron « écran de travail ↔ présentation » qui est un choix de conception assumé ailleurs dans l'app - à requalifier avec Denis (bug de câblage vs décision UX à reconsidérer) si le comportement persiste, avec une reproduction précise (écran exact, ordre des clics).
  - **Conclusion : plus aucune incohérence de câblage retrouvée nulle part dans le dépôt à cette date.**
- [CLOS - 2026-09-04, étape 3/3, CHANTIER TERMINÉ] **Chantier élimination des fenêtres de dépôt CV : Préparer un entretien (la plus complexe des 3).** `ouvrirDepotEntretien()`/`ouvrirVerificationEntretien()`/`verifierOuDemanderEntrepriseEtPoste()`/`ouvrirChoixIAEntretien()`/`ouvrirRecuperationEntretien()` (cascade de 5 fenêtres) remplacées par 5 pages routées (dispatcher `pagePrepaEntretien()`). Détail : `docs/CHANTIER_ELIMINATION_FENETRES_DEPOT_CV.md`. Seul module des 3 à mécanisme de dépôt différent (`input[type=file]`+`analyserDocumentDepose()`) - laissé inchangé à dessein. Choix d'assistant modernisé sur `htmlChoixAssistantBilanCorps()` (dette B.2). 2 bugs de mode sombre corrigés au passage. Point de vigilance retrouvé et corrigé : l'écriture de `dossier.cvTexte`/`dossier.lettreMotivation` (alimentait le prompt assistant) ré-attachée au bon endroit. `ouvrirParcoursEntretien()` conservé comme raccourci externe (« Candidater depuis la recherche »), vérifié fonctionnel depuis n'importe quelle page. Vérifié navigateur de bout en bout (dépôt, vérification 2 documents avec écriture réelle confirmée, cycle entreprise formulaire→incitation, choix assistant, réponse avec export DOCX réel, Terminé → Vos documents, raccourci externe, détour non destructif, Retour, clair + sombre, 0 erreur console). `npm test` 713 verts. **Les 3 modules du chantier (Co-construire ma lettre, Cohérence de mon dossier, Préparer un entretien) sont terminés - plus aucune fenêtre de saisie de document dans ces 3 cartes.**
- [CLOS - 2026-09-04, étape 2/3] **Chantier élimination des fenêtres de dépôt CV : Cohérence de mon dossier.** `ctDemarrerCollecte()`/`ctOuvrirCollecteComplement()` (cascade de 4 fenêtres : CV → lettre → entretien facultatif → offre/entreprise/questions) remplacées par une page à 4 blocs dépliants (même langage visuel `.bilan-preparer`). Détail : `docs/CHANTIER_ELIMINATION_FENETRES_DEPOT_CV.md`. Fonctions de dépôt par document et soumission finale (`ctDeposerDossier`) inchangées. Bascule sur `ctHtmlEtapeChoixIA()` déjà page-based (dette B.2). Cas `SaisieInexploitable` adapté (repart directement sur une collecte neuve). Vérifié navigateur de bout en bout (dépôt CV+lettre par texte collé avec relecture/masquage réelle, skip entretien, bloc offre, lancement de l'analyse → dossier créé → écran « Choisissez votre assistant », Retour non destructif, « Changer de CV », clair + sombre, 0 erreur console). `npm test` 713 verts. **Reste : Préparer un entretien.**
- [CLOS - 2026-09-04, étape 1/3] **Chantier élimination des fenêtres de dépôt CV : Co-construire ma lettre de motivation.** Cadré par Denis, plan validé, autonomie complète donnée pour les 3 modules. Détail : `docs/CHANTIER_ELIMINATION_FENETRES_DEPOT_CV.md`. Cascade de 4 fenêtres (dépôt CV → choix assistant → confirmation → réponse) remplacée par 3 vraies pages routées (dispatcher `pageCoLettre()`, même patron que Cohérence). `htmlChoixAssistantBilanCorps()` réutilisée pour le choix d'assistant (dette B.2). `ouvrirFenetreAssistantIA()` non touchée (confirmation brève, pas une fenêtre de saisie). Bug de mode sombre trouvé et corrigé au passage (`#EFF6FF` figé). Retour conforme RC-03 sur les 3 écrans (chacun revient au précédent au sens strict). Vérifié navigateur de bout en bout (dépôt, choix assistant, réponse avec export DOCX réel, Terminé → Vos documents, détour Revoir la présentation, reprise, clair + sombre, 0 erreur console). `npm test` 713 verts. **Restent : Cohérence de mon dossier, Préparer un entretien.**
- [CLOS - 2026-09-04, les 3 bugs confirmés] **RC-03 : correctif du bouton « Retour » sur Cohérence, le Lexique et « Vos documents »**, à la demande de Denis après l'audit systématique de `barreNavigation()` (voir `docs/ETAT_DES_CHANTIERS_2026-08-24.md`, entrée RC-03).
  - **[FAIT] « Vos documents » (`marquerDocumentEnregistre()`, `js/app.js`)** : ré-affiche juste la barre de nav après l'enregistrement d'un document, recalculait `barreNavigation('revelation', ...)` en dur, écrasant la vraie destination (Découverte, import direct, page réellement quittée) que `pageResultats()` calcule pour son rendu complet. Extrait `resultatsCibleRetour(depuisDecouverte)` - source unique, appelée par les deux fonctions, pour qu'elles ne divergent plus jamais. Vérifié navigateur : scénario Découverte et scénario page d'origine réelle, destination de Retour identique avant/après enregistrement, 0 erreur console. `npm test` 713 verts.
  - **[FAIT] Cohérence (`ctHtmlEtapeChoixIA()` et `ctHtmlRapport()`, `modules/coherence-transversale/ui.js`)** : passaient `barreNavigation('cv', null, null, {})` - Retour = Accueil, mot pour mot. Nouvelle fonction `ctRevenirALaPresentation()` (`_ctVoirIntro = true; pageCoherenceTransversale();`) - même mécanisme non destructif que le bouton déjà présent « Revoir la présentation » sur ces mêmes écrans. Ces 2 étapes n'ont pas d'écran « documents » dédié (le dépôt se fait par une chaîne de fenêtres depuis « Je commence ») : la présentation du module est leur vrai écran précédent. Vérifié navigateur : `ctHtmlEtapeChoixIA()`/`ctHtmlRapport()` rendus directement, clic sur Retour quitte bien l'écran (plus identique à Accueil), clair + sombre, 0 erreur console. `npm test` 713 verts.
  - **[FAIT] Lexique (`modules/lexique/index.js`)** : 6 écrans (Mode Livre, une fiche, liste des collections, une collection, favoris, bibliothèque) appelaient `barreNavigation()` **sans aucun argument** - la barre fixe n'avait alors aucun bouton Retour (absent, pas mal dirigé). Chacun reçoit désormais le même `onclickPrecedent` que son bouton « Retour à... » déjà présent dans la page (`_lexiqueAfficherRecherche()` ×3, `_lexiqueAfficherListeCollections()`, le rappel `_lexiqueRetourFiche` de la fiche, `_lexiqueAfficherFiche(_lexiqueBibliothequeFicheOrigine)` pour la bibliothèque) - aucune nouvelle fonction, juste la destination déjà éprouvée du bouton existant dans la page. Vérifié navigateur : les 6 écrans, clic sur Retour vérifié fonctionnel (fiche → recherche), clair + sombre, 0 erreur console. `npm test` 713 verts.
  - **Reste ouvert** : les 2 cas d'origine signalés par Denis (Créer mon CV 2ᵉ page, Analyser ma candidature aller-retour), non retrouvés par l'audit de code, restent à reproduire en direct avant de pouvoir les corriger.
  - **[2026-09-10, reproduction menée par Claude au navigateur, pas à pas]** :
    - **Cas 1 « Créer mon CV, 2ᵉ page » : REPRODUIT** (en comptant la page d'intro `creer-cv` comme 1ʳᵉ page, `objectif` comme 2ᵉ). Le « Retour » de `objectif` était codé en dur vers `'cv'` (`pageObjectif()`, `js/app.js`) et sautait l'intro `creer-cv` ajoutée devant lui en 2026-09-01. **CORRIGÉ commit `1fe2995`** : `barreNavigation()` reçoit `'creer-cv'` comme précédent quand `dossier.modeCreation === 'nouveau'` ET `historiquePages` contient `'creer-cv'`, sinon `'cv'` (repli inchangé). Vérifié navigateur : échelle complète `cv` ← `creer-cv` ← `objectif` ← `votre-parcours`, pas de boucle, entrées non-`nouveau` inchangées. `npm test` 780. La page 2 → page 1 (`votre-parcours` → `objectif`) fonctionnait déjà, seul le tout premier écran de travail sautait l'intro.
    - **Cas 2 « Analyser ma candidature, aller-retour permanent » : NON reproduit.** Rejoué : écran de travail « Préparer » → Retour → présentation → Retour → accueil, un cran à la fois, et ça se termine. C'est le patron à 2 niveaux assumé (`bilanRetourVersPresentation()`), pas un aller-retour. Si Denis veut que le Retour d'un écran de travail du Bilan saute la présentation, c'est une décision UX, pas un bug.
  - **RC-03 : les 3 bugs confirmés par l'audit systématique sont corrigés.**
- [CLOS - 2026-09-04] **Dette B.6 (`BRIQUES_COMMUNES.md`), revue point par point des ~10 « pastilles » résiduelles**, à la demande de Denis. Vérification contre le code plutôt que contre la doc - la moitié des points étaient périmés :
  - `.export-jetons` (formats d'export) : déjà fait le 2026-09-02, la ligne de dette était fausse.
  - `blocTypeCV()` (« CV général »/« CV spécifique ») : code mort, aucun appelant, un écran qui ne s'affichait jamais. **[CLOS - 2026-09-10, commit `26b50b7`, vérifié navigateur]** : retiré avec son unique consommateur `blocPersonnaliserCandidature()`, la var `revelerSpecifiqueCandidatureRemplie` + sa remise à zéro dans `recommencer()`, et le CSS `.type-cv-choix` / `.type-cv-option` (`css/style.css`). −66 lignes nettes. Conservés : `dossier.typeCV` (piloté par `typeCVDepuisTypeRecherche()`, lu ailleurs), `informationsCandidatureSuffisantes()` (autres appelants), `stylePastilleInline()` (autres consommateurs). Vérifié : `typeof blocTypeCV === 'undefined'` en navigateur, 7 routes historiquement liées rendues sans erreur, `recommencer()` OK, `npm test` 780.
  - `.adapt-groupe` (page Action) et les étapes « Ce qui va se passer » : **laissés tels quels par décision de Denis** - déjà des choix assumés / pas de vraie sélection à styliser en jeton.
  - **[FAIT] `stylePastilleInline()`** (`js/app.js`) : seule fonction réellement en couleurs figées, passée en `var(--...)`. Ses 2 vrais consommateurs (« Coller manuellement », « Importer dans... ») sont des boutons d'action uniques, pas de vrais choix - décision Denis : corriger seulement les couleurs, jamais les convertir en `.jeton`. Vérifié navigateur clair + sombre, `npm test` 713 verts.
  - **B.6 CLOSE** : les 2 seuls points restants ne sont pas des tâches en attente - `.pastille` générique s'éteindra d'elle-même (clause d'extinction, pas une action à planifier) et `.preparer-jeton` du Bilan est définitivement hors périmètre (décision Denis 2026-09-03).
- [CLOS - 2026-09-04] **Lexique : nouveau grand sujet « Conditions de travail »**, cadré et réalisé le même jour (idée notée le 2026-09-01, voir `docs/IDEES_A_RECLASSER.md`). 12 fiches neuves (`data/lexique.js`, univers `conditions-travail`) : Télétravail, Horaires de travail, Rythme et variété des missions, Travail de nuit, Astreintes, Déplacements professionnels, Port de charges et effort physique, Environnement de travail, Formation et accompagnement à la prise de poste, Temps partiel choisi ou subi (comparatif), Contact avec le public, Proximité domicile-travail - chacune avec son second niveau « En savoir plus », réunies dans le nouveau parcours `parcours-conditions-travail`. Libellé d'univers ajouté dans `modules/lexique/index.js` (`_LEXIQUE_LIBELLES_UNIVERS`), univers déclaré dans `scripts/checkLexique.js` (`UNIVERS_CONNUS`). Liens ajoutés depuis 2 fiches existantes (`temps-partiel` → le nouveau comparatif ; `teletravail` → `autonomie`).
  - **Doctrine tranchée par Denis** : fiches neutres, comme le reste du Lexique (« de la connaissance, jamais un diagnostic »). Le volet « comment en parler en entretien / le négocier », envisagé dans l'idée d'origine, **n'a pas été ajouté** - il reste la matière du futur module « Défendre mes conditions », pas dupliqué ici.
  - **Audit de couverture** fait contre les 25 cartes de `data/valeursProfessionnelles.js` (les 5 catégories, pas seulement celle nommée « Conditions de travail ») : 3 notions oubliées dans une première liste (être bien formé au début, travailler dehors, missions variées) repérées et ajoutées avant rédaction. Les notions non reprises (salaire, autonomie, évolution, stabilité, ambiance, sens...) sont soit déjà couvertes par une fiche existante, soit trop personnelles/subjectives pour être un terme de Lexique - vérifié, pas supposé.
  - **Vérifié** : `node scripts/checkLexique.js` → 0 erreur, 0 avertissement (153 fiches, 18 collections, 143 second niveau). Navigateur : recherche, fiche détail, « En savoir plus », le parcours complet dans l'ordre, clair + sombre, 0 erreur console. `npm test` 713 verts.
  - **Bug préexistant signalé, pas corrigé (hors périmètre)** : les listes de collections/parcours du Lexique (`.list-group-item`) gardent un fond blanc en mode sombre - vérifié présent aussi sur une collection ancienne (« Comprendre un contrat de travail »), donc pas une régression de ce chantier.
  - **Reste non fait**, par choix délibéré : le mécanisme d'ancrage d'un repère sur une condition de travail (idée d'origine), qui touche le module Repères en plus du Lexique - chantier transversal à cadrer après stabilisation, pas commencé.
- [CLOS - 2026-09-04, les 3 endroits traités] **Dette B.2 (`BRIQUES_COMMUNES.md`) : écran « Choisissez votre assistant », fusion vers `htmlChoixAssistantBilanCorps()`.** Cadré et réalisé le 2026-09-04, à la demande de Denis.
  - **Vérifié avant de coder** : Découverte était en fait **déjà migrée depuis le 2026-08-31** (la doc la disait encore séparée) ; « la page Action » citée dans la dette d'origine désignait en réalité Découverte, erreur d'attribution corrigée.
  - **[FAIT] Cohérence transversale (2026-09-04)** : les 2 écrans (`ctHtmlEtapeChoixIA` pour le 1ᵉʳ prompt, `ctHtmlEtapeChoixIAEntretienAvance` pour l'entretien avancé) migrés vers `htmlChoixAssistantBilanCorps()`, au lieu de 2 copies quasi identiques en couleurs figées (`#DCFCE7`, `#0d6efd`, `#374151`...) - corrige un vrai bug de mode sombre au passage. Ancien câblage de clic-pour-révéler des étapes retiré (devenu mort, les 4 temps sont déjà tous visibles dans l'accordéon partagé). Note vocale spécifique à l'entretien avancé conservée. Vérifié navigateur : les 2 écrans, clic assistant, clair + sombre, 0 erreur console. `npm test` 713 verts.
  - **[FAIT] Page Assistant, dernier des 3 (2026-09-04)** : `contenuRectangleChoixIA` (`js/app.js`, parcours principal `pret`/`maj`/`nouveau`) migré à son tour. Le bouton « Voir ou modifier le texte transmis », qu'on pensait devoir arbitrer, s'est révélé une fonctionnalité **légitimement propre à ce parcours** (seul à composer le texte transmis depuis des champs structurés, contrairement aux 3 autres modules qui envoient un texte déjà déposé tel quel) - ajoutée après le composant partagé, aucun arbitrage nécessaire, zéro perte. `wireEtapesDetailChoixIA()` devenu mort (0 appelant restant) supprimé. Vérifié navigateur : jetons + accordéon, bouton + badge « version personnalisée », clair + sombre, 0 erreur console. `npm test` 713 verts.
  - **B.2 ENTIÈREMENT RÉSORBÉE** : Bilan, Découverte, Cohérence et la page Assistant partagent désormais le même écran de choix d'assistant.
  - **Effet de bord, hors B.2** : pendant la vérification navigateur, conflit de port avec une session en parallèle sur `site-v2-python-server` (port 8123 fixe). Corrigé : `serveur_dev.py` lit désormais aussi la variable d'environnement `PORT`, `.claude/launch.json` passe en `"autoPort": true` - deux sessions peuvent désormais lancer le serveur de dev en parallèle sans conflit.
- [CLOS - 2026-09-04] **Dette B.4 (`BRIQUES_COMMUNES.md`) : barre d'étapes visuelle, fusion vers `barreEtapesModule()`.** Cadré et réalisé le 2026-09-04, à la demande de Denis.
  - **[FAIT] Bilan (`bilanBarreEtapes`) et Cohérence (`ctHtmlBarreEtapes`)** : les deux ne gardent plus que leur calcul spécifique de l'index courant, tout le rendu (pastilles, chevrons, états fait/courant/à venir) délégué à `barreEtapesModule()` - construite le 2026-08-31 en copiant exactement ce rendu, jamais réutilisée depuis par sa propre source. Cohérence gagne au passage la classe `.barre-etapes-bilan` (harmonisation voulue). Vérifié navigateur : les 2 barres, intégration réelle dans `pageCoherenceTransversale()`, clair + sombre, 0 erreur console. `npm test` 713 verts.
  - **Découvert avant de fusionner le 3ᵉ endroit** : « la barre de la page Action » (`pastilleEtapeAction`/`ligneEtapesAction`) n'est **pas une 3ᵉ copie du même rendu**. Ses pastilles sont de vrais boutons cliquables reliés à un accordéon (`data-accordeon`, triangle ▼ quand ouvert) - une capacité que `barreEtapesModule()` n'a pas. Un commentaire déjà présent (2026-08-30) confirme une taille de pastille volontairement conservée différente. **Décision Denis 2026-09-04 : exception permanente, ne pas fusionner** - mécanisme réellement différent (navigation par accordéon), pas une copie paresseuse ; étendre `barreEtapesModule()` exposerait Bilan/Cohérence/Découverte à un risque pour un gain qui ne les concerne pas. **B.4 CLOSE.**
  - **[FAIT] Correctif de fidélité, demandé par Denis après la fusion (2026-09-04)** : en vérifiant que la liste d'étapes de Cohérence (`CT_ETAPES`) reflétait encore ses vrais écrans - question posée par Denis en constatant que Bilan et Cohérence ont chacun 5 étapes totalement différentes en contenu (partager `barreEtapesModule()` ne veut jamais dire partager les étapes) - 2 écarts réels trouvés :
    - **« Infos complémentaires » retirée** : cette fenêtre (`ctOuvrirCollecteComplement`) est une modale qui ne montre même pas la barre, et `ctEtapeCourante()` ne pouvait de toute façon jamais la renvoyer comme étape en cours (elle survient tant que `ctObtenirDossier()` est encore faux, donc structurellement confondue avec « Vos documents »).
    - **« Entretien avancé » ajoutée** : les écrans « Aller plus loin : mon entretien » (collecte, choix de l'assistant, import de la réponse, rapport dédié - ajoutés le 2026-08-25) n'avaient **aucune étape correspondante** - la barre restait bloquée sur « Rapport » tout du long. Nouvelle fonction `ctEnEntretienAvance()` reprend exactement les conditions déjà utilisées par le dispatcher `pageCoherenceTransversale()`, jamais une 2ᵉ lecture divergente de cet état.
    - Vérifié : les 5 scénarios (documents/choix_ia/chez_assistant/rapport/entretien_avance) donnent le bon id, rendu correct (Rapport marqué fait, Entretien avancé en cours), clair + sombre, 0 erreur console. `npm test` 713 verts.
- [CLOS - 2026-09-04, Cohérence puis Bilan] **Dette B.5 (`BRIQUES_COMMUNES.md`) : page d'introduction, fusion vers `htmlPageIntroModule()`.** Cadré par Denis le 2026-09-04 (« Vas-y, commence par Cohérence », puis « Vas-y, commence par Bilan »).
  - **[FAIT] `htmlPageIntroModule(config)`** (`js/app.js`) extraite du patron déjà commun aux 2 modules (10 blocs, `docs/LANGAGE_VISUEL_COMMUN.md` section 5) - `config.sections[]` en tableau ordonné (pas de slots nommés fixes), pour que chaque module garde son propre ordre de sections (Cohérence a une section « Ce qu'on va vous demander » que Bilan n'a pas, à un endroit qui lui est propre à elle).
  - **[FAIT] `ctHtmlExplication()` (Cohérence) migrée dessus** : texte intégral et logique du bouton du bas (`detour || ctObtenirDossier() || ctObtenirDiagnostic()`, pas juste `detour`) préservés à l'identique - vérifié par capture octet près de `ctHtmlExplication(false)`/`ctHtmlExplication(true)` avant refonte, comparée à la sortie après refonte.
  - **[FAIT, décision Denis en cours de route 2026-09-04] Retrait de la barre d'étapes visuelle sur TOUTES les pages d'introduction de module** : « la page d'introduction, elle est déjà assez chargée avec des informations [...] la barre commence réellement à avoir un impact lorsque la personne est sur la page de module, pas sur la page d'introduction ». Revient sur la décision du 2026-08-31 qui l'affichait « en sourdine » sur la présentation. Appliqué à la source : `htmlPageIntroModule()` n'a pas de slot `barreEtapesHTML` (jamais réactivable par erreur), et `htmlPageIntroModuleParcours()` (`data/metiers.js`, 8 pages routées : Co-construire ma lettre, Préparer un entretien, Découverte-intro, ATS, Regard recruteur, Se tenir informé, Comparer mes pistes/Aide à la décision, Mettre à jour/Préparer ma lettre et mon entretien) perd le même bloc. `config.etapes` reste utilisé ailleurs (`afficherProgression`) pour les VRAIS écrans de travail, inchangé. Sur `ctHtmlExplication()`, la bande de reprise (`ctHtmlBandeReprise()`) reste affichée : ce n'est pas la barre d'étapes, c'est le bouton « Revenir au module »/« Revoir la présentation » + l'encart « Continuer/Recommencer ».
  - Vérifié navigateur : Cohérence (appel direct + page routée `coherence`), Co-construire ma lettre (`co-lettre`), clair + sombre, 0 erreur console. `npm test` 713 verts.
  - **[FAIT] `htmlBilanIntro()` (Bilan) migrée à son tour (même jour, 2e passage - « Vas-y, commence par Bilan »)** : section « Quelle que soit la forme de votre CV » (propre à Bilan) placée à son propre endroit dans `config.sections[]`. Texte, `classeExtra`, bouton du haut en detour, logique + classe du bouton du bas préservés à l'identique - **vérifié par capture octet près** avant/après pour `voirIntro` `false` ET `true` (`diff` : 0 différence dans les 2 cas). Vérifié navigateur : rendu direct + via `demarrerBilanCandidature()` (page réellement routée), clair + sombre, 0 erreur console. `npm test` 713 verts. **B.5 ENTIÈREMENT RÉSORBÉE.**
  - **2 tâches notées par Denis en cours de route, pas commencées** :
    1. **Bouton « Retour » incohérent** (dette RC-03 déjà connue) : sur « Créer mon CV » le Retour de la 2ᵉ page ramène directement à l'accueil au lieu de l'écran précédent ; sur « Analyser ma candidature » (Bilan) le Retour fait un aller-retour permanent entre la présentation et la 1ʳᵉ page. Retour ≠ Accueil (Retour = un écran en arrière, Accueil = retour direct à l'accueil). Denis veut un correctif systématique, pas des rustines écran par écran, qui devienne le comportement par défaut de tout futur module. Voir `docs/ETAT_DES_CHANTIERS_2026-08-24.md`, entrée RC-03.
    2. **Fenêtre d'import CV à remplacer par une page** (dette B.1, volet « fenêtre d'import CV », déjà ouvert pour « Préparer un entretien »/« Coécrire ma lettre de motivation ») - Denis ajoute **Cohérence de mon dossier** à la liste (`ctObtenirOuDeposerTexteLettre`/`ctObtenirOuDeposerTexteEntretien`, `modules/coherence-transversale/ui.js`, qui appellent `ouvrirAssistantDepotCV()`, une modale). **Précision de Denis (même jour)** : Cohérence cumule en réalité **2 fenêtres modales d'affilée** - dépôt CV/lettre/entretien, PUIS `ctOuvrirCollecteComplement()` (offre/entreprise/type de structure) - « très lourd pour l'utilisateur ». Objectif posé : les deux dans **la page du module**, pas une chaîne de fenêtres. Une carte à la fois, jamais deux en même temps ; maquette avant code.
- [CLOS - 2026-09-04] **Candidater depuis la recherche de l'accueil + secteurs de métier** - cadré par Denis le 2026-09-01, réalisé le 2026-09-04 en 5 sous-lots. Doc dédié : `docs/CHANTIER_CANDIDATURE_DEPUIS_RECHERCHE_ET_SECTEURS.md` (statut + détail des sous-lots), plan data `docs/PLAN_SECTEURS_2026-09-04.md`.
  - **Sous-lot 1** (`9fc2630`) : champ `secteur` normalisé sur les **129 fiches** (`baseMetiers` = 3 fichiers assemblés : `data/metiers.js` + `data/metiersComplementaires.js` + `data/referentielMetiersERIP.js`) - 52 libellés libres → **21 valeurs contrôlées** `SECTEURS_APP`. Les 2 filtres par libellé de `js/app.js` remplacés par des listes d'ids (`METIERS_SAISONNIER_ALIMENTAIRE` 26 / `METIERS_QUI_RECRUTENT_GENERALEMENT` 39), membres identiques, zéro régression. `tests/secteursMetiers.test.js`.
  - **Sous-lot 2** (`630f041` + `8bbf85d`) : `data/secteurs.js` = `SECTEURS_DETAIL` (21 objets `{cle, libelle, explication, metiersPhares}`), les 21 explications rédigées et validées Denis.
  - **Sous-lot 3** (`0dbc357`) : un secteur tapé dans la recherche de l'accueil → groupe « Secteurs de métier » en tête, `carteSecteurHTML` (libellé + explication + dépli « Voir les métiers », jetons `[data-metier-nom]`). CSS `.carte-resultat-secteur` / `.jeton-metier-secteur`.
  - **Sous-lot 4a** (`c431ca5`) : rattrapage « métier non répertorié » - dans l'état « Aucun résultat », bouton `[data-candidater-metier-libre]` → parcours candidature en texte libre (l'aval gère : `metierCible` = chaîne, `cv.md` = « métier OU secteur »).
  - **Sous-lot 4b** (`8d14a5a`) : la cascade de 4-5 fenêtres `ouvrirPanneauGuide` empilées (`ouvrirPanneauChoixParcours` → `ouvrirPanneauEntreprise` → `ouvrirPanneauCvExistant` / `ouvrirPanneauMajCv` → `lancerParcoursGuide`) est **retirée** et remplacée par `pageCandidaterDepuisRecherche()` (route `'candidater'`) : page dépliante = barre d'étapes + 3 blocs (Ce que vous préparez / Votre cible / Votre CV) + barre de navigation fixe, jetons partout. Écritures et destinations reprises à l'identique (`_candidaterFinaliser()`). Réconciliation « structure précise / recherche générale » portée par le bloc 2 (jetons `offre` / `simple` → `dossier.typeRecherche`). Seule fenêtre restante : `ouvrirFormulaireCoordonneesEntreprise` (partagée avec le bloc Candidature de Mon projet). Maquette `docs/MAQUETTE_CANDIDATER_DEPUIS_RECHERCHE_2026-09-04.html`. CSS `.cand-*` (tokens only, mode sombre par héritage). Testé navigateur : 4 points d'entrée, métier + secteur, cv/lettre/entretien, nouveau/maj/pret, coordonnées round-trip, mode sombre, console propre.
  - **Sous-lot 5** (`0312c37`) : bouton « Candidater pour ce secteur » → parcours partant de `dossier.secteurCible` (`modeRecherche 'domaine'`). **Passe de prompt (5b) : rien à changer** - `texteProfil` (js/app.js ~22954) transmet déjà « Secteur d'activité visé (pas un métier précis, documents adaptés au secteur) : X », `cv.md` dit « métier OU secteur » partout.
  - **npm test : 713 verts.**
- [CLOS - vérifié 2026-09-04] **Sweep « Mon/Mes -> Votre/Vos »** sur le reste de l'app, annoncé comme chantier séparé au §9.5 de `docs/ETAT_REFONTE_PARCOURS_GUIDE_2026-09-02_SOIR.md`. Recherche exhaustive du texte visible (hors commentaires de code) dans `js/app.js`, `data/metiers.js`, `modules/*/*.js` pour `Mon projet`, `Faire le point`, `Mes informations`, `Mon CV`, `Mes documents` : **zéro occurrence restante** - toutes les mentions trouvées sont des commentaires historiques (`// TACHE (refonte Mon projet...)`), déjà couverts par le vocabulaire à la 2e personne posé par le chantier « Refonte du parcours guidé » (Votre objectif / Votre parcours / Vos informations / Votre profil / Assistant / Vos documents). Seuls « Mon »/« Mes » restants dans l'app : **noms propres de modules indépendants** (« Mes Repères », « Mon Carnet »), explicitement hors périmètre du sweep. Rien à coder.
- [CLOS - déjà consigné plus haut, entrée de rappel] **Passe de convergence des autres parcours sur les écrans « Vos informations »** : voir l'entrée ci-dessus (6conv-1 → 6conv-6) sous « Refonte de la carte Mes documents » - **CONVERGENCE BILAN TERMINÉE**, Découverte partiellement alignée (non gênant). Restent, par choix délibéré (résorption chantier par chantier) : dette **B.1** de `docs/BRIQUES_COMMUNES.md` (contexte de candidature, ~5 collecteurs séparés) et l'alignement complet de Découverte au-delà de Formations/Mobilité - **ce ne sont pas des tâches de CE chantier**, elles restent suivies séparément.
- [À FAIRE - petit chantier indépendant] **Vidéo courte « activer la recherche web sur l'assistant en ligne »** (ChatGPT et autres). Cadré le 2026-09-01 : les 3 tests « hors chantier » (volet Découverte du Chantier 4, `bilan-v1.md` sur de vrais CV, protocole ChatGPT « compte perso ») ont été passés par Denis et sont conformes ; il ne reste que cette vidéo. Emplacement déjà posé dans « Comparer mes pistes » : `htmlDeclencheurDemoVideo('comparer-activer-recherche-web')`. Utile bien au-delà de Comparer (plusieurs prompts disent « si tu peux consulter des informations en ligne » : `cv.md`, `lettre-v1.md`, `coherence-transversale.md`, `bilan-v1.md`) : à brancher aussi ailleurs quand la vidéo existe. Même mécanisme que le registre `DEMOS_VIDEO_ERIP`.
- [CLOS fonctionnellement - 2026-09-10, vérifié contre le code] Recherche universelle depuis la barre de l'accueil. **FAIT** par le chantier Ressources (étapes 10 + 16) : `rechercherBaseConnaissances()` (`data/baseConnaissancesERIP.js`) fait remonter, en plus des métiers/domaines/compétences, un groupe **« Dans le Lexique »** qui parcourt **tout `LEXIQUE_FICHES`** (match titre + `variantesRecherche`, plafond 5, classé en dernier, dédoublonné), les **freins** (par `synonymes`), les **fiches Urgences** (`univers === 'urgences'`), les **fiches « type de structure »**, et le nom d'un module (`MODULES_RECHERCHABLES` / `ouvrirCibleRechercheApp`). Le filet « Chercher dans le Lexique » (`lexiqueOuvrirRecherche`) prend le relais quand rien ne matche. Règle posée par Denis le 2026-08-29 : tout nouveau bloc de contenu Lexique est trouvable sans travail supplémentaire dès qu'il a un `variantesRecherche`. **Reste, purement cosmétique, non bloquant** : décider si le groupe « Dans le Lexique » garde sa place (avant « Métiers qui pourraient vous correspondre ») ou passe tout en bas ; style de la carte (aujourd'hui identique aux cartes métier). L'idée d'un « gros chantier recherche universelle monolithique » a été explicitement abandonnée (Denis 2026-08-29 : chaque bloc branche sa propre recherche).

## Chantier futur : revisualiser le module "Découvrir mes compétences" (`[MAQUETTE VALIDÉE - en attente]`... en réalité pas encore de maquette : idée cadrée le 2026-08-29)

Denis veut retravailler ce module (`modules/decouverte-competences/`, prompt `prompts/decouverte-competences.md`). Rappel de son rôle réel (voir mémoire `project_module_decouvrir_mes_competences_role_reel`) : **bâtir un CV de zéro depuis le récit** d'une personne très éloignée de l'emploi (ex-détention, longue inactivité) - flux direct récit → compétences → CV, jamais de l'exploration. Le prompt produit `fragments` (5 formulations professionnelles + compétences + preuve par métier cité), `reciteNettoye` (récit reformulé qui alimente CV/lettre/entretien), `questionsCiblees` (comblent des manques factuels : dates, durées).

**À intégrer dans ce chantier** :
- Quelques détails que Denis veut ajouter au module (à préciser avec lui à l'ouverture).
- **La banque de tournures « nommer une difficulté sans la durcir »** (`docs/IDEES_A_RECLASSER.md`, entrée 2026-08-28) : c'est le module qui reçoit le plus de récits en vrac d'un public fragile, le retour à la personne y gagne à s'appuyer sur ces formulations.
- **La page d'accueil / page d'introduction du module** (patron déjà validé sur Mes Repères et Cohérence transversale, voir mémoire `project_page_introduction_modules`) - Denis : « quand je ferai la page d'accueil de ce module, il faudra qu'on voie ça aussi ». Les deux se traitent ensemble.

Statut : pas de maquette, pas de plan. À cadrer avec Denis quand il ouvre le chantier.

## [CLOS - 2026-09-10, vérifié contre le code] Harmoniser et généraliser la barre d'étapes visuelle à tous les modules

**CLOS.** Le composant partagé `barreEtapesModule(etapes, indexCourant)` (`js/app.js`, construit le 2026-08-31) est utilisé par **tous les parcours routés multi-étapes** : Analyser ma candidature, Cohérence de mon dossier, Découvrir mes compétences, Les mots de votre CV (ATS), Comparer mes pistes, Regard recruteur, Comprendre le cadre, Comprendre les chiffres, Co-construire ma lettre, Préparer un entretien, Reformuler et présenter mon CV, Créer mon CV. Cohérence a été alignée sur le style de référence (B.4, `da77aaa` + fusion 2026-09-04). **Exceptions assumées, pas des manques** : (1) la barre de la page Action (`pastilleEtapeAction` / `ligneEtapesAction`) = mécanisme différent (pastilles cliquables reliées à un accordéon), décision Denis 2026-09-04 ; (2) Regard extérieur « Aller plus loin » garde un compteur « Question X sur N » (suite de questions de longueur variable : 5 fixes + N de l'assistant), une barre à pastilles fixes n'y a pas de sens ; ce module est une sous-séquence de Mes Repères, pas une page routée.

*Contexte d'origine conservé ci-dessous.*

**Décidé par Denis le 2026-08-30**, pendant la maquette du Bilan consolidé. Objectif : **une seule barre de navigation visuelle**, même style et même idée, dans **tous les modules qui ont un parcours en plusieurs étapes**.

- **Référence** : la barre construite pour le Bilan / « Analyser ma candidature » (maquette `docs/MAQUETTE_BILAN_PARCOURS_CONSOLIDE_2026-08-29.html`) - **une icône par pastille**, chevrons `»` entre elles, **halo bleu** sur l'étape courante, **pastille verte** sur les étapes faites (choix A de Denis : plus encourageant pour ce public que le style actuel de Cohérence, où fait et en cours se ressemblent), **le chevron entre l'étape courante et la suivante clignote** (borné, sans changement de taille - `LECONS 1sexies`), respect de `prefers-reduced-motion`.
- **`modules/coherence-transversale/ui.js` (`ctHtmlBarreEtapes` / `CT_ETAPES`)** : aligner sur ce style (aujourd'hui : fait = même aspect qu'en cours, pas de vert). C'est la 2e barre existante, à harmoniser.
- **Extraire un composant partagé** (réutilise les classes `.pastille-etape-action*` / `.chevron-etape-action` déjà en place, jamais une nouvelle palette) et l'appliquer à chaque parcours multi-étapes : Découvrir mes compétences, Coécrire ma lettre, Préparer un entretien, Regard extérieur (« Aller plus loin »)…
- **Lien** : c'est le **bloc 1 de la structure canonique** des pages d'introduction (`docs/PAGES_INTRODUCTION_MODULES_RECUEIL.md` §2). À traiter avec le chantier « généraliser le patron page d'introduction », ou juste après lui.
- **Règle** : la barre reste **informative, non cliquable** ; **jamais un temps estimé**, seulement une position dans le parcours.

## [ABANDONNÉ - 2026-09-11, décision Denis] Langage visuel commun + 3 maquettes de référence

**Décidé par Denis le 2026-08-30**, après la consolidation du module « Analyser ma candidature ». Sa maquette (`docs/MAQUETTE_BILAN_PARCOURS_CONSOLIDE_2026-08-29.html`) est **la référence visuelle** à citer pour toute nouvelle maquette de module. Vocabulaire condensé dans **`docs/LANGAGE_VISUEL_COMMUN.md`** (créé 2026-08-30) : formes de boutons (rectangle arrondi, jamais rond), accordéons `.bloc-depli`, encarts teintés à filet gauche, pastilles de verdict dessinées, décompte « hero », icônes = objets jamais visages, jetons `var(--…)` clair + sombre, ton rassurant.

> **Abandonné le 2026-09-11.** Évalué à la demande de Denis (« Cohérence de mon dossier est-il si différent, mérite-t-il cette amélioration ? ») : le vrai code utilise déjà les mêmes briques que partout (`.cv-section`, `.bloc-depli`, boutons rectangulaires, 9 sections aussi riches que le Bilan) - vérifié en le faisant tourner dans le navigateur, capture à l'appui. Le seul écart imaginé (une teinte `--accent` propre au module) n'en est pas un : **le Bilan, la référence elle-même, n'a pas de teinte propre non plus** - les deux utilisent le bleu par défaut. Créer la maquette de documentation manquante n'aurait changé strictement rien de visible dans l'application. Reste vrai : Repères a bien sa maquette (`docs/MAQUETTE_INTRO_REPERES_2026-09-09.html`), donc 2 des 3 maquettes de référence existent (Bilan + Repères) ; Cohérence n'en aura pas, et ce n'est plus un manque.

- **But d'origine** : chaque module garde une **spécificité graphique** (au minimum sa teinte `--accent`), mais formes / mécanismes / ton identiques - **qu'aucun module ne paraisse plus important ou mieux fini qu'un autre**.
- ~~**Cohérence de mon dossier** et **Repères** n'ont **pas** de maquette HTML dans ce langage (seulement des briefs / wireframes texte). À **créer** (retrofit), pour avoir **3 maquettes de référence**. Chantier séparé, **pas urgent**.~~ Périmé : voir l'abandon ci-dessus.
- **Outils :** `docs/maquettes/_skeleton-intro-module.html` = squelette à copier (jetons clair + sombre via `body.dark`, `.bloc-depli`, encarts, boutons rectangulaires). Fichier en lecture seule : les fenêtres qui travaillent une maquette le copient, ne le modifient pas.
- **Maquettes d'intro de module DÉJÀ FAITES dans ce langage (fenêtres parallèles, 2026-08-30)** : Mon Carnet (`docs/MAQUETTE_INTRO_CARNET.html`, `6b98d9d`) ; Lexique (`docs/MAQUETTE_INTRO_LEXIQUE.html`, `8e56558`) ; Co-construire ma lettre de motivation (`docs/MAQUETTE_INTRO_LETTRE.html`, emportée dans le commit partagé `d38070b` par une fenêtre parallèle). Préparer un entretien : brief donné, en cours.
  - **Co-construire ma lettre** : copie du squelette, teinte `--accent` verte (`#15803d` / `#4ade80`), mode sombre vérifié. Points fidèles au code (`ouvrirDepotLettreV1` → `ouvrirAssistantDepotCV('pret')` → `ouvrirChoixAssistantLettreV1` → `ouvrirRecuperationLettreV1`) et au prompt `prompts/lettre-v1.md` : assistant en ligne au clavier ou à la voix ; on part d'un CV sous n'importe quelle forme (fichier / photo / texte collé), **jamais** un CV mis en forme ou construit dans l'application, mais un CV **reste exigé** (pas de bouton « Passer », le prompt bloque avant « Start ») ; écran de vérification / masquage avant transmission ; livrables = lettre Word (DOCX) + questions d'entretien et pistes de réponse en fin d'échange. **Règle posée ici** : aucune page d'introduction de module ne porte de bloc « Cet outil travaille avec » ni de recommandation vers un autre module (décision Denis 2026-08-30 : ça hiérarchise les modules) - notée en tête du fichier de maquette.
- **Discipline fenêtres parallèles** : un fichier neuf par fenêtre, `git add <ce fichier>` seulement (**jamais `git add -A`** - a déjà aspiré le WIP d'une autre fenêtre dans un commit le 2026-08-30), ne pas toucher au code ni aux docs partagés.

## [CLOS - 2026-09-11] Équilibrer les pages d'introduction des modules (quantité d'information homogène)

> **Remplacé par un audit de fidélité, décision Denis 2026-09-10** : la mesure d'origine ci-dessous (8 pages "en retard") était fausse - un bug de mesure (awk) comptait ~30-41 lignes là où les 8 pages faisaient en réalité 66-93 lignes / 11-12 blocs, déjà au niveau du Bilan. Denis a alors demandé un audit de FIDÉLITÉ (la page décrit-elle ce que le module fait réellement aujourd'hui ?) plutôt qu'un enrichissement en volume, élargi aux **16** pages d'introduction du code (pas seulement 8). Résultat : `docs/AUDIT_PAGES_INTRO_2026-09-10.md` - 12 fidèles, 4 dérives corrigées (Regard recruteur, Comprendre le cadre, Co-construire ma lettre, Lexique), section « Mes Repères » harmonisée sur toutes les intros de parcours, plus aucune intro ne nomme un module voisin, wording « parcours » aligné sur la carte « Mes documents ». Commits `e9afa84`, `1857efd`, `b461d4f`. `npm test` 780 verts à chaque étape.

<details><summary>Contexte d'origine (2026-08-30), conservé pour mémoire</summary>


**Décidé par Denis le 2026-08-30.** Constat : la page d'accueil / d'introduction d'« Analyser ma candidature » (`htmlBilanIntro()`) est **bien plus fournie** que celle de « Cohérence de mon dossier », et probablement que celles des autres modules. Résultat non voulu : impression qu'un module est plus important qu'un autre, alors qu'ils sont tous travaillés, utiles et complémentaires.

- **Objectif** : enrichir les pages d'introduction des autres modules de façon **homogène**, pour une quantité d'information à peu près **équitable** d'un module à l'autre. Cible de richesse = `htmlBilanIntro()` (`js/app.js`, ~117 lignes de contenu). S'appuyer sur `docs/LANGAGE_VISUEL_COMMUN.md` §5 et `docs/PAGES_INTRODUCTION_MODULES_RECUEIL.md`.
- **Priorité** : **pas en premier**. Denis : « revenir travailler une page d'accueil pour des modules qu'on a déjà, ce n'est pas la chose la plus importante ». Mais **c'est à faire** - marqué ici pour ne pas l'oublier.
- **Mesure du 2026-09-10 (contenu réel, hors chrome partagé)** : `htmlBilanIntro` ~117 lignes ; `_reperesRenduAccueil` (Repères) ~142 ; `ctHtmlExplication` (Cohérence) ~91 - ces 3 sont au niveau. **En retard, ~30 à 41 lignes chacune (≈ ⅓ du Bilan)** : Co-construire ma lettre, Reformuler et présenter mon CV, Préparer un entretien, Découvrir mes compétences (intro), Les mots de votre CV (ATS), Regard recruteur, Se tenir informé, Comparer mes pistes / Aide à la décision. Cause : `htmlPageIntroModuleParcours()` ne fournit que l'habillage (titre, synthèse, CTA, barre du bas, encart multilingue, bouton « Revoir la présentation ») ; le corps est `config.blocsHTML`, une chaîne HTML que chaque module remplit lui-même - donc pas d'homogénéité par construction. **Travail restant** : enrichir le `blocsHTML` de ces 8 pages au canon (10 blocs - le bloc 7 « Cet outil travaille avec… » a été retiré, voir RÈGLE ABSOLUE plus bas). C'est du contenu rassurant + peu de code. Regard extérieur n'a pas de page d'intro à lui (fusionné avec Repères, par décision) - pas un manque. **À corriger aussi** : `docs/PAGES_INTRODUCTION_MODULES_RECUEIL.md` §2 porte encore la structure à 11 blocs, la ramener à 10.
- **Périmètre** : les 8 pages listées ci-dessus. (Cohérence + Repères = 1re étape déjà CLOSE.)
- **Statut (Denis 2026-09-10)** : à faire **par la suite**. **Consigne renforcée ce jour** : sur la page d'introduction d'un module, ne parler **que de ce module** - jamais mentionner, décrire ni renvoyer vers un autre module (rappel de la RÈGLE ABSOLUE du 2026-08-30 ci-dessous, bloc 7 « Cet outil travaille avec… » supprimé du canon). L'enrichissement de chaque `blocsHTML` reste centré sur le module lui-même : à quoi il sert, ce qui va se passer, ce que ce n'est pas, bon à savoir (confidentialité + sauvegarde).
- **1re étape (Denis 2026-08-30) : Cohérence + Repères.** Ils ont **déjà** une page de présentation, plus courte. À **enrichir en code** (pas une maquette) : `ctHtmlExplication()` dans `modules/coherence-transversale/ui.js` (~L138, 3 sections aujourd'hui) et la page de présentation de Repères dans `modules/reperes/index.js`. Ajouter les sections manquantes du patron (cadrage rassurant, « ce que ce module ne fait pas », « ce que vous pourrez faire ensuite », « Bon à savoir » disquette), adaptées au vocabulaire du module. Thème sombre = `[data-theme="sombre"]` (vrai code). Ne pas toucher aux fonctions. `npm test` + navigateur clair + sombre, un commit par module.
  - **[CLOS] 2026-08-30, validé par Denis.** `ctHtmlExplication()` passée de 3 à 9 sections (commits `d5ea8fe` + `8ef5956`) ; `_reperesRenduAccueil()` enrichie de 4 sections : raison d'être compacte, « Ce qui va se passer », « Ce que ce n'est pas » titré, « Ce que vous pourrez faire ensuite » (commits `4168ac9` + `59464c4`, classes `reperes-accueil-*` réutilisées, aucun CSS ajouté). 603 tests verts, vérifié navigateur clair + sombre. Aucune fonction touchée (bouton « Je commence », barre d'étapes, dispatch).
  - **Exception à la RÈGLE ABSOLUE ci-dessous, actée par Denis 2026-08-30** : l'encart « ⚓ Mes Repères » **est conservé** dans `ctHtmlExplication()` (demandé explicitement dans le brief, présent aussi sur `htmlBilanIntro()` de référence - Mes Repères = espace personnel transversal, pas un module « pair » dans la hiérarchie). Tous les autres renvois inter-modules qui avaient été ajoutés (puce vers le module Entretien dans Cohérence ; « Analyser ma candidature » / « Cohérence de mon dossier » / « Mon Carnet » dans Repères) ont été **retirés** sur demande de Denis. Ne pas les réintroduire.

</details>

- **RÈGLE ABSOLUE (Denis 2026-08-30, posée sur la maquette Co-construire ma lettre)** : **aucune** page d'introduction de module ne porte de bloc « Cet outil travaille avec » ni de recommandation / renvoi vers un autre module (même une simple puce « ensuite, allez dans X »). Motif : ça donne de la visibilité à certains modules et en retire aux autres, ce qui les hiérarchise. Ceci **supprime le point 7 (« Cet outil travaille avec… ») de la structure canonique** de `docs/PAGES_INTRODUCTION_MODULES_RECUEIL.md` §2 et le remplace, dans le squelette, par une section recentrée sur le module lui-même. Les renvois inter-modules restent réservés au « chantier futur Cohérence entre les modules » (fin de parcours, jamais page d'intro). Seule tolérance discutée : une mention purement dissuasive dans « Ce que ce module ne fait pas » - à trancher au cas par cas avec Denis.

## Chantier futur : "Cohérence entre les modules" (croisements, recommandations et actions inter-modules)

**Cadré par Denis le 2026-08-29**, pendant la consolidation du Bilan (idée « b » écartée du chantier Bilan pour devenir un chantier à part entière, jugée trop transversale). **Bien plus tard** - « on a des choses beaucoup plus importantes à faire pour l'instant ».

**L'idée** : analyser systématiquement où les modules se recoupent, et créer des **points d'intersection** : depuis un module, une recommandation ou une action qui renvoie vers un autre module pour une fonction précise. Exemples entrevus :
- Bilan / « Analyser ma candidature » : une case « ce point me semble important pour plus tard » ; les points cochés remontent dans **Cohérence de mon dossier** comme éléments à recontrôler (ex. une incohérence de dates repérée dans le CV réapparaît quand on compare CV / lettre / entretien).
- À la fin du Bilan : renvoi honnête vers **ATS** (langage / mots-clés) et **Regard employeur** (forme / visuel) - « cet outil regarde le contenu ; aucun de ces regards seul ne donne l'image complète ». Uniquement quand ces modules existeront ; d'ici là, version légère vers l'existant (Cohérence de mon dossier, Lettre, Entretien).
- Plus généralement : `docs/IDEES_A_RECLASSER.md` (« le cœur du produit, c'est la circulation entre modules de la connaissance produite par la personne ») - à transformer en grille systématique.

**Garde-fous** : jamais un menu de navigation ni une injonction ; toujours un « pour aller plus loin » discret, en fin de parcours d'un module, jamais dans le corps. Respecter la doctrine « la réutilisation automatique se limite aux objets de candidature (CV, offre, entreprise, compétences validées) » - une note brute du Carnet, une réflexion d'un Repère ne circulent jamais seules.

**Statut** : idée cadrée, aucune analyse, aucun plan. À rouvrir une fois les modules ATS et Regard employeur au moins conçus.

## Idée future : "module de sites de référence" nommés, cherchable par mot-clé

Évoquée par Denis le 2026-08-27 (matin), pendant la conception de la barre de recherche élargie. Idée : un module à part qui associe un mot-clé (ex. "statistique", "chiffre du chômage") à un site de référence nommé et vérifié (ex. l'INSEE) - taper le mot-clé dans la barre de recherche amènerait directement à la fiche de ce site, avec une explication de ce qu'on peut y trouver. Proche dans l'esprit de "Se tenir informé"/"Veille informationnelle" et du futur "Ressources" (répertoire freins/ressources) déjà notés plus bas - à clarifier avec Denis si c'est un troisième module distinct ou une extension de l'un des deux existants, avant de le cadrer. Toute URL ajoutée devra être vérifiée avant publication (voir `docs/LECONS_A_NE_PAS_REPRODUIRE.md`, section 9.16). Pas commencé, pas cadré.

## Chantier futur : "Veille informationnelle" (module à part, pas commencé, ambitieux - volontairement mis de côté par Denis pour finir d'abord des chantiers plus accessibles)

Idée soulevée par Denis le 2026-08-27, née de la discussion sur l'élargissement de la barre de recherche. Nouveau module indépendant, sa propre tuile, ouvert à tous (bénéficiaires ET professionnels/CIP - Denis termine actuellement sa formation de CIP) - jamais réservé à un seul public. But : capitaliser des informations sur le monde de l'insertion professionnelle - législation, statistiques (chômage, métiers en tension, bassins/zones d'emploi), partenaires, événements, structures, monde associatif, difficultés du secteur - via un prompt vers une IA externe, même contrainte budgétaire que tout le reste de l'app (pas d'API, pas de serveur).

**Fonctionnement envisagé, hybride, encore à trancher dans le détail** :
- Un mode "recherche en direct" (façon "one shot") : on tape plusieurs mots-clés (avec possibilité de se focaliser plus sur l'un que sur l'autre), un contexte, des concepts - le prompt doit chercher non seulement l'information sur chaque entrée mais aussi les LIENS entre elles, avec plusieurs grilles de lecture possibles du même résultat. Possibilité de croiser plusieurs allers-retours de filtrage avant de retenir l'information utile. Le résultat peut être sauvegardé ou non par la personne, à sa discrétion.
- Un fichier de référence organisé en rubriques précises, nourri au fil du temps avec les dernières données (une donnée plus récente écrase l'ancienne), consultable instantanément via la barre de recherche par mot-clé - même principe que `data/metiers.js`, déjà dans l'app.

**Avis de Claude donné le 2026-08-27, à reprendre au moment de concevoir ce chantier** : le plus grand risque est la fiabilité de l'information si le prompt tourne sur une IA sans navigation web réelle activée - une IA en mode classique répond avec des connaissances déjà datées, avec le même risque déjà rencontré sur les URLs devinées de Regard extérieur (leçon 9.16), mais ici sur des chiffres et des lois, plus difficiles à vérifier d'un coup d'œil. Recommandé : exiger dans le prompt une IA à recherche web activée (ChatGPT ou Claude avec la recherche web, ou Perplexity), avec citation systématique de la source et de la date pour chaque fait. Recommandation aussi : une curation/vérification par Denis avant publication dans le fichier de référence partagé (plutôt qu'une génération individuelle par chaque bénéficiaire sur les données de référence), le mode "recherche en direct" restant lui pleinement individuel pour une question précise et ponctuelle.

**Statut** : chantier volontairement mis de côté par Denis - d'autres chantiers plus accessibles sont prioritaires. Portée exacte (contenu précis du prompt "multi-niveaux", format du fichier de référence, fréquence de mise à jour, périmètre géographique) pas encore cadrée, à reprendre plus tard.

> **BLOQUÉ derrière la refonte de la page d'accueil (confirmé par Denis le 2026-08-27).** Tous les points "pulses / disquette dynamique / file d'attente" ci-dessous touchent des éléments (icônes persistantes, disposition, mot "Sauvegarder") qui vont être modifiés par la refonte "Porte d'entrée". Aucun sens à les coder avant : ils seront proposés et implémentés PENDANT ce chantier, pas séparément.

- [SANS OBJET - 2026-09-10, décision inverse de Denis] Comportement dynamique de la disquette "Sauvegarder" : le mot "Sauvegarder" apparaît/disparaît selon qu'on est sur l'accueil ou dans un parcours, et réapparaît + pulse quand on arrive sur un panneau où des données ont déjà été récupérées. **Annulé** : `docs/CHANTIER_BOUTON_REVOIR_PRESENTATION.md` point B (2026-09-01) a **retiré le mot « Sauvegarder » partout** (icône seule, sur toutes les pages et sur mobile ; annule aussi le point 13 du relais). Ce qui reste et qui est FAIT : le pulse de la disquette (travail non sauvegardé / après un passage assistant) et le coordinateur de pulse unique `_pulsePersistantDemander()` (priorité disquette > aide > préférences, l'infobulle « i » passe devant, `722b972`). La partie « le mot réapparaît » n'a plus d'objet.
  - *Contexte d'origine conservé ci-dessous (ne plus l'implémenter).* Sur l'accueil, la disquette affichait le mot "Sauvegarder" à côté de l'icône. Dès que la personne s'engage dans un parcours (quitte l'accueil - ex. clique sur "Créer un nouveau CV" depuis "Mes documents", est amenée sur la page de CV), le mot disparaît, ne reste que l'icône.
  **Déclencheur du retour du mot + pulse, précisé par Denis (version qui remplace la première idée du matin, plus étroite)** : pas seulement "un message IA importé avec succès" - plus largement, **dès que la personne se retrouve sur un panneau où des données ont déjà été récupérées par le code de l'app**, quelle que soit la façon dont elles y sont arrivées : les rubriques affichent des informations déjà récupérées, OU la personne est sur une page de rapport, OU des informations étaient déjà présentes dans le code à ce moment-là. Denis précise que ce comportement existe déjà ailleurs dans l'app en production ("exactement comme on a actuellement sur la page d'écran") - **à retrouver et vérifier dans le vrai code avant de coder cette version**, ne pas repartir de zéro (voir aussi la mémoire personnelle "pulse disquette vert à chaque navigation" de sessions précédentes). Durée du pulse + réapparition du mot : 10 secondes max, règle générale déjà fixée plus bas pour toutes les icônes de la page (remplace l'ancienne idée de 15 secondes) - puis le pulse et le mot s'arrêtent, ne reste que l'icône.

- [CLOS - vérifié contre le code 2026-09-10, résolu autrement que la note d'origine] Couleur du pulse d'attention (signalé le 2026-08-27). **FAIT par la refonte de l'accueil (`[CLOS] 2026-09-01`, coordinateur `722b972`)** : les 4 pulses d'icônes persistantes (`#btnAide` `aide-pulse-attention`, `#btnPreferencesAffichage` `aide-pulse-accent`, `#btnSessionTransfert` `aide-pulse-disquette` / `-verte`) utilisent **tous la même animation `pulseAccentPersistant`** (`css/style.css` ~3323 : `box-shadow` en `color-mix(var(--accent) 40%)`) - plus aucune couleur héritée du hasard d'un emoji. Le risque de confusion que l'ambre devait résoudre est traité par le **coordinateur `_pulsePersistantDemander()`** : un seul pulse persistant à la fois (priorités disquette > aide > préférences). Seul pulse d'une autre couleur, volontaire et documenté : `pulseZoneRecitDecouverte` en **vert** (`var(--success)`) sur la zone de récit de Découverte (« c'est ici, allez-y », demandé par Denis, LECONS 1sexies).
  **Précision de fond, plus importante que la couleur (2026-08-27)** : Denis a expliqué le vrai scénario - à l'arrivée sur l'accueil, l'œil (réglages d'affichage) ET l'ampoule (aide/visite guidée) sont TOUS LES DEUX censés pulser pour inviter à cliquer, et la disquette pulse en plus dès un premier aller-retour IA réussi. Risque réel si les 2-3 pulsent EN MÊME TEMPS, même en couleur identique : œil et ampoule sont juste côte à côte, un cumul de pulses y devient un bloc confus plutôt que 2 signaux clairs. **Règle retenue : jamais plus d'un pulse actif à la fois, une file d'attente plutôt qu'un cumul** - à l'arrivée, l'œil pulse seul quelques secondes, s'arrête, puis l'ampoule prend le relais ; si la disquette doit pulser pendant qu'un autre pulse est en cours, elle attend son tour. Plus important que le choix de couleur pour résoudre le risque de confusion.
  **Précisions finales de Denis (2026-08-27)**, à respecter pour toutes les icônes persistantes de l'accueil (pas seulement œil/ampoule/disquette) :
  - **File d'attente confirmée avec un exemple concret** : si le Journal de parcours pulse (vient d'apparaître, premier Repère créé) au moment où la disquette devrait aussi pulser (retour IA reçu), la disquette attend la fin du pulse du Journal avant de démarrer le sien - jamais superposés.
  - **Durée maximale de pulse : 10 secondes, pour toutes les icônes de cette page, sans exception** - corrige à la baisse l'idée notée plus haut pour la disquette (15 secondes) : cette ancienne valeur est remplacée par cette règle générale de 10 secondes max.
  - ✅ **[FAIT - 2026-08-28, `be2b957` puis homogénéisation] Audit + homogénéisation du rythme des pulses.** Denis : "j'ai l'impression que je vais faire une crise d'épilepsie" - vrai sujet de photosensibilité. **Standard d'intensité appliqué à TOUS les pulses de l'app** (`css/style.css` + `modules/carnet|reperes|regard-exterieur/*.css`) : anneau `box-shadow` de `rgba(couleur, 0.4)` étendu à `8px`, **aucun changement de taille** (`transform: scale` retiré partout), cadence `1.8s` pour les pulses infinis (les pulses bornés type confirmation gardent leur durée courte). Seule la COULEUR varie selon le sens (bleu invitation / jaune perdu / gris sauvegarde / vert non sauvegardé / accent découverte). Les 4 plus agressifs (`pulseRetourRenforce` au retour d'un passage IA = 0.9s + grossissement, `pulseChevronEtapeAction` + `pulseCompteurIncomplet` = zoom 18 %, `clignoteLegerBleu` = 1.1s) sont ceux qui piquaient le plus. **Temps homogénéisé aussi (2026-08-28, sur demande de Denis "tout à dix secondes, tout ce que tu trouves partout")** : plus AUCUN pulse infini, et TOUS les pulses (y compris ceux déjà bornés à 3/5/8/10/12 répétitions, y compris ceux définis dans des `<style>` inline générés en JS - `pulseEditionPdf`/`cycleMessageEditionPdf` dans `js/app.js`, `pulseEtapeIA` dans `js/app.js`, `pulseValiderDecouverte` dans `decouverteParcours.js`) passent à `1.8s ease-in-out 6` (≈ 10,8 s). Les minuteries JS qui retiraient une classe de pulse (`pulse-attention` à 3,6 s, `_pulseExperiencesJusquA` à 15 s, `carnet-pulse-confirmation` à 2 s, `regard-exterieur-pulse-*` à 12 s) passent toutes à 10 000 ms. Seul `infinite` restant = le spinner de chargement `demo-video-tourner` (rotation, pas un pulse). **En plus** : ajout d'une règle globale `@media (prefers-reduced-motion: reduce)` en fin de `style.css` (plusieurs commentaires y renvoyaient alors qu'elle n'existait pas). Voir [[LECONS §1sexies]]. Référence de rythme calme : `pulseDouce` de la maquette "Porte d'entrée" (1.8s).
  - **Rappel du comportement du mot "Sauvegarder"** (déjà noté plus haut, reconfirmé) : visible sur l'accueil, disparaît dès que la personne s'engage dans un parcours (ex. clique sur "Créer un nouveau CV" depuis "Mes documents") - ne reste alors que l'icône.
  - **Le pulse s'arrête dès la première interaction, pas seulement au bout du temps fixé (2026-08-27)** : dès que la personne survole OU clique sur un élément qui pulse, le pulse doit s'arrêter immédiatement - la personne l'a déjà remarqué, inutile de continuer jusqu'à la durée maximale (10 secondes). Règle applicable à tous les pulses de la page, pas seulement à l'infobulle de "Mes documents" où elle a été testée en premier. Implémenté et vérifié dans la maquette "Porte d'entrée".
  - **Priorité étendue à l'infobulle de chaque carte (2026-08-27)** : quand une personne ouvre une fenêtre (ex. "Mes documents"), l'infobulle "i" de cette fenêtre pulse pour attirer l'attention dessus - et cette pulsation est PRIORITAIRE sur tout le reste : les 5 icônes persistantes de l'accueil (œil/ampoule/disquette/carnet/journal) suspendent leur propre pulse tant que celle de l'infobulle est active. La règle "un seul pulse actif à la fois" (voir juste au-dessus) s'applique donc à toute la page, pas seulement entre les 5 icônes persistantes - l'infobulle de la fenêtre ouverte passe toujours devant.
  - **Forme de la disquette confirmée telle quelle** : Denis a hésité en la trouvant "pleine", puis a choisi de la garder ainsi volontairement - sa forme différente des 4 autres icônes (elle seule est une pilule avec un mot) l'aide déjà à se différencier, pas besoin d'y retoucher.
- [CLOS - 2026-08-28 (`8008179`), testé en navigateur] Titre du panneau disquette : `#panneauSessionTransfert` (`index.html`) affiche désormais "💾 Sauvegarder ma session" + le texte validé ("Cette application ne garde rien automatiquement : exportez un fichier pour ne pas perdre votre travail. Vous pourrez aussi vous en servir pour reprendre votre session sur un autre appareil, si besoin."). L'`aria-label`/`title` du bouton reste "Sauvegarder ma session / changer d'appareil" (les deux fonctions, toujours exact).

- [CLOS - 2026-08-28 (`8008179`), testé en navigateur] Texte de "Confidentialité de vos données" : `ouvrirModaleConfidentialite()` (`js/app.js`) remplacé en entier par la version validée mot pour mot. **Raisonnement conservé ci-dessous** (à ne pas perdre si retouché un jour). Texte appliqué :
  > Les informations que vous renseignez sont utilisées uniquement pour personnaliser votre accompagnement. Elles permettent de construire vos documents de candidature et d'utiliser les différents outils de l'application.
  >
  > Vos informations restent disponibles tant que cette page reste ouverte. Elles ne sont pas enregistrées de manière permanente par l'application et disparaissent lorsque vous fermez ou rechargez la page.
  >
  > Des outils vous permettent de masquer vous-même les informations sensibles (nom, téléphone, e-mail) avant de les envoyer à un assistant IA, si vous le souhaitez. Rien d'autre n'est jamais envoyé automatiquement.
  >
  > 🕵️ Si vous utilisez cette application sur un ordinateur public, nous vous conseillons de l'ouvrir en navigation privée.

  **Raisonnement retenu, à ne pas perdre si le texte est retouché un jour** : (1) plus aucune énumération de modules/outils par leur nom (formulation volontairement générique "les différents outils de l'application") - Denis ne veut plus jamais avoir à retoucher ce texte à chaque ajout/fusion/suppression de module. (2) La promesse "l'identité n'est jamais transmise" a été généralisée en "rien d'autre n'est jamais envoyé automatiquement" - une seule phrase couvre tout, sans lister quels flux envoient quoi. (3) "Ce n'est jamais obligatoire" retiré volontairement (pas un oubli) : Denis anticipe que des structures d'accompagnement voudront sensibiliser les bénéficiaires à faire cette démarche - dire explicitement "pas obligatoire" pourrait se lire comme "ça n'a pas d'importance", contraire à l'esprit voulu. (4) Le passage "aucune anonymisation automatique n'est effectuée (impossible à fiabiliser)" a été retiré du texte visible - contexte donné par Denis : tentative réelle et abandonnée par le passé (pas de répertoire fiable de noms/prénoms possible), décision assumée de laisser la démarche entièrement libre plutôt que de forcer quoi que ce soit. (5) Navigation privée : conseil donné UNIQUEMENT pour un ordinateur public, sans détail (pas de raccourcis clavier, pas de noms de navigateurs) - volontairement minimaliste, l'emoji 🕵️ (déjà utilisé dans le vrai texte actuel) est conservé tel quel.

- [CLOS - vérifié contre le code 2026-09-10] **Empêcher les vraies fenêtres d'action de se fermer au clic extérieur.** **CONFORME** : `ouvrirFenetreERIP()` (`js/app.js`) porte un commentaire explicite « la fermeture au clic sur le fond assombri (en dehors de la boîte) est retirée » et ne pose QUE deux fermetures - la croix `#fenetreERIPFermerBtn` et la touche `Escape` (`_surToucheFenetreERIP`). Aucun `overlay.addEventListener('click', …)`. Les panneaux des 6 cartes d'accueil (`ouvrirCarteAccueil`, `data/metiers.js`) passent par `ouvrirFenetreERIP()` - donc couverts. Rien à corriger. *(Contexte d'origine : garde-fou pour l'implémentation de la maquette « Porte d'entrée », faite le 2026-09-01 sans introduire de fermeture au clic extérieur.)*
  - **État du vrai site actuel (re-vérifié dans le code le 2026-08-28)** : conforme, rien à corriger ici. `ouvrirFenetreERIP()` (`js/app.js:3601`) ne pose QUE deux fermetures - clic sur `#fenetreERIPFermerBtn` et touche `Escape` (`surToucheFenetreERIP`) ; aucun `addEventListener('click')` sur l'overlay.
  - **Où est le problème** : Denis a testé la **maquette "Porte d'entrée"** (travaillée sur un autre compte, à implémenter quand il aura plus de budget d'utilisation - voir la section "Maquette Porte d'entrée VALIDÉE" plus bas). Dans cette maquette, la fenêtre "Mes documents" se fermait à tort au clic extérieur ; corrigé dans la maquette elle-même. **Cette tâche existe pour garantir que ce comportement correct est bien repris lors de l'implémentation de la maquette dans le vrai code** - la refonte de l'accueil réorganise les cartes, la barre de recherche, la réception des informations et ajoute une page d'introduction par module : le risque que ce point précis passe à la trappe est réel, d'où son maintien explicite en tâche. Raison donnée par Denis, observée en réel : bénéficiaires sur PC portable sans souris, peu à l'aise avec le pavé tactile, clics accidentels qui fermaient une fenêtre en cours de remplissage - frustration réelle et importante. Corrigé dans la maquette "Porte d'entrée" (la fenêtre "Mes documents" fermait à tort au clic extérieur, retiré).
- [CLOS] Barre de recherche figée sur la maquette "Porte d'entrée" (signalé le 2026-08-27 par Denis, corrigé le même jour). **Vraie cause trouvée** (pas un problème de navigateur/clavier/environnement - hypothèse initiale erronée, écartée par Denis lui-même via test en navigation privée) : `chercher()` appelait `masquerResultat()` dès que le texte tapé faisait moins de 2 caractères pour cacher le tiroir de résultats, mais cette fonction vide aussi `champRecherche.value` - donc à chaque frappe (le 1er caractère fait toujours <2), le champ se vidait aussitôt, invisible pour la personne. Corrigé en séparant `masquerDropdown()` (cache seulement, utilisé pendant la frappe) de `masquerResultat()` (cache + vide, réservé à la fermeture réelle : croix, clic dehors, Échap). Testé en direct lettre par lettre et mot complet dans le navigateur de Claude - le texte reste bien affiché. Détail complet : [[LECONS 9.21]] dans `docs/LECONS_A_NE_PAS_REPRODUIRE.md`. Rotation du texte du placeholder restée à 15 secondes.
- [CLOS] Croix de fermeture orange au premier survol sur "Mes documents" (signalé le 2026-08-27 par Denis, 2 captures d'écran). Cause : contour de focus par défaut du navigateur (couleur dépendante des réglages système/accent Windows de Denis), pas une couleur choisie dans le code. Corrigé par une règle `:focus-visible` explicite en gris neutre (`var(--text-muted)`), qui prime désormais sur le défaut du navigateur, quel que soit le système. Testé en direct (couleur calculée confirmée, aucune règle en conflit). Détail complet : [[LECONS 9.22]]. Point additionnel découvert au passage, pas corrigé (pas dans le budget de cette correction) : Tab depuis cette croix sort de la fenêtre modale vers les boutons du bas au lieu d'y rester piégé - à traiter dans un futur chantier accessibilité.
- [CLOS - 2026-09-10, commit `cd690e0`, vérifié navigateur] **Aide contextuelle (`AIDE_PAGES`) ajoutée sur les modules qui n'en avaient pas.** L'ampoule disait « Aucune visite guidée disponible » sur `co-lettre`, `prepa-entretien`, `reformuler-cv`, `ats`, `regard-recruteur`, `comparer-pistes`, `se-tenir-informe-intro`, `decouverte-intro`. Une entrée par route (2-3 items : ce que fait le module + « Où vous en êtes » + navigation), **chaque entrée ne parle que de son module**. Alias `AIDE_PAGES['ats-intro' | 'regard-recruteur-intro' | 'aide-decision-intro']` → l'aide du module (car `naviguerVers('ats' | ...)` sans travail engagé pose `pageActuelle` sur la route d'intro). **Non touché** : « Comprendre les chiffres » (chantier en cours côté Denis, autre poste) ; « Découvrir mes compétences » en **parcours** (route `decouverte`) était déjà couvert par `AIDE_FENETRES` via `detecterFenetreAideActive()`. Vérifié : les 8 routes trouvent ≥ 2 sélecteurs présents, recap + visite guidée OK, 0 erreur console, `npm test` 780.
- [À FAIRE] Retravailler le questionnaire "Retour sur votre expérience" (signalé le 2026-08-27) : le formulaire actuel (Google Forms) a été construit avant que Denis n'ait tous les modules actuels de l'app - à revoir ensemble une prochaine fois, pas aujourd'hui.
- [CLOS - 2026-09-09] **Refonte de l'écran d'accueil du module Lexique** (celui avec la barre de recherche, pas la page de présentation). Constat Denis : trop vide. Refondu en 3 niveaux d'après `docs/MAQUETTE_LEXIQUE_ACCUEIL_2026-09-09.html` (validée en 5 versions) : Niveau 1 titre + accroche positive + recherche agrandie (texte fixe) + ligne d'exemples cliquables tirés au sort (toujours sur une seule ligne) ; Niveau 2 « Comprendre un sujet » = 6 thèmes tirés au sort parmi les 36 collections (icône + titre + nb de fiches par thème, jamais de total) + « Voir tous les thèmes » ; Niveau 3 « Explorer » = 3 lignes (Parcourir par domaine [nouvel écran, 18 domaines A→Z] / Mode Livre / Mes favoris). **Page vivante** : un minuteur de 10 s renouvelle thèmes + exemples ensemble, pause + remise à zéro au survol, rien en `prefers-reduced-motion`. **Mode Livre** devient une liste unique A→Z de toutes les fiches (plus de regroupement par univers). Zéro régression fonctionnelle (recherche, Umami, liens profonds, détour, chaîne Retour). Commits `e86ceea` + `e4b38ac`. Détail : `modules/lexique/ARCHITECTURE_TECHNIQUE.md` étape 14. **Exception d'icône assumée (Denis, 2026-09-09)** : la collection `collection-garde-enfant` garde l'ourson 🧸 (qui a un visage) malgré la règle « pas d'icône avec une tête » ; Denis alerté, décision de le garder. Ne pas la « corriger ».
- [CLOS - 2026-09-11, commit `a40d334`] **Lexique : statuts et structures du handicap (RQTH, AAH, invalidité...) - déroulé complet et aides en formation.** **Rédigé directement par Claude** (Denis a demandé d'aller plus loin que la matière préparée, `data/lexique.js` édité en direct) : 4 fiches créées (`pension-invalidite`, `pch`, `carte-mobilite-inclusion`, `rlh`) intégrées à la collection « Comprendre le handicap et l'emploi » ; 3 entrées `LEXIQUE_SECOND_NIVEAU` ajoutées (RQTH - formation/reconversion, MDPH - délai, AAH - reprise d'emploi) ; corps de la fiche `agefiph` corrigé (retire l'affirmation périmée sur le financement de formation). `node scripts/checkLexique.js` 0 erreur (322 fiches / 36 collections / 155 second niveau), `npm test` 781 verts, vérifié navigateur (rendu, relations croisées, recherche réelle). Document de matière source conservé : `docs/MATIERE_LEXIQUE_HANDICAP_2026-09-11.md`.

<details><summary>Ancienne trace [À FAIRE] avant rédaction, conservée pour l'historique</summary>

 **État des lieux vérifié le 2026-09-11 dans `data/lexique.js`** : le constat de Denis (ci-dessous) est confirmé exact, rien n'a changé depuis le 09-09. 4 fiches manquent entièrement (pension d'invalidité, PCH, CMI, RLH - la 4e optionnelle/technique), 3 fiches existantes sont à enrichir (Agefiph, RQTH, AAH) sur les points signalés par Denis. Matière sourcée (service-public.gouv.fr + agefiph.fr, liens vérifiés 2026-09-11, aucun montant chiffré) préparée dans `docs/MATIERE_LEXIQUE_HANDICAP_2026-09-11.md` : Denis tranche le découpage exact et rédige les fiches. **[Vérifié en profondeur le 2026-09-11, à la demande de Denis]** le point de vigilance signalé sur l'Agefiph n'était pas un simple resserrement : l'Agefiph a réellement suspendu le financement des frais pédagogiques de formation (demandeurs d'emploi depuis août 2024, salariés depuis janvier 2025), confirmé par l'absence de cette aide dans son catalogue actuel - la section correspondante du document a été corrigée avant toute communication, avec 2 formulations possibles qui n'affirment rien de faux. Denis 2026-09-09 : les principales structures sont déjà là (MDPH, Agefiph et FIPHFP, Cap Emploi, France Travail, Mission Locale, + comparatif) et les principaux statuts aussi (RQTH, AAH, OETH, ESAT, Emploi accompagné, collection « Comprendre le handicap et l'emploi »), mais les fiches courtes donnent surtout le « quoi ». À enrichir dans les « En savoir plus » : qui délivre le statut, comment ça marche, auprès de qui commencer les démarches, avantages, limites, conditions. **Point explicite de Denis** : mettre en avant que la RQTH ouvre des aides financières en cas de reconversion / formation (aujourd'hui seule la fiche Agefiph l'effleure). **Statuts sans fiche dédiée à créer** : pension d'invalidité (citée dans « RQTH ou invalidité ? » mais pas expliquée), PCH (prestation de compensation), CMI (carte mobilité inclusion) ; éventuellement RLH (reconnaissance de la lourdeur du handicap). Méthode : Claude prépare la matière (sources service-public.fr / Agefiph / CNSA, liens vérifiés - LECONS 9.16), Denis tranche et rédige les fiches. **Formulation sans montants chiffrés** (doctrine du Lexique : pas de chiffres qui bougent). Recoupe la piste « Enrichissement du contenu du Lexique » de `docs/IDEES_A_RECLASSER.md`.

</details>
- [CLOS - 2026-09-10, commits `5e97b71` + `7733a99`, vérifié navigateur clair + sombre, `npm test` 780] **Bilan (« Analyser ma candidature ») : « Retour » de la barre du bas un cran à la fois.** `bilanRetour(ecran)` reçoit désormais l'écran d'où l'on part :
  - **« Coller la réponse » → « Choisir mon assistant »** (`bilanRetour('import-ia')`) : nouvelle primitive `bilanStoreAbandonnerDiagnostic()` (diagnostic + demandes + propositions à null, candidature intacte) exposée via le passe-plat `bilanAbandonnerDiagnostic()`. Re-choisir un assistant recrée un diagnostic frais. Non destructif.
  - **« Choisir mon assistant » → « Préparer »** (`bilanRetour('choix-ia')`) : re-pose `_etatBilan.preparer` avec `relectureFaite: true` + `cvTexte = candidature.cv` (le **CV déjà masqué**, jamais re-relu), drapeau `_etatBilan.preparerRetour` pour passer la garde « pas de candidature » du dispatch. `btnPreparerVersAssistant` remet le drapeau à null. Non destructif (situation + offre survivent déjà via `dossier`).
  - **« Préparer » et « Rapport » → présentation** : inchangé (leur vrai écran précédent).
  - Échelle complète vérifiée : Coller la réponse → Choisir mon assistant → Préparer → présentation → accueil, un cran à la fois. Flux frais (sans retour) non régressé.
- [EN COURS - lots 1 et 2 FAIT, lot 3 quasi clos] **« Comprendre les chiffres » : remplir la moitié droite, vide, des rectangles.** Constat Denis (grand écran) : chaque carte n'utilise que sa moitié gauche. Maquette finale : `docs/MAQUETTE_COMPRENDRE_LES_CHIFFRES_COMPACT_2026-09-09.html` (v9, notée 18/20 ; la sortie « garder / imprimer » a depuis été abandonnée, voir lot 3). Reste au lot 3 : le prompt de veille + le test terrain.
  - **Lot 1 [FAIT - commits `f8ab1a1` `1cf6938` + suivant, npm test 780, vérifié navigateur clair + sombre]** : carte chômage au niveau département en deux colonnes (`--avec-cote`) - gauche inchangée, droite « Où se situe la Dordogne » (barre départements : Cantal 4,7 % / Pyrénées-Orientales 12,7 %, marque = le département ; barre régions : Pays de la Loire 6,6 % / Hauts-de-France 9,8 %, marque = la Nouvelle-Aquitaine ; repère plein emploi). Bloc « L'évolution du chômage » toujours visible (plus de plier/déplier), pleine largeur : trimestres à gauche (inchangé), **courbe pluriannuelle** à droite - 4 territoires (dép/région/France/UE) en bascule multi-sélection, ligne pointillée plein emploi, survol partout (ligne guide + infobulle multi-valeurs). Données réelles ajoutées au digest `2026-06.md` : `serie_annuelle` (Dordogne + France 2019-2025 INSEE ; N-A 2023-2025 ; UE 2022-2025 Eurostat) + section « Où se situer » (extrêmes INSEE T1 2026). **Les 2 infobulles existantes (survol des chiffres, survol des barres trimestrielles) préservées.** Parser étendu (`ouSeSituer`, `serieAnnuelle`, classeur reconnaît 'ue').
  - **Lot 2 [FAIT - 2026-09-09, npm test 780, vérifié navigateur clair + sombre]** : (A) phrase de synthèse « En quelques mots » en tête (section `## Synthèse courte` du digest, une phrase par territoire tirée de la « Lecture du territoire », + garde-fou fixe) ; (B) un repère sous chaque chiffre - ligne `repere:` du digest, sinon brouillon calculé (position vs moyenne française + sens sur un an) ; graphe trimestriel : tous les labels affichés (T3 25 ne manque plus) ; (C) colonnes droites des 4 autres cartes (champ `cote()` par carte : catégorie A à E + total A+B+C ; « longue durée » 1 an / 2 ans + repère national ; emploi salarié défini + contexte région ; offres « pas une embauche » + repère 4/10). Textes statiques + chiffres extraits du digest par regex, rien d'inventé. Retour Denis appliqué au passage : les deux graphes d'évolution alignés (colonnes plafonnées).
  - **Données** : le 2026-09-09, Denis a fourni les fichiers officiels (`sl_etc_2026T1.xls` INSEE, `une_rt_a` Eurostat). `serie_annuelle` de la courbe recalée dessus (N-A, France métro, UE) ; Dordogne inchangée. Ajouté : ligne `histo:` (fourchette depuis 1982, plus haut / plus bas) affichée sous le graphe des trimestres, valeurs en `.chiffre` avec infobulle de source. **Reste à la prochaine passe** : part d'offres durables (dép), variation trimestrielle emploi salarié départemental, années antérieures à 2019 si on veut allonger la courbe.
  - **Lot 3 [À FAIRE]** — améliorations au-delà de la maquette + intégration veille :
    - *Module* : (1) ~~distribution complète des ~96 départements / 13 régions sur les barres « Où se situer » - au survol, « à ce niveau : Loiret, Yonne… »~~ **[ABANDONNÉ 2026-09-10]** Denis a posé la question de la valeur ajoutée : savoir que la Dordogne est à côté de la Corrèze / la Creuse ne change rien pour une personne qui cherche un emploi dans le 24/87, ni pour un CIP (qui cite le chiffre départemental + la comparaison région/France, déjà dans le module). Coût réel (script Python + fichier de données + maintenance par trimestre + UI + tests) pour une info anecdotique. La barre « Où se situer » actuelle (min/max national + curseur du département) est **conservée**, elle a une vraie valeur garde-fou. Données INSEE T1 2026 déjà extraites si on revient dessus : Cantal 4,7 % / Pyrénées-Orientales 12,7 % / Dordogne 7,8 % / Haute-Vienne 7,4 % ; DOM 13,5 à 19,3 %. ; (2) ~~« Garder / imprimer ces chiffres » - feuille à emporter en rendez-vous conseiller~~ **[ABANDONNÉ 2026-09-10]** Denis : le public cible n'imprime pas, un CIP qui alimente un rapport fait un copier-coller. Coût (maquette + `@media print` propre + tests) non justifié par l'usage. Le bouton « Imprimer la vue d'ensemble » existant (basique, `Ctrl+P`) est **conservé** tel quel. ; (3) ~~« Où se situer » aux niveaux région / France / UE~~ **[ABANDONNÉ 2026-09-10]** Même question de valeur posée par Denis : au niveau région, la comparaison région/France est déjà affichée à côté du chiffre (la barre n'ajoute rien) ; aux niveaux France/UE, la vue dédiée « La France dans l'Union européenne » fait déjà la comparaison sur 11 indicateurs (doublon). La colonne droite reste réservée au niveau département, proprement (rien d'affiché ailleurs, pas de bug). ; (4) [FAIT 2026-09-10, `4bf2dba`] `repere:` écrits pour Haute-Vienne (5 cartes), Nouvelle-Aquitaine (4 cartes ; pas de puce offres à ce niveau) et France (chômage, demandeurs, emploi salarié). Avant : seule la Dordogne. Phrases = synthèse des chiffres déjà validés du digest, vérifiées navigateur aux 3 niveaux. ; (5) [FAIT 2026-09-10, `39124ff`] **bug signale par Denis : quand on choisissait la Haute-Vienne, le bas du module montrait encore du contenu Dordogne seul.** Corrige : « Calendrier de saisonnalite » devient commun (nuance Haute-Vienne moins saisonniere) ; « Les gros employeurs » dedouble en 2 encarts replies (Dordogne 24 / Haute-Vienne 87), celui du departement choisi en premier ; etiquette « A savoir » (plus « ecrit a la main ») ; ligne Haute-Vienne de « Metiers en tension » remplie avec les vrais chiffres BMO 2026 (10 690 projets, ~31 % saisonniers, top metiers 87 recuperes via l'outil statistiques.francetravail.org) ; « La lecture du territoire » (texte unique couvrant les deux departements) n'est plus affichee en vue par territoire (« En quelques mots » fait deja le travail), elle reste en vue d'ensemble. Part jugee difficile Haute-Vienne recuperee (52 % BMO 2026, dept 87) et integree a la ligne. « Ecrit a la main » retire partout dans le module (`f3785d0`) : pieds d'encart -> « Reperes revus chaque annee », note de la lecture -> « Texte de synthese ». Reste (autre module, non traite) : « les reperes sont ecrits a la main » dans `data/metiers.js` (module « Comprendre le cadre »), formulation de transparence voulue la-bas - a voir avec Denis si on harmonise.
    - *Veille (« 2e temps »)* : [FAIT - 2026-09-11, `2e0f981`] **prompt `[CHIFFRES]` de `outils/veille.html` etendu** : indicateur 6 « Ou se situer » (extremes departement/region, meme tableau INSEE que l'indicateur 1) + blocs `[OU-SE-SITUER]` et `[SYNTHESE-COURTE]` dans le texte assemble ; parseur (`lireChiffres` + nouvelles `parserOuSeSituer`/`parserSyntheseCourte`) + generateur (`construireChiffresMd`) mis a jour pour ecrire directement les sections `## Ou se situer : comparaison des territoires` et `## Synthese courte` dans le `.md`, au bon format (verifie contre le vrai parseur du module). Le prompt reste **un departement a la fois** (menu deroulant existant Dordogne/Haute-Vienne, a lancer 2 fois et fusionner a la main, comme avant). **Choix assume** : `repere:`, `serie_annuelle:` et `histo:` restent HORS de ce prompt (un assistant generique produit des reperes plats, et les deux autres viennent d'un fichier officiel telecharge par Denis, pas d'une recherche web) - documente dans `docs/VEILLE_PROMPTS.md` § 5ter.1, remis a jour sur le vrai texte du code (il etait perime). [FAIT - 2026-09-11, `45bb546`] **mecanisme « sites bloques detectes a l'import »** : `analyserSitesBloques()`/`rendreAnalyseSitesBloques()` reperent les lignes « valeur: non trouvee » et les adresses citees dans un texte colle, distinguent les sites deja dans `SITES_BLOQUES` des adresses nouvelles. Branche automatiquement sur l'import `[CHIFFRES]` ; bouton manuel « Reperer les sites bloques » sur Portrait et Union europeenne (pas de parseur dedie pour ces deux-la). N'ecrit jamais le fichier tout seul - invite a signaler l'adresse pour qu'elle soit ajoutee avec un commit. Le **script d'extraction du xls INSEE** n'est plus un prerequis (son seul usage, le point distribution, est abandonne) - a garder en reserve seulement si la saisie manuelle de `serie_annuelle`/`histo` devient penible.
    - *Finition* : test terrain avec une vraie personne du public.
- [FAIT - 2026-09-11, `9036e9c`] **Expliquer la reforme d'inscription automatique a France Travail (1er janvier 2025) dans la fiche methode du module ET dans le Lexique.** Demande de Denis du 2026-09-09 (question sur « en partie a cause du changement des regles d'inscription », phrase de la lecture du territoire de « Comprendre les chiffres »), notee trop tard le 2026-09-11 (voir entree du 2026-09-11 ci-dessous). Fait : (1) nouvelle section « La reforme de janvier 2025 » dans `modules/comprendre-les-chiffres/contenu/methode/taux-insee-ou-personnes-inscrites.md` (loi pour le plein emploi du 18 decembre 2023, inscription automatique des beneficiaires du RSA et de leur conjoint/concubin/pacse, contrat d'engagement, rupture de serie statistique) ; (2) nouvelle fiche Lexique `inscription-automatique-france-travail-2025` (univers emploi-recrutement, jamais de chiffre date, relations vers `rsa` / `contrat-d-engagement` / `demandeur-emploi-categories`), ajoutee a la collection « Lire les chiffres de l'emploi ». `node scripts/checkLexique.js` 0 erreur (323 fiches), `npm test` 781. Verifie : fiche trouvable par recherche (variantes testees), section rendue dans la fiche methode.
- [FAIT - 2026-09-11] **Lexique : audit complet + enrichissement de plusieurs familles, a la demande de Denis en vue de la cloture prochaine du projet.**
  - Fiches ajoutees : GEIQ (`9b21f20`, avec la variante « GIEQ »), OPCO, pension d'invalidite enrichie (critere/demarche) ; puis 4 familles (`9a3c0fa`) : formation (AREF ou RFFT, AIF, projet de transition professionnelle, POEI enrichie pour mentionner la POEC), territoire (QPV, FRR, emploi franc, collection « Comprendre les zonages du territoire »), fin de contrat (abandon de poste, CSP, ORE, arret de travail/IJ), aide a domicile (CESU) ; puis « AAH ou pension d'invalidite ? » ; puis 6 situations de vie (`836db3a`) : accident du travail/maladie professionnelle, proche aidant/AJPA, monoparentalite/ASF, conge maternite-paternite (protection de l'emploi), violences (fiche existante enrichie avec prudence : aide universelle d'urgence + demission legitime, **jamais** presente le conge specifique victime de violences conjugales comme actif car c'est une proposition de loi non encore adoptee (examen prevu octobre 2026).
  - **Correction importante** (`f73e8e7`) : la fiche `emploi-franc` presentait a tort ce dispositif comme actif ; verification faite en preparant les fiches « Comprendre le cadre » -> le dispositif a pris fin le 31 decembre 2024, non reconduit en 2025. Corrigee.
  - **Balayage complet anti-doublons** (`d081b6b`) demande par Denis apres 2 doublons trouves par hasard : 3 fiches fantomes trouvees et fusionnees au total (`abandon-poste` -> `abandon-de-poste`, `conseil-de-prud-hommes` (deja complete) <- `ore` supprimee, `offre-raisonnable-d-emploi` (deja complete) <- `ore` supprimee). Verifie : 0 id/titre en double, 0 reference cassee (relations/fichesEntree/fichesOrdonnees), 0 collection dupliquee, sur les 339 fiches.
  - `node scripts/checkLexique.js` 0 erreur/avertissement a chaque etape. `npm test` 781.
- [FAIT - 2026-09-11, `83bd77c`] **« Comprendre le cadre » : 7 fiches nouvelles trouvees absentes lors d'un balayage complet du Lexique contre le module.** Denis a demande de verifier, pour l'ensemble des themes du Lexique (pas seulement les 6 ci-dessus), s'ils sont traites quelque part dans « Comprendre le cadre ». Methode : script Node comparant les 339 fiches du Lexique au contenu reel des 15 rayons (hors `balayage/` et `methode/`), puis verification manuelle precise par recherche exacte de phrase. Resultat : sur 111 fiches Lexique de type structure/dispositif, 104 avaient deja une page dediee - corpus globalement sain. 7 sujets absents partout (meme dans les brouillons de veille), traites dans l'ordre de priorite propose :
  - rayon `emploi/` : `emploi-csp`, `emploi-abandon-de-poste`, `emploi-qpv-frr` (emploi franc explicitement present comme arrete), `emploi-cesu` ;
  - rayon `handicap/` : `handicap-pension-invalidite`, `handicap-proche-aidant` ;
  - rayon `accompagnement/` : `accompagnement-geiq` - decouverte au passage qu'une structure locale deja verifiee (`asso-geiq-24`, GEIQ du Bergeracois, verifiee le 2026-09-08) dormait dans `accompagnement.collecte.md` sans jamais avoir eu de fiche pour l'expliquer.
  - Chaque `.collecte.md` de rayon etendu avec les sources + le materiau brut correspondant. **Toutes les nouvelles sources sont dans `liens-verifies.txt` SANS DATE** (pas encore ouvertes au navigateur par Denis) : ces 7 fiches ne sont **pas encore relues/validees**, contrairement au reste de chaque rayon - chaque README de rayon liste precisement quelles sources restent a verifier avant publication (memes garde-fous que les paquets B/C).
  - Verifie : toutes les references `sources:` des 7 fiches existent dans leur collecte, tous les ids existent dans `liens-verifies.txt`, frontmatter YAML valide. `npm test` 781 (fichiers de contenu, hors perimetre Node).
  - **[VALIDE - 2026-09-11]** Denis a ouvert et verifie une par une les 14 adresses (contenu, titre, sujet) : toutes correctes et a jour. Dates reportees dans `liens-verifies.txt`, statut des 3 README de rayon (emploi/handicap/accompagnement) mis a jour de « pas encore relu » a « valide le 2026-09-11 ». Les 7 fiches sont maintenant au meme niveau de validation que le reste de chaque rayon.
- [FAIT - 2026-09-11, `369dd09`] **Lexique : nouvelle fiche ACRE (aide a la creation ou reprise d'entreprise).** Trouvee lors d'une nouvelle passe de balayage demandee par Denis (« d'autres termes qui pourront beneficier a des personnes en recherche d'informations »). Fiche univers `entrepreneuriat-independance`, **jamais de montant chiffre** (doctrine du Lexique) : explique le mecanisme (exoneration de cotisations sur 12 mois) et **le changement reel de 2026** (l'ACRE n'est plus automatique, reservee a certains publics, demande sous 45 jours aupres de l'Urssaf). Reliee a `auto-entrepreneur` (reciproque) et `qpv`. `node scripts/checkLexique.js` 0 erreur (340 fiches, 37 collections). `npm test` 781.
- [FAIT - 2026-09-12, `e212ed9`] **« Comprendre les chiffres » : series annuelles dans Croiser plusieurs chiffres (point a) + infobulle de source generalisee aux encarts lateraux.** Denis, en testant le module : « Croiser plusieurs chiffres... ce n'est pas veritablement croiser, c'est un doublon de ce qu'on a plus haut » - diagnostic : 3 des 7 series prevues (longue duree, demandeurs, offres) sont desactivees faute de donnees de veille, seul le chomage trimestriel restait utilisable. **Point (a) approuve par Denis** (« Oui, fais l'ACRE et le point (a) ») : le taux de chomage annuel (`serieAnnuelle`, 2019-2025, deja present au digest mais jamais exploite ici) rejoint les series croisables pour les 4 territoires, avec un nouveau champ `periode` (trimestre/annee) qui empeche le melange avec le trimestriel, sur le meme principe que le garde-fou %/nb existant. **Point (b)** (remplir les 3 series encore vides) reste hors de portee sans nouveau passage de veille, reporte tel quel.
  - **Infobulle de source generalisee** : en testant, Denis a signale un par un 5 encarts lateraux dont les chiffres n'avaient pas l'infobulle de source deja presente sur les 5 chiffres cles (« Ou se situe la Dordogne », « Comprendre les categories », « Ce que "longue duree" veut dire », « Ce que ce chiffre compte », « Lire ce chiffre »). Meme mecanisme deja en place (classe `.chiffre`, la source est retrouvee automatiquement dans le bloc parent) applique aux 5, aucune nouvelle donnee necessaire.
  - `npm test` 781 verts. Verifie navigateur clair + sombre (survol de chaque encart, bascule theme, garde-fou trimestre/annee testes ensemble et separement).
  - **[FAIT - 2026-09-12, `006cb28`] Suite demandee par Denis : « est-ce que d'autres chiffres apporteraient plus de clarte ? si oui, lance-toi. »** Avis rendu avant d'agir : la part d'offres durables a une vraie valeur (deja citee en repere fixe, nuance utile sur la qualite des offres) ; le taux d'emploi/activite non (redondant avec le chomage deja affiche, doublon du meme genre que celui denonce par Denis, mieux a sa place dans le Portrait du territoire s'il est ajoute un jour). Sur la part d'offres durables, **option 1 lancee** (demarrer le suivi maintenant) puis **option 2 cherchee** (un historique tout fait, comme le fichier INSEE fourni par Denis pour le chomage) : aucune source avec historique trouvee (WebFetch sur les pages Data Emploi 24/87) - seul le trimestre en cours est publie. Digest restructure en 2 entrees departementales reelles (Dordogne 39 %, Haute-Vienne 41 %, jusque-la presentees a tort comme identiques) avec une ligne `serie:` demarree ce trimestre ; `VEILLE_PROMPTS.md` + le prompt de `veille.html` avertissent que chaque futur trimestre doit reporter cette serie avant d'ajouter son point, sinon elle repart de zero. Pas encore assez de points pour rejoindre Croiser (il en faut au moins 2). Corrige au passage un defaut existant : le repere « X embauches sur 10 » s'affichait a tort a tous les niveaux de lecture (region/France) alors qu'il n'existe qu'au departement.
  - **Idees ecartees pour Croiser, sur question de Denis** (population, vieillissement, niveau de formation, natalite) : deja couvertes dans « Le portrait du territoire » (`portrait-2026.md`, sections « Population et densite », « L'age de la population », « Le niveau de formation »), en repere annuel structurel - les dupliquer dans Croiser recreerait le meme doublon que celui signale au depart, ces donnees bougeant trop lentement pour justifier un suivi trimestriel/annuel comparatif.
  - **[FAIT - 2026-09-12, `84e6384`] Remarque de Denis sur la courbe pluriannuelle** : le curseur en croix laissait croire a une lecture continue et precise, alors que le survol se cale seulement sur l'annee la plus proche (seul point reellement lu). Curseur passe en pointeur (main), coherent avec le reste des elements survolables du module. Verifie (`getComputedStyle`), `npm test` 781 verts.
  - **[FAIT - 2026-09-12, `63502c1` + `e59e00d`] Emploi salarie (priorite demandee par Denis parmi les 3 chiffres en reserve).** Contrairement aux offres durables, l'INSEE publie un vrai historique - reparti sur 5 bulletins trimestriels distincts (« Informations rapides », un par trimestre) plutot qu'un seul fichier telechargeable. Les 5 (T1 2025 a T1 2026) ouverts un par un et recoupes avec les valeurs region/France deja publiees dans le digest (coherentes). **2 corrections trouvees en le faisant** : (1) au niveau departement, la carte montrait un decompte absolu perime (125 900 emplois, 2022) au lieu de la variation trimestrielle attendue par la carte (`valeurEstVariation`) - phrase generee incoherente ("...a evolue de 125 900 emplois salaries"), corrigee en alignant departement sur region/France (vraie variation INSEE, meme source) ; (2) bug trouve en croisant chomage (4 points) + emploi salarie (5 points) dans Croiser : le graphe combine plaçait les labels par ordre de premiere apparition au lieu d'un ordre chronologique, le trimestre en plus d'une serie plus longue atterrissait a tort en fin d'axe - corrige par un vrai tri chronologique. 4 nouvelles cases actives dans Croiser (emploi salarie par trimestre, un territoire par case). `VEILLE_PROMPTS.md` + `veille.html` mis a jour (variation sur les 4 territoires, report de serie d'un trimestre a l'autre). `npm test` 781 verts, verifie navigateur (chiffre correct aux 2 departements, graphe combine avec axe et infobulle corrects).
  - **[FAIT - 2026-09-12, `9e01120` + `465b3bf`] Catégorie A (demandeurs) et longue durée.** Même démarche que l'emploi salarié, résultat différent : aucun historique lisible trouvé (Data Emploi ne donne que le trimestre en cours ; un jeu de données data.gouv.fr existe peut-être mais pas encore vérifié - signalé à Denis dans `VEILLE_PROMPTS.md`/`veille.html` s'il veut le télécharger et regarder). Démarrées comme les offres durables : point du trimestre en cours ajouté (`serie:`) pour les 4 territoires (départements, Nouvelle-Aquitaine, France). Au passage : la France entière pour la longue durée existait déjà dans le digest (46,0 %, DREETS) mais noyée dans le texte de la Nouvelle-Aquitaine au lieu d'être sa propre entrée - sortie en entrée à part entière. **Point signalé, pas corrigé** : les entrées Nouvelle-Aquitaine/France de la catégorie A restent en % de variation alors que le département affiche un décompte absolu (`unite: 'nb'` de la carte) - même famille de défaut que celui corrigé sur l'emploi salarié, mais pas traité ici (périmètre plus large, à reprendre séparément si besoin).
  - **[FAIT - 2026-09-12, `9e01120`] Croiser plusieurs chiffres : bascule "Par trimestre" / "Par année" (demande de Denis).** Au lieu d'un texte expliquant de ne pas mélanger trimestre et année, séparation physique en 2 lectures : on choisit d'abord, seule la lecture choisie affiche ses cases à cocher. Changer de lecture décoche automatiquement le groupe qu'on quitte, rafraîchit le graphique sans re-rendre toute la page (garde le repli/déplié du bloc et le défilement). Reprend le patron déjà utilisé pour "Vue simple/Vue d'ensemble". Le garde-fou trimestre/année dans le code devient inutile (impossible de cocher les deux) et est retiré ; le garde-fou %/nombre reste. `npm test` 781 verts, vérifié navigateur (bascule dans les deux sens, cases décochées au changement, graphique correct).
- [FAIT - 2026-09-12, `04bad17` + `a8a5e5f`] **« Le patron aussi a ses difficultes » - le 3e regard (beneficiaire, professionnel, employeur), construit sur 2 fronts.** Idee de Denis (par curiosite, en discutant de Croiser), confirmee et etendue ensuite (« ce n'est pas accroitre le projet, c'est lui donner la place qu'initialement on devrait avoir »).
  - **Lexique (`04bad17`)** : 3 fiches a partir de la matiere preparee (`docs/MATIERE_LEXIQUE_DIFFICULTES_EMPLOYEUR_2026-09-12.md`) - `obligations-employeur-embauche`, `periode-essai-cote-employeur` (complement a `periode-essai`, pas un doublon), `licenciement-procedure-employeur` (complement a `licenciement`). La 4e fiche prevue (cout d'un salarie) devient un enrichissement de `cout-employeur` plutot qu'une fiche a part, comme recommande dans la matiere. Rattachees a la collection existante « Comprendre le monde de l'entreprise ». `checkLexique` 0 erreur (343 fiches).
  - **Comprendre le cadre (`a8a5e5f`)** : nouveau rayon « Employeurs et entrepreneurs » (16e rayon), 7 fiches plus completes que le Lexique (a qui s'adresser, ce qui change souvent) - les memes 3 sujets approfondis, plus le cout reel d'un salarie (reduction generale des cotisations, sans chiffrer le seuil qui a deja bouge en 2025), devenir employeur pour la premiere fois (Tese), les aides financieres pour recruter, les obligations de securite et de formation (DUERP). Rattache a la famille « Emploi et parcours ». Enregistre partout ou un rayon doit l'etre (RAYON_LABEL, questions d'« Affiner ma recherche », familles, case de l'orienteur). **Point signale a Denis, pas tranche** : la case de l'orienteur (« Je veux comprendre le point de vue d'un employeur ») est une premiere - l'orienteur parle normalement a la 1re personne du beneficiaire, jamais utilise pour ce genre de rayon avant ; a valider ou reformuler.
  - **Ton verifie sur les deux fronts** : decrit des mecanismes juridiques et economiques, ne conclut jamais sur le comportement attendu d'une personne en recherche d'emploi.
  - `npm test` 781 verts. Verifie navigateur clair + sombre : rayon visible, 7 fiches chargees, « Pres de chez moi » filtre par territoire (2 structures de sante au travail trouvees et verifiees par recherche web, une par departement).
  - **[FAIT - meme jour, `78edd3a`] Verification de Denis faite** : les 17 lignes (10 sources + 7 structures) verifiees et datees. Point releve par Denis : `spst-19-24-perigueux` declenche un avertissement « site non securise » (probablement un certificat SSL mal configure cote site), contenu confirme correct - note ajoutee dans le README du rayon pour ne pas surprendre plus tard. Corrige au passage, signale par Denis en testant : le lien texte « ouvrir l'annuaire local » de l'ecran « Pres de chez moi » (partage par tous les rayons) devient un vrai bouton.
  - **[FAIT - meme jour, 3 fiches] 1er tour de relecture de couverture demande par Denis** (« est-ce qu'on a couvert tout ce qu'un employeur doit connaitre et respecter ? ») : 3 manques confirmes et combles - le suivi medical au-dela de l'embauche (VIP renouvelee, visite de reprise), la complementaire sante obligatoire depuis 2016 (avec la nuance prevoyance-cadres-seulement, pour ne pas presenter une obligation universelle qui n'existe pas), et les seuils de representation du personnel (CSE a 11 salaries, delegue syndical a 50). Rayon passe de 7 a 10 fiches.
  - **[FAIT - meme jour, 2 fiches] 2e tour de relecture demande par Denis** (OETH/Agefiph ? egalite femmes-hommes ? clause sociale BTP ?) : verifie avant de coder, pas de doublon a l'aveugle - l'OETH etait deja couverte avec l'angle employeur explicite dans `handicap-obligation-emploi-lourdeur` (rayon Handicap), simple renvoi ajoute, pas de nouvelle fiche. 2 vrais manques combles : la clause sociale cote entreprise titulaire d'un marche public (deja couverte cote beneficiaire, jamais cote obligation contractuelle de qui gagne le marche - sources reutilisees a l'identique, deja verifiees) et l'index de l'egalite professionnelle (aucune couverture nulle part, obligatoire a partir de 50 salaries). Rayon passe de 10 a 12 fiches.
  - **[FAIT - meme jour, `63035fb`]** Les 5 dernieres lignes verifiees et datees par Denis. Les 22 lignes du rayon sont maintenant toutes datees, rayon considere relu et valide.
  - **[FAIT - 2026-09-12, `96dd280`] Volet "Comprendre les chiffres" de la demande employeur, fait apres coup** : Denis a remarque que 2 taches annoncees (les indicateurs creations/defaillances par secteur, le prompt de veille employeur unifie) avaient ete notees mais pas faites pendant que Claude enchainait sur les 2 tours de relecture du rayon Comprendre le cadre - signale honnetement, puis traite. Creations et defaillances d'entreprises par secteur ajoutees dans « Le portrait du territoire » (section "Le territoire bouge-t-il", deja prevue mais pauvre - total de creations seul, aucun detail sectoriel) : creations 2025 par secteur pour les 4 territoires (source INSEE dediee), defaillances 2025 pour la Nouvelle-Aquitaine (total, evolution, secteurs les plus touches) - **premiere fois que les defaillances apparaissent dans le module**. Detail departemental des defaillances non trouve avec une source officielle fiable (seulement des reprises de presse locale, ecartees) : point de prudence explicite dans le texte. Reste dans le portrait annuel (pas dans Croiser), conforme a la reco deja discutee : un detail par secteur n'a pas de serie temporelle exploitable. `npm test` 781 verts, verifie navigateur.
  - **[FAIT - 2026-09-12, `cc3a130`]** Prompt de collecte dedie « Employeurs et entrepreneurs » ajoute dans `outils/veille.html` (3 selecteurs de rayon + dictionnaire RAYONS partage), couvrant a la fois les dispositifs/textes de loi et signalant les 2 sources chiffrees a suivre. **Trouvaille au passage** : le BODACC (open data des annonces commerciales, teste en reel via son API) comble le trou departemental des defaillances signale plus haut - ajoute au portrait avec un point de prudence (notion d'annonce, pas de defaillance statistique ; aucun detail sectoriel). Rien ne reste a faire sur ce fil.
- [CLOS - 2026-09-09 (`f5e3582`)] **Boucle Retour : « Comprendre le cadre » et « Comprendre les chiffres ».** Le balayage du 2026-09-09 (7 modules, voir `LECONS` section 2) avait faussement listé ces deux-là comme déjà corrects. Ils bouclaient : depuis l'accueil du module, « Retour » de la barre du bas ouvrait la présentation EN DÉTOUR, qui affiche « Revenir au module », qui rouvre l'accueil, etc. Correctif : `_comprendreLeCadreRetour()` / `_comprendreLesChiffresRetour()` (détour = false) sur la barre du bas ; la présentation en mode normal sort vers la carte « Se tenir informé ». Vérifié navigateur (accueil → Retour → présentation normale → Retour → accueil de l'appli ; détour « Revoir la présentation » → « Revenir au module » revient à l'écran interne quitté). `LECONS` section 2 corrigée (9 modules). `npm test` 780.
- [CLOS - 2026-09-09] **P1 de l'audit des premières pages : écrans-portes de « Comprendre le cadre » et « Comprendre les chiffres ».** Les deux notés 11/20 (`docs/AUDIT_PREMIERES_PAGES_MODULES_2026-09-09.md`) : à froid, un encart « indiquez votre territoire/département » + un bouton + un grand vide, aucun aperçu du contenu. Décision Denis : « je veux qu'on voie le contenu, mais qu'il soit inaccessible tant que je n'ai pas choisi le territoire ; dès que je l'ai choisi, tout ça se débloque ». Fait : sous l'appel à l'action, un aperçu verrouillé (`.comprendre-le-cadre-apercu-verrou` / `.comprendre-les-chiffres-apercu-verrou`) qui ré-affiche le vrai contenu — pour le cadre : les 4 familles de rayons + les outils ; pour les chiffres : la liste des questions traitées (5 chiffres clés + portrait annuel + enquête employeurs + question en direct), jamais les nombres, qui dépendent du département. Contenu interne `inert` + `pointer-events: none`, tout le bloc cliquable (clic / Entrée / Espace) rouvre `demanderDepartementSiInconnu`. Le choix fait, la porte disparaît et l'accueil réel s'affiche, inchangé. Commits `df6cc31` (cadre) + `e406c2f` (chiffres). `npm test` 780. Vérifié navigateur clair + sombre, clic-pour-débloquer, déblocage effectif. `ARCHITECTURE_TECHNIQUE.md` des deux modules à jour.
- [À FAIRE - **corrigé 2026-09-11 : en réalité ~99 % fait**, pas 75 %. Cahier : `docs/CHANTIER_L1_CO_LETTRE_MOTIVATION.md` (contrat figé, Mode A). Vérifié étape par étape contre le cahier : les 12 étapes sont FAIT sauf la 12e, où il ne reste qu'**un seul point, et il est explicitement `[À FAIRE, Denis]`** (tester le prompt sur 3-4 vrais CV) - rien à coder de mon côté sur ce chantier.] **Refonte du prompt L1 « Co-construire ma lettre de motivation »** (nouveau prompt `prompts/lettre-co.md` qui reçoit `texteProfil('lettre')` + la stratégie CV, comme L2/E1 ; suppression du « Start », des notes /10, du débordement sur l'entretien ; sortie = 1 version longue 1 page A4 + 1 version courte pour corps de mail).
  - **[FAIT] Étapes 1-8 + 11 (2026-09-07, commits `6069dee` / `a3398d6` / `bfbbb3b` / `7a0e000` / `5659cdd`, + `10235d7` pour `data/metiers.js`)** : contrat figé + maquette (`docs/MAQUETTE_L1_CO_LETTRE_ECRANS_2026-09-07.html`), prompt `lettre-co.md` écrit + enregistré + branché sur l'écran « Choisir mon assistant » (jeton `{CONSIGNE_TRANSMISSION_CV}`), écran 2 devenu page dépliante 2 blocs (CV + « L'entreprise et le poste » via `bilanCorpsCiblageOffreHTML()` + `contenuCiviliteRecruteurCandidature()`), écran 4 avec bouton « Copier la version courte », bug d'import `versions[0]` corrigé, `analyserReponseIALettre` capte `texteCourt`. `lettre-v1` retiré de `FICHIERS_PROMPTS_EXTERNES` (plus chargé).
  - **[PARTIELLEMENT FAIT] Étape 9** :
    - **[FAIT - 2026-09-10, commit `2409a72`, vérifié navigateur clair + sombre, `npm test` 780]** trou de confidentialité corrigé : bloc « Relire, vérifier, corriger, masquer » (obligatoire) inséré comme bloc 2 de l'écran 2 (entre « Votre CV » et « L'entreprise et le poste », renumérotée 3), sur le modèle de `_prepLE` (drapeau `_coLettreCvRelu`, brique `bilanDemanderRelectureCv`). Le CV collé en texte reste sur la page, le CTA « Choisir mon assistant » est bloqué tant que la relecture / masquage n'est pas faite. Chemin fichier / image inchangé (déjà masqué par la modale). Avant : le CV collé partait brut à l'assistant via « Copier mon CV » de l'écran 4.
    - **[FAIT - 2026-09-10, commit `cd96502`, vérifié navigateur, `npm test` 780]** complémentarité L1 / L2 / entretien portée par les **tuiles et infobulles de l'accueil** (option A, décision Denis) - jamais sur les pages d'introduction (une intro ne parle que de son module). Chaque libellé décrit son module : « Co-construire ma lettre » = « … phrase par phrase, **en prenant le temps** » ; « Préparer ma lettre et mon entretien » = « … **directement** comme base … **rapidement** ». Le §5 d'origine du cahier (renvoi « voir Préparer ma lettre et mon entretien » sur l'intro L1) est **abandonné**.
    - **[FAIT - 2026-09-10, commit `c1e46df`, vérifié navigateur]** retrait des 2 renvois inter-modules des pages d'introduction (RÈGLE ABSOLUE : une intro ne parle que de son module). Intro « Préparer ma lettre et mon entretien » (`data/metiers.js` ~3822) : l'encart nommant « Co-construire ma lettre » / « Préparer un entretien » de l'accueil devient une description de ce mode seul (« c'est le chemin rapide »). Intro « Découvrir mes compétences » (`~7209`) : la puce « Enchaîner sur « Co-construire ma lettre… » » devient une capacité générique (« vous servir de ce CV comme base pour votre lettre… »).
    - **[FAIT - 2026-09-10, commit `a31b4dc`, vérifié navigateur]** retrait des 5 renvois dissuasifs restants (décision Denis : cohérence stricte, une intro ne nomme jamais un autre module). `data/metiers.js` ~4695-4696 (Reformuler CV : renvois vers « Analyser ma candidature » / « Créer un nouveau CV » / « Mettre à jour mon CV »), ~7204 (Découverte), ~7283 + ~7300 (ATS). Chaque phrase reformulée pour rester sur le module lui-même. **Plus aucun renvoi inter-modules dans les pages d'introduction.** Seule exception laissée : `_introBlocComplementariteChiffres()` (~7473), rectangle « Comprendre le cadre + Comprendre les chiffres vont ensemble » - décision Denis D16 (2026-09-06), même famille « Se tenir informé », sans lien de renvoi.
    - **Étape 9 : TERMINÉE.**
  - **[FAIT - Étape 10 TERMINÉE]** :
    - **[commit `c31a4e0`]** bloc « Vos coordonnées » sur l'écran 4, AVANT la zone de collage. Rappel lecture seule de `dossier.identite` ; si nom / téléphone / courriel manquent → encart + bouton « Compléter mes coordonnées » (non bloquant, le DOCX se génère quand même). Réutilise `bilanOuvrirFenetreCoordonnees()`, qui gagne un paramètre optionnel `onEnregistre` (défaut inchangé). Re-rendu en place du seul bloc après enregistrement.
    - **[commit `0526f50`]** bouton « Aperçu / Imprimer » de la lettre (entre DOCX et « version courte »). `_coLettreLettreImprimableHtml()` (en-tête depuis `dossier.identite` + lieu/date + objet + corps) rendu dans un iframe `srcdoc`, panneau plein écran Imprimer / Fermer (patron `bilanOuvrirApercuFicheSynthese`). Au clic : réutilise `dossier.ia.lettre` si importée, sinon parse la zone de collage, sinon message. Vérifié navigateur clair + sombre, `npm test` 780.
  - **[Étape 12, PARTIELLEMENT FAIT]** :
    - **[FAIT - 2026-09-10, non-régression du chrome vérifiée au navigateur, clair + sombre, 0 erreur console]** §3quinquies : bande « Revoir la présentation » sur les écrans 2/3/4 ; barre du bas Retour + Accueil sur chaque écran de travail ; chaîne Retour RC-03 (écran 4 → 3 → 2 → intro normale), non destructive (`_coLettreDocument` conservé à chaque cran) ; intro normale → Retour → panneau « Me préparer à candidater » (`carteRetour: 'preparer'`) ; détour « Revoir la présentation » → « Revenir au module » → retour à l'écran quitté ; encart « Continuer / Recommencer » + synthèse sur l'intro quand une lettre existe, avec confirmation sur « Recommencer ». Les ajouts des étapes 9 (bloc relecture) et 10 (bloc coordonnées + Aperçu/Imprimer) n'affectent pas le chrome. **Point mineur constaté, pré-existant, non traité** : le « Réinitialiser » global (`recommencer()`) ne remet pas à zéro les variables module `_coLettre*` (`_coLettreDocument` / `_coLettreEcran` / `_coLettreCvRelu` / `_coLettreDetour`) - `_coLettreRecommencer()` (bouton du module) le fait.
    - **[À FAIRE, Denis]** test du nouveau prompt sur 3-4 vrais CV (profil riche / maigre / reconversion / premier emploi).
    - **[FAIT 2026-09-11, confirmation Denis]** `prompts/lettre-v1.md` supprimé (orphelin depuis l'étape 11).
- [À FAIRE - NOTÉ le 2026-09-04, pas encore cadré, PROCHAIN chantier après RC-03 (cas restants) et le plan d'élimination des fenêtres de dépôt CV] **Coécrire ma lettre de motivation et l'ancienne carte « J'ai un CV » (aujourd'hui « Préparer ma lettre et mon entretien », mode `pret`) : à regarder ensemble.** Denis les rapproche parce que les deux permettent d'écrire/retravailler une lettre de motivation - il veut notamment comparer **les questions posées** dans chacun (cohérence, doublons éventuels, ce qui devrait être unifié). Pas encore de détail donné - Denis compte creuser lui-même avant la prochaine session. **Ordre de passage explicite donné par Denis** : ce chantier vient APRÈS avoir terminé ce qui est en cours (les 2 cas RC-03 restants à reproduire, et le plan de suppression des fenêtres de dépôt CV en faveur d'une page, en cours d'audit). Ne pas démarrer avant, ne pas le confondre avec la dette B.1 "fenêtre d'import CV" (sujet voisin - modalité de dépôt - mais celui-ci porte sur les questions/le contenu, pas la fenêtre). **[Vérifié 2026-09-11]** Les 2 conditions de départ sont remplies : le plan de suppression des fenêtres de dépôt CV est CLOS (les 3 modules terminés le 2026-09-04/07) et les 2 cas RC-03 restants ont été rejoués en direct sans qu'aucune anomalie ne se reproduise (CLOS le 2026-09-04). **Ce chantier est donc débloqué**, mais Denis a dit vouloir d'abord creuser lui-même la comparaison des questions posées par les 2 parcours avant qu'on en parle ensemble - toujours son analyse à faire, pas une tâche de code prête.

## Maquette "Porte d'entrée" VALIDÉE le 2026-08-27 - prochaine étape : implémentation dans le vrai code

**Maquette entièrement validée par Denis** ("elle est enfin exactement comme je la voulais"). Copie finale sauvegardée dans le dépôt pour continuité inter-sessions/inter-comptes : [`docs/MAQUETTE_ACCUEIL_PORTE_ENTREE_VALIDEE_2026-08-27.html`](MAQUETTE_ACCUEIL_PORTE_ENTREE_VALIDEE_2026-08-27.html) (contrairement à `MAQUETTE_ACCUEIL_BOUSSOLE_2026-08-27.html`, qui reste un premier jet rejeté - celle-ci est la version définitive à reprendre).

**Contexte budget (2026-08-27)** : Denis a déjà consommé ~82% de son quota hebdomadaire Claude au moment de la validation. Décision prise : ne pas se précipiter sur l'implémentation dans le vrai code cette semaine - trop risqué avec un budget aussi réduit sur une page aussi visible (l'accueil). Reprendre l'implémentation avec un budget frais, en blocs testés un par un, jamais tout d'un coup.

**Exigence explicite de Denis, à respecter impérativement pendant l'implémentation : ZÉRO RÉGRESSION.** Contexte donné : une maquette précédente (autre session/conversation) avait été appliquée au vrai code à 80% sans problème, mais les 20% restants avaient été "la croix et la bannière" (très pénible, plein d'accrocs) - détail exact de ce qui avait mal tourné non retrouvé à ce stade, mais la leçon est claire : une intégration mockup → vrai code qui commence facilement peut mal finir. **Méthode retenue pour l'implémentation, à suivre strictement** :
1. Un bloc à la fois, jamais un gros pavé de changements d'un coup - chaque bloc implémenté, testé EN DIRECT sur le vrai code (pas seulement une copie isolée), puis commité avant de passer au suivant.
2. Avant de toucher/renommer/déplacer un élément existant de l'accueil (id, classe, fonction), vérifier par grep qu'il n'est référencé nulle part ailleurs dans le dépôt (autre JS, CSS, suivi Umami) - un id renommé "à l'aveugle" est une source classique de régression silencieuse.
3. La barre de recherche doit être reconnectée au VRAI moteur de recherche existant (`rechercherBaseConnaissances`, `rechercherMetiersDepuisTexte`, `rendreResultatsRechercheERIP`, `js/app.js`) - la maquette n'a que 2 entrées de démonstration (Boulanger, CDI), jamais les vraies données. À traiter comme un bloc à part, testé avec plusieurs vraies recherches avant/après.
4. Checklist de non-régression à la fin de CHAQUE bloc, pas seulement à la toute fin : mode sombre, export/import (disquette), suivi Umami, les 3 parcours CV (voir point navigation ci-dessous), Carnet, Repères/Journal de parcours.
5. Pour les modules pas encore construits (Ressources, Veille informationnelle, etc.) : carte présente et cliquable dans la nouvelle structure à 6 cartes, mais son contenu affiche simplement "en développement" - déjà prototypé dans la maquette (`ouvrirEnConstruction()`), aucune nouvelle idée à inventer pour ce point. **Précision de Denis (2026-08-27)** : rester volontairement simple pour l'instant, ne PAS construire maintenant la vraie "page d'introduction de module" déjà existante ailleurs dans l'app (voir mémoire personnelle `project_page_introduction_modules` - implémentée sur Cohérence transversale) - Denis la fera pour ces modules manquants plus tard, quand il aura davantage de budget d'utilisation. Confirmé aussi : pour les 3 parcours CV déjà réels, le clic sur une sous-carte ("Créer un nouveau CV"...) lance DIRECTEMENT le vrai parcours, sans aucune page intermédiaire - déjà le cas, rien à changer.

**Question de la barre de navigation (`barreNavigation()`, `.barre-navigation-fixe`, `css/style.css:1117`, `js/app.js:3391`), clarifiée le 2026-08-27** : c'est un composant déjà existant en production (précédent/suivant), déjà utilisé dans de nombreux parcours de l'app dont les 3 parcours CV ("Créer un nouveau CV" / "J'ai déjà un CV" / "Mettre à jour mon CV"). Denis veut qu'elle reste impérativement présente sur ces 3 parcours. **Vérifié : rien à construire ici** - la nouvelle page d'accueil ne fait que réutiliser le lanceur déjà existant de "Mes documents" (`ouvrirFenetreERIP` + sous-cartes déjà réelles) vers ces mêmes parcours déjà câblés ; tant que ce lanceur n'est pas reconstruit from scratch, cette barre continue de s'afficher automatiquement, sans action supplémentaire. Cas à part confirmé par Denis : le parcours "Découvrir mes compétences" amène sur la page Action, qui a déjà sa propre barre de navigation interne - cette page ne doit PAS recevoir la nouvelle barre, hors sujet ici.
- [CLOS] Accents manquants sur toute la maquette "Porte d'entrée" (signalé le 2026-08-27 par Denis, gravité soulignée explicitement). Cause : un réflexe de prudence valable pour un seul cas technique (corruption de tracés SVG via un outil) s'était étendu par erreur à tout le texte visible de la maquette, écrit entièrement en ASCII sans accents - y compris des phrases pourtant recopiées mot pour mot depuis le vrai texte déjà validé de l'application. Corrigé intégralement (titres, cartes, panneaux, messages de recherche, commentaires de code). Règle verrouillée en tête du fichier des leçons, section **0bis** de `docs/LECONS_A_NE_PAS_REPRODUIRE.md` : français impeccable, tous les accents, à vérifier systématiquement sur tout écrit/édition/création à partir de maintenant.
- [CLOS] Comportement de la barre de recherche à corriger (signalé le 2026-08-27 par Denis, captures d'écran à l'appui, comparé à la vraie barre de production) : (1) aucun contour bleu visible au clic/à la saisie - corrigé avec `.recherche:focus-within` (bordure + léger halo bleu, dès que le champ à l'intérieur reçoit le focus) ; (2) la barre grossissait légèrement dès la première lettre tapée - cause : la croix d'effacement passait de `display:none` à `display:flex` au premier caractère, ce qui changeait la hauteur calculée de la ligne flex. Corrigé en gardant la croix toujours présente dans la mise en page (`display:flex` fixe) et en ne faisant varier que sa visibilité (`visibility`/`opacity`), plus une hauteur explicite fixée sur la barre. Testé en direct : hauteur strictement identique avant/après frappe (49,6px les deux fois).

## Idées en réserve, PAS des tâches validées (à ne coder sur aucune d'elles sans validation explicite de Denis)

**Voir aussi `docs/IDEES_A_RECLASSER.md`** (créé le 2026-08-27) : une idée proposée pendant la discussion d'un module, mais qui ne convient finalement pas à ce module, y est notée avec son contexte d'origine et une recommandation de meilleure destination - différent de la présente section, qui liste des idées déjà correctement rattachées à leur futur module.

- Repères théoriques de développement personnel (Maslow, cycle de Hudson, masques sociaux...) pour le module Lexique - idée retrouvée le 2026-08-24, terrain sensible (jamais présenter une théorie comme s'appliquant à la personne), aucune conception commencée. Détail complet : `docs/ETAT_DES_CHANTIERS_2026-08-24.md`, partie 7.
- Amélioration des vidéos/écran tampon (IA externe) - pistes en réserve, pas un chantier actif.
- OCR image pour l'import de CV - écarté pour l'instant (qualité insuffisante vs IA), à réévaluer une fois l'app stabilisée.

---

## Chantier "rapport en accordéons" (module Regard extérieur / Repères)

**STATUT : TERMINÉ, déclaré clos par Denis le 2026-08-27.** Toutes les tâches ci-dessous sont `[CLOS]`, testées de bout en bout, 599/599 tests automatiques verts. Les points encore ouverts sont listés séparément dans "Reste à faire", ce sont des chantiers À PART, pas des tâches inachevées de celui-ci.

- [CLOS] (2026-08-26) 17 codes de freins détectés dans les deux prompts (`freinsIdentifies`/`freinsIdentifiesDelta`).
- [CLOS] (2026-08-26) Champ `questionsConcretes` (questions factuelles à poser à un professionnel/une IA) dans les deux prompts.
- [CLOS] (2026-08-26) Rapport en accordéons — Vue d'ensemble : "Ce qui ressort de vos Repères", "Ce qui vous freine", "De quoi parler avec votre conseiller(ère)", "Pistes de ressources à explorer", "Questions à poser", "Qui voir".
- [CLOS] (2026-08-26) Vue par catégorie regroupée par Repère (accordéon par catégorie, seuil de 2 Repères minimum, pastille "Synthèse").
- [CLOS] (2026-08-26) Vue par Repère : un accordéon par Repère, icône de sa catégorie devant le titre.
- [CLOS] (2026-08-26) Boutons "Garder comme Repère" (Question/Idée/Approfondir/Discuter) sur les rubriques de la vue d'ensemble.
- [CLOS] (2026-08-26) Icônes des 3 onglets (Vue d'ensemble/par catégorie/par Repère).
- [CLOS] (2026-08-26) Barre d'onglets fixe restaurée dans "Mes rapports" (comme la maquette) + bug de scope multi-cartes corrigé.
- [CLOS] (2026-08-26) "Ce qui vous freine" clarifie R1/R2 en vrais titres (bug de rendu corrigé).
- [CLOS] (2026-08-26) Freins affichés pour les rapports "Aller plus loin" (lisaient le mauvais champ).
- [CLOS] (2026-08-27) Question systématique d'accompagnement ("êtes-vous accompagné, par qui, quel statut") : ajoutée au flux de questions, ET le garde-fou qui bloquait tout le parcours quand l'IA renvoyait 0 question par catégorie a été supprimé, ET le bouton "Aller plus loin" caché dans la fenêtre "Voir mon analyse" est désormais toujours visible. Vérifié de bout en bout via un vrai clic sur le vrai bouton.
- [CLOS] (2026-08-26) Prompt : URL obligatoire dès qu'un site précis de la liste fermée est nommé (les deux prompts).
- [CLOS] (2026-08-26) Prompt : hiérarchie à 3 niveaux inspirée de Maslow pour les freins - un besoin vital/de stabilité ne peut plus jamais être décrit comme "sans lien avec le parcours d'emploi" (les deux prompts).
- [CLOS] (2026-08-26) Prompt : règle "À qui tu écris" - la personne reste le sujet de chaque phrase, jamais ses Repères comparés entre eux (les deux prompts).
- [CLOS] (2026-08-26) Prompt : exemple JSON de `questionsApprofondissement` corrigé (biais qui laissait croire que 3 catégories sur 4 sont normalement vides).

- [CLOS] (2026-08-27) Prompt : consigne de vigilance avant de conclure à zéro question par catégorie (relire chaque Repère un par un, jamais un arrêt précoce) - sans forcer de question creuse.
- [CLOS] (2026-08-27) Architecture finale des questions "Aller plus loin" : remplace le 3e filet par Repère (jugé hors-sujet par Denis - "on s'en fout des Repères") par 5 questions fixes (accompagnement/statut, freins prioritaires, métier-secteur, urgence immédiate, sujet non évoqué), TOUJOURS posées en premier, ADDITIVES avec les questions de l'IA (jamais un filet de repli conditionnel) - compteur "Question X sur Y" qui s'adapte automatiquement. Question accompagnement enrichie avec exemples de structures (France Travail, Mission Locale, Cap Emploi, PLIE) et de statuts (demandeur d'emploi, reconversion, étudiant, alternance, stagiaire). Vérifié de bout en bout (0 question IA -> 5 ; 2 questions IA -> 7).
- [CLOS] (2026-08-27) Personnalisation de "Qui voir" pour un rapport "Aller plus loin" : nouveau champ `quiVoirPersonnalise` dans le 2e prompt, rempli uniquement si une structure de suivi est nommée noir sur blanc dans les réponses, jamais deviné - sinon le texte générique (3 cas) reste affiché. Jamais lu pour un rapport '1ere'/'2e-regard' (l'info n'existe pas à ce stade). Vérifié de bout en bout : structure identifiée -> personnalisé ; rien -> générique ; autres types de rapport -> toujours générique.

## Chantier : bloc `data/freins.js` (freins -> ressources) - ✅ TERMINÉ le 2026-08-28 (9 étapes + nettoyage + garde-fou)

**Ce n'est PAS un module** (décision `docs/CHANTIER_RESSOURCES.md` §2ter : un module autonome ferait doublon avec l'annuaire de PCGI 87). C'est un **bloc de données** `data/freins.js`, indexé par les 17 codes de frein de la liste fermée (`prompts/regard-exterieur.md`), **source unique de vérité côté données**. Principe : l'assistant classe le frein (fiable), le CODE garantit les ressources (déterministe). `freinsIdentifies`/`freinsIdentifiesDelta` (`[CLOS]` 2026-08-26) préparent déjà le terrain côté prompt.

**Périmètre confirmé avec Denis (2026-08-28) : Regard extérieur UNIQUEMENT.** Pas le Bilan candidature, pas ailleurs. Les ressources d'un frein identifié doivent apparaître, lisibles, dans **les 3 vues du rapport Regard extérieur** : vue d'ensemble, vue par catégorie, vue par Repère.

**Forme d'une entrée** : `{ code, titre, definition, commentLever, pistesDeReflexion:[], quiVoir:[], ressources:[{libelle, url, motsCles:[], verifieLe}], synonymes:[], sensible?, urgence? }`. 17 entrées. `sensible: true` sur `violences` et `addiction` uniquement (hors vue-liste en accès libre, formulation prudente). Textes au registre "Ton" (voir chantier ton ci-dessus).

**Séquence validée (Denis, 2026-08-28), dans cet ordre** :
0. ✅ **[FAIT - commit `bbce37f`] `data/freins.js`** - le bloc (17 entrées, champ `niveau: vital|stabilite|emploi`) + `<script>` dans `index.html` + `tests/freinsRepertoire.test.js` (6 tests, 602/602). Aucun effet visible utilisateur.
0bis. ✅ **[FAIT - 2026-08-28] `## Ordre de traitement` dans `prompts/regard-exterieur.md`** (+ variante courte dans l'approfondissement) : (1) cohérence des Repères -> (2) freins -> (3) niveau de chaque frein -> (4) le discours d'ensemble s'organise autour du frein le plus urgent, l'emploi vient après. Répond au retour de Denis : le 1er prompt traitait tous les freins comme périphériques à la recherche d'emploi.
1. ✅ **[FAIT - 2026-08-28] Rendu dans le rapport Regard extérieur** : chaque frein de `freinsIdentifies` est enrichi avec l'entrée `FREINS_REPERTOIRE[code]` (définition, comment lever, pistes de réflexion, qui voir, **ressources cliquables** via `_regardExterieurRenduPistesRessourcesListe` + avertissement « vous quittez l'application »). Tri par `niveau` : **vital -> stabilité -> emploi -> code inconnu en dernier**. Ligne « numéro d'urgence » (115, 3919) en tête d'un frein vital. Rendu dans la **vue d'ensemble** (`_regardExterieurRenduFreins`, enrichie) ET **en tête de la vue par Repère** (3e param `freins` ajouté). Repli silencieux (libellé + justification seuls) si `data/freins.js` non chargé ou code sans entrée. Vérifié navigateur : tri OK, ressources cliquables, urgence stylée (ambre, jamais rouge), mode sombre OK, **aucun bloc si zéro frein** (rendu identique à avant), 0 erreur console, 602/602 tests.
2. ✅ **[FAIT - 2026-08-28] Suivi Umami des freins** (était l'ancienne étape 7) : `_regardExterieurTrackFreins()` déclenche un événement `regard_exterieur_frein_affiche` `{code, type}` **par code, à la détection** (dans les `_regardExterieurTraiterReponse*`), jamais au rendu. Idem `regard_exterieur_signal_detresse`.
3. ✅ **[FAIT - 2026-08-28] Champ `reperesConcernes` -> vue par catégorie.** Prompt : `freinsIdentifies[].reperesConcernes: ["R2"]` (regard-exterieur.md) + `freinsIdentifiesDelta[]` `action:"ajouter"` (approfondissement) + prose (traçabilité : au moins un Repère qui exprime vraiment le frein, sinon ne pas le mettre). Parser : `_regardExterieurResoudreReperesConcernes()` résout les numéros R1/R2 en `repereId` (même mécanisme que les éclairages), numéro halluciné ou hors sélection silencieusement ignoré, doublons retirés. Rendu : `_regardExterieurRenduVueParCategorie()` prend `freins` en 4e param ; chaque catégorie affiche en bas les freins rattachés à au moins un de ses Repères (sous-accordéons, même profondeur que les Repères). Un frein sans `reperesConcernes` (analyse ancienne) n'apparaît dans aucune catégorie mais reste en vue d'ensemble + vue par Repère -> jamais perdu. Vérifié navigateur : rattachement correct (mobilité -> catégorie du Repère « pas de voiture », pas ailleurs), R halluciné ignoré, frein sans rattachement toujours visible ailleurs, 0 erreur console, 602/602 tests.
4. ✅ **[FAIT - 2026-08-28, commit `fbd3527`] Allègement des prompts** : la liste fermée de ~40 sites ET le champ `pistesRessources` sont retirés des deux prompts (`regard-exterieur.md` / `regard-exterieur-approfondissement.md`). Sections « Deux registres » / « Registre d'accompagnement » remplacées par une consigne courte : ne citer aucun site, URL ni numéro dans le texte (**seule exception le 3114**, cas signal de détresse) ; l'app affiche les ressources depuis `data/freins.js` à partir des codes de `freinsIdentifies`/`freinsIdentifiesDelta` rattachés aux Repères. Schémas JSON nettoyés + re-validés (blocs valides). `_REGARD_EXTERIEUR_SITES_AUTORISES` : ajout des 5 URL d'aide alimentaire / mobilité solidaire qui étaient dans la liste fermée du prompt mais pas dans la liste blanche du code -> cette liste devient la **référence unique d'URL vérifiées** (validation défensive + `data/freins.js`). `tests/freinsRepertoire.test.js` re-pointé vers cette liste blanche. 602/602.
   - **Pas de fichier dédié pour le 1er prompt** : tranché - les écarts entre 1re analyse et Deuxième regard sont trop faibles (seulement un cadrage préfixé) ; les sections `Ordre de traitement` / `Ton` / signal de détresse servent les deux usages. `regard-exterieur.md` reste le fichier commun.
   - ✅ **[FAIT - 2026-08-28, commit `e142d8d`] Nettoyage du code `pistesRessources` mort.** Retiré de `modules/regard-exterieur/index.js` : `_regardExterieurNormaliserPistesRessources()` + `_regardExterieurUrlAutorisee()` (plus aucun appelant), le champ `pistesRessources` des 3 parseurs, l'accordéon « Pistes de ressources à explorer » de la vue d'ensemble, `_regardExterieurBlocPistesRepere()` + ses 2 appels, le paramètre `avecBoutonGarder` de `_regardExterieurRenduPistesRessourcesListe()` (toujours faux). Conservé : `_REGARD_EXTERIEUR_SITES_AUTORISES` (référence des URL vérifiées pour `data/freins.js` + le test), `_regardExterieurRenduPistesRessourcesListe()` (fiche frein), `_REGARD_EXTERIEUR_AVERTISSEMENT_LIEN`. Rien n'est déployé -> aucun risque « ancien rapport stocké » réel. Vérifié navigateur : nouveau ET ancien format rendus à l'identique, plus d'accordéon « Pistes de ressources », fiche frein intacte. -48 lignes nettes.
5. ✅ **[FAIT - 2026-08-28, commit `d84ee75`] Frein -> Repère : le Repère stocke le `code`.** Bouton « Garder ce frein dans mes Repères » dans le sous-accordéon « Ce qui vous freine » -> crée un Repère type « À discuter » qui porte le champ `frein` (le code, jamais une copie des pistes). Fiche re-rendue à jour à chaque affichage dans « Mes Repères » via la façade `regardExterieurRenduFicheFrein()` (jamais figée - [[LECONS 9.13]]). **Titre :** le libellé du frein (« Se déplacer »), jamais imposé à la personne (limite 80 car. = frustration, tranché Denis) - il se complète tout seul depuis le CONTENU : premiers mots du texte (auto-suggestion), puis formulation de l'assistant si passage en Regard extérieur (`reperesAppliquerNettoyageIA` **préfixe** au lieu de remplacer pour un Repère-frein). **Provenance rapport vs barre de recherche : périphérique, pas tracée** (tranché Denis). **Plusieurs Repères d'un même code autorisés** (`mobilite` = pas de permis / pas de voiture / trajet trop long...), distingués par le complément. Repère ordinaire inchangé ; dégradation silencieuse si `data/freins.js` absent. Vérifié navigateur (clair + sombre, flux complet), 602/602 tests. **Aucune modif de prompt nécessaire** : le code fait la complétion aux deux moments (hors ligne + reformatage de la sortie IA).
6. ✅ **[FAIT - 2026-08-28, commit `054f596`] Barre de recherche : `synonymes` -> `code` -> fiche.** Groupe « Ce qui peut vous freiner » dans `rechercherBaseConnaissances()` (`data/baseConnaissancesERIP.js`) : matche les freins de `data/freins.js` par `synonymes` + `titre`. Mot tapé contenu dans un synonyme (« voiture » -> « pas de voiture ») ; sens inverse pour les synonymes ≥ 4 car. (« je n'ai pas de voiture » -> `mobilite`), garde anti-faux-positif (« business » ≠ « bus »). **Freins sensibles inclus si la personne tape le mot** (violence, alcool...) - décision Denis, fiche à formulation prudente. Clic -> nouvelle façade `regardExterieurOuvrirFicheFrein(code)` : ouvre la fiche (mêmes pistes/ressources qu'un rapport) dans une fenêtre ERIP + le bouton « Garder ce frein » (même mécanisme qu'étape 5). Un clic frein n'est PAS un engagement de parcours (pas d'`effacerSauvegarde`). Vérifié navigateur (matching, clic, garder -> saisie -> titre auto-complété, code inconnu, mode sombre), 602/602 tests.
7. ✅ **[FAIT - 2026-08-28, commit `3a01f80`] Bouton « Point sur ma situation ».** Nom tranché par Denis (« Synthèse » écarté : froid + sous-entend que l'app juge). **Périmètre : TOUT le rapport**, pas seulement les freins (tranché Denis - « ni fiche-freins, ni vue-liste »). Sur une carte de « Mes rapports » ET dans « Voir mon analyse » -> aperçu avant impression d'un document remis en forme : (1) Ce que j'ai partagé, (2) Ce qui ressort, (3) Ce qui peut me freiner (fiches à jour de `data/freins.js`), (4) Mes questions pour le rendez-vous ; bloc détresse (3114) en tête si `signalDetresse`, calme. Mécanique : document HTML autonome (`_regardExterieurHtmlPointSituation`) dans un `iframe` `srcdoc`, imprimé via `iframe.contentWindow.print()` - **jamais `window.print()` sur la page hôte** (même patron que `modules/coherence-transversale/ui.js`). Aucune dépendance : impression / PDF via la boîte du navigateur. Façade `regardExterieurOuvrirPointSituation(rapport)`. Vérifié navigateur (doc complet, cas détresse, rapport vide -> repli, les 2 points d'entrée), 602/602 tests. **La « vue-liste légère Ce qui me freine » est abandonnée** (Denis : « ni l'une ni l'autre »).
8. ✅ **[FAIT - 2026-08-28, commit `41669db`] Freins comme rubrique du Lexique.** Nouvel univers `freins-parcours` (« Ce qui peut freiner un parcours »). Les 17 fiches sont **générées depuis `data/freins.js`** et ajoutées à `LEXIQUE_FICHES` au chargement (IIFE en fin de `data/freins.js`, inerte côté Node) - **rien n'est recopié dans `data/lexique.js`**. `id` = `freins-<code>` (pluriel, pas de collision avec la fiche `frein-emploi` notion générale). `variantesRecherche` = les synonymes -> cherchables dans le Lexique. Détail de la fiche : sous la définition (`corps`), le bloc pistes + ressources **à jour** via `regardExterieurRenduFicheFrein(code, {sansDefinition:true})`. **Liens croisés** : carte `_FREINS_LEXIQUE_VOIR_AUSSI` dans `data/freins.js` (santé/handicap -> rqth/médecine du travail..., formation -> cpf/vae...) + une poignée de freins reliés à `frein-emploi` (pas les 17, pour éviter un mur de renvois) ; le Lexique rend la relation dans les deux sens. **`violences` / `addiction` inclus normalement** (décision Denis - fiche prudente + n° d'urgence déjà dans `data/freins.js`). Vérifié navigateur (17 fiches, rubrique Mode Livre, détail sans définition dupliquée, 115 pour hébergement, recherche Lexique, liens bidirectionnels, mode sombre), 602/602 tests.
9. ✅ **[FAIT - 2026-08-28, commit `a38f03d`] Bouton « 🗝️ Mes freins identifiés (N) ».** Sur la ligne d'onglets de « Mes rapports », **groupé avec la recherche à droite** (`.reperes-rapports-actions-droite`, `margin-left:auto`) - loin des filtres par type : décision Denis, « c'est ce qu'on cherche dans ses rapports, pas un filtre ». Icône clé (jamais l'ancre du Carnet, jamais fataliste). **Affiché uniquement s'il y a ≥ 1 frein** - jamais un « voici vos problèmes » dans le vide. `regardExterieurFreinsConsolides()` agrège tous les codes de frein de tous les rapports (`freinsIdentifies` + `freinsIdentifiesDelta`) + les freins gardés comme Repère ; dédoublonné par code, trié vital -> stabilité -> emploi. `regardExterieurOuvrirMesFreins()` : fenêtre listant chaque frein en accordéon - **fiche à jour** + « Ressorti dans N de vos analyses » + les Repères **encore existants** auxquels il est lié. **Historique = les RAPPORTS, pas les Repères** (tranché Denis) : supprimer un Repère retire seulement son lien ; un frein ne disparaît que si on supprime le dernier rapport/Repère-frein qui le contenait. Vérifié navigateur (consolidation, dédoublonnage, tri, suppression d'un Repère -> frein conservé + lien disparu, bouton absent si zéro frein, mode sombre), 602/602 tests.

**Chantier freins : les 9 étapes + le nettoyage `pistesRessources` + le garde-fou « Repère vide » sont FAITS (2026-08-28).**

✅ **[FAIT - 2026-08-28, commit `c964655`] Garde-fou « Repère vide avant 1er passage ».** Un Repère sans texte (titre seul) ne peut plus partir à l'analyse - l'assistant ne ferait que deviner. Ce n'est PAS un mur, c'est guidé : dans le panneau « Quels Repères envoyer ? », un Repère vide a sa case décochée + désactivée, bordure pointillés, contenu atténué, une invite douce (ambre, jamais rouge), et son bouton devient « + Ajouter quelques mots » (même édition en place) ; dès qu'un texte est saisi, la case se réactive et se coche (`_regardExterieurSyncSelectionItemVide`). Si TOUS les candidats sont vides, « Continuer » reste désactivé. Sur « Mes Repères », une phrase discrète sous un Repère vide. **Aucune condition côté prompt** (tranché Denis) - c'est l'app qui décide ce qu'elle envoie. Le 2e passage / « Aller plus loin » n'est pas concerné. Vérifié navigateur (flux complet, mode sombre), 602/602 tests. **Détection par mots-clés : toujours NON** (cohérent avec le signal de détresse).

**Décidé, à garder** :
- On ne traite PAS "l'opposé du frein" (atouts/motivation) - trop proche d'un diagnostic de caractère.
- **Pas de freins dans le module Bilan candidature - tranché par Denis le 2026-08-28.** Regard extérieur uniquement.
- Rubrique déjà nommée dans le rapport : "Ce qui vous freine".
- Toute URL hors liste fermée du prompt doit être vérifiée (WebFetch) avant ajout - [[LECONS 9.16]]. L'étape 1 n'utilise QUE des URL de la liste fermée (déjà vérifiées lors d'un chantier antérieur).
- Fichiers frères, hors périmètre, notés : `data/annuaires.js` (annuaires par territoire : PCGI 87, DORA...) et un bloc "Urgences" (numéros nationaux, dont le **3114** prévention du suicide). Voir `docs/CHANTIER_RESSOURCES.md` parties 1 et 2.

- [EN COURS - PRIORITÉ, sensible] **Signal de détresse / risque suicidaire dans le rapport Regard extérieur.** Contexte : Denis a vécu ce cas en stage (appel d'un bénéficiaire menaçant de se suicider, 1re semaine) - point de vigilance maximal.
  - ✅ **[FAIT - 2026-08-28] Côté prompt.** Section `## Priorité absolue : signal de détresse` ajoutée dans `prompts/regard-exterieur.md` ET `regard-exterieur-approfondissement.md` (statut inconditionnel, comme le 3919). Champs `signalDetresse` (bool) + `messageDetresse` (string) ajoutés aux 2 schémas JSON. Le **3114** (vérifié par Denis) est cité en numéro seul, comme le 115 ; `3114.fr` ira dans le futur bloc Urgences. **Garde-fou faux positifs** : le signal ne se déclenche QUE si un élément renvoie à la mort / se faire du mal ; l'épuisement, le ras-le-bol, « tout laisser tomber » face à une SITUATION ne déclenchent PAS (exemples négatifs écrits dans le prompt, dont celui de Denis « laisse tomber, j'en peux plus, j'ai envie de tout laisser tomber »). Message formulé comme une proposition bienveillante, jamais un constat. Pas de détection par mots-clés côté code (une regex sur texte libre EST la cause des faux positifs).
  - ✅ **[FAIT - 2026-08-28] Côté app.** (1) Les 2 parsers (`_regardExterieurAnalyser1erPassage` / `_regardExterieurAnalyser2ePassage`) extraient `signalDetresse` (booléen strict `=== true`) + `messageDetresse` ; (2) `_regardExterieurRenduBlocDetresse(contenu)` rend un **bloc distinct tout en haut** du rapport (avant les onglets), sur fond ambre `--warning-bg-subtle` (calme, jamais rouge/erreur), toujours ouvert, le **3114** en grand et cliquable `tel:3114` ; texte de repli sûr si l'assistant a oublié `messageDetresse` (jamais un « 3114 » seul). Câblé dans les 3 types de rapport (`regardExterieurRenduContenuRapport`) ET la vue live « Voir mon analyse » (`regardExterieurRevoirDerniereAnalyse`) ; (3) événement Umami `regard_exterieur_signal_detresse` `{type}` déclenché **à la détection** (dans les `_regardExterieurTraiterReponse*`), pas au rendu, drapeau nu sans contenu personnel. **Vérifié en navigateur** : signal true -> bloc en tête + `tel:3114` ; signal false -> rendu identique à avant (zéro régression) ; message vide -> repli ; mode sombre OK (aucun texte invisible) ; 0 erreur console ; 602/602 tests Node.
  - **Filet de sécurité côté code (le signal ne dépendait QUE de l'assistant externe) - plan en 2 étapes, validé par Denis le 2026-08-29.**
    - ✅ **[FAIT - 2026-08-29] Étape 1 : filet SILENCIEUX (mesure seule).** `modules/regard-exterieur/index.js` : `_REGARD_EXTERIEUR_DETRESSE_TOURNURES` (≈ 20 tournures entières sans ambiguïté, **à relire par Denis, CIP**) + `_REGARD_EXTERIEUR_DETRESSE_IDIOMES` (« mourir de rire », « me tuer à la tâche »… retirés avant comparaison) + `_regardExterieurDetresseFiletCode()` (normalisation propre : `normaliserTexte()` puis ponctuation/apostrophes → espace) + `_regardExterieurComparerDetresse(signal, type)`. À chaque analyse (1ère / 2e-regard / aller-plus-loin), compare le filet code au `signalDetresse` de l'assistant ; **si divergence uniquement**, événement Umami **anonyme** `regard_exterieur_detresse_desaccord` `{ sens: 'code_seul' | 'ia_seul', type }` - **aucun texte de la personne, aucun changement d'écran**. Objectif : mesurer sur quelques semaines si l'assistant rate vraiment des cas, et si la liste tient la route, avant de décider l'étape 2. Scanne le texte des Repères envoyés (titre + source + texte). Vérifié navigateur : 12/12 cas du filet (5 vrais positifs, 2 idiomes rejetés, « j'en peux plus / tout laisser tomber » rejeté, neutre rejeté) ; 4/4 scénarios de comparaison (divergence → événement au bon `sens`/`type`, accord → rien) ; 0 erreur console ; 602/602 tests Node ; `checkLexique` 0 erreur.
    - **[ABANDONNÉ - décision Denis 2026-09-10] Étape 2 : rendre le filet VISIBLE et ADDITIF.** Analyse : gain concret faible (le bloc 3114 dans ~2-3 cas de bord : assistant qui esquive le sujet, JSON illisible) ; coût réel = une regex sur texte libre qui devient **visible**, donc risque de faux positif sur le sujet le plus sensible de l'app. Aucune donnée Umami `code_seul` ne justifie l'ajout à ce jour. **On garde l'étape 1** (filet silencieux + mesure `regard_exterieur_detresse_desaccord`) et le bloc 3114 piloté par l'assistant. **À reconsidérer seulement** le jour où l'application tournera sur un serveur avec des données précises sur la personne (décision Denis) - pas dans l'architecture statique actuelle. Idée d'origine conservée pour mémoire : le code affichait lui-même le bloc 3114 (texte de repli validé) quand une tournure est repérée et que l'assistant ne l'a pas fait ; scan à l'envoi + booléen persisté pour couvrir le cas « JSON illisible ».
    - **Détection par mots-clés VISIBLE sans garde-fou de mesure : toujours NON.** Une regex brute sur texte libre reste la cause classique des faux positifs (« mourir de rire ») ; l'étape 1 sert justement à calibrer avant tout affichage.
  - **PAS un 18e code de frein** : `data/freins.js` reste à 17. "Suicide" n'est pas un frein à classer par l'assistant, c'est un signal qui déclenche toujours la ligne de crise. Le 3114 vit dans le futur bloc "Urgences".
  - **Alarme / notification temps réel pour Denis : ABANDONNÉ (Denis, 2026-08-29).** L'app est un site statique sans serveur : impossible d'envoyer un mail ou une alerte. Et même faisable, ce ne serait pas bon - les conditions (réactivité, responsabilité, cadre) ne sont pas réunies. Décision commune à tout ce qui relève de l'alerte : on ne le fait pas. Le seul signal qui remonte reste l'événement Umami anonyme `regard_exterieur_signal_detresse` (drapeau nu, sans contenu personnel), consultable à froid dans le tableau de bord.

## Chantier Ressources - 2e moitié : Urgences + Annuaire - ✅ TERMINÉ le 2026-08-29 (16 étapes)

**Livré (commits `937eceb` → `036c9fc`), 602/602 tests, `node scripts/checkLexique.js` 0 erreur 0 avertissement, vérifié navigateur (clair + sombre, 0 erreur console) :**
1. **Socle** (`937eceb`) — `demanderDepartementSiInconnu(cb)` (`js/app.js`) : demande le département (24/87/ailleurs) au moment où une explication en dépend, une seule fois, même clé localStorage que la carte « Ressources à explorer ». + `docs/TRAVAILLER_AVEC_DENIS.md` section « Vérifications systématiques » : **brancher la recherche à la création** de tout bloc de contenu (décision Denis 2026-08-29 : pas de « gros chantier recherche universelle » plus tard).
2. **Urgences → rubrique Lexique** (`e87bc79`) — `data/urgences.js` (`URGENCES_NUMEROS` 15/17/18/112/115/119/3114/3919 + `URGENCES_PORTES` 5 situations vitales). Univers `urgences` (« Numéros d'aide et d'urgence »), **2 fiches générées** (« Numéros d'urgence nationaux » + « Besoins vitaux : à qui s'adresser »), numéros **`tel:` cliquables**. Bouton « Ressources locales de mon territoire » → `demanderDepartementSiInconnu` → 87 ouvre PCGI 87, ailleurs affiche CCAS/MDS. Nouvelle façade `lexiqueOuvrirFiche(id)`. Recherche : catégorie « Aide et urgence » (accueil) + `variantesRecherche` (Lexique).
3. **Annuaire → renvois** (`17b390a`) — `ANNUAIRE_NATIONAL` = DORA (`dora.inclusion.gouv.fr`, vérifié WebFetch). `ouvrirRenvoiAnnuaire()` : 87 = PCGI 87 + DORA ; 24 / ailleurs = DORA seul (**aucun lien local inventé**). `_LEXIQUE_FICHES_TYPE_STRUCTURE` (france-travail, mission-locale, cap-emploi, plie, caf, siae, iae) → encart « voir l'annuaire » sous ces fiches. Recherche : catégorie « Trouver une structure » (fiche d'abord, renvoi ensuite).
4. **Liens cliquables « Mes freins identifiés »** (`bf76c93`) — « Ressorti dans N analyses » = une puce par rapport ; « En lien avec « Titre » » = le Repère cliquable. Le rapport / Repère s'affiche **dans la même fenêtre** avec un « ← Retour à mes freins » qui re-rend la liste à l'identique (décision Denis : le retour ramène exactement là où on était). Repère supprimé = plus de lien ; le frein reste (historique = les rapports).
5. **Liens croisés Lexique** (`6caae6e`) — 5a : `_FREINS_LEXIQUE_VOIR_AUSSI` (langue→fle, illettrisme→illettrisme, illectronisme→illectronisme, judiciaire→casier-judiciaire). 5b (autorisé Denis) : **4 nouvelles fiches Lexique** écrites — `illettrisme` (⟷ `fle` à-ne-pas-confondre), `fle`, `illectronisme`, `casier-judiciaire` (B1/B2/B3 vérifié service-public.gouv.fr).
6. **4 fiches Lexique de plus + structures locales sur chaque frein** (`845883c`) — 6a : `aide-alimentaire`, `hebergement-urgence` (voir-aussi `urgence-besoins-vitaux`), `aide-materielle`, `violences` (factuel, calme : 3919 / 17, CIDFF / France Victimes) + `_FREINS_LEXIQUE_VOIR_AUSSI` câble les 4. 6b (décision Denis) : bouton **« 📍 Des structures près de chez vous »** au bas de **chaque fiche frein** (`_regardExterieurRenduContenuFicheFrein`) → partout où elle s'affiche → `demanderDepartementSiInconnu` → `ouvrirRenvoiAnnuaire` (87 = PCGI 87 + DORA, ailleurs = DORA). Câblé dans les 3 points de délégation ; l'early-return évite de créer un Repère par erreur.
7. **5 fiches Lexique concept + enrichissement `data/freins.js`** (`patch_lexique_7.py`) — faits **vérifiés WebSearch avant écriture** (persistant = corpus) : `aide-a-la-mobilite` (aide mobilité France Travail : déplacement/repas/hébergement > 60 km ou 2 h ; **ne finance pas le permis** - aide au permis B France Travail supprimée le 1er avril 2026 ; pistes : région, CPF, auto-écoles sociales), `surendettement` (dossier Banque de France, gratuit ; commission ~5-6 semaines ; délais / réaménagement / effacement ; CCAS ou MDS pour monter le dossier), `aides-garde-enfants` (**AGE**, remplace l'AGEPI depuis 2024, enfants < 12 ans, plus réservée aux parents seuls, forfait, demande espace perso France Travail dans le mois ; + complément CAF, crèches à tarif social), `assistante-sociale` (secteur, rattachée au Département / MDS ou CCAS mairie, gratuite, point de départ quand les difficultés se cumulent), `addictologie` (**CSAPA**, gratuit et anonyme, produits + comportements jeux/écrans, proches accueillis, pas besoin d'avoir décidé d'arrêter ; Drogues Info Service 0 800 23 13 13). `_FREINS_LEXIQUE_VOIR_AUSSI` : `mobilite→aide-a-la-mobilite`, `addiction→addictologie`, et les **2 liens `caf` faibles remplacés** (`endettementAdministratif→surendettement + assistante-sociale`, `gardeEnfants→aides-garde-enfants + caf`). Cross-links rendus **bidirectionnels** (vérifié navigateur : `_lexiqueRenduFiche`). + un **2e `pistesDeReflexion`** (une question, jamais une solution) ajouté à `materiel`, `violences`, `addiction` dans `data/freins.js`. **17/17 freins ont maintenant une cible Lexique.**

8. **Écran « Trouver une structure » enrichi 24 / ailleurs** (`b26030d`) — le 24 n'était plus qu'un renvoi DORA (perçu comme « traité moins bien »). Désormais : **24** = page « Emploi & insertion à Bergerac » de la Ville (recense France Travail, Mission Locale, Cap emploi, PLIE, E2C, CIDFF — vérifiée) + Mission Locale du Bergeracois (PLIE + O2R, vérifiée) + DORA. **24 / ailleurs** = phrase de cadrage honnête (« le recensement a d'abord été fait pour le 87… ») + **portes universelles nommées par type** (CCAS, Maison des solidarités, France Services, Point Conseil Budget) — jamais une adresse inventée. **ailleurs** = en plus, des **mots-clés prêts à taper** dans un moteur de recherche. 87 inchangé. + aide contextuelle `renvoi-annuaire` (l'ampoule ne faisait rien sur cette fenêtre depuis l'étape 3).
9. **Info-bulle + ligne d'explication « Garder comme Repère »** (`6e8e443`, `134e83f`) — `texteAideRepere()` : phrase canonique unique (validée Denis, distincte de « note » = le Carnet), reprise telle quelle. `title=` au survol sur les 3 variantes du bouton (`_reperesRenduBoutonAncre`, `_regardExterieurBoutonGarderRepere`, `_regardExterieurBoutonGarderFrein`) + classe hook `aide-repere-ancre`. **Ligne d'explication TOUJOURS visible** (`htmlLigneAideRepere()`) : décision Denis = **uniquement sur les écrans à UN seul bouton** — fiche Lexique (`initAncrageLexiqueReperes`) et fiche frein autonome (`regardExterieurOuvrirFicheFrein`). Les rapports multi-boutons (Regard extérieur, Bilan) gardent le survol + leur bloc dépliable « Qu'est-ce qu'un Repère ? ».
10. **Filet « chercher dans le Lexique » depuis l'accueil** (`58874cb`) — `lexiqueOuvrirRecherche(texte)` : ouvre le Lexique avec le mot déjà saisi + résultats (un clic). Affiché sous « Aucun résultat » quand l'accueil ne trouve rien. + accent corrigé (« Aucun résultat », guillemets français). + **option 3** (synonymes de freins qu'on tape vraiment) : `csapa` / `addictologie` → `addiction`, `assistante sociale` → `endettementAdministratif`, `agepi` / `aide à la garde` → `gardeEnfants`. On abandonne l'idée d'indexer tout le Lexique dans l'accueil (bruit + maintenance).
11. **Correctif cartes de recherche + seconds niveaux + `checkLexique`** (`30d5134`, `eee20ed`) — bug visuel : cartes frein / urgence / structure de la recherche accueil, titre et description collés (classe `.carte-resultat-texte` = `flex` en ligne) → repassées au gabarit bloc des cartes métier. **`node scripts/checkLexique.js` était au rouge depuis l'étape 5b** (validateur distinct de `npm test`, non lancé — [[LECONS 9.26]]) : les 12 fiches du chantier freins dépassaient le plafond de 3 `variantesRecherche`. Plafond **porté à 4** (décision Denis), 10 fiches ramenées à 4 mots-clés (les mots du quotidien retirés — « alcool », « dettes »… — restent couverts par les synonymes des freins, vérifié). Script rendu conscient des fiches projetées au runtime (`urgence-*`, `freins-*`). + **9 entrées `LEXIQUE_SECOND_NIVEAU`** (nuance utile / exemples concrets, faits vérifiés : FICP, CJC, bulletins B2/B3, SIAO…) sur `surendettement`, `assistante-sociale`, `aides-garde-enfants`, `addictologie`, `illettrisme`, `casier-judiciaire`, `fle`, `aide-alimentaire`, `hebergement-urgence`. `checkLexique` : 0 erreur.
12. **7 fiches concept (ateliers / accompagnement / mobilité internationale)** (`5fc8a81`) — `visite-entreprise` (à-ne-pas-confondre `pmsmp`), `atelier-thematique`, `mobilite-internationale` (Erasmus+, Corps européen de solidarité, EURES — vérifié), `job-dating` (job dating + forums), `parrainage-vers-l-emploi` (vérifié travail-emploi.gouv.fr), `groupe-de-pairs`, `o2r` (« Offre de Repérage et de Remobilisation » — vérifié). 4 variantes chacune. Trouvables dans le Lexique ; depuis l'accueil, le filet « chercher dans le Lexique » les atteint en un clic.
13. **Renvoi local par fiche** (`7f92a07`) — `_LEXIQUE_RENVOI_LOCAL_PAR_FICHE` (`modules/lexique/index.js`). **Type `structure`** (`plie`, `mission-locale`) : l'encart « Près de chez vous » **remplace** le bouton annuaire générique — 24 → lien direct de la structure du Bergeracois (`missionlocaledubergeracois.com/le-plie`, `/`, vérifiés) ; 87 → PCGI 87 + DORA ; ailleurs → porte nommée (jamais une recherche libre pour une structure à homonymes). **Type `atelier`** (`freins-mobilite`/`-handicap`/`-formation`, `cv-lecture-recruteur`, `lettre-motivation`, `illectronisme`, `visite-entreprise`, `atelier-thematique`, `mobilite-internationale`) : encart **ajouté** — 24 → l'ERIP du Bergeracois (`.../erip`), ateliers mensuels gratuits, phrase adaptée au sujet, **jamais de date annoncée** (renvoi vers le programme) ; 87 / ailleurs → `ouvrirRenvoiAnnuaire`. Icône calendrier (ateliers) vs épingle (structures) : sur une fiche frein les deux encarts restent distincts ([[LECONS 9.15]]). Décision Denis : les **catégories du catalogue d'ateliers** (emploi, orientation, vie quotidienne…) **ne deviennent pas** des fiches Lexique (trop larges, fiches creuses) — seule `mobilite-internationale` était un vrai concept manquant.
14. **« Changer de département » sur toutes les fenêtres de renvoi** (`552112d`, restylé `46daa58`) — retour Denis : une fois le département mémorisé, aucune fenêtre ne permettait de revenir dessus. `oublierDepartementRessources()` + `htmlLienChangerDepartement()` (`js/app.js`) → **vrai bouton bordé** (`btn-outline-secondary`, pas un lien texte — [[LECONS 9.6]] 3e récidive) avec une ligne de question au-dessus. Câblé dans `ouvrirRenvoiAnnuaire`, les 2 fenêtres de `_lexiqueRenvoiLocalParFiche`, `_lexiqueRenvoiRessourcesLocales` ; chacun oublie le choix et rouvre SA propre fenêtre. `ouvrirFenetreRessourcesExplorer` avait déjà le sien. → [[LECONS 9.27]] : un choix proposé doit toujours être révocable.
15. **Fiches récentes enrichies + collections** (`46daa58`) — les 17 fiches ajoutées au chantier étaient « faibles » (audit : ≤ 1 relation, souvent juste `frein-emploi`, jamais citées). Corrigé : `mobilite-internationale` → `stage` + `alternance` + **2e niveau « Par où commencer »** (Info Jeunes / Eurodesk, EURES, Mission Locale < 26 — vérifié `info-jeunes.fr`) ; `visite-entreprise` → `projet-professionnel` ; `atelier-thematique` → `mission-locale` ; `job-dating` → `reseau-pour-emploi` ; `parrainage-vers-l-emploi` ⟷ `groupe-de-pairs` + `reseau-pour-emploi` ; `o2r` → `mission-locale` (+ `frein-emploi` gardé). 2 liens croisés : `illectronisme` ⟷ `illettrisme` (à ne pas confondre), `casier-judiciaire` ⟷ `discrimination-embauche`. **2 collections** : « Quand la vie pèse sur le parcours » (9 fiches du quotidien), « Se faire aider : accompagnements et mises en relation » (7). `checkLexique` : **0 avertissement** (plus aucune fiche orpheline). Incident au passage : un patch Python de masse ancré sur un motif non unique a supprimé 3 fiches, rattrapé par `checkLexique` + `git checkout` → [[LECONS 9.24]] point 4.
16. **Groupe « Dans le Lexique » dans la recherche d'accueil + lien 24 → page ERIP** (`036c9fc`) — désormais **tout** le corpus Lexique ressort de la barre d'accueil : `rechercherBaseConnaissances()` a un nouveau groupe `« Dans le Lexique »` (icône 📖) qui couvre les ~130 fiches concept au-delà des groupes freins / Urgences / structures. **Garde-fous décidés avec Denis** : match `titre` + `variantesRecherche` seulement (jamais le corps), **plafond 5**, groupe **classé en dernier**, dédoublonnage contre ce qui est déjà affiché (`univers === 'urgences'`, `_LEXIQUE_FICHES_TYPE_STRUCTURE`, préfixe `freins-`). Carte `carteLexiqueHTML` → `data-lexique-fiche-ouvrir` (handler de clic déjà en place depuis l'étape 2). Le filet « Chercher dans le Lexique » (étape 10) reste pour les requêtes qui ne matchent vraiment rien. **Et** : `RESSOURCES_DEPARTEMENTS['24'].lien` (fenêtre « Ressources à explorer », `ouvrirFenetreRessourcesExplorer`) repointé du catalogue de réservation brut `linscription.com` vers `missionlocaledubergeracois.com/erip` (page de présentation, tous publics, handicap inclus) — même cible que les encarts « ateliers » du Lexique.

**Décisions prises pendant ce chantier :**
- **Géolocalisation navigateur : écartée** (échange Denis 2026-08-29). `navigator.geolocation` donne lat/long, pas un département ; le géocodage inverse = service tiers ou polygones lourds, faux près des frontières (24 et 87 sont limitrophes), popup natif anxiogène pour ce public, peu fiable sur ordinateur (wifi/IP). Gain = économiser un clic une fois. On garde `demanderDepartementSiInconnu` (demande une fois, mémorise). Vraie voie « auto » si besoin un jour : pré-réglage au déploiement (URL dédiée partenaire, ou `?dep=87`).
- **Forme 2 (indications par fiche selon le département)** = gros chantier futur, PAS commencé. Chaque fiche « type de structure » donnerait, département connu, un pointeur concret (87 → PCGI 87, 24 → le site de la structure). Modèle de données fiche × département → lien. **À cadrer d'abord avec les partenaires PCGI 87** : étendre leur concept au 24, ou Denis prend la relève via ses propres fiches. Ne pas coder avant cet accord.
- Pas de bloc Urgences sur l'accueil (tranché Denis : personne en urgence aiguë n'ouvre l'app ; le bon usage est la consultation à froid, via le Lexique).
- Pas de reconstruction d'annuaire dans APP (doublon PCGI 87). Pour le 24 : on **renvoie** vers la page de la Ville de Bergerac + la Mission Locale du Bergeracois + DORA (étape 8) - jamais une liste maison à maintenir, jamais un lien inventé.
- « Recherche universelle depuis l'accueil » : on ne fait PAS le gros chantier monolithique. Chaque bloc branche sa propre recherche (checklist), l'objectif est atteint progressivement.
- Micro-piste « accès accueil → rubrique freins » : **abandonnée** (tout passe par le Lexique + la recherche).

### ⚠️ Dépendances avec le méga-chantier « Réorganisation de la page d'accueil »

**À traiter dans le chantier Accueil, pas avant.**

- **La barre de recherche de l'accueil est déjà branchée sur ce chantier.** `rechercherBaseConnaissances()` (`data/baseConnaissancesERIP.js`) fait remonter, en plus du corpus métiers/compétences : les **freins** (via `synonymes`), les **fiches Urgences** (`univers === 'urgences'`), les **fiches « type de structure »** (`_LEXIQUE_FICHES_TYPE_STRUCTURE`), et depuis l'étape 16 **tout le reste du corpus Lexique** (groupe `« Dans le Lexique »`, plafond 5, dédoublonné, classé en dernier). Toute évolution de la barre d'accueil doit préserver ces quatre branches.
- **Fait (étape 16, option A retenue).** Denis voulait que **tout** ce qui est dans le Lexique ressorte de la barre d'accueil. C'est en place : groupe `« Dans le Lexique »` + le filet « Chercher dans le Lexique » (étape 10) en secours pour les requêtes qui ne matchent rien. L'ancienne décision « pas de recherche universelle monolithique » reste respectée : le groupe est borné (plafond 5, titre + variantes seulement).
- **Reste à faire dans le chantier Accueil** : décider si le groupe « Dans le Lexique » garde sa place actuelle (avant « Métiers qui pourraient vous correspondre ») ou passe tout en bas ; le style de la carte (aujourd'hui identique aux cartes métier). Purement cosmétique, aucun blocage.
- **Règle posée par Denis le 2026-08-29** : chaque nouveau bloc de contenu Lexique est désormais trouvable depuis la barre d'accueil sans travail supplémentaire (le groupe `« Dans le Lexique »` parcourt tout `LEXIQUE_FICHES`), à condition de renseigner `variantesRecherche`. Voir `docs/TRAVAILLER_AVEC_DENIS.md`, « Vérifications systématiques ».

---

### (Archive) Cadrage d'origine du chantier

**Contexte.** Le chantier « Ressources » a un modèle à 3 niveaux (`docs/CHANTIER_RESSOURCES.md`). Le **Niveau 2 (Frein -> bonne porte) = `data/freins.js`, FAIT** (les 9 étapes ci-dessus). Restent le **Niveau 1 (Urgences)** et le **Niveau 3 (Annuaire)**. Denis (2026-08-28) : on ne reconstruit PAS d'annuaire (doublon avec PCGI 87 où APP est intégrée) ; mais on doit quand même « en parler ». Les deux vivent **dans / autour du module Lexique** + la barre de recherche.

**Forme validée (2026-08-28) :**

| | Niveau 1 - Urgences | Niveau 3 - Annuaire |
|---|---|---|
| Nature | vraie **rubrique de contenu** (on l'ouvre, on la lit) | **mécanisme de renvoi** (aucun contenu recopié, juste des liens sortants) |
| Donnée | `data/urgences.js` - numéros nationaux (15/17/18/112/115/119/**3114**/3919) + portes besoins vitaux (alimentaire -> CCAS/MDS ; énergie coupée -> MDS + FSL Maintien ; expulsion -> ADIL + CCAS ; zéro ressource -> CCAS ; hygiène -> accueils de jour) | **réutiliser `RESSOURCES_DEPARTEMENTS` + `ouvrirFenetreRessourcesExplorer` qui EXISTENT DÉJÀ** dans `js/app.js` (~l.27832) - 24 -> ERIP Bergeracois, 87 -> Inkéo C&C / PCGI 87. Ajouter **DORA** (national). Ne PAS créer un `data/annuaires.js` parallèle. |
| Dans le Lexique | nouvel **univers** `urgences` (« Numéros d'aide et d'urgence »), fiches **générées** depuis `data/urgences.js` (mêmes mécaniques qu'étape 8 : `_LEXIQUE_LIBELLES_UNIVERS` + hook dans `_lexiqueRenduFiche`), numéros **`tel:` cliquables**, « voir aussi -> page Urgences de PCGI 87 » en bas | encart secondaire en bas des fiches Lexique « type de structure » (Mission Locale, Cap emploi, France Travail...) : « pour en trouver une près de chez vous -> [annuaire] » |
| Barre de recherche | synonymes (« urgence », « 3114 », « 115 », « expulsion », « coupure électricité »...) -> la rubrique / la fiche | nouvelle catégorie de résultats « structure / annuaire » dans `rechercherBaseConnaissances()` : type de structure, « où trouver une association », un nom de structure qu'APP n'a pas en fiche -> encart de renvoi. **Règle : si APP a une fiche Lexique, la montrer D'ABORD, le renvoi en dessous.** |
| Territoire | national, rien à filtrer | filtré par le département mémorisé (`departementRessourcesMemorise()`, déjà en place) |

**Étapes pressenties (à affiner avant de coder) :**
1. `data/urgences.js` + `<script>` dans `index.html` (après `data/freins.js`).
2. Univers Lexique `urgences` + fiches générées + hook `_lexiqueRenduFiche` (rendu `tel:` + portes besoins vitaux) + « voir aussi PCGI 87 ». Vérifier accents, mode sombre.
3. Synonymes -> recherche (main + Lexique), comme pour les freins.
4. Niveau 3 : ajouter DORA à `RESSOURCES_DEPARTEMENTS` (ou une liste sœur), + l'encart de renvoi sous les fiches Lexique « type de structure » (il faut un marqueur sur ces fiches - certaines n'existent pas encore : pas de fiche `ccas`, `mds`, `france-services`).
5. Catégorie « structure / annuaire » dans `rechercherBaseConnaissances()` (règle : fiche d'abord, renvoi ensuite).

**Ponts et dépendances :**
- **Niveau 3 <-> « Recherche universelle depuis la barre de l'accueil »** (tâche « Autres tâches en attente », `#rechercheERIPInput`) : la catégorie « structure / annuaire » fait partie du même effort d'élargissement de la barre de recherche. À traiter ensemble ou au moins de façon cohérente (même encart flottant, mêmes règles d'affichage). Ne pas coder deux mécanismes concurrents.
- **Niveau 3 <-> `RESSOURCES_DEPARTEMENTS` / `ouvrirFenetreRessourcesExplorer` existants** : dépendance forte - construire dessus, pas à côté. Le choix de département est déjà mémorisé en `localStorage`.
- **Niveau 3 <-> couverture du corpus Lexique** : l'encart « trouver une structure près de chez vous » a besoin de fiches « type de structure » marquées. Il en existe (`france-travail`, `mission-locale`, `cap-emploi`, `plie`...) mais pas toutes (`ccas`, `mds`, `france-services` absentes). Soit on ajoute ces fiches, soit l'encart n'apparaît que là où une fiche existe.
- **Niveau 1 <-> signal de détresse (3114)** : le 3114 vit déjà dans le bloc « signal de détresse » du rapport Regard extérieur (formulation « proposition bienveillante »). La rubrique Urgences est la version **consultable à froid**. Garder les deux **cohérents** : même numéro, ton calme, jamais rouge.
- **Niveau 1 réutilise le patron de l'étape 8** (freins -> rubrique Lexique) : univers + fiches générées + hook `_lexiqueRenduFiche`. Risque faible, mécanique prouvée. Attention id : préfixe qui n'entre pas en collision avec un id existant ([[LECONS 9.25]]).
- **Indépendant** du chantier « Réorganisation de la page d'accueil » : rien ici ne touche l'accueil (Denis a tranché : pas de bloc Urgences sur l'accueil).

**Petites pistes différées du chantier freins** (statut au 2026-08-29, toutes soldées) : liens cliquables depuis « Mes freins identifiés » -> **FAIT** (étape 4) ; accès direct accueil -> rubrique Lexique -> **abandonné** (tout passe par le Lexique + la recherche) ; `_FREINS_LEXIQUE_VOIR_AUSSI` -> **17 des 17 freins ont une cible** (étapes 5, 6 et 7). `mobilite` et `addiction` ont désormais leur fiche concept (`aide-a-la-mobilite`, `addictologie`, étape 7).

### Autres tâches en attente
- [CLOS - 2026-08-28, branche `logiciels-rubrique-cv`, commit `2d17fcc`, 596/596 tests + test navigateur réel] Passer les 5 questions fixes de « Aller plus loin » de texte libre à choix guidés. **Périmètre livré (Option A, validée avec Denis un jour de fatigue - version minimale sûre, pas la spec complète de la note d'origine)** : les 3 premières questions passent en cases à cocher + une petite zone de texte « préciser » (nouveau champ optionnel `precisionLabel` par question), en **réutilisant le mécanisme `choix` déjà en place** (celui des questions de l'assistant) - zéro changement du pipeline collecte -> 2e prompt. Q4 et Q5 restent en texte libre.
  - Q1 accompagnement : France Travail / Mission Locale / Cap Emploi / PLIE / Une autre structure / Je ne suis pas accompagné(e) ; précision = structure + statut.
  - Q2 freins : 15 libellés tirés de la liste fermée des 17 codes du prompt, **sans `violences` ni `addiction`** (décision Denis - trop lourds à cocher pour ce public, restent possibles via la zone « Autre » et repérés par l'assistant depuis les Repères). Ordre : besoins vitaux -> stabilité -> langue/lecture/numérique -> discrimination.
  - Q3 métier/secteur : ~11 grands secteurs (petite liste large, pas les ~30 de `data/metiers.js`) ; précision = métier précis.
  - **Vérifié pendant le chantier** : le `<select>` secteur/domaine « à réutiliser depuis le module CV/Projet » évoqué dans la note **n'existe pas** (le CV saisit le domaine en texte libre). Une liste courte dédiée a été créée à la place.
  - **Reporté (Option B, spec complète de la note)** : découpage Q1->2 et Q3->2 (7 questions), vrai mode `choixUnique` (radio) pour le statut, plafond « max 2 », `<select>` secteurs dérivé de `metiers.js`, saisie métier multi-valeurs (tags), widget « Autre » avec suppression au clic. À faire plus tard si le besoin se confirme.
- **[CLOS - 2026-08-29, commit `770ca80`, 602/602 tests + test navigateur réel. Chantier 2 « contexte optionnel du Deuxième Regard ».]** Le design (validé 2026-08-28) est implémenté.
  - **Livré** (`modules/regard-exterieur/index.js`, `prompts/regard-exterieur.md`, `regardExterieur.css`, `js/app.js`) :
    - **Opt-in** après le choix des Repères : fenêtre « Voulez-vous répondre à quelques questions pour affiner ce Deuxième Regard ? (facultatif) », deux boutons de même poids. **Non** → flux identique à avant. **Oui** → les 5 questions.
    - **5 questions** (`_REGARD_EXTERIEUR_QUESTIONS_DEUXIEME_REGARD`), flux léger dédié qui réutilise les **gabarits de rendu** de « Aller plus loin » (compteur « X sur 5 », barre, `regard-exterieur-choix-btn`, astuce dictée Win+H, Précédente/Suivante), jamais son état. Q1 = boutons sur les Repères de la sélection, **0 à 2, plafond 2** + zone de texte ; Q3 = cases à cocher `_REGARD_EXTERIEUR_FREINS_CHOIX` (mêmes 15 libellés que « Aller plus loin ») + précision ; Q2/Q4/Q5 = texte libre.
    - **Cadrage** (`_regardExterieurConstruireCadrageDeuxiemeRegard`) : bloc **(b)+(c)** ajouté à **chaque** Deuxième Regard (le passage doit ouvrir vers une direction concrète, jamais une redite — registre d'accompagnement déjà défini dans le prompt) ; bloc **(d)** ajouté **seulement si** le contexte optionnel a des réponses (consigne texte libre + réponses formatées + ligne « Repères centraux » + consigne `quiVoirPersonnalise`). Le paragraphe **« Ton » (item a) était déjà** dans `regard-exterieur.md` (l.55) → rien à ajouter.
    - **`quiVoirPersonnalise`** : ajouté au **schéma JSON de `regard-exterieur.md`** (Option 1, tranchée par Denis 2026-08-29 — jamais un champ hors schéma, [[LECONS §1bis]]). Parsé aussi par `_regardExterieurAnalyser1erPassage` (toujours vide sur une 1ère analyse, zéro régression vérifiée) ; le rapport `2e-regard` le stocke et l'affiche (accordéon « Qui voir » personnalisé au lieu des 3 cas génériques).
    - **Correctif dark mode** au passage : `.regard-exterieur-choix-btn` non sélectionné avait un texte quasi-noir invisible sur carte foncée (**bug latent partagé avec « Aller plus loin »**) → `color: var(--text-strong)` + `border: var(--border-strong)` ([[LECONS 9.10]] / 9.14). Corrigé pour les deux flux.
    - Événement Umami `regard_exterieur_deuxieme_regard_contexte` (`{ repondu, nbReponses }`). Aide contextuelle `regard-exterieur-deuxieme-regard-optin`.
  - **Décisions prises pendant l'implémentation (évidences)** : Q3 → réutilisation de `_REGARD_EXTERIEUR_FREINS_CHOIX` (déjà calibré : violences/addiction absents, formulation d'élicitation) ; flux léger dédié plutôt qu'entrelacé avec `_regardExterieurQuestionsAPlat` (couplé aux catégories de Repère) ; (b)/(c) toujours, (d) conditionnel.
  - **(Archive) Spec d'origine** (validée 2026-08-28, transcript `f6f1b9f0` ~12h10 ; confiance estimée ~17/20, faiblesse : Q1 peut recevoir « je ne sais pas », mitigé en demandant démarche/échéance/personne/lieu). Les 5 questions ont été reprises quasi mot pour mot (voir `_REGARD_EXTERIEUR_QUESTIONS_DEUXIEME_REGARD` dans le code). Les 4 ajouts au cadrage prévus : (a) Ton — **déjà présent** dans `regard-exterieur.md` (l.55), non réajouté ; (b) garde-fou « au moins une piste concrète » ; (c) l'escalier (difficulté → au moins une direction, « parmi les pistes possibles, regarder du côté de… », vérifier le local avec le CIP) ; (d) consigne texte libre. (b)+(c) fusionnés en un seul paragraphe dans `_REGARD_EXTERIEUR_CADRAGE_DR_DIRECTION`, aligné sur le registre d'accompagnement déjà défini dans le prompt (exemple l.86).

---

## Nuit du 30 au 31/08/2026 — Bilan « Analyser ma candidature », 2e batch de retours + pages d'introduction (délégation complète de Denis, présentation le 31/08)

Contexte : Denis a délégué l'intégralité (« je te laisse tout faire, je ne prendrai pas de décision »), présentation prévue le 31/08. Retours détaillés + méthode : `docs/RETOURS_BILAN_BATCH2_2026-08-30.md`.

- **[CLOS - `c459a5e`]** Filet anti tiret cadratin sur tout texte d'assistant (Prompt 1 et 2). `bilanAssainirTypographie` / `bilanAssainirTypographieProfond` (`modules/bilan-candidature/modeles/utilitaires.js`), appliqué après extraction JSON dans `diagnosticResponseParser.js` et `ameliorationResponseParser.js`. 5 tests. 608 verts.
- **[CLOS - `d5ada8d`]** Fin de parcours claire, acces au CV sur les 3 modes de correction. `htmlBilanFinaliserCVBloc()` / `brancherEvenementsBilanFinaliserCVBloc()` : « Ouvrir mon CV ici » (atelier) + « Copier tout le texte de mon CV » + explication (fichier / site hébergeur / modèle à imprimer). Sur l'écran de clôture (remplace l'ancien bouton conditionné à `cvModifieDansERIP`), sur l'écran « aucune-automatisation » (retitré « Quelques points à placer vous-même », plus de « Choisir un autre mode de travail »), et en ligne compacte sur le rapport. Bouton « Ouvrir l'atelier CV » des recos de structure → ouvre vraiment l'atelier (plus d'aller-retour assistant). Vérifié navigateur.
- **[CLOS - `d092d4e`]** « Revoir la présentation » = vrai bouton (pilule contour accent). Bandeau « Vous avez une analyse en cours » agrandi + pulsation bornée ~10 s (`prefers-reduced-motion` → aucune).
- **[CLOS - `d9c1e57`]** Rapport : ligne de résumé sous chaque titre de bloc (`.bloc-depli-resume` + classe `.a-resume`). « Analyse par dimension » affiche le décompte par couleur, jamais additionné. « Garder comme repère » + « CV anonymisé » regroupés sous « Pour information », rectangles pointillés. Vérifié navigateur.
- **[CLOS - `2a01513` / `b5f9faa` / `6031a33`]** Pages d'introduction : Carnet + Lexique (pages routées) ; Lettre + Entretien (fenêtre ERIP défilante). Voir la section « Généraliser le patron page d'introduction » plus haut.
- **[CLOS - `a490821`]** Découverte : maquette de présentation + proposition de parcours consolidé (docs).
- **[CLOS - `dc57b90` (points 1-2) + `17a8239` (ligne de cadrage + 2e compactage)]** Mini-chantier `prompts/bilan-v1.md` saturé.
  1. **Carte 3 sans question sur un CV léger** : FAIT dans `dc57b90` — `phraseAChiffrer` ne vise plus seulement un chiffre (« si le CV est trop peu détaillé pour qu'un chiffre ait un sens, vise un autre élément concret absent : ce que la personne faisait, pour qui, avec quoi, dans quel contexte, avec quel résultat ») + règle section 4 « sur un CV pauvre, privilégier les recommandations sur le contenu d'une expérience précise ».
  2. **Synthèse trop maigre** : FAIT dans `dc57b90` — `resumeNarratif` = 4 à 6 phrases.
  3. **Ligne de cadrage renforcée (compte perso)** : FAIT dans `17a8239` — la ligne 8 (2e ligne de cadrage, déjà forte) gagne « sans annoncer d'étapes ni procéder par échanges successifs : ... produit en une seule réponse, pas un dialogue » (pendant du point 5a, qui avait exclu ce fichier des 20 prompts). Ligne 1 (standard maison des 16 prompts) non touchée.
  - **2e passe de compactage** (`17a8239`) : 21832 → 21299 octets. Redondances retirées (« tu ne vois pas le document » 2×→1× ; plafond amplificatrices restaté en section 4 → renvoi section 2 ; section 1, `extraitConcerne`, `phraseAChiffrer`, axes Posture/Adéquation resserrés). **Zéro règle/exception/placeholder perdu** — 641 tests verts (50 garde-fous bilan). Aller plus loin = restructurer = risqué avant validation, non fait.
  - **CLÔTURÉ 2026-09-01** : Denis a passé le protocole de test ChatGPT (compte perso connecté), conforme. Le chantier « bilan-v1 saturé » est clos.

---

## Nuit du 31/08/2026 (2e partie) — consignes complementaires de Denis (delegation complete)

- **[CLOS - `fae2f9a`]** "Retour" de la page de presentation d'un module ramene au **menu Boite a outils** (`retourVersBoiteAOutils()` = `naviguerVers('cv')` + `ouvrirChoixPreparationAccueil()`), jamais a l'accueil principal (le bouton "Accueil" de la barre reste pour ca). Cable sur : Bilan, Carnet, Lexique, Coherence, Reperes (pages d'intro). Ecrans de travail inchanges.
- **[CLOS - `801ba08`]** Lettre / Entretien / Decouverte : **vraies pages de presentation routees** (routes `co-lettre`, `prepa-entretien`, `decouverte-intro`) au lieu de fenetres ERIP. `htmlPageIntroModuleParcours()` + `brancherPageIntroModuleParcours()` + `_introBlocSynthese()` dans `data/metiers.js`. Chaque page affiche, si un travail existe deja, une **synthese** (lettre : objet + "Copier le texte" + "Voir ma lettre" ; entretien : extrait de la presentation + "Voir ma preparation" ; decouverte : "Voir mon CV") et un CTA adapte ("Recommencer..."). Le CTA lance la cascade de fenetres EXISTANTE (jamais touchee). Tuiles Boite a outils : `naviguerVers(route)`. `tests/_domStub.js` : stub des 3 nouvelles fonctions de page.
- **[CLOS - `dc57b90`]** `prompts/bilan-v1.md` : compactage (~23,7 -> ~21,7 Ko malgre 2 ajouts ; garde-fou catalogue<->prompt vert ; 11 placeholders intacts) + Carte 3 sur CV leger (`phraseAChiffrer` peut viser un detail concret, pas seulement un chiffre ; nouvelle regle section 4 "sur un CV pauvre, privilegier les recommandations sur le contenu d'une experience precise") + `resumeNarratif` = 4 a 6 phrases. Cote app : ecran "Vos chiffres" -> "Vos precisions", encart reformule. **Validé par Denis sur de vrais CV le 2026-09-01, conforme.** Mémoire `project_prompt_bilan_v1_sature` a jour.
- **[CLOS - `6595652`]** `docs/MAQUETTE_DECOUVERTE_CONSOLIDE_2026-08-31.html` : maquette du parcours consolide (5 ecrans) avec **barre de navigation visuelle du module** (5 pastilles faite/en cours/a venir). Proposition, pas d'implementation. Voir `docs/CONSOLIDATION_DECOUVERTE_PROPOSITION_2026-08-31.md`.
- **[CLOS]** Implementer le parcours Decouverte consolide d'apres la maquette : **fait** -- fusions 1+2+3 (`5ee40e3`) et 6+7, parcours devenu une page routee (`5b022a2`), barre de navigation du module en place et etendue avec le repere "Mon CV" jusqu'a la page "Creer mon CV" (`2aaa648`). Voir la section "Nuit du 31/08 (5e partie)" plus haut. Reste (petit, non bloquant) : les sections de la page "Preparer" sont toujours visibles, pas de vrais accordeons `.bloc-depli` (l'ecran se re-rend a chaque choix, un `<details>` se refermerait) -- a rediscuter avec Denis seulement si ca le gene a l'usage.

---

## Nuit du 31/08/2026 (3e partie) — barre de navigation + maquette Decouverte + plan chantier

- **[CLOS - `da77aaa`]** `barreEtapesModule(etapes, indexCourant)` (js/app.js) : barre d'etapes generique, meme langage visuel que `bilanBarreEtapes` (pastille courante jaune + halo, chevrons `bi-chevron-double-right` bleus, etape faite verte). En tete des 3 pages routees Lettre / Entretien / Decouverte (`htmlPageIntroModuleParcours`), en sourdine tant que le parcours n'est pas commence.
- **[CLOS - `f77769e`]** Maquette `docs/MAQUETTE_DECOUVERTE_CONSOLIDE_2026-08-31.html` : ecran "Choisir l'assistant" -> distinction "Sans compte a creer" / "Avec une connexion" (comme le Bilan). Relecture ligne a ligne de `decouverteParcours.js` : l'ecran 1 de la maquette etait trop simplifie -> corrige (domaine, stage, bloc "recherche de candidature" avec entreprise/site/lien d'offre, civilite + nom du recruteur, couleurs de l'entreprise). Nouveau paragraphe "3bis. Zero regression" dans `docs/CONSOLIDATION_DECOUVERTE_PROPOSITION_2026-08-31.md` : table ecran par ecran des 8 etapes actuelles -> ou vit chaque fonction. Conclusion : aucune fonction perdue.
- **[CLOS - 2026-09-10, verifie contre le code]** Page "Preparer" depliante (modele `htmlBilanPreparer`) pour Lettre, Entretien, Decouverte (fusion ecrans 1+2+3), Coherence. **Entierement absorbe par le chantier "elimination des fenetres de depot CV" (`docs/CHANTIER_ELIMINATION_FENETRES_DEPOT_CV.md`, CLOS 2026-09-04/07)** : `pageCoLettre()` (3 pages routees), `pagePrepaEntretien()` + `_prepaEntretienRendrePreparer()` (3 blocs `<details>` sur une page), `ctHtmlCollecte()` + `ctRendreCollecte()` (page a 4 blocs depliants), et fusion Decouverte 1+2+3 -> `pageVotreParcours`/"Preparer" (`5ee40e3`). `ouvrirAssistantDepotCV` n'est plus appelee qu'en repli scan/photo, jamais modifiee. Le CTA ne lance plus de cascade de fenetres. Plan `docs/CHANTIER_PAGES_PREPARER_MODULES.md` = superviseur, garde comme trace.

---

## Nuit du 31/08/2026 (4e partie) — Decouverte devient une page + plan "supprimer la fenetre de depot du CV"

- **[CLOS - `dcdb771`]** Barre d'etapes visuelle dans le parcours Decouverte actuel (additif, 8 etapes -> 5 reperes).
- **[CLOS - `5b022a2`]** **Parcours "Decouvrir mes competences" : de fenetre modale a PAGE routee** (`decouverte`). afficherEtape() ecrit dans #app (chrome de page : "Quitter (reprendre plus tard)", barre d'etapes, contenu inchange, barre Retour/Continuer, barre fixe du bas). La machine a etats reste la closure de ouvrirDecouverteCompetences() ; `_decouverteRenduEtape` + `_decouverteNumeroCourant` exposes ; `pageDecouverte()` redessine l'etape courante. masquer/reafficher/fermer adaptes. tests/_domStub.js : stub `pageDecouverte`. 608 verts, verifie navigateur (etapes 1->4 en page, "Quitter" -> presentation -> "Reprendre" -> reprise a la bonne etape).
- **[CLOS - `8ae5bd7`]** **Refonte visuelle du parcours Decouverte** : chaque etape dans une carte `.cv-section` ; `.carte` des choix resserrees + teinte accent a l'actif (plus de bleu pale `#F6FAFF`/`#CFE2FF` en dur) ; pastille "choix retenu" -> `.decouverte-pastille-choisie` (jetons) ; boutons "Modifier" -> `var(--warning-*)` ; separateurs -> `var(--border)`. Overrides CSS scopes a `.decouverte-parcours` (jamais `.carte` ailleurs). Aucune fonction/donnee touchee. 608 verts, verifie navigateur.
- **[CLOS - 2026-09-10, verifie contre le code]** Supprimer la fenetre `ouvrirAssistantDepotCV` qui "pop" pour deposer le CV dans **Lettre / Entretien / Coherence**, l'integrer comme bloc d'une page depliante "Preparer". **FAIT dans le chantier "elimination des fenetres de depot CV"** (`docs/CHANTIER_ELIMINATION_FENETRES_DEPOT_CV.md`) : Co-construire ma lettre (etape 1/3, CLOS 2026-09-04), Coherence de mon dossier (etape 2/3, CLOS 2026-09-04), Preparer un entretien (etape 3/3, CLOS 2026-09-04, puis 3 ecrans fusionnes en une page `_prepaEntretienRendrePreparer()` le 2026-09-07). `ouvrirAssistantDepotCV` jamais modifiee, appelee seulement en repli scan/photo. Plus aucune fenetre de saisie de document dans ces 3 cartes.

- **[CLOS - `8ae5bd7` / `5ee40e3`]** **Parcours Decouverte : refonte visuelle + fusion d'ecrans.** (a) Chaque etape dans une carte `.cv-section`, `.carte` des choix resserrees + teinte accent, plus de couleur bleu pale codee en dur, boutons "Modifier" en `var(--warning-*)` -- overrides scopes a `.decouverte-parcours`. (b) **Fusion 1+2+3 -> page "Preparer"** (3 sections : recit obligatoire / coordonnees nom+prenom obligatoires / visee FACULTATIVE) ; **fusion 6+7 -> ecran "Vos competences"** (validation + bloc "Quelques precisions" si questions). `obtenirDefinitionEtape` : 1/4/5/6/8. Contenu et cablage des sections = fonctions d'origine, non reecrits. 608 tests verts, verifie navigateur (saisie preservee au re-rendu, navigation coherente). **Reste** : la maquette montre des accordeons `.bloc-depli` -- ici sections toujours visibles (l'ecran 1 se re-rend a chaque choix, un `<details>` se refermerait) ; a rediscuter avec Denis si genant.

- **[CLOS - `60eb040`]** **Decouverte : accompagnement enrichi du parcours.** Retour Denis : le module etait "creux, vide", "plus de vieux que de neuf", ~6/20, alors qu'il sera le plus utilise (personnes sans CV). 2 helpers aux jetons du theme : `_decouverteEncart(icone, texteHTML, variante)` (filet gauche + icone objet, variantes accent/ok/warn) et `_decouverteBandeau(texteHTML)` (bandeau rassurant centre). Enrichissements : **Preparer** = bandeau d'ouverture + cadrage par section (recit : intro chaleureuse, liste "des idees de choses a raconter", astuce Win+H en encart, encart de reassurance ; coordonnees : rappel confidentialite en encart ; visee : "a quoi ca sert" + encart "vous ne savez pas encore ? c'est normal"). **Choisir votre assistant** = bandeau + encart "Sans compte = le plus simple". **Coller la reponse** = encart mode d'emploi de la copie. **Vos competences** = bandeau qui celebre ("vous ne partiez pas de rien", nombre d'experiences) + encart sur le choix des formulations. **Etape 8 renommee "Completer mon CV"** + bandeau "derniere etape avant votre CV". Deduplication : intros propres a `etapeRecit`/`etapeIdentite`/`etapeDecouverte` retirees, le cadrage vient de l'enrobage `etapePreparer`/`etapeCompetences`. 608 verts, verifie navigateur clair + sombre. NB : inputs `.form-control` blancs en mode sombre = comportement **app-wide** (present aussi dans le Bilan), hors perimetre.

### Nuit du 31/08 (5e partie) — retours Denis sur les fenetres du parcours Decouverte (6 points)

- **[CLOS - `bb724c7`]** Point 1 : zone de saisie du recit `rows` 10 -> 5 (hauteur ~134 px) ; pulse **vert** (`pulseZoneRecitDecouverte`, `var(--success)`, 1.8s x 6 ~11 s) a la **premiere** arrivee sur "Preparer" seulement (drapeau `_decouvertePulseRecitVu`, remis a zero par `fermerDecouverteCompetences`). Vert demande explicitement par Denis. `prefers-reduced-motion` neutralise.
- **[CLOS - `854bf4f`]** Point 4 : ecran "Choisissez votre assistant" de Decouverte -> **composant partage `htmlChoixAssistantBilanCorps`** (chantier M2 du Bilan, 2026-08-30) au lieu du rendu maison a couleurs codees en dur. Groupes etiquetes, sous-labels, accordeon "Ce qui va se passer + vos donnees". Cablage `[data-assistant-decouverte]` (copie presse-papiers + transition) conserve tel quel.
- **[CLOS - `30face6`]** Point 2 : ecran "Vos coordonnees" modernise -> **libelles `<label>` visibles au-dessus de chaque champ** (le placeholder seul disparaissait a la frappe) + 3 groupes titres ("Votre identite" / "Pour vous joindre - facultatif" / "Ou vous habitez - facultatif"). ids et `data-civilite` inchanges. Direction choisie par Denis (option "libelles + groupes").
- **[CLOS - `2762c7d`]** Point 3 : ecran "Ce que vous visez" -> **cartes riches** pour les 3 choix (metier/domaine/stage) : icone dans un rond teinte accent + titre + une ligne d'explication. `carteChoix()` gagne un 6e param `description` (retro-compatible). CSS scope `.decouverte-parcours` (`.carte-choix-decouverte`, `.carte-choix-icone`, `.carte-choix-desc`), jetons clair + sombre. Comportement de selection inchange. Direction choisie par Denis (option "cartes riches").
- **[CLOS - `3721478` (b/c/d) + `12451a7` (a)]** Point 5 : filet de securite "compte perso" (l'assistant commente le prompt / repond en texte au lieu du JSON quand on est connecte a son compte).
  - **5b/c/d `3721478`** : constantes `DECOUVERTE_PHRASE_RELANCE` / `DECOUVERTE_PHRASE_REDEMANDER_JSON` + helpers `_decouverteEncartPhrasePrete` / `_decouverteBrancherCopiePhrase` (bouton "Copier cette phrase" -> `copierTexteVersPressePapier`). Encart "Si vous utilisez votre propre compte" sur l'ecran "Choisissez votre assistant" (5b, prevention) ; meme encart, toujours visible, sur "Collez la reponse" (5c, prioritaire) ; et sur echec de `executerAnalyseInitiale` (5d), le message d'erreur est complete par la phrase prete a coller pour redemander le bloc JSON. Jetons clair + sombre. Jamais le mot "IA".
  - **5a `12451a7` + `17a8239`** : remplacement EXACT (script Node) de la 1re ligne de cadrage, identique dans **20 prompts actifs**, par une version plus ferme ("...applique-la directement et entierement des maintenant, sans donner ton avis sur sa formulation, sans demander de confirmation, sans annoncer d'etapes, en une seule reponse..."). `bilan-v1.md` traite a part dans `17a8239` (sa ligne 8, deja forte, renforcee ; 2e compactage -533 octets) -- voir la section "mini-chantier bilan-v1.md" plus haut. `bilan-v1-extraction-prototype.md` exclu (non actif : aucun code ne le charge, seulement 2 commentaires + 1 doc ABANDONNE le citent -> a garder comme trace de decision). 641 tests verts.
- **[CLOS - `2aaa648` / `fa54eb5` / `1883673`. Plan : `docs/CHANTIER_DECOUVERTE_PAGE_ACTION.md`]** Point 6 : `pageResultats()` ("Passons a l'action") adaptee quand `depuisDecouverte` (deplace tout en haut de la fonction, avant `docActif`) :
  - **`2aaa648`** : `DECOUVERTE_NAV_ETAPES` gagne un 6e repere "Mon CV" (visible en gris des le debut du parcours) ; barre du haut = `barreEtapesModule(DECOUVERTE_NAV_ETAPES, dossier.cvTermine ? 6 : 5)` au lieu de `afficherProgression('resultats')`.
  - **`fa54eb5`** : `docActif` force sur `'cv'` ; selecteur "Choisissez votre action" (CV/Lettre/Entretien) masque (un seul choix = pas de selecteur).
  - **`1883673`** : **une seule barre** -- `ligneEtapesAction` (Adaptation -> Exporter) n'est plus affichee depuis Decouverte (retour Denis : "je ne veux pas de sous-barre... a titre informatif si un repere couvre plusieurs ecrans") ; titre "Passons a l'action" -> "Creer mon CV" (fixe, ne change pas par accordeon).
  - **Ne jamais forker** : tout est dans des branches `if (depuisDecouverte)`. Verifie a CHAQUE sous-etape : les 3 entrees de `pageResultats` (Decouverte / Mon projet / import session) -- seule l'entree Decouverte change. 641 tests verts, aucune erreur console.

---

## 31/08/2026 (soir) — Module « Comparer mes pistes » (aide a la decision d'orientation) : conception figee, implementation lancee

Conception, maquette de flux v8 (`docs/MAQUETTE_COMPARER_PISTES_FLUX_2026-08-31.html`), banque de questions, situations + sources, 5 prompts et 3 tests reels du prompt de collecte : tous menes avec Denis le 31/08 (l'autre compte Claude tenait le depot). Spec complete : **`docs/CHANTIER_AIDE_DECISION_ORIENTATION.md`** (reecrit, section 8 = plan bloc par bloc).

- **[CLOS - ce commit]** Consolidation dans le depot : `prompts/comparer-detection.md` / `comparer-collecte.md` / `comparer-meme-categorie.md` / `comparer-situations.md` / `comparer-aller-plus-loin.md` ; maquette de flux copiee dans `docs/` ; `CHANTIER_AIDE_DECISION_ORIENTATION.md` reecrit (conception figee + plan) ; `BRIQUES_COMMUNES.md` (repertoire freins gagne "Comparer mes pistes" comme consommateur) ; en-tete `data/freins.js` (perimetre etendu) ; `LANGAGE_VISUEL_COMMUN.md` (ligne maquette). Aucun code applicatif touche.
- **[CLOS - 2026-09-01, blocs 1 a 9]** Module `modules/comparer-pistes/index.js` (route `comparer-pistes`, page routee, meme patron que Decouverte). 9 ecrans : ajouter mes pistes (3 facons) -> pistes comprises (detection prompt 0 + etiquettes + routage deterministe + liaison `data/freins.js` + bloc B 10 series) -> collecter (prompt de collecte avec indicateurs codes + garde-fou de fiabilite) -> une fiche par piste (encadre "a verifier" en liste sourcee, bloc C) -> deux angles (aiguillage mixte) / superposer (reglettes construites depuis la collecte, sans aller-retour obligatoire) / dans le temps (frise prompt 3) -> aller plus loin (prompt 4, optionnel) -> ce que je retiens (points conseiller, tableau des sources, garder une piste via `reperesBoutonAncre`, copier le recapitulatif). CTA de `pageIntroAideDecision` **active** + texte mis a jour (retire ce qui ne collait plus, sans alourdir). Recherche accueil branchee (`MODULES_RECHERCHABLES`). Suivi d'usage (`_comparerTrack` : session, ecran atteint, prompt copie, reponse collee, garde-fou, recap copie). 5 prompts `prompts/comparer-*.md`. 639 tests verts (~40 nouveaux), verifie navigateur clair + sombre a chaque bloc. Commits `230fa53` -> `c7a98e3`.
- **[A FAIRE - petit chantier independant, voir la section « Petits chantiers » ci-dessous]** Video courte "activer la recherche web sur ChatGPT" (emplacement `htmlDeclencheurDemoVideo('comparer-activer-recherche-web')` deja pose dans Comparer). *Test terrain avec le public cible : en cours cote Denis, pas un item de liste.*
  - *[FAIT - Denis, 2026-09-01]* CAP Carreleur mosaiste : confirme, il existe bien au reseau BTP CFA Nouvelle-Aquitaine (qui couvre la Dordogne) -- `https://www.btpcfa-na.fr/formation/cap-carreleur-mosaiste/`. C'etait une donnee d'exemple de la maquette de flux, aucun code a modifier.
- **[CLOS - 2026-09-01, bloc 10]** Point d'entree transversal : bouton "Comparer cette piste" (`comparerBoutonPanier`) sur les cartes metier de la recherche + le panneau `ouvrirPanneauChoixParcours`. Panier `_comparerPanier` (max 3), barre fixe "N pistes a comparer", `ouvrirComparerPistes()` lit le panier et pre-remplit les pistes (etiquette `metier`). Handler en capture pour ne pas ouvrir le parcours de candidature de la carte. 640 tests verts, verifie navigateur clair + sombre.
- **[CLOS - 2026-09-01] Panier visible a l'interieur du module (retour Denis).** Les pistes mises de cote depuis une fiche metier / la recherche apparaissaient seulement a l'ecran 0 bis : la personne croyait sa selection perdue. Recap "Pistes deja ajoutees" en tete de l'ecran 0 (nom + pastille de couleur + retrait), renommage / couleur / etiquette restant a l'ecran 0 bis. Deux puces ajoutees a `pageIntroAideDecision` (arrivee avec des metiers pre-remplis ; forme de comparaison choisie automatiquement et corrigeable). Commit `3709f36`. 640 tests verts, verifie navigateur clair + sombre.
- **[CLOS - 2026-09-01] Mettre plus de 3 pistes de cote, en choisir 2 ou 3 (retour Denis).** Panier `_comparerPanier` : max 3 -> max 8 (`_COMPARER_PANIER_MAX`). Au-dela de 3 pistes, l'ecran 0 devient un choix : cocher les 2 ou 3 a regarder ensemble ("2 c'est l'ideal, 3 au maximum"), les autres restent en reserve (`etat.pistesReserve`), rien n'est elimine. Echange piste active / piste de cote a tout moment ; les 3 actives reprennent les 3 couleurs de la palette (`comparerRecolorer`). `etat.pistes` reste l'ensemble <= 3 analyse -> ecran 0 bis et suite inchanges. "Continuer" bloque tant qu'il y a moins de 2 pistes cochees. Commit `5bb4f77`. 640 tests verts, verifie navigateur (panier a 5, echange, blocage a 1, chemin "Je nomme" inchange, sombre).
- **[CLOS - 2026-09-01, autre compte, `4f73468`]** Bandeau de reprise et bouton "Revoir la presentation" : "Comparer mes pistes" aligne sur Coherence dans le chantier `docs/CHANTIER_BOUTON_REVOIR_PRESENTATION.md` (marque CLOS `a7ae3f6`). "Quitter (reprendre plus tard)" -> bouton partage "Revoir la presentation" (haut a gauche, icone `bi-signpost-split`) ; encart "Continuer / Recommencer" + gel + pulse quand on revient dans le module via le CTA de la presentation avec une comparaison en cours ; presentation en detour (bouton "Revenir au module" haut + bas, "Retour" revient au module) ; flags `_comparerDetourPresentation` / `_comparerReprisePendante` remis a zero par `comparerReinitialiser()`. Verifie en navigateur le 2026-09-01 que ce jeu de fonctions coexiste sans regression avec le panier visible + le choix des 2-3 pistes (points A/B ci-dessus) : detour aller-retour sans perte d'etat, reprise avec gel puis "Continuer" qui leve le gel, aucune erreur console.
- **[CLOS - 2026-09-01] "Ma comparaison" : le sac persistant (retour Denis, maquette `docs/MAQUETTE_COMPARER_PISTES_SAC_PERSISTANT_2026-09-01.html` validee).** 4 blocs, 1 commit chacun.
  - **bloc 1 (`9495faa`)** : fusion du panier et de la session en un objet unique `_comparerEtat` au niveau module (via `comparerEtatNeuf()`), qui persiste entre deux entrees et se nourrit depuis l'exterieur (`comparerPanierAjouter` ecrit dedans). Plus de tableau `_comparerPanier`. `comparerLancerAvecPanier()` n'efface plus la session (ajouter une piste depuis une fiche metier alors qu'une comparaison est en cours ne la detruit plus : bug corrige). `comparerReinitialiser()` est le seul point qui remet `_comparerEtat` a null. Barre fixe : "Ma comparaison : N piste(s)" / "Ouvrir".
  - **bloc 2 (`e669637`)** : marquage "deja etudie le ..." pose a l'etape Collecter quand la reponse de l'assistant est validee (`comparerMarquerEtudiees`, appariement par nom). Date de derniere etude (re-etude = mise a jour). Badge par piste + phrase de rappel dans l'encadre "Ma comparaison" ; `comparerDateCourte()` ("1er sept."). N'empeche jamais de re-etudier une piste. Correctif : la barre pouvait rester visible sur le module -> `comparerMajBarrePanier()` en fin de `ouvrirComparerPistes()`.
  - **bloc 3 (`117fdd8`)** : deux suppressions. Croix par piste (deja en place). "Tout effacer" : bouton en bas de l'encadre (ecran 0) + lien de la barre fixe renomme ; les deux ouvrent la meme confirmation (`comparerToutEffacer`) qui nomme ce qui part et rappelle que Mes Reperes / Mon Carnet ne sont pas touches ; pas d'annulation.
  - **bloc 4 (`462d9db`)** : sauvegarde disquette. `_comparerEtat` EST l'etat sauvegarde (clone JSON, null si vraiment rien) ; `comparerPistesExporter/RestaurerEtatDepuisSauvegarde` font le vrai travail ; le pont `js/app.js` etait deja branche -> le sac ride l'export / import de session et Reinitialiser / Restaurer. Verifie navigateur : round-trip JSON complet, reprise a l'ecran sauvegarde.
  - 641 tests verts (+1 `comparerDateCourte`). Smoke test integre : sac de 4 pistes -> ecran 0 (4 cases, 3 cochees) + bouton "Revoir la presentation" + "Tout effacer" + barre cachee, detour aller-retour sans perte. Coexiste avec l'alignement "bouton presentation" (`4f73468`).
  - **Suivi d'usage, 3 evenements ajoutes (choisis avec Denis, 2026-09-01)** : `comparer_piste_ajoutee_depuis_fiche` (une piste entre dans la comparaison depuis une fiche metier / la recherche -> le point d'entree transversal sert-il ?), `comparer_forme` `{forme: cote_a_cote | dans_le_temps | mixte | orientation_d_abord}` (ce que les gens comparent vraiment), `comparer_piste_reetudiee` `{nombre}` (une piste deja etudiee est relancee dans une nouvelle comparaison -> le sac persistant sert-il vraiment ?). Meme discipline que l'existant : aucun contenu, seulement des compteurs et des libelles fermes, `try/catch` non bloquant. Verifie navigateur : les 3 se declenchent au bon moment, le 1er collage n'envoie pas `comparer_piste_reetudiee`, le 2e oui.
- **[A FAIRE - futur]** Fusion eventuelle des prompts 2+3+4 : **ecartee** (voir echange Denis 2026-09-01 : elle ne reduit pas le nombre de passages, la specialisation des prompts est un atout ; le vrai gain a deja ete fait en rendant le prompt 2 automatique). Entree depuis un projet enregistre (pas de besoin avere aujourd'hui : garder une piste passe deja par Mes Reperes via `reperesBoutonAncre`).

<!-- NB 2026-08-31 : le bloc detaille du chantier "Decouverte accompagnement enrichi" (60eb040) qui s'etait retrouve colle ici par une edition concurrente a ete remis a sa place, section "Nuit du 31/08 (5e partie)" plus haut. -->

---

## Nuit du 31/08 (6e partie) — bug import + retours divers Decouverte

- **[CLOS - `523e738`]** Bug reel (Denis) : sur les ecrans "Collez la reponse" (Bilan, Coherence), apres un 1er echec d'import, tout nouvel essai avec le MEME contenu valide echouait (statut `echec_parsing` refuse par la garde `!== 'genere'`) et le message d'erreur ne partait pas. Corrige : les 3 orchestrateurs (`bilanSoumettreReponseDiagnostic`, `ctSoumettreReponseDiagnostic`, `ctSoumettreReponseEntretienAvance`) acceptent `echec_parsing` a la reprise ; `onEffacer` + `onCollerManuel` + remise a blanc en tete de handler sur les 7 configs de collage. 644 tests verts (+3). Repertorie dans `docs/ERREURS_ET_BUGS.md`.
- **[CLOS - `20db6e6`]** Retour Denis (captures) : pilules (civilite, metiers/secteurs), onglets "Experience N", bouton "Je valide" faisaient "ancien modele". Modernises et passes aux jetons du theme, scopes a `.decouverte-parcours`.
- **[CLOS - 2026-09-10, vérifié contre le code]** Encart multilingue sur les pages d'intro. **FAIT** (`3148512` + version courte `5685995`) : `htmlEncartMultilingue(courte)` (`data/metiers.js`) = `<details>` « Vous préférez une autre langue ? » + texte long validé (traduire la page + écrire dans sa langue + sortie française ; dictée : Windows+H = langue de l'ordinateur, sinon Google Docs / micro du téléphone). Posé sur les 7 pages d'intro de parcours via `htmlPageIntroModuleParcours` + Bilan + Cohérence ; version courte sur Carnet / Lexique / Repères. Jetons du thème, clair + sombre.
- **[A FAIRE - chantier Accessibilite, PAS maintenant]** Option "je ne suis pas francophone" (voir memoire `idee-mode-navigation-debutant-avance`). Quand active : envisager de produire aussi le CV et la lettre dans la langue de la personne, **pas pour envoi**, seulement pour qu'elle puisse lire / comprendre / verifier sa version francaise.
- **[PARKE - decision Denis 2026-08-31 : "on ne fait pas du tout, on n'attaque pas"]** (a) detection "saisie inexploitable" (texte incomprehensible -> l'assistant "analyse" quand meme, l'app propose d'importer dans les experiences pro) : faisable (drapeau `analyseImpossible` cote prompt + detection + ecran dedie cote code, sur les 1ers passages seulement) mais pas lance. (b) raccourci "CV en cours" sur l'accueil qui saute des etapes du parcours Decouverte non termine. (c) page action pas adaptee quand on y arrive hors `_decouverteVersResultats` (drapeau transitoire ; piste : `dossier.cvOrigine = 'decouverte'` persistant). Reste dans `docs/CHANTIER_DECOUVERTE_PAGE_ACTION.md` a reprendre plus tard.

## Nuit du 31/08 (7e partie) — retours Denis suite (plan `docs/CHANTIER_RETOURS_DECOUVERTE_ET_TRANSVERSE_2026-08-31.md`)

- **[CLOS - `fe7ce79`]** Decouverte : `afficherEtape()` ne restaure la position de defilement QUE pour un re-rendu du meme ecran (drapeau `_decouverteEtapeRendueDom`). Changement d'ecran / 1re entree / retour depuis 'resultats' -> `scrollTo(0,0)`. Corrige "j'entre dans le module et je suis au milieu-bas, sous la zone verte".
- **[CLOS - `f5e8634`]** Chantiers 2+3 : `dossier.decouverteTerminee` (fait persistant, pose par `finaliserEtNaviguerVersResultats`). `pageResultats()` : `depuisDecouverte = window._decouverteVersResultats || dossier.decouverteTerminee` -> page Action adaptee ("Creer mon CV", barre du module) sur TOUS les chemins d'entree, pas seulement le drapeau transitoire. Banniere "Voir mon CV" sur `pageDecouverteIntro` : affichee UNIQUEMENT si `decouverteTerminee` (avant : des le 1er import, elle sautait les etapes Competences/Completer + l'encart de reprise + menait a l'ancienne page). Reset du drapeau : `fermerDecouverteCompetences` (recommencer), "Creer un nouveau CV" / "J'ai deja un CV" / "Mettre a jour" (carte Mes documents + panneau guide). `ouvrirAssistantDepotCV` **pas touchee** (reset aux points d'entree). 644 verts, 3 entrees de `pageResultats` verifiees.
- **[CLOS - `3148512`]** Chantier 6 (parcours) : `htmlEncartMultilingue()` (brique partagee) = `<details>` "Vous preferez une autre langue ? Cliquez ici pour plus d'informations" + texte long valide (traduire la page + ecrire dans sa langue + sortie francaise ; dictee : Windows+H = langue de l'ordi, sinon Google Docs / micro du telephone). Sur les 7 pages d'intro de parcours (Decouverte/Lettre/Entretien/Comparer/ATS via `htmlPageIntroModuleParcours` + Bilan + Coherence). Jetons du theme, clair + sombre. **A inscrire dans les futures maquettes** (dont les 3 parcours "Mes documents").
- ~~[A FAIRE] Chantier 6 (fin) : Carnet / Lexique / Reperes~~ -> **CLOS `5685995`**, voir ligne plus bas (version courte, option 2 de Denis).
- ~~[A FAIRE - bloque sur repro] Chantier 5~~ -> **CLOS `5685995`**, voir ligne plus bas.
- ~~[A FAIRE - le plus gros] Chantier 4 : saisie inexploitable~~ -> **CLOS 2026-09-01** (4 prompts sur 6, Lettre/Entretien ecartes), voir plus bas.
- **[NOTE]** ATS (a la construction du module) : recevra l'encart reprise + gel comme Bilan/Coherence/Decouverte/Comparer/Regard (exigence 5 ajoutee a `docs/CHANTIER_ATS.md`). Carnet/Lexique/Reperes : PAS de gel (outils de captation permanente, decision Denis 31/08).
- **[CLOS - `5685995`]** Chantier 5 : a l'echec d'un import Decouverte, un SEUL bloc d'aide (avant : encart "compte perso" toujours visible + encart "redemander le JSON" separe -> doublon). Deux lignes : "s'il a surtout commente/discute" -> `DECOUVERTE_PHRASE_RELANCE` ; "s'il a repondu hors format JSON / sans elements" -> `DECOUVERTE_PHRASE_REDEMANDER_JSON`. Message `fragments: []` : ne blame plus le copier-coller. Helpers `_decouverteBlocEchecImport` / `_decouverteLignePhraseEchec` / `_decouverteBrancherBlocEchecImport`. Le cas "texte inexploitable" (`fragments: []` = signal implicite) sera traite en ecran dedie au Chantier 4.
- **[CLOS - `5685995`]** Chantier 6 (fin) : Carnet / Lexique / Reperes -> `htmlEncartMultilingue(true)` version courte (traduire la page + ecrire dans sa langue seulement -- pas de CV/lettre/dictee). Decision Denis "option 2".
- ~~[A FAIRE - le seul restant] Chantier 4 : saisie inexploitable~~ -> **CLOS 2026-09-01** (ligne ci-dessous). Tout le plan `docs/CHANTIER_RETOURS_DECOUVERTE_ET_TRANSVERSE_2026-08-31.md` (chantiers 1 a 6) est termine.
- **[CLOS - 2026-09-01, 4 prompts sur 6 ; Lettre/Entretien ecartes sur decision Denis ; TESTE par Denis, conforme]** Chantier 4 (saisie inexploitable). Meme mecanisme partout : sortie de secours `{ "analyseImpossible": true, "message": "..." }` cote prompt -> detectee cote code -> ecran dedie bienveillant qui ramene au depot/recit, jamais un message d'echec de format ni un document bati sur du vide. Denis a teste le 2026-09-01 : volet Decouverte (vrai assistant + texte incomprehensible), `bilan-v1.md` (vrais CV), protocole ChatGPT "compte perso" -- les trois conformes.
  - **[CLOS - `c4ae281` / `fb62ca5`]** Decouverte : `decouverte-competences.md` + `analyserReponseDecouverte` (+ `fragments: []` = meme signal) -> `_decouverteBlocSaisieInexploitable` ("Revenir a mon recit" -> etape 1 + croix "tout effacer", pistes = questionsCiblees). Umami `decouverte_saisie_inexploitable`. **Teste par Denis 2026-09-01, conforme.**
  - **[CLOS - `b001154`]** Bilan : `bilan-v1.md` + `diagnosticResponseParser` -> erreur metier `SaisieInexploitable` -> ecran "le texte analyse ne ressemble pas a un CV" + "Revenir au depot du CV" (`bilanEntrerPreparation`). 1 test de garde parser.
  - **[CLOS - `947263d`]** CV ("Passons a l'action") : `cv.md` + `analyserReponseIACV` -> `{succes:false, analyseImpossible:true}` -> ecran "il manque des informations" + "Completer mes informations" (`naviguerVers('objectif')`). Umami `erreur_import` raison `profil_inexploitable`.
  - **[CLOS - `ad5bfe4`]** Coherence de mon dossier : `coherence-transversale.md` + `ctParserReponseDiagnostic` -> `SaisieInexploitable` -> ecran "pieces non exploitables" + "Revenir au depot" (`ctDemarrerCollecte`). 1 test de garde parser. **Uniquement** le handler diagnostic, pas l'entretien avance.
  - **[ECARTE - decision Denis 2026-09-01 : "on ne touche pas Lettre/Entretien"]** Lettre (`lettre-v1.md`) et Entretien (`entretien.md`) : prompts **conversationnels** (seance de dialogue progressif, pas une analyse en un coup), lances **apres** le travail CV. Pas de bloc JSON unique de decision ; le cas "rien d'exploitable" n'y arrive quasiment jamais. Detail dans `docs/CHANTIER_RETOURS_DECOUVERTE_ET_TRANSVERSE_2026-08-31.md`.
  - **Croix "tout effacer"** : sans objet pour Bilan / CV / Coherence (ils repartent d'un depot neuf, aucune zone de texte a vider a la main). Specifique a Decouverte, qui revient sur la meme zone de recit.

## Session du 2026-09-12 — audit croise Lexique/Cadre/Chiffres + zones d'emploi

**Contexte** : audit demande par Denis sur les 3 modules "Se tenir informe" (Lexique, Comprendre le cadre, Comprendre les chiffres) - zones d'ombre, enrichissement croise entre modules. A debouche sur plusieurs chantiers enchaines. Session tres longue, plusieurs comptactages de contexte a prevoir : **cette section est la reference a relire en priorite** si le fil se perd.

### Fait et ferme dans cette session

- **[CLOS]** Recherche d'accueil ne trouvait pas "Comprendre les chiffres" (aucune entree de recherche depuis sa creation) -> corrige, `data/baseConnaissancesERIP.js`.
- **[CLOS]** Mecanisme "Pour aller plus loin" entre Comprendre le cadre et le Lexique : bouton persistant (`definirRetourModuleExterne()`, `js/app.js`), champ `voir_aussi_lexique` sur les fiches Cadre, 5 paires posees (`emploi-csp`->`csp`, 4 fiches employeurs<->Lexique), toutes verifiees non dupliquees (2 ont ete enrichies au lieu d'etre coupees - voir memoire `feedback_lexique_jamais_couper_pour_lien_cadre`). Doublon entre "Retour a la recherche" et le bouton qui colle corrige (parametres independants de `lexiqueOuvrirFiche()`).
- **[CLOS]** Meme mecanisme generalise vers Comprendre les chiffres (`definirRetourModuleExterne()` accepte maintenant une `cibleRoute` quelconque, pas seulement 'lexique'). 2 paires posees : `emploi-cdd-cdi-saisonnier` et `emploi-interim` -> bloc "Comment on est embauche ici". Analyse de fond faite : pas d'autre bon candidat trouve dans l'etat actuel du contenu (RSA x2 juges trop faibles, rayon "creer" sans candidat).
- **[CLOS]** Portrait annuel (`portrait-2026.md`) : 3 des 5 lignes `[a verifier]` resolues avec de vraies donnees trouvees (garde d'enfant CAF, logement INSEE Flash NA, densite medecins generalistes INSEE - fichiers xlsx fournis par Denis). Les 2 restantes (sante/acces emploi, difficultes financieres reprise d'emploi) resteront **structurellement** non chiffrees : aucune source n'existe, ce n'est pas une tache a refaire.
- **[CLOS]** Prompt de veille (`outils/veille.html`, bloc H "Freins peripheriques") enrichi avec consignes de recherche precises (pas juste des adresses) pour ces 3 sources.
- **[CLOS - `0aee17b`]** Nouveau bloc **"Les ecarts a l'interieur du departement"** dans Comprendre les chiffres (Vue d'ensemble, niveau departement uniquement) :
  - Feasibilite verifiee : l'INSEE publie un taux de chomage par zone d'emploi, trimestriel, meme cadence que le reste du module (fichier officiel telecharge directement, `chomage-zone-t1-2003-t1-2026.xlsx`).
  - Dordogne = 3 zones (Bergerac, Perigueux, Sarlat-la-Caneda ; Nontron n'est PAS une zone d'emploi a part). Haute-Vienne = 2 zones (Limoges, Saint-Junien).
  - Maquette validee par Denis : `docs/MAQUETTE_ZONES_EMPLOI_2026-09-12.html`.
  - Contenu reel : `modules/comprendre-les-chiffres/contenu/zones-emploi.md` (format dedie : frontmatter `sources` cle/titre/url, corps `# <dep>` / `## <zone>` / `valeur:` / `sources:` / champs stats optionnels / paragraphes libres).
  - Code : `_comprendreLesChiffresParserZonesEmploi`, `_comprendreLesChiffresChargerZonesEmploi`, `_comprendreLesChiffresRenduZonesEmploi`, `_comprendreLesChiffresZoneBarre` (`modules/comprendre-les-chiffres/index.js`) + CSS dediee (`comprendre-les-chiffres.css`, section "BLOC : Les ecarts a l'interieur du departement").
  - Chaque zone a maintenant des **chiffres secondaires en barres** (retour Denis explicite : "ne pas noyer les chiffres dans le texte") : chomage au sens du recensement 2023, taux de pauvrete, residences secondaires, niveau de vie median, population, part emploi salarie, gros etablissements, part industrie, secteur qui domine (texte). Donnees reelles fournies par Denis (dossiers complets INSEE par zone d'emploi, copies-collees a la main - le telechargement automatique/xlsx ne marche pas sur ces pages, site INSEE lui-meme instable "une fois sur deux").
  - Trouvailles fortes a garder : Sarlat a un chomage trimestriel bas (7,2%) mais un chomage **au sens du recensement plus haut que Perigueux** (11,7% contre 10,9%) - preuve chiffree que son bon chiffre du trimestre est un instantane, pas une vraie solidite (piste demandee explicitement par Denis, France Strategie cite Sarlat nommement). Saint-Junien confirme sa relance (Hermes 2017, filiere cuir) sur 2 mesures independantes (6,5% trimestriel ET 10,7% recensement, le plus bas des 5 zones).
  - **Teste au navigateur, clair et sombre** (Dordogne + Haute-Vienne, barres + chiffres secondaires + sources cliquables + disparition correcte en region/France/UE/vue simple). Commite (`0aee17b`).

### [A FAIRE] - dans l'ordre de priorite discute avec Denis

1. **[CLOS - `0aee17b`]** "La lecture du territoire" comparait les 2 departements l'un a l'autre dans le meme paragraphe. Devenu un texte separe par territoire (parseur, rendu, contenu `2026-06.md`, regle absolue ajoutee au prompt de veille). Teste au navigateur sur les 4 niveaux.
2. **[CLOS]** Prompt de veille mis a jour pour couvrir `zones-emploi.md` (indicateur 7 de `assemblerChiffres()` dans `outils/veille.html` : meme fichier INSEE que l'indicateur 1, colonne zone d'emploi au lieu de departement, juste la valeur trimestrielle - le reste du contenu, plus structurel, n'a pas besoin d'un rafraichissement aussi frequent).
3. **[CLOS - re-verifie le 2026-09-12, diagnostic initial errone]** "Y a-t-il des offres visibles ?" : en verifiant en direct, la carte n'est PAS vide au niveau departement (elle affiche bien le chiffre), et le message "pas publie a ce niveau" deja code fonctionne correctement en region/France/UE (rien a faire de ce cote, comportement deja bon). Le vrai probleme trouve etait ailleurs : le "repere" de cette carte nommait l'autre departement ("le departement voisin (Haute-Vienne)...") - exactement le meme defaut que "la lecture du territoire" corrige juste avant. Corrige dans la foulee (commit `7b6bef1`), meme regle absolue ajoutee au prompt de veille pour tout texte libre.
4. **[CLOS - `2026-09-12`]** Meme mecanisme "ecarts" que les zones d'emploi, mais **au niveau region** (quels departements de Nouvelle-Aquitaine ont le meilleur/pire chomage, pourquoi). Rendu generique factorise : `_comprendreLesChiffresRenduEcartsGenerique(items, sourcesMap, opts)` dans `modules/comprendre-les-chiffres/index.js`, reutilise a l'identique par `_comprendreLesChiffresRenduZonesEmploi()` (niveau departement) et la nouvelle `_comprendreLesChiffresRenduDepartementsRegion()` (niveau region, gardee sur `niveau==='na'`) - aucune duplication. Contenu reel et sourcee : `modules/comprendre-les-chiffres/contenu/departements-region.md` (Deux-Sevres 6,0 % le plus bas, Lot-et-Garonne 8,2 % le plus haut, Dordogne et Haute-Vienne resituees entre les deux sans jamais se nommer l'une l'autre). Verifie au navigateur clair + sombre : bloc present et complet en region (barres + explications + 3 sources cliquables), absent en departement/France/UE. `npm test` 781 verts.
   - **[CLOS - 2026-09-12]** Niveau **France/regions** : meme mecanisme, un niveau plus haut. Meme fichier source (feuille "Region" de `sl_etc_2026T1.xls`, 27 lignes) : extremes de France metropolitaine (Pays de la Loire 6,6 %, Hauts-de-France 9,8 %) + Nouvelle-Aquitaine resituee entre les deux (7,3 %), sans jamais nommer un autre departement/region suivi hors de ce bloc de comparaison lui-meme (la regle du point 1 vise la lecture du territoire et les reperes, pas ce bloc d'ecarts dont le but assume est justement de comparer des territoires nommes). Recherche de fond faite (pas de generalite) : Pays de la Loire a la plus forte part d'emploi industriel de France (16,0 % contre 11,4 % national) et un agroalimentaire 2x plus present qu'ailleurs (20 % de l'emploi industriel regional contre 14 %, sources INSEE Analyses + tableau de bord regional) ; Hauts-de-France doit son taux au plus fort recul industriel de toutes les regions entre 1999 et 2014 (-30 % d'emplois industriels, -128 000, textile -70 %, metallurgie -30 %, heritage du bassin minier - sources INSEE). Nouvelle fonction generique reutilisee sans duplication : `_comprendreLesChiffresRenduRegionsFrance()` (gardee sur `niveau==='fr'`), contenu `modules/comprendre-les-chiffres/contenu/regions-france.md` (cree). Verifie navigateur clair et sombre, present uniquement au niveau France, absent en departement/region/UE. Prompt de veille etendu (indicateur 9 + bloc `[REGIONS-FRANCE]`). `npm test` 781 verts.
5. **[CLOS - 2026-09-12]** Age et niveau de diplome par decoupe, aux 3 niveaux (zone d'emploi, departement/region, region/France). Feasibilite verifiee et confirmee : le "Dossier complet" INSEE (`insee.fr/fr/statistiques/2011101?geo=<DEP|REG|ZE2020>-<code>`) publie les tableaux POP T5 (population par tranches d'age) et FOR T3 (diplome le plus eleve) a TOUS les niveaux geographiques, y compris zone d'emploi (correction du diagnostic precedent dans ce meme fichier, qui etait errone - la limite ne venait pas de l'INSEE mais du fait que ces tableaux n'avaient pas encore ete recuperes). Denis a recupere lui-meme les 12 tableaux (7 departement/region + 5 zones d'emploi), le site repondant une fois sur deux comme d'habitude. 3 nouveaux champs stats ajoutes : `part_65_et_plus`, `part_sans_diplome`, `part_diplome_superieur` (`COMPRENDRE_LES_CHIFFRES_ZONES_STATS`), integres aux 5 zones de `zones-emploi.md`, aux 4 departements de `departements-region.md` et aux 3 regions de `regions-france.md`, avec analyse de fond (pas de generalite) a chaque fois qu'un chiffre le justifiait : Sarlat (age eleve = fragilite de plus, pas une compensation), Limoges (diplome superieur = ville universitaire/administrative) et Saint-Junien (sans diplome = identite manufacturiere) pour les zones ; Haute-Vienne/universite de Limoges au niveau departement ; Pays de la Loire (jeune + qualifiee) et Hauts-de-France (difficulte liee a la structure de l'emploi, pas au vieillissement) au niveau region. **Bug trouve et corrige dans la foulee** : le parseur de stats (`_comprendreLesChiffresParserZonesEmploi`) ne reconnaissait pas les cles contenant un chiffre (regex `[a-z_]+` sans `0-9`) - `part_65_et_plus` passait en texte brut au lieu d'une barre ; corrige (`[a-z0-9_]+`), verifie navigateur clair et sombre apres correction sur les 3 niveaux. `npm test` 781 verts.
6. **[CLOS - 2026-09-12]** "La lecture du territoire" (Vue d'ensemble) enrichie. Diagnostic confirme par Denis : Nouvelle-Aquitaine et France n'avaient qu'1 paragraphe tres court (2 phrases), sans meme mentionner le taux de chomage pour la NA - plus pauvre que la Synthese courte de la Vue simple, l'inverse de ce que la Vue d'ensemble doit etre. Reecrit en 2 paragraphes par territoire, meme profondeur que Dordogne/Haute-Vienne, en reutilisant les chiffres deja sources dans `2026-06.md` (chomage, demandeurs, longue duree, emploi salarie, offres) avec une vraie interpretation (ex. NA : "plus de demandeurs, plus de longue duree, emploi salarie qui recule... dessinent un marche du travail regional qui se tend", pas juste une liste de chiffres). **Etendu au niveau Union europeenne, demande complementaire de Denis** : nouvelle section "## Lecture" dans `ue-2026.md`, rendue par une nouvelle carte "La lecture de cette comparaison" dans `_comprendreLesChiffresRenduVueUE()` (`modules/comprendre-les-chiffres/index.js`) - explique que le calcul est harmonise (meme methode Eurostat, l'ecart 8,3 % vs 6,1 % est reel, pas un artefact de calcul), signale l'ecart encore plus marque chez les jeunes (20,7 % vs 15,1 %), et donne une piste de comprehension generale (modele de "flexisecurite" des pays nordiques) sans juger la France - reste volontairement leger, comme demande. Verifie navigateur clair et sombre sur les 4 niveaux (departement inchange). `npm test` 781 verts.
7. **[CLOS - 2026-09-12]** "Offres visibles" vide en region/France : alimente. Donnees reelles recuperees sur France Travail Data Emploi (panoramas `.../emploi/panorama/REG/75` et `.../emploi/panorama/NAT/FR`, onglet "Rapprochement > Offres d'emploi" pour la tendance annuelle) : Nouvelle-Aquitaine 894 100 offres sur douze mois (-5,4 % sur un an), France entiere 9 294 000 offres (-6,6 % sur un an), + la part d'embauches de plus d'un mois (35 % aux deux niveaux, periode T3 2025 - differente de celle des offres, signalee telle quelle). Contenu ajoute dans `modules/comprendre-les-chiffres/contenu/2026-06.md` (section "Offres d'emploi collectees", + 2 sources en frontmatter). Aucun code touche : le mecanisme carte/repere/source est deja generique par territoire, verifie navigateur sur les 3 niveaux (departement inchange, region et France desormais peuplees, "cote" laterale toujours reservee au niveau departement par conception d'origine - comportement volontaire, pas un manque). Prompt de veille (indicateur 5) etendu pour couvrir region + France a chaque trimestre desormais. UE non concernee (le niveau UE n'affiche pas cette carte). `npm test` 781 verts.
8. **[CLOS pour Comprendre les chiffres - 2026-09-12, reste Lexique + Comprendre le cadre]** "Deserts medicaux", volet medecins generalistes. Denis a choisi le bloc "ecarts" complet (comme zones-emploi/departements-region/regions-france), avec des **extremes NATIONAUX** (pas regionaux, a la difference du bloc chomage) - a commencer par Comprendre les chiffres. Feasibilite verifiee : serie INSEE TCRED-SANTE-PERSONNELS "Densite des medecins generalistes pour 100 000 habitants", disponible par departement ET par region (2025), meme famille idBank que Dordogne (010760995) deja utilisee dans le portrait annuel. Extremes trouves et verifies sur INSEE directement (pas une source secondaire) : departements Hautes-Alpes (288, le mieux loti) / Eure-et-Loir (84, le moins bien loti) ; regions Bretagne (170) / Centre-Val de Loire (114). Vraie recherche de fond faite pour chaque extreme (pas de generalite) : Hautes-Alpes = plus forte proportion de jeunes medecins de France, +45 % de generalistes ces dernieres annees, attractivite du cadre de vie ; Eure-et-Loir = -12 % entre 2020 et 2023, la proximite de Paris n'aide pas (les jeunes medecins choisissent la capitale, pas sa peripherie, qui n'a pas de faculte de medecine) ; Bretagne = 2 facultes (Rennes, Brest), 3e region la plus attractive de France pour les etudiants en fin de cursus, 6 medecins sur 10 en exercice y ont fait leur dernier cycle ; Centre-Val de Loire = meme probleme qu'Eure-et-Loir (un de ses departements), pas de pole de formation attractif. Nouvelle-Aquitaine (165) et Haute-Vienne (166) tirees vers le haut par le CHU et la faculte de Limoges, explicitement cite comme facteur dans les 2 blocs.
    - Code : nouveaux fichiers `medecins-departements-region.md` (groupe "na") et `medecins-regions-france.md` (groupe "fr"), nouvelles fonctions `_comprendreLesChiffresRenduMedecinsDepReg()`/`_comprendreLesChiffresRenduMedecinsRegFr()` (`modules/comprendre-les-chiffres/index.js`), meme parseur et meme rendu generique que les blocs chomage - aucune duplication. **Petit defaut corrige au passage** : `_comprendreLesChiffresRenduEcartsGenerique()` avait le texte "Taux de chomage localise" ecrit en dur dans la note sous les barres (jamais remarque tant que seul le chomage l'utilisait) - parametre en `opts.noteValeur`, retro-compatible pour les 3 blocs chomage existants (comportement inchange).
    - Le departement suivi n'a pas de bloc "ecarts" dedie pour ce sujet (pas de decoupe par zone d'emploi publiee par l'INSEE pour cet indicateur) : `portrait-2026.md` complete a la place, avec un repere citant les 2 extremes nationaux et un renvoi vers la Vue d'ensemble niveau region.
    - Verifie navigateur clair et sombre sur les 4 niveaux (present en region et France uniquement, coexiste avec le bloc chomage du meme niveau sans conflit, absent en departement/UE). `npm test` 809 verts.
    - **[CLOS - 2026-09-12] Medecins specialistes, 2e profession du meme chantier.** Meme methode que les generalistes, extremes NATIONAUX verifies directement sur INSEE : departements Paris (702, tres largement le plus haut, en hausse continue) / Ain (79, le plus bas) ; regions Ile-de-France (263) / Centre-Val de Loire (147). Recherche de fond faite pour chaque extreme (pas de generalite) : Paris = concentration des grands hopitaux universitaires, cliniques privees specialisees et facultes de medecine d'Ile-de-France ; Ain = proximite de Lyon qui attire/retient les specialistes formes dans la region (delai de plus d'un an pour certains rendez-vous, ex. ophtalmologie) ; Centre-Val de Loire = meme probleme que l'Eure-et-Loir pour les generalistes (pas de pole hospitalo-universitaire attractif, proximite de l'Ile-de-France qui profite a cette derniere). Dordogne (115) et Haute-Vienne (215) resituees, la Haute-Vienne nettement plus proche du profil parisien grace au CHU de Limoges ; Nouvelle-Aquitaine (191) resituee au niveau region. **Piege trouve et corrige pendant la recherche** : un filtre `indexOf(nom)>-1` confondait "Ile-de-France" avec "France metropolitaine hors Ile-de-France", et "Corse" avec "Corse-du-Sud" - corrige en filtre "commence par" (`indexOf===0`) plus verification du titre de la page INSEE.
      - Code : nouveaux fichiers `specialistes-departements-region.md` et `specialistes-regions-france.md`. **Refactorisation faite avant d'ajouter le contenu** (repere une duplication qui allait se reproduire une 2e fois) : les fonctions/etats specifiques aux generalistes remplaces par un registre generique `COMPRENDRE_LES_CHIFFRES_PROFESSIONS_SANTE` (`modules/comprendre-les-chiffres/index.js`) - un objet par metier (titre, icone, fichiers, `noteValeur`, intros), chargement paresseux et rendu factorises dans `_comprendreLesChiffresRenduProfessionsSante()`, appelee une seule fois depuis la Vue d'ensemble. Ajouter un futur metier (dentistes, pharmaciens, infirmiers) ne demandera qu'une entree dans ce registre, plus aucune nouvelle fonction. Commit de refactorisation isole et verifie (zero regression) avant le commit de contenu.
      - `outils/veille.html` etendu au meme titre : `MEDECINS_DECOUPES_TERRITOIRES` (generalistes seuls) remplace par `PROFESSIONS_SANTE_DECOUPES` (tableau de metiers, meme principe que le registre cote application), `assemblerMedecinsDecoupes()` boucle sur le tableau et produit un bloc par metier. **Petit defaut trouve et corrige au passage** : le texte genere disait "Densite des medecins medecins specialistes" (doublon, le libelle du metier contenait deja "medecins") - corrige.
      - Verifie navigateur clair et sombre (les 2 blocs coexistent en region/France sans conflit, absents en departement/UE) et prompt de veille verifie directement dans le navigateur (`assemblerMedecinsDecoupes()` produit bien les 2 sections avec les bons identifiants INSEE). `npm test` 809 verts.
    - **[CLOS - 2026-09-12] Chirurgiens-dentistes, 3e profession.** Extremes NATIONAUX verifies directement sur INSEE (une vingtaine de departements testes, dont tous les candidats plausibles trouves par recherche independante, pour ecarter tout departement plus extreme non teste) : departements Paris (163, tres largement le plus haut) / Creuse (31, le plus bas, "deux fois moins de dentistes que la moyenne nationale" - France Bleu) ; regions Provence-Alpes-Cote d'Azur (98) / Normandie (47, aucune faculte dentaire sur son territoire - L'Information Dentaire). Dordogne (47) et Haute-Vienne (47) quasi identiques, resituees ensemble pres de la Creuse. **Trouvaille non generique** : contrairement aux medecins ou la faculte de Limoges tire la Haute-Vienne vers le haut, elle n'a aucun effet ici, car il n'existe pas de faculte dentaire a Limoges (les etudiants limousins partent a Bordeaux) - explique pourquoi Haute-Vienne et Dordogne se retrouvent au meme niveau, une conclusion qui aurait ete fausse par simple analogie avec les medecins.
    - **[CLOS - 2026-09-12] Pharmaciens, 4e profession - patron different des 3 precedents.** Extremes NATIONAUX verifies sur INSEE (une quinzaine de departements testes, y compris tous les candidats ruraux et parisiens plausibles) : departements Haute-Vienne (151, le plus haut - **le departement suivi est lui-meme l'extreme nationale**, ce qui change la structure du bloc : plus de "Dordogne ET Haute-Vienne resituees entre deux autres extremes", mais Haute-Vienne comme extreme et Dordogne (90) resituee entre elle et l'autre extreme) / Seine-Saint-Denis (66, le plus bas, installations bloquees par un desaccord ARS/prefecture - Le Quotidien du Pharmacien) ; regions Provence-Alpes-Cote d'Azur (125) / Pays de la Loire (92, la moins bien dotee malgre son dynamisme : 3e region a la croissance demographique la plus forte de France, le rythme des nouvelles officines - encadre par des quotas revises seulement de temps en temps - ne suit pas cette croissance). **Sens invise par rapport aux 3 autres metiers** : ce sont les departements RURAUX (Haute-Vienne, mais aussi Creuse/Cantal/Hautes-Alpes verifies au passage) qui sont les mieux dotes en pharmaciens, pas les grandes villes - a l'oppose du schema medecins/dentistes ou Paris domine ; verifie explicitement pour ne pas plaquer le meme raisonnement par automatisme. Cause identifiee pour la Haute-Vienne : Limoges a bien une faculte de pharmacie (contrairement a la dentisterie), renforcee par le poids historique de l'ancien Limousin, cite comme 1re region de France pour les officines par habitant.
    - Code (dentistes + pharmaciens, meme passage) : 4 nouveaux fichiers de contenu, 2 nouvelles entrees dans le registre `COMPRENDRE_LES_CHIFFRES_PROFESSIONS_SANTE` (aucune nouvelle fonction, exactement le gain attendu de la refactorisation faite au tour precedent). `outils/veille.html` : `PROFESSIONS_SANTE_DECOUPES` etendu aux 2 nouveaux metiers, description et intitule du bloc mis a jour. **Defaut trouve et corrige dans le meme passage** : l'icone choisie au premier jet pour les dentistes (`bi-emoji-smile`) etait un visage souriant, contraire a la regle absolue "jamais d'icone avec un visage" - remplacee par `bi-clipboard2-pulse` avant tout commit.
    - Verifie navigateur clair et sombre (les 4 blocs medecins/specialistes/dentistes/pharmaciens coexistent en region et en France sans conflit, tous absents en departement/UE) et prompt de veille verifie dans le navigateur (2 nouvelles sections avec les bons identifiants INSEE). `npm test` 809 verts.
    - **[DECISION PRISE, pas un TODO - 2026-09-12] Infirmiers, 5e profession demandee : ECARTEE pour l'instant.** La serie INSEE TCRED utilisee pour les 4 metiers ci-dessus ("Densite des infirmiers diplomes d'Etat") s'arrete en 2021 (derniere publication le 24/10/2023, jamais mise a jour depuis - contrairement aux 4 autres series, toutes a jour 2025). Les sources alternatives trouvees ne couvrent pas exactement la meme population : l'Ordre National des Infirmiers publie une cartographie annuelle mais uniquement en images (PDF non exploitable directement) ; l'Assurance Maladie (Ameli) publie un fichier XLSX par departement mais uniquement pour les infirmiers **liberaux** (pas l'ensemble comme la serie INSEE, qui compte aussi les salaries). Options presentees a Denis (utiliser la serie arretee en la datant 2021, changer de perimetre vers "infirmiers liberaux" via Ameli, ou laisser de cote) : **Denis a choisi de laisser le sujet de cote pour l'instant**, plutot que d'introduire une incoherence de date ou de perimetre dans un bloc dont les 4 autres metiers sont a jour 2025. A reprendre seulement si une source a jour et de meme perimetre apparait.
    - **[CLOS - 2026-09-13] Meme sujet cote Comprendre le cadre et Lexique.** Exploration prealable faite (agent dedie) : aucun des 16 rayons existants de Comprendre le cadre ni aucun des 16 univers existants du Lexique ne correspond au sujet (tous concus autour de l'emploi/administratif). **2 choix architecturaux presentes a Denis avec recommandation, options + benefices/risques :**
      - Comprendre le cadre : **creer un nouveau rayon "Sante"** plutot que de forcer la fiche dans "Budget" ou "Mobilite" (angles secondaires par rapport au sujet reel). Denis a choisi cette option.
      - Lexique : **creer un nouvel univers "acces-aux-soins"** plutot que de reutiliser "protection-sociale" (trop large) ou "sante-travail" (reserve a la sante *au travail* : RQTH, medecine du travail, accident du travail - verifie en relisant les fiches existantes de cet univers, aucune n'est generaliste). Denis a choisi cette option.
      - Code : `scripts/checkLexique.js` (UNIVERS_CONNUS + 1), `docs/CHANTIER_LEXIQUE.md` section 5 (liste des univers mise a jour, **defaut trouve et corrige au passage** : "Conditions de travail" manquait deja dans cette liste avant ce chantier), `modules/lexique/index.js` (`_LEXIQUE_LIBELLES_UNIVERS` + 1, sinon l'univers se serait affiche par son slug technique au lieu d'un libelle lisible - trouve et corrige en verifiant le rendu au navigateur, pas seulement le script de controle).
      - Contenu : `modules/comprendre-le-cadre/contenu/sante/` (README, `sante.collecte.md`, 1 fiche `sante-desert-medical.md` avec `voir_aussi_lexique` et `voir_aussi_chiffres`), `data/lexique.js` (entree `desert-medical`, univers `acces-aux-soins`, relation reciproque avec `css` pour eviter l'avertissement "fiche jamais citee"). Sources reelles verifiees une par une (jamais fabriquees) : Annuaire sante Ameli, page Ameli sur les organisations coordonnees territoriales, Geoconfluences (definition), CPAM Dordogne et CPAM Haute-Vienne (adresses/telephone verifies), France Services. **Choix de perimetre assume** : pas de liste nominative de CPTS locales (une demi-douzaine en Dordogne, perimetre trop instable dans le temps pour etre fige dans une fiche) - renvoi generique vers sante.fr a la place, note explicitement dans `sante.collecte.md` comme un "reste a faire" possible.
      - Rayon "Sante" branche partout ou les 16 autres le sont : `RAYON_LABEL`, `COMPRENDRE_LE_CADRE_FAMILLES` (famille "Situations et publics"), `ORIENTEUR_CASES` (case "Je n'arrive pas a trouver un medecin pres de chez moi", formulee sur la situation jamais sur l'etat de sante de la personne, meme garde-fou que handicap/etranger/justice), `COMPRENDRE_LE_CADRE_QUESTIONS_RAYON` (2 questions, meme garde-fou), `outils/veille.html` (`RAYONS` + `CADENCE_DEFAUT`, cadence trimestrielle).
      - Verifie navigateur clair et sombre : fiche complete (sources datees, boutons "Pour aller plus loin" vers le Lexique et vers Comprendre les chiffres, tous deux testes en cliquant dessus), entree Lexique ouverte depuis ce lien avec le bon libelle d'univers, recherche d'accueil retrouve bien "desert medical" (categorie module ET categorie Lexique), orienteur affiche la nouvelle case. `npm test` 809 verts, `node scripts/checkLexique.js` 0 erreur et 0 avertissement.
      - **Question de Denis apres coup** : ce nouveau contenu est-il repris dans les balayages de l'outil de veille ? Reponse : 2 mecanismes distincts existent, un seul concerne ce chantier. Le **Controle** (rotation qui reverifie les faits d'un rayon, `Object.keys(RAYONS)` dynamique) couvre deja "Sante" automatiquement, cadence trimestrielle deja posee. Le **Balayage** (`BAL_DOMAINES`, prompt trimestriel qui scanne les nouveaux textes reglementaires) ne couvrait que 9 domaines choisis, sans lien avec la liste des rayons (plusieurs rayons anciens - logement, budget, justice, garde, francais, juridique, creer, employeurs, accompagnement - n'y sont pas non plus) : absence normale, pas un oubli de ce chantier. **Denis a demande d'ajouter un 10e domaine** : "Sante / acces aux soins (zonage ARS, aides a l'installation des medecins, desert medical)" - ajoute a `BAL_DOMAINES`, verifie dans le prompt genere par `assemblerBalayage()` (aucune autre modification necessaire : le validateur `coherenceBalayage()` lit `BAL_DOMAINES` dynamiquement).
      - **Audit demande par Denis apres coup ("verifie que rien n'a ete oublie")** : passage systematique sur les 4 registres du module (`RAYON_LABEL`, `COMPRENDRE_LE_CADRE_FAMILLES`, `ORIENTEUR_CASES`, `COMPRENDRE_LE_CADRE_QUESTIONS_RAYON`) + `outils/veille.html` (`RAYONS`, `CADENCE_DEFAUT`) : les 4 registres cote application comptent bien 17 rayons chacun, coherents entre eux. **1 defaut trouve et corrige, anterieur a ce chantier** : le rayon "employeurs" (ajoute le 2026-09-12, avant ce chantier) etait present dans `RAYONS` mais absent de `CADENCE_DEFAUT` - sans consequence fonctionnelle grave (`rayonDu()` retombe sur 'trimestriel' par defaut), mais la grille de reglage de cadence l'aurait affiche a tort sur "mensuel" (premier choix du menu) tant que personne n'y touchait. Corrige (`employeurs: 'trimestriel'`), coherent avec les rayons de portee proche (creer, accompagnement). **1 defaut de comptage trouve et corrige** dans ce meme document : le point 9 juste en dessous disait encore "5 paires" et "15 autres rayons" alors que la nouvelle paire `sante-desert-medical`->`desert-medical` en fait la 6e - corrige.
      - **2 points signales a Denis, non corriges (necessitent son avis, pas une simple faute) :**
        1. `modules/comprendre-le-cadre/ARCHITECTURE_TECHNIQUE.md` affiche encore "14 rayons" a une dizaine d'endroits - **mais c'est une note datee** ("vérifiés... entre le 2026-09-05 et le 2026-09-06"), donc pas fausse pour la periode qu'elle decrit ; elle n'a simplement jamais ete completee pour documenter les rayons ajoutes depuis (employeurs le 2026-09-12, sante le 2026-09-13). A juger : vaut-il la peine d'ajouter une note de suite dans ce document technique, ou est-ce suffisamment couvert par ce fichier-ci (TACHES_VALIDEES.md) ?
        2. `docs/VEILLE_PROMPTS.md` (section 5ter, la reference officielle des prompts de "Comprendre les chiffres") ne documente ni le prompt "profil des decoupes" (age/diplome, 2026-09-12) ni le prompt "professions de sante" (medecins/specialistes/dentistes/pharmaciens) : les deux existent et fonctionnent (`outils/veille.html`), mais n'ont jamais ete ajoutes a ce document de reference, y compris avant ce chantier. Pas quelque chose de casse, mais la doc de reference est incomplete depuis 2 chantiers maintenant.
9. **[CLOS - 2026-09-13]** Audit complet Comprendre le cadre <-> Lexique au-dela des 6 paires deja faites, et Lexique <-> Comprendre les chiffres (jamais commence). Demande par Denis, mis de cote plusieurs fois faute de temps, repris et clos dans cette session.
   - **Cadre -> Lexique** : 119 fiches restantes auditees via 3 recherches independantes (une par famille de rayons, sujet reel de chaque fiche confronte a data/lexique.js). **102 fiches reliees, 175 liens `voir_aussi_lexique` ajoutes**, chaque id verifie present dans data/lexique.js avant ecriture (script de validation puis d'application, aucune fiche ne l'avait deja). **14 fiches sans candidat pertinent, laissees telles quelles** (pas de lien force) : `mobilite-acheter-ou-reparer-un-vehicule`, `garde-complement-mode-de-garde-cmg`, `garde-horaires-decales-urgence`, `jeunes-aides-financieres`, `etranger-naturalisation-grandes-etapes`, `etranger-permis-de-conduire-etranger`, `justice-travail-formation-en-detention`, `former-aides-moins-connues-financer-formation`, `former-demission-reconversion`, `employeurs-aides-a-embauche`, `employeurs-devenir-employeur`, `employeurs-egalite-professionnelle`, `employeurs-mutuelle-obligatoire`, `employeurs-obligations-sante-securite` (aucune entree Lexique correspondante n'existe pour ces sujets precis - rien invente).
   - **3 faux-amis reperes et ecartes par les recherches elles-memes** (memes mots, sens differents) : `radiation` (Cadre budget : radiation du fichier FICP / Lexique : radiation de France Travail), `sous-traitance` (Cadre : delegation d'accompagnement par France Travail / Lexique : sous-traitance de marche public entre entreprises), `css` (Cadre `employeurs-mutuelle-obligatoire` : mutuelle d'entreprise obligatoire / Lexique `css` : Complementaire sante solidaire, aide de l'Assurance maladie).
   - **5 candidats de confiance moyenne, presentes a Denis avec la reserve de chacun - tous retenus (2026-09-13)** : `mobilite-financer-le-permis` -> `cpf` (le CPF n'est qu'un des leviers de la fiche, pas son unique sujet) ; `francais-se-former-pour-le-travail` -> `cpf` (meme reserve, ajoute a `aif` deja pose) ; `logement-demande-de-logement-social` -> `plafond-de-ressources` (lien plus generique que les autres, la fiche definit deja ce terme dans son propre glossaire) ; `francais-lecture-ecriture-competences-de-base` -> `illettrisme` (ajoute a `competences-de-base`/`clea` deja poses - candidat tenu a part parce que nommer "illettrisme" pourrait friser un diagnostic sur le lecteur, Denis a tranche que le lien reste utile) ; `justice-vers-l-emploi` -> `casier-judiciaire` (ajoute a `iae` deja pose).
   - **Lexique -> Comprendre les chiffres** : mecanisme qui n'existait pas du tout (seul Cadre -> Chiffres existait). Construit a l'identique : champ `voirAussiChiffres` sur une entree de `data/lexique.js`, bouton generique vers l'accueil de Comprendre les chiffres, meme aller-retour via `definirRetourModuleExterne()` que celui deja eprouve cote Cadre (`modules/lexique/index.js`). **5 candidats trouves** par recherche reelle (pas de generalite) : `chomage-au-sens-du-bit`, `demandeur-emploi-categories`, `inscrit-a-france-travail-ou-chomeur-bit` et `demandeur-emploi-longue-duree` (les 4 correspondent presque mot pour mot aux fiches methode deja presentes dans Comprendre les chiffres - `comment-lire-un-taux-de-chomage.md`, `taux-insee-ou-personnes-inscrites.md`) et `desert-medical` (deja cree ce meme jour). **Aucun autre candidat serieux** : les chiffres purement statistiques (offres, emploi salarie, creations/defaillances d'entreprises, age/diplome par decoupe) n'ont pas de mot correspondant dans le Lexique, qui reste oriente vocabulaire administratif et dispositifs plutot qu'indicateurs chiffres.
   - Verifie navigateur a chaque etape (fiche a 2 liens, fiche a 5 liens, aller-retour Lexique <-> Chiffres testes en cliquant, mode sombre), `npm test` 924 verts, `checkLexique.js` 0 erreur/avertissement. 3 commits distincts (102 fiches Cadre, mecanisme + 5 entrees Lexique -> Chiffres).
10. **[DECISION PRISE, pas un TODO]** 2 candidats de lien Cadre->Chiffres juges trop faibles pour etre ajoutes (`accompagnement-rsa`, `budget-rsa-prime-activite-reprise-emploi` -> chiffre "part de RSA parmi les demandeurs") : volontairement laisses de cote, a reconsiderer seulement si un jour ce chiffre s'enrichit.
11. **[CLOS - 2026-09-12]** Bloc "Croiser plusieurs chiffres" : le message generique "(pas encore de serie)" distingue maintenant 3 cas via un champ `raisonVide` sur chaque case de `COMPRENDRE_LES_CHIFFRES_CROISABLES` (`modules/comprendre-les-chiffres/index.js`) : `inexistante` ("cette serie n'existe nulle part : aucune source connue ne publie l'historique de ce chiffre"), `a-recuperer` ("cette serie existe tres probablement quelque part, mais n'a pas encore ete recuperee"), et par defaut `chantier` ("ce chiffre n'est pas encore suivi par l'application, personne ne l'a encore construit") pour une future case sans raison precisee. Classification verifiee au fil du diagnostic demande par Denis : `offres` (aucun territoire, aucune serie) = `inexistante` (deja confirme cette session, aucune source connue n'a d'historique) ; `demA24`/`demA87` (demandeurs, un seul point) et `ld24`/`ld87`/`ldna` (longue duree, un seul point) = `a-recuperer` (le meme jeu de donnees data.gouv.fr STMT qui alimente deja la serie complete de la Nouvelle-Aquitaine et de la France existe tres probablement aussi a un niveau plus fin, juste pas encore extrait). Verifie navigateur (departement et region, les 6 cas vides affichent chacun le bon message, `demAna`/`ldfr` qui ONT une vraie serie n'apparaissent plus dans la liste). **Suite immediate, retour Denis** : la case `offres` n'aura jamais de donnee (categorie `inexistante`) - supprimee de `COMPRENDRE_LES_CHIFFRES_CROISABLES` plutot que laissee grisee indefiniment. Les 5 autres cases vides (categorie `a-recuperer` : demandeurs et longue duree, departement/region) sont gardees telles quelles a dessein : des que la donnee sera ajoutee au digest trimestriel, la case s'active toute seule (calcul dynamique sur la longueur de `serie`), sans toucher au code - les supprimer aurait oblige a s'en souvenir pour les recreer plus tard. `npm test` 809 verts.
    - **Suite immediate, les 5 series recuperees dans la foulee (retour Denis "va les chercher")** : 1re tentative via le fichier "par zone d'emploi" agrege (Bergerac+Perigueux+Sarlat = Dordogne, Limoges+Saint-Junien = Haute-Vienne) - **ecartee** : ce fichier n'existe qu'en donnees "Brutes" (non desaisonnalisees), la version CVS-CJO a ete arretee au niveau zone d'emploi/departement en 2021 par la statistique publique. Melanger du brut avec le chiffre CVS-CJO deja affiche aurait cree un faux saut dans la courbe. **2e tentative, la bonne** : le fichier Dares "Demandeurs d'emploi... Stock France et regions (trimestrielles, CVS-CJO)" (data.gouv.fr) contient en realite une vraie colonne Departement (101 departements, pas seulement des totaux regionaux), CVS-CJO, avec anciennete d'inscription, jusqu'a mi-2026 - exactement la bonne source. Verification croisee reussie : le calcul Nouvelle-Aquitaine tombe sur 45,6 % au 1er trimestre 2026, exactement le chiffre deja publie. Pour la Dordogne/Haute-Vienne, le nouveau chiffre (16 900 / 14 300 personnes categorie A, 47 % / 46 % longue duree) differe legerement de l'ancien (17 500 / 14 500, source France Travail Data Emploi) - retour Denis : **aligner le chiffre affiche sur la nouvelle source** plutot que de laisser une petite incoherence visuelle entre le chiffre et le dernier point de la courbe. Fait : `2026-06.md` mis a jour (valeurs, series de 8 trimestres pour demA24/demA87/ld24/ld87/ldna, sources, reperes, prose de la lecture du territoire corrigee au passage - l'ancien texte disait "toutes categories" pour une baisse qui ne concernait en realite que la categorie A, corrige). Les 5 cases sont maintenant actives dans "Croiser plusieurs chiffres". Verifie navigateur (plus aucune case vide en departement ni en region, carte "demandeurs" et "longue duree" a jour, graphique genere sans erreur). `npm test` 809 verts.
12. **[CLOS - 2026-09-12]** Outil de veille (`outils/veille.html`) : separation stricte recherche / redaction sur les 3 modules "Se tenir informe", demande de fond de Denis apres avoir constate que "la lecture du territoire" etait pauvre parce que le prompt qui la produisait etait lance sur une IA de recherche web quelconque, sans l'exigence de style/vision de la maison. Principe applique partout : un 1er prompt ne recolte QUE des faits bruts sources (peut etre lance sur ChatGPT/Perplexity), un 2e prompt separe, explicitement reserve a une conversation Claude Code, redige le texte final a partir de ces faits deja valides - jamais l'inverse.
    - **Comprendre les chiffres** (`e304b7d`) : `assemblerChiffres()` ne demande plus `[LECTURE]`/`[SYNTHESE-COURTE]` ; nouveau `assemblerLectureChiffres()` + `lireLectureChiffres()` (Claude). Meme principe pour la comparaison UE (`assemblerLectureUe()`).
    - **Comprendre le cadre** : le pipeline `assembler1()` (collecte, "UNIQUEMENT collecter, pas rediger") -> `assembler2()` (redaction) etait deja correctement separe des la conception initiale ; seul le texte du prompt 2 disait encore "Claude ou GPT" - corrige en "Uniquement avec Claude". Verifie que la mise a jour d'une fiche existante (bouton "Traiter" depuis le Controle) passe par le meme pipeline, aucun raccourci ne contourne la separation.
    - **Lexique** (`e43a7e1`) : `assemblerLexique()` ne recolte plus que des faits bruts (`[LEXIQUE-BRUT]`) ; nouveau `lireCollecteLexique()` + `assemblerRedactionLexique()` (Claude) qui redige corps/univers/registres/variantes/relations a partir des faits deja valides.
    - **Trou trouve et corrige dans la foulee** (`2511447`) : l'outil "Importer un texte a la main" (pour les sites bloques type Legifrance) avait son PROPRE prompt independant, qui redigeait encore fiches et entrees de lexique completes en un seul passage - il contournait entierement la separation ci-dessus. Corrige (branches "cadre" et "lexique" ; la branche "chiffres" etait deja correcte).
    - **Audit final** : les 19 fonctions `assembler*`/`preparer*` de l'outil passees en revue une par une. Fonctions de verification/tri factuel (Controle, Controle-source, Triage, Triage-mots, Balayage, Confronter, Associations, Portrait annuel, Verif-chiffres) confirmees saines (consigne explicite "ne reecris rien" ou chiffres structures uniquement) - carve-out assume par Denis, pas de redaction publiee dans ces cas.
    - Verifie navigateur (cycle complet teste pour chaque module), `npm test` 809 verts a chaque etape.
13. **[CLOS - 2026-09-13]** Suite demandee par Denis apres l'audit de l'item 9, qui n'avait couvert qu'un seul sens (Cadre -> mots deja existants du Lexique). Trois chantiers distincts, tous clos :
    - **[CLOS] Comprendre le cadre -> Comprendre les chiffres** : les 121 fiches restantes auditees (2 recherches independantes, meme methode que l'item 9 mais cherchant un vrai recoupement avec un indicateur suivi, pas un mot du Lexique). **4 candidats retenus** (barre stricte : sujet exact de la fiche mesure par un indicateur reellement suivi) : `mobilite-se-deplacer-sans-voiture` (part des menages sans voiture), `garde-trouver-un-mode-de-garde` (taux de couverture des places d'accueil), `francais-lecture-ecriture-competences-de-base` (part de la population en difficulte avec les competences de base), `employeurs-egalite-professionnelle` (ecart de salaire femmes-hommes) - les 4 chiffres viennent de `portrait-2026.md`. 117 autres fiches sans candidat (majorite procedurale, pas de phenomene mesure). Verifie navigateur, `npm test` 924 verts.
    - **[CLOS] Nouveaux mots a creer dans le Lexique** : 259 termes des mini-glossaires locaux du Cadre examines (2 recherches independantes), 175 deja couverts par le Lexique sous un autre nom, 35 juges trop etroits, **49 recommandes** - liste complete dans `docs/AUDIT_NOUVEAUX_MOTS_LEXIQUE_2026-09-13.md`. **Denis a valide l'ensemble des 49 le 2026-09-13.** Les 49 fiches Lexique ont ete redigees en 6 lots (commits `e02e238`, `52b8e51`, `312ddce`, `aa60737`, `1f68c74`), chacune avec au moins une relation interne (`voir-aussi` ou `a-ne-pas-confondre`) vers une entree existante. Un point de vigilance gere en route : collision de nom entre le nouveau "Prescription (delai)" (delai judiciaire aux prud'hommes) et la fiche Lexique existante "Prescription" (orientation par un conseiller) - titre distinct + relation `a-ne-pas-confondre` reciproque entre les deux. **Bouclage fait** : les 45 fiches sources du Cadre (49 mots, 4 fiches en portant 2 chacune) ont chacune recu le lien retour `voir_aussi_lexique:` vers leur nouvelle fiche Lexique, meme mecanisme que les 102 fiches de l'item 9. `node scripts/checkLexique.js` 0 erreur/0 avertissement, `npm test` 924 verts a chaque etape.
    - **[CLOS] Nouveaux mots a creer depuis Comprendre les chiffres lui-meme** : audit demande par Denis en cours de route (angle initialement oublie). **5 candidats trouves** : chomage au sens du recensement, taux d'emploi, population active, correction des variations saisonnieres (CVS) - confiance forte, structurent la lecture de plusieurs fiches methode/UE sans etre definis nulle part - et zonage ARS (ZIP/ZAC/indicateur APL) - confiance moyenne, seulement 2 occurrences, deja partiellement couvert en creux par `desert-medical`. **Valides et rediges le 2026-09-13** (ids `chomage-recensement`, `taux-d-emploi`, `population-active`, `correction-variations-saisonnieres`, `zonage-ars`), avec le champ `voirAussiChiffres` (lien Lexique -> Comprendre les chiffres) sur les 4 qui s'y pretaient. Relations reciproques ajoutees sur `chomage-au-sens-du-bit`, `inscrit-a-france-travail-ou-chomeur-bit` et `desert-medical`. Verifie au navigateur (bouton "Pour aller plus loin" + retour "Revenir a Lexique"). `checkLexique.js` 0 erreur/0 avertissement, `npm test` 924 verts.
14. **[CLOS - 2026-09-13, perimetre reduit a Lexique -> Cadre]** Demande de Denis : que les liens entre les 3 modules de "Se tenir informe" (Comprendre le cadre, Comprendre les chiffres, Lexique) fonctionnent **dans les deux sens partout ou il y a une vraie matiere**, quelle que soit la porte d'entree - pas seulement les 3 sens uniques construits a ce jour (Cadre -> Lexique, Cadre -> Chiffres, Lexique -> Chiffres). Le bouton "Revenir a..." deja en place n'est qu'une memoire de navigation (il ramene la ou on etait), pas une vraie reference independante dans l'autre sens.
    - **Reconnaissance faite (avant cadrage complet avec Denis)** : mesure exacte des 3 sens manquants.
      - **Chiffres -> Cadre** : 7 fiches Cadre pointent aujourd'hui vers Comprendre les chiffres (`voir_aussi_chiffres:`) - `employeurs-egalite-professionnelle`, `francais-lecture-ecriture-competences-de-base`, `garde-trouver-un-mode-de-garde`, `mobilite-se-deplacer-sans-voiture`, `emploi-interim`, `emploi-cdd-cdi-saisonnier`, `sante-desert-medical`. Chacune est une cible candidate pour un lien retour.
      - **Chiffres -> Lexique** : 9 fiches Lexique portent `voirAussiChiffres` (`css`, `demandeur-emploi-categories`, `chomage-au-sens-du-bit`, `demandeur-emploi-longue-duree`, `inscrit-a-france-travail-ou-chomeur-bit`, `chomage-recensement`, `taux-d-emploi`, `correction-variations-saisonnieres`, `zonage-ars`). Meme logique, 9 cibles candidates.
      - **Lexique -> Cadre** : environ 151 fiches Cadre pointent vers le Lexique (`voir_aussi_lexique:`, 102 fiches de l'item 9 + 49 de l'item 13) - un potentiel volume bien plus grand, qui pose une vraie question de perimetre (tout relier, ou seulement les liens les plus utiles ?).
    - **Difference technique importante entre les deux chantiers** : **Lexique -> Cadre est le plus facile a construire.** La donnee existe deja dans un sens (`voir_aussi_lexique:` sur les fiches Cadre) : il suffit de batir l'index inverse (id Lexique -> fiches Cadre qui le citent) et d'ajouter un encart dans `_lexiqueRenduFiche()` (`modules/lexique/index.js`, meme endroit que `encartVoirAussiChiffres`, ligne ~861) - pas de nouvelle donnee a saisir a la main. **Chiffres -> {Cadre, Lexique} est plus lourd** : Comprendre les chiffres n'a pas un seul type de bloc de contenu, mais une vingtaine de fonctions de rendu differentes selon le type de fiche (zones d'emploi, regions, methode, portrait...) - il faudrait decider, bloc de contenu par bloc de contenu, ou et comment inserer un lien retour, ce qui est un vrai travail de conception, pas un ajout mecanique.
    - **Decision de Denis (2026-09-13)** : **on ne construit pas l'axe Cadre <-> Chiffres.** Seuls les deux sens suivants sont a construire : **Lexique -> Cadre** et **Chiffres -> Lexique** (en plus des 3 sens deja existants). Raison donnee par Denis : Comprendre les chiffres est une page dense, concue pour lire comment plusieurs indicateurs interagissent et ce que ca dit d'un territoire - pas pour retrouver un seul chiffre isole. Y renvoyer quelqu'un pour un seul mot ou une seule fiche du Cadre le perd dans un contenu qui ne repond pas a sa question precise.
    - **Piste soulevee par Denis pour les 7 liens Cadre -> Chiffres deja existants** (pas un chantier immediat, une idee a reprendre a part avec une maquette, doute d'interface) : remplacer la navigation vers Comprendre les chiffres par une information affichee directement sur place, dans la fiche Cadre elle-meme (infobulle, ou bloc "en savoir plus" depliable en bas de fiche) qui extrait et montre le chiffre precis concerne, sans faire quitter le Cadre. Objectif : cibler uniquement l'information voulue, garder le lecteur dans son contexte de lecture. A cadrer separement (maquette + validation Denis) avant tout code, ce n'est pas la meme demande que les 2 sens ci-dessus.
    - **[CLOS - 2026-09-13] Lexique -> Cadre** : construit et verifie au navigateur, pour les ~219 mots concernes (tous relies, pas de sous-ensemble : la donnee etant deja generee automatiquement, limiter le perimetre n'aurait fait qu'ajouter une regle d'exclusion arbitraire).
      - `scripts/genererRenvoisCadre.js` (nouveau) : relit `voir_aussi_lexique:` sur toutes les fiches de `modules/comprendre-le-cadre/contenu/**/*.md` et ecrit l'index inverse dans `data/lexique-renvois-cadre.js` (`LEXIQUE_RENVOIS_CADRE`, id Lexique -> liste `{ficheId, rayon, titre}`). Fichier genere, jamais edite a la main - une seule source de verite (les fiches Cadre).
      - **Fraicheur garantie automatiquement** : `scripts/checkLexique.js` appelle desormais `genererRenvoisCadre.js` en mode verification et echoue si le fichier genere est perime - Denis n'a rien de plus a retenir, la meme commande `node scripts/checkLexique.js` couvre aussi ce nouveau mecanisme.
      - `modules/lexique/index.js` : nouvel encart `encartVoirAussiCadre` dans `_lexiqueRenduFiche()` (meme emplacement que `encartVoirAussiChiffres`), un bouton par fiche Cadre reliee (une fiche Lexique peut etre citee par plusieurs fiches Cadre - teste sur `aif`, 2 fiches). Clic branche dans `_lexiqueBrancherFiche()`.
      - `modules/comprendre-le-cadre/index.js` : nouvelle fonction publique `comprendreLeCadreOuvrirFiche(rayon, ficheId, provenanceLabel, provenanceRoute, provenanceRetour)`, symetrique de `lexiqueOuvrirFiche()` - charge uniquement le rayon concerne (pas les 17), ouvre directement la fiche (ecran 'fiche'), pose le bouton "Revenir a Lexique" (`definirRetourModuleExterne`). Le "Retour" interne de la fiche ramene ensuite normalement a la liste du rayon (`_comprendreLeCadreAllerVersPrecedent`, deja prevu).
      - `index.html` : `data/lexique-renvois-cadre.js` charge juste apres `data/lexique.js`.
      - Verifie au navigateur : cas 1 fiche et cas plusieurs fiches, clair et sombre, aller-retour Lexique <-> Cadre (fiche exacte, pas juste l'accueil du module), "Retour" interne apres arrivee directe, fiche sans lien (aucun encart, pas de regression). `checkLexique.js` 0 erreur, `npm test` 924 verts.
    - **[ABANDONNE - 2026-09-13] Chiffres -> Lexique** : Denis a demande le detail technique avant de trancher (les 9 mots vivent dans 5 mecanismes de rendu differents de Comprendre les chiffres - les 5 chiffres cles du trimestre, la comparaison Union europeenne, le bloc zones d'emploi, les fiches methode en depliant, le bloc sante/medecins - et `zonage-ars` n'a meme pas encore de case a l'ecran ou s'accrocher). Au vu de ce cout reparti sur 5 chantiers separes pour seulement 9 mots, **Denis juge le benefice trop faible face a la complication : abandonne, ne pas reprendre sans nouvelle demande explicite.**
    - **Cadre <-> Chiffres (idee infobulle/bloc depliable, ligne ci-dessus)** : reste UNE idee ouverte, distincte de ce chantier, a reprendre separement si Denis le souhaite un jour (maquette + validation avant tout code).
    - **Bilan final de l'item 14** : sur les 3 sens manquants identifies, seul **Lexique -> Cadre** a ete construit. Cadre <-> Chiffres n'a jamais ete retenu, et Chiffres -> Lexique a ete examine puis abandonne par Denis. Chantier clos.
