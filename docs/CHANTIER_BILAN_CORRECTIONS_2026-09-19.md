# Chantier « Analyser ma candidature » — corrections du 2026-09-19

> Session en cours de compactage (90% atteint). Ce fichier fige l'état exact pour reprendre sans perte de contexte. À lire en premier avant de continuer ce chantier.

## Règle générale posée par Denis pendant ce chantier (s'applique à TOUT le reste)

Denis a réaffirmé plusieurs fois, de façon de plus en plus stricte : **la seule fenêtre modale tolérée dans toute l'application est l'écran tampon avant l'assistant** (« Avant de continuer vers X », Ctrl+V, « J'ai compris », décompte). Tout le reste — choix de l'assistant, collage de la réponse, vérification/édition d'informations, sélection de recommandations — doit être **en page**, jamais une fenêtre flottante (`ouvrirFenetreERIP`).

Cette règle a été appliquée rétroactivement à plusieurs endroits qui n'étaient pas dans la liste initiale (ex. l'alerte « Titre et accroche »). **Tout nouveau `ouvrirFenetreERIP()` trouvé dans le module Bilan doit être questionné à la lumière de cette règle avant d'être laissé tel quel.**

Autre règle rappelée : quand on déplace un bouton « Continuer » avec une condition d'activation, la condition doit être recalculée sur les données réelles (jamais un drapeau figé « déjà validé une fois »), pour que Retour → ré-avancer ne re-bloque jamais artificiellement.

## Liste complète des points donnés par Denis pour ce module

1. ✅ **FAIT** — Fusionner bloc 3 (« Votre situation ») et bloc 4 (« L'offre visée ») de l'écran Préparer en un seul bloc, sur le modèle du bloc 3 de « Reformuler et présenter mon CV ». Commit `394ae75`.
2. ✅ **FAIT** — Supprimer le bouton/bloc « Ouvrir mon CV ici » (ancien Atelier CV, `ouvrirAtelierCV`/`htmlAtelierCV`) sur l'écran Rapport — aucune valeur ajoutée. Garder « Organiser mon CV ». Commit `9a14d1d`. **Note** : les fonctions `ouvrirAtelierCV()`/`htmlAtelierCV()`/`brancherEvenementsAtelierCV()` (~500 lignes, js/app.js autour de la ligne 21850-22350 à l'époque) sont maintenant **du code mort** (plus aucun appelant actif) — pas supprimées faute de temps, à nettoyer si Denis le demande ou lors d'un futur passage d'hygiène.
3. ✅ **FAIT** — Écran « Rapport », bloc « Votre CV organisé » : remplacer les 3 boutons (Modifier mes expériences / Mes coordonnées / Modifier mon CV, 3 fenêtres différentes) par un seul bouton **« Mettre à jour mes informations »**, qui navigue **en page** vers la page « Vos informations » (route `'projet'`). Commit `9a14d1d`.
4. ✅ **FAIT** — Clic sur une expérience à corriger (liste des recommandations) : ouvrait l'ancienne fenêtre `ouvrirFenetreExperiences()` (« Vos expériences ») — navigue maintenant vers `'projet'` avec le bloc `CONFIG_BLOC_EXPERIENCES_PRO` ouvert d'emblée. Commit `9a14d1d`.
5. ✅ **FAIT** — Carte 1 (« Je veux écrire avec mes propres mots ») et Carte 2 (« Aidez-moi à trouver les bons mots ») : la barre fixe du bas (Retour/Accueil/Continuer) avait disparu, remplacée par une ligne de boutons reconstruite à la main (`bilanBoutonQuitterCorrection`). Remplacé par la vraie `barreNavigation()`/`.barre-navigation-fixe` partagée. Commit `7c4d951`.
6. ✅ **FAIT** — Carte 2 : recommandations affichées sur 2 colonnes (`.bilan-correction-copier-grille`, repli 1 colonne sous 700px). Commit `7c4d951`.
7. ✅ **FAIT** — Carte 2 : bouton « Traiter plusieurs recommandations avec l'assistant » agrandi (`btn-lg`, gras). Commit `7c4d951`.
8. ✅ **FAIT** (interjection en cours de chantier, pas dans la liste initiale) — Alerte « Titre et accroche du CV non détectés » (écran Rapport) : le lien « Modifier moi-même » ouvrait `bilanOuvrirEditionCV()` (fenêtre) — se déplie maintenant **en place**, dans la même carte (`_bilanAlerteTitreAccrocheOuvert`), réutilise `contenuTitreAccroche()`/`wireTitreAccroche()`. Le lien « Coordonnées » navigue vers `'projet'` (bloc Vous). Commit `be867dd`.
9. ✅ **FAIT** — « Organiser mon CV » : ouvrait l'ancien wizard modal (`structurerTexteExistant()` → `ouvrirAssistantDepotCV()`, étape 3, sans écran tampon). Remplacé par un vrai sous-parcours **en page** : `_etatBilan.organisation = { ecran: 'choix-assistant'|'coller', onTerminer }`, nouvelles fonctions `htmlBilanOrganisationChoixIA()`/`htmlBilanOrganisationColler()`/`brancherEvenementsOrganisation()`/`brancherEvenementsOrganisationColler()`/`bilanRetourOrganisation()` (toutes dans js/app.js, juste après `htmlBilanEtapeImportIA()`). Réutilise le moteur d'import existant (`analyserReponseImport`/`SPECIFICATION_IMPORT`/`ouvrirEcranValidationImport()`) pour la validation finale. `structurerTexteExistant()`/`assurerCVStructure()` (data/metiers.js) redirigent vers ce nouveau sous-état. Testé bout en bout au navigateur (choix assistant → écran tampon → collage → import réel → écran de vérification → retour rapport avec données appliquées). Commit `8a5b37f`.

   **Décision prise en cours de route** : `ouvrirEcranValidationImport()` (« Vérifiez les informations importées ») **reste une fenêtre modale** — décision assumée, pas oubliée. Raison : c'est une brique **massivement partagée** (utilisée aussi par le tout premier passage du diagnostic Bilan lui-même, ligne ~23365, ET par `ouvrirAssistantDepotCV()` — le wizard générique utilisé par ATS, Cohérence transversale, Regard recruteur mode texte, Découverte, page Action...). La convertir en page serait un chantier à part entière avec un risque de régression sur 6+ modules, largement hors du périmètre « Analyser ma candidature » d'aujourd'hui. **Si Denis insiste pour que cette fenêtre précise disparaisse aussi, il faut le lui faire confirmer explicitement avant d'y toucher, et prévoir une session dédiée.**

### Points PAS ENCORE traités (dans l'ordre où ils avaient été annoncés à Denis)

10. ✅ **FAIT** — « Traiter plusieurs recommandations avec l'assistant » (Carte 2) : remplacé par un sous-parcours en page `_etatBilan.correction.lot = { ecran: 'selection'|'style'|'choix-assistant'|'coller', ids, demandes }` (js/app.js, juste après `htmlBilanOrganisation*`, avant `htmlBilanRapport()`). 4 écrans : sélection + édition libre du texte (mutation directe de `reco.contenu`, testée), style d'écriture (`contenuRectangleStyleCV('cv', true, false)` + `appliquerDefautsStyleCV('cv')` pour les préselections, sans le bouton intégré), choix assistant (patron `htmlBilanOrganisationChoixIA`, mémorisation `_bilanDernierAssistantAmeliorationId`), collage (écran tampon + décompte + import via `bilanSoumettreReponseAmeliorationLot`). Les 3 anciennes fonctions modales (`bilanOuvrirSelectionAmeliorationLot`/`bilanOuvrirChoixAssistantLot`/`bilanAfficherEtapeCollageAmeliorationLot`) sont devenues du code mort, marquées comme telles, pas supprimées. CSS : `.bilan-lot-textarea` (theme-aware, même correctif que `.reformuler-textarea`, bug app-wide découvert au passage). Testé bout en bout au navigateur, clair + sombre, y compris édition de texte + retour en arrière (texte édité conservé) : commit à suivre.
    Anciennes fonctions modales remplacées (devenues code mort, js/app.js) : `bilanOuvrirSelectionAmeliorationLot()`, `bilanOuvrirChoixAssistantLot()`, `bilanAfficherEtapeCollageAmeliorationLot()`.

11. ✅ **FAIT** — Étape « Réglez le style d'écriture » avant le 2e passage IA du lot : écran 2 du point 10 ci-dessus, codé en même temps. **Précision tranchée avec Denis en cours de route** : ce même besoin ne s'applique PAS à « Organiser mon CV » (point 9) - voir point 19 ci-dessous, prompt d'extraction pure, jamais de rédaction.

12. ✅ **FAIT** — Carte 3 (« Je préfère être accompagné du début à la fin ») : vraie `barre-navigation-fixe` sur les 5 écrans (`preparation-chiffres`, `generation`, `relecture`, `completude`, `aucune-automatisation`) via `htmlAssistanceBoutonQuitter()` (partagée, convertie comme `bilanBoutonQuitterCorrection()` au point 5) + conversions dédiées pour `preparation-chiffres` (Retour/Continuer dans la barre, « Passer cette étape » en lien secondaire au-dessus) et `aucune-automatisation` (Retour vers le choix du mode + Accueil). Lien **« Revenir au rapport complet »** ajouté en haut de chacun des 5 écrans (`htmlAssistanceLienRapport()`/`bilanRevenirAuRapportDepuisAssistance()`), juste sous le repère « Étape X sur N » — proposition de placement retenue, jamais dans la barre fixe.
    **Découverte importante en cours de route** : la génération de la Carte 3 (bouton « Générer les propositions ») appelait encore l'ancienne fenêtre modale `bilanOuvrirChoixAssistantLot()` (celle-ci ouvrait même `window.open()` directement, **sans aucun écran tampon** — exactement le défaut que ce chantier corrige partout ailleurs). Ce n'était PAS du code mort comme cru un temps pendant le point 10 (corrigé). Migré vers un nouveau sous-parcours en page `_etatBilan.assistance.lot = { ecran: 'choix-assistant'|'coller', occurrences, itemsLot, objectifsParId, demandes }` (js/app.js, juste après `bilanAssistanceObjectifsParId()`), même patron que `_etatBilan.correction.lot` (point 10) adapté pour transporter `occurrences`/`objectifsParId` (poste visé + chiffres saisis). Les 3 anciennes fonctions modales (`bilanOuvrirSelectionAmeliorationLot`/`bilanOuvrirChoixAssistantLot`/`bilanAfficherEtapeCollageAmeliorationLot`) sont maintenant **vraiment** du code mort (plus aucun appelant), marquées comme telles.
    Testé bout en bout au navigateur, clair + sombre : écran génération → choix assistant en page → écran tampon → collage → import réel → retour automatique à l'écran relecture avec les propositions appliquées ; écrans `preparation-chiffres`/`aucune-automatisation` vérifiés isolément (barre fixe + lien rapport présents, pas d'erreur).

13. ❌ **À FAIRE** — Barre d'étapes du haut cliquable pour Bilan (`bilanBarreEtapes()`/`BILAN_ETAPES_BARRE`). Difficulté confirmée par l'audit : **complexe** — chaque étape de la barre correspond à une combinaison de drapeaux d'état composites (`_etatBilan.preparer`, `.correction`, `.assistance`, `diagnostic.statut`...), pas un numéro d'écran simple ni une route. Il faudra un mécanisme `js:` (comme `_creerCvRouteParIndex`) qui **resimule** l'état correspondant avant de rappeler `pageBilanCandidature()`, avec un vrai risque de casser un diagnostic/une correction en cours si on saute en arrière n'importe où. **Ne pas se lancer sans un vrai moment de réflexion dédié.**

14. ✅ **FAIT** — Bouton Continuer dans la barre fixe partout dans Bilan. Cartes 1/2 (point 5), toute la zone Assistance/Carte 3 (point 12), les sous-parcours « Organiser mon CV »/« Traiter plusieurs recommandations » (points 9/10) étaient déjà faits. Dernier restant : `htmlBilanPreparer()`, bouton « Choisir mon assistant » déplacé dans la vraie barre fixe (`onclickSuivant: 'bilanPreparerContinuer()'`, nouvelle fonction extraite de l'ancien `addEventListener` fermé sur des variables locales — lit `_etatBilan.preparer` directement, même objet, aucune perte). Condition d'activation (`cvPresent && relu`) recalculée à chaque rendu via `options.suivantDesactive`, jamais un drapeau figé (règle transversale de ce chantier). Testé navigateur : désactivé avec message tant que CV/relecture manquent, activé et fonctionnel une fois complété.

15. ✅ **FAIT** — Bulle de navigation « Éditer mon CV » (`bilanBoutonEditerCV()`, js/app.js), placée à côté de « Revoir la présentation » (même emplacement, même forme `.btn-revoir-module`, même point d'injection unique `bilanBoutonRevoirPresentation()` donc visible sur tous les écrans du module). Visible dès que `dossierAStructureExperiences()` est vrai. Mène vers la page « Vos documents » existante (route `'resultats'`) via `naviguerVers('resultats')`. **Découverte utile** : aucun flag dédié n'a été nécessaire pour le retour - `naviguerVers()` capture déjà automatiquement `_pageOrigineAvantResultats = 'bilan'` à chaque entrée sur `'resultats'` (mécanisme générique déjà utilisé par Découverte/l'import direct, `resultatsCibleRetour()`), donc « Retour » depuis « Vos documents » ramène tout seul vers le Rapport. Testé bout en bout au navigateur, clair + sombre.

16. ❌ **À FAIRE** (trouvé en cours de route, pas encore discuté avec Denis) — `bilanOuvrirGenerationTitreAccroche()` (js/app.js ~19721 à l'époque) : bouton « Générer une proposition » sur l'alerte Titre/accroche, ouvre encore une fenêtre modale (`ouvrirFenetreERIP`) avec choix d'assistant **sans écran tampon**. Même défaut que partout ailleurs. **Pas encore signalé à Denis explicitement** — à mentionner avant d'y toucher, ou à corriger dans la même veine que le reste si le principe général suffit à trancher.

17. ❌ **À FAIRE** (signalé par Denis pendant que je travaillais sur le point 10, mis de côté à sa demande explicite — « tu vas les mettre de côté, une fois que tu auras fini tu referas aussi ces tâches ») — Écran « Preparer », bloc 3 fusionné (« Votre situation et l'offre visée », point 1 ci-dessus) : au clic sur une des 6 pastilles de situation (`[data-preparer-situation]`), le bloc **se referme automatiquement**, quel que soit le choix. Il ne doit **jamais** se refermer tout seul sur ce clic - la personne doit pouvoir enchaîner sur la saisie de l'offre (entreprise/offre visée) dans le même bloc, sans qu'il se referme sous elle.
    **Cause identifiée (lecture de code, pas encore corrigée)** : `htmlBilanPreparer()` (js/app.js ~19005) calcule l'attribut `open` du `<details id="preparerBloc3">` avec `cvPresent && relu && !situationConnue` - `situationConnue` devient vrai dès qu'une pastille est choisie (`definirObjectifCandidature()` renseigne `dossier.objectif`), donc le rerender qui suit le clic (`brancherEvenementsBilanPreparer()` ~23205) enlève `open` et le bloc se ferme. Correctif à prévoir : ne plus reconditionner `open` sur `!situationConnue` une fois que la personne a une interaction en cours dans ce bloc (ex. un flag local `etat.situationTouchee` - qui existe déjà, `etat.situationTouchee = true` au clic - à utiliser aussi pour garder le bloc ouvert, pas seulement pour la logique de dépôt).
    Denis a précisé aussi : **vérifier que l'information saisie (situation + offre) remonte bien jusqu'au prompt envoyé à l'assistant** - à contrôler en même temps (`bilanLireCiblageOffre()`, `saisie.objectif`/`saisie` transmis à `bilanDemanderDiagnostic`/construction du prompt), pas juste corriger la fermeture visuelle.

18. ❌ **À FAIRE** (signalé par Denis pendant le travail sur le point 10, mis de côté à sa demande explicite ; précisé/complété pendant le point 16, également mis de côté) — Écran « Rapport » (`htmlBilanRapport()`) : la rubrique **« Pour information »** (les 2 accordéons **« Garder comme repère »** et **« CV anonymisé avant l'analyse »**, js/app.js ~20316-20349, assemblés ligne ~20652 `'<div class="bilan-groupe-info">...' + htmlGarderCommeRepere + htmlAnonymisation + '</div>'`) **ne sert à rien à cet endroit** : cette explication existe déjà sur la page d'introduction du module. **Supprimer toute cette partie** (les 2 accordéons + leur regroupement `.bilan-groupe-info`) de l'écran Rapport.
    À vérifier avant de coder : que la page d'introduction du module Bilan explique bien déjà les deux (le bouton « Garder comme repère » présent sur chaque carte, et le fait que le CV est anonymisé avant l'analyse) - sinon reporter l'info manquante là-bas au même passage plutôt que la faire disparaître complètement.
    **Complément donné par Denis (pendant le point 16)** : ne pas laisser cet emplacement vide une fois « Pour information » supprimée - **y mettre à la place un vrai encart mis en avant** invitant à modifier le CV avec les recommandations faites plus haut dans le rapport, **avec le bouton « Éditer mon CV » dedans** (celui du point 15, actuellement une petite bulle discrète en haut de page à côté de « Revoir la présentation »). Raison donnée : cette bulle du haut est « oubliée, négligée, entre toutes les pastilles et tous les boutons, pas très clair », alors qu'éditer le CV représente « la moitié du parcours de ce module » - ça mérite une vraie place, pas une pastille perdue. Décision d'implémentation à prendre à ce moment-là : garder la bulle du haut en plus (répétée à 2 endroits) ou la retirer du haut et ne laisser que ce nouvel encart (probablement préférable - éviter la duplication - mais à confirmer avec Denis si un doute subsiste une fois le reste du contenu du rapport revu).

19. ✅ **TRANCHÉ, PAS D'ACTION** (question posée par Denis pendant le codage du point 10, résolue par lecture du prompt réel) — Denis a demandé si le 2e passage IA de « Organiser mon CV » sert uniquement à structurer les informations (auquel cas pas besoin de style d'écriture) ou à réellement rédiger/adapter le CV (auquel cas l'écran de style doit y être intégré). **Vérifié dans `prompts/extraction-cv.md`** : c'est un prompt d'**extraction pure**, qui interdit explicitement toute reformulation - « Ton unique rôle est de lire le texte d'un CV déposé et d'en extraire les informations factuelles qui y figurent — **jamais de les reformuler, les améliorer, les compléter ou en inventer** » (ligne 5), répété section « Ce que tu ne dois jamais faire » : « Ne reformule pas, n'améliore pas, ne corrige pas le style d'une mission ou d'un intitulé : recopie fidèlement ce qui est écrit. » **Conclusion communiquée à Denis** : « Organiser mon CV » ne fait QUE structurer/ranger en rubriques, jamais écrire - le style d'écriture n'a donc pas sa place dans ce parcours précis. **Aucun écran de style à ajouter à `_etatBilan.organisation`.** Le vrai réglage de style reste à sa place actuelle : le 2e passage IA classique (`cv.md`), qui, lui, réécrit réellement (accroche, missions reformulées) et consomme déjà `dossier.preferencesIAParType.cv` - rien à changer là.

## Les deux audits transversaux (autres modules, reportés à une session dédiée)

Denis a explicitement dit de les garder de côté et de finir Bilan d'abord. Résumé pour ne pas avoir à relancer les agents :

**Audit 1 — bouton Continuer dans la barre fixe, tous modules** : le parcours principal (Créer un nouveau CV) est déjà 100% conforme. Quasiment tous les autres modules (Bilan — traité ci-dessus —, Cohérence transversale, ATS, Un regard sur mon CV, Comparer mes pistes, Découverte de compétences, Comprendre le cadre, Comprendre les chiffres, Préparer lettre/entretien, Co-construire ma lettre) ont un Continuer intégré dans le contenu de page plutôt que dans la barre fixe — pattern systémique à reprendre module par module. Cas le plus cassé déjà identifié et traité (Bilan/Assistance). Décision à prendre avant de généraliser : les pages d'introduction de module (~10 écrans) gardent volontairement leur CTA dans le contenu — garder cette exception ou l'annuler aussi ?

**Audit 2 — barre d'étapes du haut cliquable, tous modules** : Créer un nouveau CV déjà conforme (`_creerCvRouteParIndex`). Mettre à jour mon CV / Préparer lettre-entretien : partiellement conforme (déjà cliquable sur « Vos documents », pas sur les écrans internes du module — 3 premières étapes partagent un seul écran interne). Facile à rendre cliquable : Co-construire ma lettre, Préparer un entretien (états internes 1:1 avec les étapes). Complexe (états composites ou écrans partagés par plusieurs repères) : Reformuler et présenter mon CV, Analyser ma candidature (Bilan), Cohérence transversale (**décision explicite de Denis du 2026-08-25 de la garder non cliquable — à reconfirmer avant d'y toucher**), Comprendre le cadre, Découverte de compétences. Mixte facile/complexe : Un regard sur mon CV, ATS, Comparer mes pistes, Comprendre les chiffres.

## Repères techniques utiles pour la suite

- `_etatBilan` (js/app.js ~11555) : `{ correction, assistance, organisation, preparer, preparerRetour, voirIntro, reprisePendante }`.
- Pattern établi pour un sous-parcours « choix assistant + collage » en page dans Bilan : voir `htmlBilanOrganisation*()`/`brancherEvenementsOrganisation*()` (point 9) — **modèle à copier-adapter pour le point 10**.
- `_depuisBilanVersProjet` (js/app.js ~11549 zone) : flag pour que `pageProjet()` fasse pointer son "Retour" vers `'bilan'`. Déjà utilisé aux points 3, 4, 8.
- Tests : `npm test` (931 verts au dernier commit). Vérification navigateur obligatoire (js/app.js non couvert par les tests Node) — méthode éprouvée ce chantier : `bilanStoreDefinirCandidature({id, cv})` + `bilanStoreDefinirDiagnostic({candidatureId, statut:'complete', resultat:{...}})` pour simuler un diagnostic sans repasser par un vrai import.
- Serveur dev déjà lancé (`site-v2-python-server`, port 8123) — ne pas en relancer un autre.

## Ce qu'il faut faire en premier à la reprise

1. Relire ce fichier en entier.
2. Reprendre le point 10 (Traiter plusieurs recommandations) : le plan est prêt, il reste à écrire le code.
3. Continuer dans l'ordre 11 → 16, un point à la fois, test navigateur + commit à chaque étape (discipline suivie tout le long de ce chantier).
4. Ne pas relancer les deux audits transversaux (déjà faits, résumés ci-dessus) tant que Bilan n'est pas terminé.
